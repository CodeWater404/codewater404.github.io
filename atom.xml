<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Code Water</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://blog.codewater.top/</id>
  <link href="https://blog.codewater.top/" rel="alternate"/>
  <link href="https://blog.codewater.top/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, Code Water</rights>
  <subtitle>阿水</subtitle>
  <title>Code Water</title>
  <updated>2026-07-13T12:40:49.000Z</updated>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="网络" scheme="https://blog.codewater.top/tags/%E7%BD%91%E7%BB%9C/"/>
    <content>
      <![CDATA[<p>“网站打不开”这句话背后可能是完全不同的问题：网络本身断了、中间某一跳路由出故障、域名解析错了，或者目标服务压根没在监听。不分层排查，只会对着一个打不开的页面反复刷新，猜半天猜不出所以然。</p><span id="more"></span><p>这是 Linux 命令系列的一篇，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>；跟之前写的 <a href="/%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%8E%92%E6%9F%A5%E6%9C%8D%E5%8A%A1%E9%97%AE%E9%A2%98%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4">Linux 服务器问题排查</a> 是互补关系——那篇讲的是端口、进程、日志（<code>ss</code>、<code>lsof</code>），这篇补上”网络本身通不通、DNS 解析对不对”这一层。</p><h2 id="ping：先确认最基本的连通性"><a href="#ping：先确认最基本的连通性" class="headerlink" title="ping：先确认最基本的连通性"></a>ping：先确认最基本的连通性</h2><p><code>ping</code> 基于 ICMP 协议，向目标主机发包并等待回应，用来测试连通性和延迟：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">ping -c 3 baidu.com</span><br><span class="line"><span class="comment">#     -c 3：只发 3 个探测包就停止，不加这个参数会一直发，要手动 Ctrl-C 停止</span></span><br><span class="line"></span><br><span class="line">64 bytes from 124.237.177.164: icmp_seq=0 ttl=54 time=124.8 ms</span><br><span class="line">64 bytes from 124.237.177.164: icmp_seq=1 ttl=54 time=125.6 ms</span><br><span class="line">64 bytes from 124.237.177.164: icmp_seq=2 ttl=54 time=127.1 ms</span><br><span class="line"><span class="comment">#     time：这一个包的往返延迟，数值越大网络越慢</span></span><br><span class="line"><span class="comment">#     ttl：包剩余的存活跳数，能大致反映经过了多少层网络设备（不用深究具体数值）</span></span><br></pre></td></tr></table></figure><p>有个陷阱必须提前知道：<strong>ping 不通不等于网络真的不通</strong>。很多服务器和防火墙会主动屏蔽 ICMP 协议（出于安全考虑，避免被用来扫描存活主机），这种情况下 <code>ping</code> 完全无响应，但目标服务本身其实是正常的、能正常访问。遇到 <code>ping</code> 不通，不能直接下结论”网络挂了”，还得结合后面的手段继续排查。</p><h2 id="traceroute：定位问题出在哪一跳"><a href="#traceroute：定位问题出在哪一跳" class="headerlink" title="traceroute：定位问题出在哪一跳"></a>traceroute：定位问题出在哪一跳</h2><p>如果 <code>ping</code> 确实不通，下一步是 <code>traceroute</code>——它能显示数据包从本机到目标主机，中间经过的每一跳路由，帮你定位问题出在链路上的哪一环：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">traceroute baidu.com</span><br><span class="line"></span><br><span class="line"> 1  192.168.1.1 (192.168.1.1)  2.336 ms  2.079 ms  2.250 ms</span><br><span class="line"> 2  10.169.144.154 (10.169.144.154)  31.583 ms</span><br><span class="line">    10.169.144.158 (10.169.144.158)  13.839 ms</span><br><span class="line">    10.169.144.186 (10.169.144.186)  7.618 ms</span><br><span class="line"> 3  10.185.174.254 (10.185.174.254)  10.458 ms  6.176 ms  6.550 ms</span><br><span class="line"><span class="comment">#     每一行是一跳，默认每跳发 3 个探测包（所以后面有 3 个时间值）</span></span><br><span class="line"><span class="comment">#     第 2 跳三次探测返回了不同的地址，说明这一跳背后是多条负载均衡的路径，属正常现象</span></span><br><span class="line"><span class="comment">#     某一跳如果显示 * * *，说明这一跳没有响应——可能是这个节点屏蔽了探测包，也可能是真的断在这里</span></span><br></pre></td></tr></table></figure><p><code>traceroute</code> 的原理并不复杂：它给数据包设置一个从 1 开始递增的 TTL（存活跳数），TTL 为 1 的包到第一跳路由器就会被丢弃，路由器同时会回一个”TTL 耗尽”的 ICMP 消息，<code>traceroute</code> 借此拿到第一跳的地址；接着发 TTL 为 2 的包获取第二跳，以此类推，直到包真正到达目标主机为止。</p><p>实战判断很直接：从某一跳开始持续超时或丢包，问题大概率就出在那一跳之后的链路上——如果那一跳是本地路由器，问题在本地网络；如果是运营商中间节点，问题可能不在你这边，得等或者换网络；如果一直跑到最后几跳才出问题，大概率是目标网络那边的事。</p><h2 id="dig-nslookup：DNS-解析诊断"><a href="#dig-nslookup：DNS-解析诊断" class="headerlink" title="dig &#x2F; nslookup：DNS 解析诊断"></a>dig &#x2F; nslookup：DNS 解析诊断</h2><p><code>ping</code> 能通、<code>traceroute</code> 也能跑到目标，网站却还是打不开？这时候该查的是域名解析对不对——域名解析错了，访问的可能压根不是你以为的那台服务器。<code>dig</code>（Domain Information Groper）是专门查询 DNS 记录的工具，默认查询目标域名的 A 记录（域名对应的 IPv4 地址）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">dig baidu.com</span><br><span class="line"></span><br><span class="line">;; QUESTION SECTION:</span><br><span class="line">;baidu.com.INA</span><br><span class="line"></span><br><span class="line">;; ANSWER SECTION:</span><br><span class="line">baidu.com.573INA124.237.177.164</span><br><span class="line">baidu.com.573INA111.63.65.103</span><br><span class="line"></span><br><span class="line">;; Query time: 12 msec</span><br><span class="line">;; SERVER: 100.100.100.100#53(100.100.100.100)</span><br><span class="line"><span class="comment">#     QUESTION SECTION：查询了什么，这里是查 baidu.com 的 A 记录（域名对应的 IPv4 地址）</span></span><br><span class="line"><span class="comment">#     ANSWER SECTION：解析结果，一个域名可能对应多个 IP（负载均衡的常见做法）</span></span><br><span class="line"><span class="comment">#     Query time：这次查询耗时多久</span></span><br><span class="line"><span class="comment">#     SERVER：这次查询实际使用的是哪个 DNS 服务器——如果解析结果不对，先看看是不是用错了 DNS 服务器</span></span><br></pre></td></tr></table></figure><p><code>nslookup</code> 是另一个查 DNS 的工具，功能和 <code>dig</code> 类似但输出格式更简洁：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">nslookup baidu.com</span><br><span class="line"></span><br><span class="line">Server:100.100.100.100</span><br><span class="line">Address:100.100.100.100#53</span><br><span class="line"></span><br><span class="line">Non-authoritative answer:</span><br><span class="line">Name:baidu.com</span><br><span class="line">Address: 124.237.177.164</span><br><span class="line"><span class="comment">#     一样能看出用的哪个 DNS 服务器（Server）、解析出来的地址（Address）</span></span><br><span class="line"><span class="comment">#     但信息量明显没有 dig 详细——没有 TTL、没有查询耗时</span></span><br></pre></td></tr></table></figure><p>日常排查优先用 <code>dig</code>，信息更全；<code>nslookup</code> 胜在几乎所有系统都自带，个别精简系统没装 <code>dig</code> 时可以顶上用。</p><h2 id="常用参数速查表"><a href="#常用参数速查表" class="headerlink" title="常用参数速查表"></a>常用参数速查表</h2><table><thead><tr><th>命令&#x2F;参数</th><th>作用</th></tr></thead><tbody><tr><td><code>ping -c N</code></td><td>只发送 N 个探测包后停止，不加会一直发</td></tr><tr><td><code>ping</code> 输出里的 <code>time</code></td><td>单个包的往返延迟</td></tr><tr><td><code>ping</code> 输出里的 <code>ttl</code></td><td>包剩余存活跳数，大致反映经过的网络层数</td></tr><tr><td><code>traceroute -m N</code></td><td>最多探测 N 跳就停止</td></tr><tr><td><code>traceroute -w N</code></td><td>每次探测等待响应的超时时间（秒）</td></tr><tr><td><code>traceroute</code> 输出里的 <code>* * *</code></td><td>该跳没有响应，可能屏蔽了探测包或链路确实断在这里</td></tr><tr><td><code>dig 域名</code></td><td>查询域名的 A 记录，输出 ANSWER SECTION、Query time、SERVER 等信息</td></tr><tr><td><code>dig</code> 输出里的 <code>SERVER</code></td><td>这次查询实际使用的 DNS 服务器</td></tr><tr><td><code>nslookup 域名</code></td><td>查询域名解析结果，输出比 dig 简洁，没有 TTL 和查询耗时</td></tr></tbody></table><h2 id="综合排查流程"><a href="#综合排查流程" class="headerlink" title="综合排查流程"></a>综合排查流程</h2><p>网站&#x2F;服务连不上，按这个顺序排查效率最高：</p><ol><li><strong><code>ping</code> 测网络层连通性</strong>——通了，跳到第 3 步；不通，先确认目标是不是屏蔽了 ICMP（比如用 <code>curl</code> 试一下能不能正常拿到响应），排除这个可能之后再进第 2 步</li><li><strong><code>traceroute</code> 定位断在哪一跳</strong>——找到持续超时&#x2F;丢包开始的那一跳，判断问题出在本地网络、中间运营商还是目标网络</li><li><strong><code>dig</code> 确认域名解析对不对</strong>——解析出来的 IP 跟预期不符，问题在 DNS 这一层，检查用的 DNS 服务器或者域名解析配置</li><li><strong>解析也对、网络也通，但服务还是连不上</strong>——大概率是端口没监听或者防火墙拦截，这部分排查见 <a href="/%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%8E%92%E6%9F%A5%E6%9C%8D%E5%8A%A1%E9%97%AE%E9%A2%98%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4">Linux 服务器问题排查</a> 里 <code>ss</code>、<code>lsof</code> 的用法</li></ol><p>这四步覆盖了从”网络本身”到”DNS”再到”目标服务”的完整链路，按顺序排查比凭感觉瞎试快得多。</p><h2 id="写到这里"><a href="#写到这里" class="headerlink" title="写到这里"></a>写到这里</h2><p><code>ping</code>、<code>traceroute</code>、<code>dig</code> 这三个工具分别对应网络排查的三个不同层次——连通性、路径、解析。记住这个分层，遇到”连不上”这类模糊的问题，就能很快把它拆解成一个个具体的、可以验证的假设，而不是对着报错信息干瞪眼。</p>]]>
    </content>
    <id>https://blog.codewater.top/%E7%BD%91%E7%BB%9C%E8%BF%9E%E9%80%9A%E6%80%A7%E8%AF%8A%E6%96%AD-ping-traceroute%E4%B8%8Edig%E5%AE%9E%E6%88%98/</id>
    <link href="https://blog.codewater.top/%E7%BD%91%E7%BB%9C%E8%BF%9E%E9%80%9A%E6%80%A7%E8%AF%8A%E6%96%AD-ping-traceroute%E4%B8%8Edig%E5%AE%9E%E6%88%98/"/>
    <published>2026-07-13T12:40:49.000Z</published>
    <summary>网站打不开怎么分层排查：ping 测连通、traceroute 定位断点、dig 查 DNS 解析。</summary>
    <title>网络连不上怎么排查：ping、traceroute 与 dig/nslookup 实战</title>
    <updated>2026-07-13T12:40:49.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="sed" scheme="https://blog.codewater.top/tags/sed/"/>
    <category term="awk" scheme="https://blog.codewater.top/tags/awk/"/>
    <content>
      <![CDATA[<p>搜”命令行批量替换文本”，答案里有的用 <code>sed</code>，有的用 <code>awk</code>，看着好像都能解决问题，但不知道自己该学哪个、什么场景该用哪个。两者确实经常一起出现在同一批搜索结果里——但它们解决的其实是不同维度的问题：<code>sed</code> 面向”行”做替换、删除、插入；<code>awk</code> 面向”列”做结构化处理。搞清楚这个区别，选工具就不用再猜了。</p><span id="more"></span><p>这是 Linux 命令系列的一篇，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>。</p><h2 id="sed-基本用法：按行处理文本"><a href="#sed-基本用法：按行处理文本" class="headerlink" title="sed 基本用法：按行处理文本"></a>sed 基本用法：按行处理文本</h2><p><code>sed</code>（stream editor，流编辑器）默认逐行读取输入，对每一行执行指定的编辑命令后输出结果，不提供交互式编辑界面——写脚本自动化批量处理文本，正是它的设计目的。最核心的命令是 <code>s</code>，替换文本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">sed <span class="string">&#x27;s/old/new/&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     把每一行里第一个匹配到的 &quot;old&quot; 替换成 &quot;new&quot;，只替换每行的第一处</span></span><br><span class="line"></span><br><span class="line">sed <span class="string">&#x27;s/old/new/g&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     加上 g（global），一行里所有匹配到的 &quot;old&quot; 都替换，不只是第一个</span></span><br></pre></td></tr></table></figure><p>默认情况下 <code>sed</code> 不会修改原文件，只是把处理结果打印到标准输出。想直接改文件本身，要加 <code>-i</code>（in-place）——这里有个必须提前知道的跨平台坑：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># macOS / BSD sed</span></span><br><span class="line">sed -i <span class="string">&#x27;&#x27;</span> <span class="string">&#x27;s/old/new/&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     BSD sed 要求 -i 后面必须紧跟一个备份文件后缀参数，不想要备份就传一个空字符串 &#x27;&#x27;</span></span><br><span class="line"><span class="comment">#     漏了这个空字符串参数，sed 会把下一个参数（也就是替换脚本本身）当成后缀，直接报错</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># GNU sed（Linux 发行版默认自带的 sed）</span></span><br><span class="line">sed -i <span class="string">&#x27;s/old/new/&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     GNU sed 的 -i 不需要额外参数，直接原地修改，不生成备份</span></span><br><span class="line"><span class="comment">#     想要备份就不带空格直接写后缀：sed -i.bak &#x27;s/old/new/&#x27; file.txt，会生成 file.txt.bak</span></span><br></pre></td></tr></table></figure><p>同一条 <code>sed -i</code> 命令，在 macOS 上能跑通，搬到 Linux 服务器上可能报错（或者反过来）——写跨平台脚本时这是最容易被忽略的细节。</p><p>除了替换，<code>sed</code> 还能按行号删除或者操作某个范围：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">sed <span class="string">&#x27;2d&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     删除第 2 行（打印到标准输出，加 -i 才会真正改文件）</span></span><br><span class="line"></span><br><span class="line">sed <span class="string">&#x27;2,4d&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     删除第 2 到第 4 行（含首尾）</span></span><br><span class="line"></span><br><span class="line">sed -e <span class="string">&#x27;s/foo/bar/&#x27;</span> -e <span class="string">&#x27;s/baz/qux/&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     -e：一次执行多个替换表达式，不用为每个替换单独跑一次 sed</span></span><br></pre></td></tr></table></figure><h3 id="sed-常用参数速查表"><a href="#sed-常用参数速查表" class="headerlink" title="sed 常用参数速查表"></a>sed 常用参数速查表</h3><table><thead><tr><th>参数&#x2F;命令</th><th>作用</th></tr></thead><tbody><tr><td><code>s/old/new/</code></td><td>替换命令，只替换每行第一个匹配</td></tr><tr><td><code>s/old/new/g</code></td><td>加 <code>g</code>，替换每行所有匹配</td></tr><tr><td><code>-i</code></td><td>原地修改文件；macOS&#x2F;BSD 必须带空字符串参数 <code>-i &#39;&#39;</code>，GNU&#x2F;Linux 直接 <code>-i</code> 即可</td></tr><tr><td><code>Nd</code></td><td>删除第 N 行，如 <code>2d</code></td></tr><tr><td><code>N,Md</code></td><td>删除第 N 到第 M 行</td></tr><tr><td><code>-e</code></td><td>一次执行多个表达式，不用重复调用 sed</td></tr></tbody></table><h2 id="awk-基本用法：按列处理结构化文本"><a href="#awk-基本用法：按列处理结构化文本" class="headerlink" title="awk 基本用法：按列处理结构化文本"></a>awk 基本用法：按列处理结构化文本</h2><p><code>awk</code> 是一门以”按行扫描、按字段处理”为核心的小型编程语言，处理思路跟 <code>sed</code> 不一样——它默认把每一行按空白字符拆成若干”字段”，用 <code>$1</code>、<code>$2</code> 这些变量取某一列：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&quot;alice 25 engineer&quot;</span> | awk <span class="string">&#x27;&#123;print $1&#125;&#x27;</span></span><br><span class="line"><span class="comment">#     $1 是第一个字段，输出 alice</span></span><br><span class="line"><span class="comment">#     $0 是整行原始内容，$2、$3 依次是第二、第三个字段</span></span><br></pre></td></tr></table></figure><p>几个内置变量，日常最常用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">awk <span class="string">&#x27;&#123;print NF&#125;&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     NF：当前这一行有多少个字段（Number of Fields）</span></span><br><span class="line"></span><br><span class="line">awk <span class="string">&#x27;&#123;print NR, $0&#125;&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     NR：到目前为止已经处理了多少行（Number of Records），是所有输入累计的行号</span></span><br><span class="line"><span class="comment">#     注意 NR 跨文件也是累计的——如果同时处理多个文件，&quot;当前文件内的行号&quot;要用 FNR，不是 NR</span></span><br></pre></td></tr></table></figure><p>按条件过滤是 <code>awk</code> 真正擅长的场景：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">awk <span class="string">&#x27;$3 &gt; 100 &#123;print $1&#125;&#x27;</span> data.txt</span><br><span class="line"><span class="comment">#     只有第 3 列的值大于 100 的行，才输出这一行的第 1 列</span></span><br><span class="line"><span class="comment">#     条件写在 &#123;&#125; 前面，满足条件的行才会执行后面 &#123;&#125; 里的动作</span></span><br><span class="line"></span><br><span class="line">awk <span class="string">&#x27;BEGIN &#123;print &quot;开始处理&quot;&#125; &#123;sum += $2&#125; END &#123;print &quot;总计：&quot;, sum&#125;&#x27;</span> data.txt</span><br><span class="line"><span class="comment">#     BEGIN 块：在处理任何一行之前执行一次，常用来打印表头或者初始化变量</span></span><br><span class="line"><span class="comment">#     END 块：在处理完所有行之后执行一次，常用来输出汇总统计结果</span></span><br><span class="line"><span class="comment">#     中间的主体部分对每一行都执行一次，这里是把第 2 列累加进 sum</span></span><br><span class="line"></span><br><span class="line">awk -F<span class="string">&#x27;,&#x27;</span> <span class="string">&#x27;&#123;print $2&#125;&#x27;</span> data.csv</span><br><span class="line"><span class="comment">#     -F：指定字段分隔符，默认是空白字符，处理 CSV 这类逗号分隔的文件要显式指定</span></span><br></pre></td></tr></table></figure><h3 id="awk-常用参数与内置变量速查表"><a href="#awk-常用参数与内置变量速查表" class="headerlink" title="awk 常用参数与内置变量速查表"></a>awk 常用参数与内置变量速查表</h3><table><thead><tr><th>参数&#x2F;变量</th><th>作用</th></tr></thead><tbody><tr><td><code>$0</code></td><td>整行原始内容</td></tr><tr><td><code>$1</code>、<code>$2</code>…</td><td>第 1、第 2……个字段</td></tr><tr><td><code>NF</code></td><td>当前行的字段总数（Number of Fields）</td></tr><tr><td><code>NR</code></td><td>累计处理到第几行，跨文件累计（Number of Records）</td></tr><tr><td><code>FNR</code></td><td>当前文件内的行号，跨文件会重新计数</td></tr><tr><td><code>-F</code></td><td>指定字段分隔符，默认是空白字符</td></tr><tr><td><code>BEGIN &#123;...&#125;</code></td><td>处理任何一行之前执行一次，常用来初始化</td></tr><tr><td><code>END &#123;...&#125;</code></td><td>处理完所有行之后执行一次，常用来输出汇总</td></tr></tbody></table><h2 id="同一个任务，两种工具的差距在哪"><a href="#同一个任务，两种工具的差距在哪" class="headerlink" title="同一个任务，两种工具的差距在哪"></a>同一个任务，两种工具的差距在哪</h2><p>假设有一份访问日志，每行是”路径 响应时间(ms)”，想找出响应时间超过 500ms 的所有路径。用 <code>awk</code> 几乎是原生支持：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">awk <span class="string">&#x27;$2 &gt; 500 &#123;print $1&#125;&#x27;</span> access.log</span><br><span class="line"><span class="comment">#     一行搞定：按第 2 列的数值判断，满足条件就输出第 1 列</span></span><br></pre></td></tr></table></figure><p>用 <code>sed</code> 做同样的事，会很别扭——<code>sed</code> 的正则匹配本质是”文本模式匹配”，不理解”这一列的值是不是大于 500”这种数值比较，得先把数值范围拆成一堆正则模式去匹配文本形式，写出来又长又脆弱，稍微换一种数字格式就得重新调整正则。这不是 <code>sed</code> 的缺陷，是它压根不是为这类场景设计的——<code>sed</code> 面对的是”文本”，<code>awk</code> 面对的是”字段和数值”。</p><h2 id="该用哪个：判断标准"><a href="#该用哪个：判断标准" class="headerlink" title="该用哪个：判断标准"></a>该用哪个：判断标准</h2><ul><li>只是替换、删除、插入某几行文本，不涉及”按列取值”或者”数值比较”——用 <code>sed</code>，更直接</li><li>需要”取第几列””按某一列的值筛选””跨行做统计汇总”——用 <code>awk</code>，这是它的设计初衷</li><li>两者也能组合使用，比如先用 <code>awk</code> 提取出需要的列，再用 <code>sed</code> 调整输出格式，但没必要为了”只用一个工具”硬凑一个别扭的写法</li></ul><h2 id="写到这里"><a href="#写到这里" class="headerlink" title="写到这里"></a>写到这里</h2><p><code>sed</code> 和 <code>awk</code> 都是几十年历史的经典 Unix 工具，功能远不止本文列的这些（<code>sed</code> 支持完整的模式空间和保持空间操作，<code>awk</code> 本身就是一门小型编程语言，支持自定义函数、数组），但日常排查和批处理，本文这些用法已经能覆盖大部分场景。选错工具通常不是因为哪个”更强”，而是没看清任务本质是”处理行”还是”处理列”。</p>]]>
    </content>
    <id>https://blog.codewater.top/sed%E4%B8%8Eawk%E5%AE%9E%E6%88%98%E5%AF%B9%E6%AF%94-%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86%E5%B7%A5%E5%85%B7%E6%80%8E%E4%B9%88%E9%80%89/</id>
    <link href="https://blog.codewater.top/sed%E4%B8%8Eawk%E5%AE%9E%E6%88%98%E5%AF%B9%E6%AF%94-%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86%E5%B7%A5%E5%85%B7%E6%80%8E%E4%B9%88%E9%80%89/"/>
    <published>2026-07-13T12:38:05.000Z</published>
    <summary>sed 面向行替换，awk 面向列处理：s 命令、-i 跨平台坑、NF/NR 怎么用与选型判断。</summary>
    <title>sed 与 awk 实战对比：什么时候该用哪个</title>
    <updated>2026-07-13T12:38:05.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="rsync" scheme="https://blog.codewater.top/tags/rsync/"/>
    <content>
      <![CDATA[<p>同步一个几个 G 的目录到服务器，哪怕只改了几个文件，用 <code>scp</code> 或 <code>cp</code> 还是得把整个目录重新传一遍——网络稍微差一点，等待时间就长得离谱。<code>rsync</code> 解决的正是这个问题：只传输真正变化的部分，其余的原样跳过。</p><span id="more"></span><p>这是 Linux 命令系列的一篇，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>。</p><h2 id="rsync-为什么”增量”"><a href="#rsync-为什么”增量”" class="headerlink" title="rsync 为什么”增量”"></a>rsync 为什么”增量”</h2><p><code>rsync</code>（remote sync）是一个文件&#x2F;目录同步工具，既能同步本地磁盘之间的目录，也能通过 SSH 同步到远程主机，默认行为就是只处理源和目标之间有差异的部分，没变化的文件直接跳过。它判断”变了没”，不是简单看文件名或者改动时间——它用的是一套块级别的对比算法（由 Andrew Tridgell 提出，通常直接叫 rsync algorithm）：把文件切成一个个数据块，两边同时计算校验和，只有校验和对不上的块才会真正传输，没变的块直接跳过不传。</p><p>这意味着即使是同一个文件，哪怕只改了中间一小段，<code>rsync</code> 也只传那一小段变化的数据，不会把整个文件重传一遍。目录同步同理：目录里没变的文件直接跳过，只有新增、删除、内容变化的文件才会被处理。这是它比 <code>cp</code>&#x2F;<code>scp</code> 快的根本原因，不是”压缩得更好”这类表面优化。</p><h2 id="最常用的参数组合：-avz"><a href="#最常用的参数组合：-avz" class="headerlink" title="最常用的参数组合：-avz"></a>最常用的参数组合：-avz</h2><p>日常用 <code>rsync</code> 基本都是这个组合起手：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">rsync -avz ./local_dir/ user@host:/remote/path/</span><br><span class="line"><span class="comment">#     -a：archive，归档模式，等价于同时打开 -rlptgoD 这几个选项——</span></span><br><span class="line"><span class="comment">#         递归子目录、保留符号链接、保留权限、保留修改时间、保留属组属主、保留设备文件和特殊文件</span></span><br><span class="line"><span class="comment">#         简单说就是&quot;尽量把文件的原始状态完整保留&quot;，日常同步基本都要带上</span></span><br><span class="line"><span class="comment">#     -v：verbose，显示每个被传输的文件名，方便确认到底同步了什么</span></span><br><span class="line"><span class="comment">#     -z：compress，传输过程中压缩数据，跨网络传输时能省带宽；本地磁盘之间同步不需要这个</span></span><br></pre></td></tr></table></figure><p><code>-a</code> 是这几个参数里最关键的一个：不加 <code>-a</code> 的话，<code>rsync</code> 默认不会递归进子目录，也不会保留权限和时间戳这些元信息，同步出来的结果跟预期会差很多。</p><h2 id="P：进度条和断点续传"><a href="#P：进度条和断点续传" class="headerlink" title="-P：进度条和断点续传"></a>-P：进度条和断点续传</h2><p>同步大文件的时候，光有 <code>-avz</code> 还不够——看不到进度，网络一断就得从头再来：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">rsync -avzP bigfile.iso user@host:/remote/path/</span><br><span class="line"><span class="comment">#     -P：--partial 和 --progress 两个选项的简写</span></span><br><span class="line"><span class="comment">#     --progress：实时显示当前文件的传输进度（速度、百分比、预计剩余时间）</span></span><br><span class="line"><span class="comment">#     --partial：传输中断时保留已经传完的部分，下次续传时接着传，不用从头开始</span></span><br></pre></td></tr></table></figure><p><code>-P</code> 对大文件、不稳定的网络环境特别有用——没有 <code>--partial</code> 的话，传到一半中断，之前传的部分会被删掉，下次又得从零开始。</p><h2 id="–delete：让目标和源完全一致"><a href="#–delete：让目标和源完全一致" class="headerlink" title="–delete：让目标和源完全一致"></a>–delete：让目标和源完全一致</h2><p>前面的同步只会”新增&#x2F;更新”文件，源目录里删掉的文件不会影响目标目录——目标那边的旧文件会一直留着。想让目标和源保持完全一致（源删了，目标也跟着删），要加 <code>--delete</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rsync -avz --delete ./local_dir/ user@host:/remote/path/</span><br><span class="line"><span class="comment">#     --delete：目标目录里凡是源目录没有的文件，都会被删除</span></span><br><span class="line"><span class="comment">#     这是个危险参数——用反了方向（源和目标搞反），可能会把不该删的文件删掉</span></span><br></pre></td></tr></table></figure><p><code>--delete</code> 属于”威力大但容易出事”的参数，用之前一定要先确认源和目标的方向没搞反。</p><h2 id="–dry-run：先演练一遍，别直接动手"><a href="#–dry-run：先演练一遍，别直接动手" class="headerlink" title="–dry-run：先演练一遍，别直接动手"></a>–dry-run：先演练一遍，别直接动手</h2><p>尤其是带了 <code>--delete</code> 这种会删除文件的操作，实际执行前应该先跑一遍演练模式，只看会发生什么，不真的执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rsync -avz --delete --dry-run ./local_dir/ user@host:/remote/path/</span><br><span class="line"><span class="comment">#     --dry-run（也可以用简写 -n）：只打印会做的操作，不实际传输或删除任何文件</span></span><br><span class="line"><span class="comment">#     确认输出的文件列表符合预期之后，去掉 --dry-run 再跑一次真正执行</span></span><br></pre></td></tr></table></figure><p>只要命令里带了 <code>--delete</code>，先 <code>--dry-run</code> 看一眼输出，是能避免”手滑删错文件”最简单也最有效的办法。</p><h2 id="排除规则：跳过不想同步的文件"><a href="#排除规则：跳过不想同步的文件" class="headerlink" title="排除规则：跳过不想同步的文件"></a>排除规则：跳过不想同步的文件</h2><p>项目目录里的 <code>.git</code>、<code>node_modules</code> 这类文件，同步时通常不需要带上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">rsync -avz --exclude=<span class="string">&#x27;.git&#x27;</span> --exclude=<span class="string">&#x27;node_modules&#x27;</span> ./project/ user@host:/remote/project/</span><br><span class="line"><span class="comment">#     --exclude：排除匹配指定模式的文件或目录，可以写多次排除多种内容</span></span><br><span class="line"></span><br><span class="line">rsync -avz --exclude-from=<span class="string">&#x27;exclude-list.txt&#x27;</span> ./project/ user@host:/remote/project/</span><br><span class="line"><span class="comment">#     --exclude-from：从文件里批量读取排除规则，规则多的时候比堆一串 --exclude 清晰</span></span><br><span class="line"><span class="comment">#     exclude-list.txt 里每行写一条规则，比如：</span></span><br><span class="line"><span class="comment">#     .git</span></span><br><span class="line"><span class="comment">#     node_modules</span></span><br><span class="line"><span class="comment">#     *.log</span></span><br></pre></td></tr></table></figure><h2 id="远程同步和一个经典的坑：源目录带不带末尾斜杠"><a href="#远程同步和一个经典的坑：源目录带不带末尾斜杠" class="headerlink" title="远程同步和一个经典的坑：源目录带不带末尾斜杠"></a>远程同步和一个经典的坑：源目录带不带末尾斜杠</h2><p><code>rsync</code> 同步远程主机走的是 SSH 通道，语法跟 <code>scp</code> 类似但功能强得多：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rsync -avz ./local_dir/ user@host:/remote/path/</span><br><span class="line"><span class="comment">#     基于 SSH 传输，需要目标主机开着 SSH 服务、当前用户有权限登录</span></span><br><span class="line"><span class="comment">#     跟 scp 语法接近，但增量传输、断点续传这些能力 scp 都没有</span></span><br></pre></td></tr></table></figure><p>源目录路径末尾带不带 <code>/</code>，行为是不一样的——这是 <code>rsync</code> 最容易踩的坑，官方手册专门用例子说明过：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">rsync -av /src/foo /dest</span><br><span class="line"><span class="comment">#     源路径不带末尾斜杠：把 foo 这个目录本身同步过去</span></span><br><span class="line"><span class="comment">#     结果是 /dest/foo/ 里存放 foo 目录的内容——多出了一层 foo 目录</span></span><br><span class="line"></span><br><span class="line">rsync -av /src/foo/ /dest/foo</span><br><span class="line"><span class="comment">#     源路径带末尾斜杠：表示&quot;同步这个目录里的内容&quot;，不是目录本身</span></span><br><span class="line"><span class="comment">#     这条命令和上面那条命令，最终产生的结果是一样的——都是 /dest/foo/ 里存放 foo 的内容</span></span><br><span class="line"><span class="comment">#     区别在于目标路径要不要手动多写一层 foo</span></span><br></pre></td></tr></table></figure><p>简单记忆：源路径末尾的 <code>/</code> 决定”要不要在目标里多包一层目录”——同步目录内容用 <code>foo/</code>，同步目录本身（连同目录名）用 <code>foo</code>（不带斜杠），目标那边对应地要不要手动加上这层目录名。搞混这个经常导致同步出来的目录结构和预期差一层，排查起来莫名其妙。</p><h2 id="rsync-常用参数速查表"><a href="#rsync-常用参数速查表" class="headerlink" title="rsync 常用参数速查表"></a>rsync 常用参数速查表</h2><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-a</code></td><td>archive，归档模式，等价于 <code>-rlptgoD</code>：递归子目录、保留符号链接&#x2F;权限&#x2F;时间戳&#x2F;属组属主&#x2F;设备文件</td></tr><tr><td><code>-v</code></td><td>verbose，显示每个被传输的文件名</td></tr><tr><td><code>-z</code></td><td>compress，传输时压缩数据，跨网络同步能省带宽，本地同步不需要</td></tr><tr><td><code>-P</code></td><td><code>--partial --progress</code> 的简写，显示传输进度并支持断点续传</td></tr><tr><td><code>--delete</code></td><td>让目标和源完全一致，删除目标里源没有的文件——危险参数，用前确认方向没搞反</td></tr><tr><td><code>--dry-run</code> &#x2F; <code>-n</code></td><td>演练模式，只打印会执行的操作，不真的传输或删除任何文件</td></tr><tr><td><code>--exclude</code></td><td>排除匹配指定模式的文件或目录，可以写多次</td></tr><tr><td><code>--exclude-from</code></td><td>从文件批量读取排除规则，规则多时比堆一串 <code>--exclude</code> 清晰</td></tr></tbody></table><h2 id="写到这里"><a href="#写到这里" class="headerlink" title="写到这里"></a>写到这里</h2><p><code>rsync</code> 的核心就是这几件事：增量传输省时间、<code>-P</code> 应付大文件和不稳定网络、<code>--delete</code> 配 <code>--dry-run</code> 保证安全、<code>--exclude</code> 跳过不需要的内容。这几个参数组合起来，日常的目录同步和轻量级备份基本够用——更复杂的定时增量备份场景，通常是在这套参数基础上配合 cron 或者专门的备份工具，但底层同步逻辑还是这些。</p>]]>
    </content>
    <id>https://blog.codewater.top/rsync%E5%A2%9E%E9%87%8F%E5%90%8C%E6%AD%A5%E5%AE%9E%E6%88%98-%E5%B8%B8%E7%94%A8%E5%8F%82%E6%95%B0%E4%B8%8E%E8%BF%9C%E7%A8%8B%E5%90%8C%E6%AD%A5/</id>
    <link href="https://blog.codewater.top/rsync%E5%A2%9E%E9%87%8F%E5%90%8C%E6%AD%A5%E5%AE%9E%E6%88%98-%E5%B8%B8%E7%94%A8%E5%8F%82%E6%95%B0%E4%B8%8E%E8%BF%9C%E7%A8%8B%E5%90%8C%E6%AD%A5/"/>
    <published>2026-07-13T12:35:28.000Z</published>
    <summary>rsync 为什么比 scp/cp 快：增量同步原理、-avz/-P/--delete 参数与远程同步实战。</summary>
    <title>rsync 增量同步实战：常用参数、排除规则与远程同步</title>
    <updated>2026-07-13T12:35:28.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="Emacs" scheme="https://blog.codewater.top/tags/Emacs/"/>
    <category term="readline" scheme="https://blog.codewater.top/tags/readline/"/>
    <content>
      <![CDATA[<p>命令敲到一半打错了个字符，很多人的反应是按住 <code>←</code> 挪半天光标，或者干脆全删了重打。其实 bash、zsh 这些 shell 默认就内置了一整套 Emacs 风格的编辑快捷键，光标跳词、整段删除、剪切粘贴全都有对应按键，不需要任何配置就能用。</p><p>这篇讲清楚这套快捷键怎么用，外加一个进阶技巧：命令太长太复杂的时候，直接把它甩进编辑器里改。</p><span id="more"></span><p>这是终端效率小系列第三篇，承接 <a href="/%E4%B8%8D%E7%86%9F%E6%82%89%E7%9A%84%E5%91%BD%E4%BB%A4%E6%80%8E%E4%B9%88%E5%BF%AB%E9%80%9F%E6%90%9E%E6%87%82-man%E9%A1%B5%E9%9D%A2%E7%BB%93%E6%9E%84%E4%B8%8Eapropos%E9%80%9F%E6%9F%A5">不熟悉的命令怎么快速搞懂</a> 和 <a href="/vim%E5%9F%BA%E6%9C%AC%E6%93%8D%E4%BD%9C-%E6%A8%A1%E5%BC%8F%E5%88%87%E6%8D%A2%E5%88%B0%E4%BF%9D%E5%AD%98%E9%80%80%E5%87%BA">vim 基本操作</a>——本篇最后一个技巧会直接用到上一篇学的 vim 保存退出操作。</p><h2 id="为什么这套快捷键几乎所有-shell-都能用"><a href="#为什么这套快捷键几乎所有-shell-都能用" class="headerlink" title="为什么这套快捷键几乎所有 shell 都能用"></a>为什么这套快捷键几乎所有 shell 都能用</h2><p>命令行的编辑功能，bash 靠的是 GNU Readline 库，zsh 靠的是自己的 ZLE（Zsh Line Editor）。两者设计上的默认编辑模式都是 Emacs 风格——但这里有个很容易踩的坑：<strong>zsh 有条隐藏规则，只要 <code>$EDITOR</code> 或 <code>$VISUAL</code> 的值里包含字符串 “vi”（<code>vim</code>、<code>nvim</code> 都算在内），zsh 启动时就会自动切换成 vi 键位，而不是 Emacs 键位</strong>。开发者把 <code>$EDITOR</code> 设成 <code>vim</code> 或 <code>nvim</code> 是很常见的习惯，这种情况下 zsh 默认给的其实是 vi 键位，本文这些 Emacs 快捷键很可能大部分不生效或者行为完全对不上。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="variable">$EDITOR</span></span><br><span class="line"><span class="comment">#     先看一眼这个变量的值，只要包含 &quot;vi&quot;（vim、nvim 都算），zsh 大概率已经是 vi 键位了</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">bindkey</span> -e</span><br><span class="line"><span class="comment">#     写进 ~/.zshrc，强制 zsh 使用 Emacs 键位，不再受 $EDITOR 内容影响</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">set</span> -o vi</span><br><span class="line"><span class="comment">#     bash 里想切换成 vi 风格的编辑模式（用 hjkl 那一套）用这个</span></span><br><span class="line"><span class="comment">#     但切过去之后，本文这些 Emacs 快捷键大部分失效，两套模式二选一</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">set</span> -o emacs</span><br><span class="line"><span class="comment">#     切回 Emacs 模式，bash 默认就是这个，不受 $EDITOR 影响——这点和 zsh 不一样</span></span><br></pre></td></tr></table></figure><p>不确定自己当前是哪种键位，直接试一下 <code>Ctrl-A</code> 能不能跳到行首：能跳就是 Emacs 键位；没反应或者屏幕上蹦出一个奇怪符号，大概率是 vi 键位——vi 键位下得先按 <code>Esc</code> 进命令模式，<code>0</code> 才是跳到行首。</p><h2 id="行内光标移动：不用方向键也能秒跳"><a href="#行内光标移动：不用方向键也能秒跳" class="headerlink" title="行内光标移动：不用方向键也能秒跳"></a>行内光标移动：不用方向键也能秒跳</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Ctrl-A</span><br><span class="line"><span class="comment">#     跳到行首（A = ahead / 行的开头）</span></span><br><span class="line"></span><br><span class="line">Ctrl-E</span><br><span class="line"><span class="comment">#     跳到行尾（E = end）</span></span><br><span class="line"></span><br><span class="line">Alt-B</span><br><span class="line"><span class="comment">#     向左跳一个单词（B = back）</span></span><br><span class="line"></span><br><span class="line">Alt-F</span><br><span class="line"><span class="comment">#     向右跳一个单词（F = forward）</span></span><br></pre></td></tr></table></figure><p><code>Alt-B</code>&#x2F;<code>Alt-F</code> 在部分终端里按不出效果——很多终端默认不把 <code>Alt</code> 当 Meta 键处理，需要在终端设置里打开”Use Option as Meta Key”（macOS Terminal.app 的选项，iTerm2 类似）。打不开设置的话，用 <code>Esc</code> 代替 <code>Alt</code> 也一样：先按 <code>Esc</code> 松开，再按 <code>b</code> 或 <code>f</code>，效果等价于 <code>Alt-B</code>&#x2F;<code>Alt-F</code>。</p><h2 id="删除和剪切：Ctrl-W-U-K-存进-kill-ring，Ctrl-Y-取回"><a href="#删除和剪切：Ctrl-W-U-K-存进-kill-ring，Ctrl-Y-取回" class="headerlink" title="删除和剪切：Ctrl-W&#x2F;U&#x2F;K 存进 kill-ring，Ctrl-Y 取回"></a>删除和剪切：Ctrl-W&#x2F;U&#x2F;K 存进 kill-ring，Ctrl-Y 取回</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Ctrl-W</span><br><span class="line"><span class="comment">#     删除光标前面的一个单词（向后删）</span></span><br><span class="line"></span><br><span class="line">Alt-D</span><br><span class="line"><span class="comment">#     删除光标后面的一个单词（向前删）——Ctrl-W 的反方向</span></span><br><span class="line"></span><br><span class="line">Ctrl-K</span><br><span class="line"><span class="comment">#     删除从光标到行尾的所有内容</span></span><br><span class="line"></span><br><span class="line">Ctrl-Y</span><br><span class="line"><span class="comment">#     粘贴——把上一次删除的内容粘贴回来（yank）</span></span><br></pre></td></tr></table></figure><p><code>Ctrl-U</code> 单独拎出来说，因为它在 bash 和 zsh 里行为不一样，是个真实存在的坑：bash 的 <code>Ctrl-U</code>（Readline 的 <code>unix-line-discard</code>）只删从光标到行首的部分，光标后面的内容原样保留；zsh 的 <code>Ctrl-U</code>（不管 vi 键位还是 Emacs 键位）绑定的都是 <code>kill-whole-line</code>，不管光标停在哪，直接清空整行。同一个按键，bash 和 zsh 下的效果是不同的，用 zsh 的人如果照着 bash 的经验去理解 <code>Ctrl-U</code>，会觉得”怎么光标后面的内容也被删了”。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Ctrl-U</span><br><span class="line"><span class="comment">#     bash：删除从光标到行首的内容，光标后面的保留</span></span><br><span class="line"><span class="comment">#     zsh：不管光标在哪，直接清空整行——这是 zsh 的 kill-whole-line，跟 bash 不是一回事</span></span><br></pre></td></tr></table></figure><p>这几个删除命令背后有个共同点：删掉的内容不是直接消失，而是存进一个叫 kill-ring 的剪贴区，<code>Ctrl-Y</code> 粘贴的就是这里最新的一条。</p><h2 id="清屏和历史命令反向搜索"><a href="#清屏和历史命令反向搜索" class="headerlink" title="清屏和历史命令反向搜索"></a>清屏和历史命令反向搜索</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Ctrl-L</span><br><span class="line"><span class="comment">#     清屏，光标当前这一行会被挪到清空后的屏幕顶部，命令内容不受影响</span></span><br><span class="line"></span><br><span class="line">Ctrl-R</span><br><span class="line"><span class="comment">#     反向搜索历史命令，输入关键词实时匹配之前敲过的命令</span></span><br><span class="line"><span class="comment">#     再按一次 Ctrl-R 跳到更早的一条匹配，按 Enter 执行，按 Esc 只取用不执行</span></span><br></pre></td></tr></table></figure><p><code>Ctrl-R</code> 是历史命令搜索里最常用的一个，比按 <code>↑</code> 翻半天历史记录快得多——尤其是敲过成百上千条命令之后，翻方向键基本翻不到想要的那条。</p><h2 id="进阶技巧：把命令行甩进编辑器里改"><a href="#进阶技巧：把命令行甩进编辑器里改" class="headerlink" title="进阶技巧：把命令行甩进编辑器里改"></a>进阶技巧：把命令行甩进编辑器里改</h2><p><code>Ctrl-A</code>&#x2F;<code>Ctrl-E</code> 这类单字符编辑对付一行命令够用，但遇到又长又复杂的命令——比如带了好几个换行的多行脚本片段，或者一长串 JSON——在命令行里用方向键和退格键改，效率很低。这种场景该用 <code>Ctrl-X Ctrl-E</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Ctrl-X Ctrl-E</span><br><span class="line"><span class="comment">#     把当前命令行的内容甩进编辑器，用完整的编辑器能力改</span></span><br><span class="line"><span class="comment">#     改完保存退出，编辑结果自动回填到命令行——不会直接执行，只是替换命令行内容</span></span><br></pre></td></tr></table></figure><p>用哪个编辑器由 <code>$VISUAL</code>、<code>$EDITOR</code> 这两个环境变量决定，Bash 官方文档写得很明确：依次尝试 <code>$VISUAL</code>、<code>$EDITOR</code>，两个都没设置的话，最后退回 <code>emacs</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> EDITOR=vim</span><br><span class="line"><span class="comment">#     把默认编辑器设成 vim，Ctrl-X Ctrl-E 打开的就是 vim</span></span><br><span class="line"><span class="comment">#     没设置这个变量时，bash 会依次尝试 $VISUAL、$EDITOR，都没有就用 emacs</span></span><br></pre></td></tr></table></figure><p>进了编辑器之后，改完用上一篇学的 <code>:wq</code> 或 <code>:x</code> 保存退出（这正是本文系列第二篇的用武之地），命令行会自动变成编辑后的内容，回车才会真正执行。</p><p>有个地方要注意：这个功能在 bash 里是 Readline 自带的，默认已经绑定在 <code>Ctrl-X Ctrl-E</code> 上，装好 bash 就能用；但 zsh 不一样，ZLE 默认编辑模式虽然也是 Emacs，这个”甩进编辑器”的功能却不是默认绑定的，得在 <code>~/.zshrc</code> 里手动开启：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">autoload</span> -Uz edit-command-line</span><br><span class="line"><span class="comment">#     加载 zsh 自带的 edit-command-line 功能模块</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">zle</span> -N edit-command-line</span><br><span class="line"><span class="comment">#     把它注册成一个 ZLE 可用的编辑命令</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">bindkey</span> <span class="string">&#x27;^x^e&#x27;</span> edit-command-line</span><br><span class="line"><span class="comment">#     绑定到 Ctrl-X Ctrl-E，跟 bash 保持一致的按键习惯</span></span><br></pre></td></tr></table></figure><p>写完存好 <code>.zshrc</code>，<code>source ~/.zshrc</code> 或者重开一个终端窗口生效。</p><h2 id="vim-nvim-跳转到指定行"><a href="#vim-nvim-跳转到指定行" class="headerlink" title="vim&#x2F;nvim 跳转到指定行"></a>vim&#x2F;nvim 跳转到指定行</h2><p>编辑长文件时，与其打开后再一步步找到目标行，不如打开的时候就直接带上跳转目标：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">vim +42 access.log</span><br><span class="line"><span class="comment">#     打开文件后直接跳到第 42 行，光标停在那一行</span></span><br><span class="line"></span><br><span class="line">vim +/ERROR access.log</span><br><span class="line"><span class="comment">#     打开文件后跳到第一个匹配 &quot;ERROR&quot; 的位置，等价于打开后手动 /ERROR 回车</span></span><br></pre></td></tr></table></figure><p><code>+42</code> 是 <code>+:42</code> 的简写形式——vim 允许在命令行用 <code>+&#123;Ex命令&#125;</code> 指定打开文件后自动执行的命令，<code>42</code> 本身就是一个合法的行号跳转 Ex 命令；<code>+/pattern</code> 同理，等价于打开后立刻执行一次 <code>/pattern</code> 搜索。</p><p>如果已经在 vim 里面了，跳转用 <code>:42</code> 或者 <code>42G</code>——后者就是<a href="/vim%E5%9F%BA%E6%9C%AC%E6%93%8D%E4%BD%9C-%E6%A8%A1%E5%BC%8F%E5%88%87%E6%8D%A2%E5%88%B0%E4%BF%9D%E5%AD%98%E9%80%80%E5%87%BA">上一篇 vim 文章</a>里提到的 <code>G</code> 命令，前面加数字表示跳到第几行，和 <code>gg</code>&#x2F;<code>G</code> 是同一套逻辑的延伸。</p><h2 id="终端效率小系列：写到这里"><a href="#终端效率小系列：写到这里" class="headerlink" title="终端效率小系列：写到这里"></a>终端效率小系列：写到这里</h2><p>三篇写下来，从 <a href="/%E4%B8%8D%E7%86%9F%E6%82%89%E7%9A%84%E5%91%BD%E4%BB%A4%E6%80%8E%E4%B9%88%E5%BF%AB%E9%80%9F%E6%90%9E%E6%87%82-man%E9%A1%B5%E9%9D%A2%E7%BB%93%E6%9E%84%E4%B8%8Eapropos%E9%80%9F%E6%9F%A5">不熟悉的命令怎么快速搞懂</a>，到 <a href="/vim%E5%9F%BA%E6%9C%AC%E6%93%8D%E4%BD%9C-%E6%A8%A1%E5%BC%8F%E5%88%87%E6%8D%A2%E5%88%B0%E4%BF%9D%E5%AD%98%E9%80%80%E5%87%BA">vim 的基本操作</a>，再到这篇 Emacs 风格的命令行编辑——串起来就是一套完整的终端使用习惯：查文档、编辑文件、编辑命令行，三件事都能用最少的按键完成。剩下的就是练：这些快捷键只有敲进肌肉记忆之后才会真正提速，光看不练效果有限。</p>]]>
    </content>
    <id>https://blog.codewater.top/%E5%91%BD%E4%BB%A4%E8%A1%8C%E7%BC%96%E8%BE%91%E8%BF%9B%E9%98%B6-Emacs%E5%BF%AB%E6%8D%B7%E9%94%AE%E4%B8%8E%E7%94%A9%E8%BF%9Bvim%E7%BC%96%E8%BE%91/</id>
    <link href="https://blog.codewater.top/%E5%91%BD%E4%BB%A4%E8%A1%8C%E7%BC%96%E8%BE%91%E8%BF%9B%E9%98%B6-Emacs%E5%BF%AB%E6%8D%B7%E9%94%AE%E4%B8%8E%E7%94%A9%E8%BF%9Bvim%E7%BC%96%E8%BE%91/"/>
    <published>2026-07-12T16:30:02.000Z</published>
    <summary>bash/zsh 默认是 Emacs 键位：Ctrl-A/E/W/U/K/Y 怎么用、Ctrl-X Ctrl-E 甩进编辑器改命令，附 vim 跳转指定行技巧。</summary>
    <title>命令行编辑进阶：Emacs 快捷键、甩进 vim 编辑命令行与跳转到指定行</title>
    <updated>2026-07-12T16:30:02.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="vim" scheme="https://blog.codewater.top/tags/vim/"/>
    <content>
      <![CDATA[<p>“怎么退出 vim”是程序员圈子里流传最广的梗之一——一个编辑器要是连怎么关掉都成了笑话，说明它一定有什么地方和你以为的不一样。</p><p>这个”不一样”，就是 vim 最核心的概念：模式。搞懂这一件事，剩下的操作都顺理成章。</p><span id="more"></span><p>这是终端效率小系列第二篇，承接 <a href="/%E4%B8%8D%E7%86%9F%E6%82%89%E7%9A%84%E5%91%BD%E4%BB%A4%E6%80%8E%E4%B9%88%E5%BF%AB%E9%80%9F%E6%90%9E%E6%87%82-man%E9%A1%B5%E9%9D%A2%E7%BB%93%E6%9E%84%E4%B8%8Eapropos%E9%80%9F%E6%9F%A5">不熟悉的命令怎么快速搞懂：man 页面结构、apropos 搜索与 –help 速查</a>。</p><h2 id="vim-最核心的概念：模式"><a href="#vim-最核心的概念：模式" class="headerlink" title="vim 最核心的概念：模式"></a>vim 最核心的概念：模式</h2><p>大部分编辑器打开就能直接打字。vim 不是——它默认打开的是 <strong>Normal 模式</strong>（也叫命令模式），这时候敲键盘不是在输入文字，是在下命令。想打字，得先按 <code>i</code> 切换到 <strong>Insert 模式</strong>；打完字想回去下命令，按 <code>Esc</code> 切回 Normal 模式。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">打开 vim → 默认在 Normal 模式（敲字母是命令）</span><br><span class="line">按 i     → 切到 Insert 模式（可以打字了）</span><br><span class="line">按 Esc   → 切回 Normal 模式（打字模式关闭，字母重新变成命令）</span><br></pre></td></tr></table></figure><p>新手最常见的翻车场景就是这个：不知道自己在 Normal 模式，对着屏幕一通乱按，结果文件被改得面目全非——因为那些字母全被当成了命令，不是文字。记住一条：<strong>不确定自己在哪个模式，先按 <code>Esc</code>，回到 Normal 模式再说</strong>，这是最安全的复位动作。</p><h2 id="先学怎么退出"><a href="#先学怎么退出" class="headerlink" title="先学怎么退出"></a>先学怎么退出</h2><p>呼应开头的梗，这个必须先讲：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">:q</span><br><span class="line"><span class="comment">#     退出。文件没有改动过才能用，改过没保存会报错拒绝退出</span></span><br><span class="line"></span><br><span class="line">:q!</span><br><span class="line"><span class="comment">#     强制退出，不保存任何改动——改错了想放弃这次编辑，用这个</span></span><br><span class="line"></span><br><span class="line">:w</span><br><span class="line"><span class="comment">#     只保存，不退出，继续编辑</span></span><br><span class="line"></span><br><span class="line">:wq</span><br><span class="line"><span class="comment">#     保存并退出，不管文件有没有改动过，都会写入磁盘</span></span><br><span class="line"></span><br><span class="line">:x</span><br><span class="line"><span class="comment">#     也是保存并退出，但只在文件真的被改动过时才写入磁盘；</span></span><br><span class="line"><span class="comment">#     如果没有改动，:x 不会碰这个文件，文件的修改时间也不会变</span></span><br><span class="line"><span class="comment">#     日常用 :wq 和 :x 效果几乎一样，区别只在&quot;文件没改动时要不要触碰修改时间&quot;这种边缘场景</span></span><br></pre></td></tr></table></figure><p>所有这些命令都以 <code>:</code> 开头，输入前记得先确认自己在 Normal 模式（如果还在 Insert 模式，先按 <code>Esc</code>）。</p><h2 id="Normal-模式下移动光标"><a href="#Normal-模式下移动光标" class="headerlink" title="Normal 模式下移动光标"></a>Normal 模式下移动光标</h2><p>vim 不太依赖方向键，用几个字母键代替（键盘老式布局上这几个键离主键位更近，不用挪手，很多人这么解释这个设计，具体历史已经不可考）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">h j k l</span><br><span class="line"><span class="comment">#     依次是 左 下 上 右，代替方向键</span></span><br><span class="line"><span class="comment">#     记不住就想 j 像个往下的箭头，k 在它上面对应往上</span></span><br><span class="line"></span><br><span class="line">w</span><br><span class="line"><span class="comment">#     跳到下一个单词的开头</span></span><br><span class="line"></span><br><span class="line">b</span><br><span class="line"><span class="comment">#     跳到上一个单词的开头（back）</span></span><br><span class="line"></span><br><span class="line">e</span><br><span class="line"><span class="comment">#     跳到当前/下一个单词的结尾</span></span><br><span class="line"></span><br><span class="line">0</span><br><span class="line"><span class="comment">#     跳到当前行的行首</span></span><br><span class="line"></span><br><span class="line">$</span><br><span class="line"><span class="comment">#     跳到当前行的行尾</span></span><br><span class="line"></span><br><span class="line">gg</span><br><span class="line"><span class="comment">#     跳到文件开头</span></span><br><span class="line"></span><br><span class="line">G</span><br><span class="line"><span class="comment">#     跳到文件结尾</span></span><br></pre></td></tr></table></figure><p>前面加数字表示重复次数：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">5j</span><br><span class="line"><span class="comment">#     往下移动 5 行，比按 5 次 j 快</span></span><br><span class="line"></span><br><span class="line">3w</span><br><span class="line"><span class="comment">#     往后跳 3 个单词</span></span><br></pre></td></tr></table></figure><h2 id="行内跳转：按字符查找与括号匹配"><a href="#行内跳转：按字符查找与括号匹配" class="headerlink" title="行内跳转：按字符查找与括号匹配"></a>行内跳转：按字符查找与括号匹配</h2><p><code>hjkl</code>&#x2F;<code>w</code>&#x2F;<code>b</code>&#x2F;<code>e</code> 这些是逐字符或逐单词移动，遇到”跳到这一行某个具体符号上”的场景（比如跳到某个逗号、跳到配对的括号），还有更快的办法：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">f,</span><br><span class="line"><span class="comment">#     向右查找光标所在行里下一个 &quot;,&quot; 字符，光标直接停在它上面（find）</span></span><br><span class="line"></span><br><span class="line">t,</span><br><span class="line"><span class="comment">#     向右查找下一个 &quot;,&quot; 字符，光标停在它前面一位（till，&quot;直到&quot;但不包含）</span></span><br><span class="line"></span><br><span class="line">F,</span><br><span class="line"><span class="comment">#     向左查找上一个 &quot;,&quot; 字符，光标停在它上面</span></span><br><span class="line"></span><br><span class="line">T,</span><br><span class="line"><span class="comment">#     向左查找上一个 &quot;,&quot; 字符，光标停在它后面一位</span></span><br><span class="line"></span><br><span class="line">;</span><br><span class="line"><span class="comment">#     重复上一次 f/t/F/T 查找，跳到下一个匹配，不用重新敲一次 f 加字符</span></span><br><span class="line"></span><br><span class="line">%</span><br><span class="line"><span class="comment">#     跳到光标所在括号 ( &#123; [ 的配对括号上，来回按 % 可以在一对括号之间横跳</span></span><br></pre></td></tr></table></figure><p>一行很长的代码里要定位到某个符号，<code>f</code> 加上那个字符，比一个字符一个字符挪快得多；改代码时经常要确认括号有没有配对齐，把光标放在括号上按 <code>%</code> 直接跳到另一半，比肉眼数括号可靠。</p><h2 id="Normal-模式下编辑文本"><a href="#Normal-模式下编辑文本" class="headerlink" title="Normal 模式下编辑文本"></a>Normal 模式下编辑文本</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">i</span><br><span class="line"><span class="comment">#     在光标当前位置之前插入，进入 Insert 模式</span></span><br><span class="line"></span><br><span class="line">a</span><br><span class="line"><span class="comment">#     在光标当前位置之后插入（append），进入 Insert 模式</span></span><br><span class="line"></span><br><span class="line">o</span><br><span class="line"><span class="comment">#     在当前行下面新起一行，进入 Insert 模式</span></span><br><span class="line"></span><br><span class="line">O</span><br><span class="line"><span class="comment">#     在当前行上面新起一行，进入 Insert 模式</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">dd</span></span><br><span class="line"><span class="comment">#     删除光标所在的整行（同时会把这行内容存到剪贴板，等于剪切）</span></span><br><span class="line"></span><br><span class="line">yy</span><br><span class="line"><span class="comment">#     复制光标所在的整行（yank，不删除）</span></span><br><span class="line"></span><br><span class="line">p</span><br><span class="line"><span class="comment">#     粘贴——把上一次 dd 或 yy 存下来的内容，粘贴到光标下面</span></span><br><span class="line"></span><br><span class="line">u</span><br><span class="line"><span class="comment">#     撤销上一步操作</span></span><br><span class="line"></span><br><span class="line">Ctrl-r</span><br><span class="line"><span class="comment">#     重做（撤销&quot;撤销&quot;）</span></span><br><span class="line"></span><br><span class="line">x</span><br><span class="line"><span class="comment">#     删除光标所在的这一个字符——精简版的 dd，只删一个字符不是一整行</span></span><br><span class="line"></span><br><span class="line">r字符</span><br><span class="line"><span class="comment">#     把光标所在字符替换成指定字符，替换完自动回到 Normal 模式，不用再按 Esc</span></span><br><span class="line"><span class="comment">#     比如光标停在一个打错的字母上，按 rx 就把它换成 x</span></span><br><span class="line"></span><br><span class="line">cw</span><br><span class="line"><span class="comment">#     修改一个单词——删除光标到当前单词结尾的内容，直接进入 Insert 模式等着输入新内容</span></span><br><span class="line"><span class="comment">#     等于先 dw 再 i，但 cw 是更常用的固定搭配，一步到位</span></span><br><span class="line"></span><br><span class="line">.</span><br><span class="line"><span class="comment">#     重复上一次的修改操作——改完一处后，把光标移到下一个类似的地方按 .，</span></span><br><span class="line"><span class="comment">#     不用重新敲一遍命令，改多处同样的错误时特别省事</span></span><br></pre></td></tr></table></figure><p><code>i</code>&#x2F;<code>a</code>&#x2F;<code>o</code>&#x2F;<code>O</code> 这四个进 Insert 模式的方式，区别就在光标最终停在哪：<code>i</code> 停在原地之前，<code>a</code> 停在原地之后，<code>o</code>&#x2F;<code>O</code> 分别在下面&#x2F;上面新开一行。</p><h2 id="Visual-模式：选中一段范围再操作"><a href="#Visual-模式：选中一段范围再操作" class="headerlink" title="Visual 模式：选中一段范围再操作"></a>Visual 模式：选中一段范围再操作</h2><p>前面的编辑命令大多是”整行”或”一个单词”级别，想自己框选一段范围再操作，要用 Visual（可视）模式。进入方式有三种，选中范围的方式不一样：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">v</span><br><span class="line"><span class="comment">#     进入字符可视模式，逐字符选中——像鼠标拖选一样，选中范围不受行/单词边界限制</span></span><br><span class="line"></span><br><span class="line">V</span><br><span class="line"><span class="comment">#     进入行可视模式，整行整行地选，光标上下移动就是按行扩大选区</span></span><br><span class="line"></span><br><span class="line">Ctrl-v</span><br><span class="line"><span class="comment">#     进入块可视模式，选出一个矩形范围，常用来同时给多行的相同列位置加字符</span></span><br></pre></td></tr></table></figure><p>选中范围之后，跟前面学过的编辑命令组合使用，效果作用在整个选区上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">d</span><br><span class="line"><span class="comment">#     删除选中的范围</span></span><br><span class="line"></span><br><span class="line">y</span><br><span class="line"><span class="comment">#     复制选中的范围（yank）</span></span><br><span class="line"></span><br><span class="line">c</span><br><span class="line"><span class="comment">#     删除选中的范围，直接进入 Insert 模式，改成新内容</span></span><br></pre></td></tr></table></figure><p>举例：用 <code>V</code> 选中三行按 <code>d</code>，一次删掉三行；用 <code>v</code> 选中一段文字（不是整行）按 <code>y</code>，只复制这一小段。选错了想取消，按 <code>Esc</code> 退出 Visual 模式，不会做任何改动。</p><h2 id="查找和替换"><a href="#查找和替换" class="headerlink" title="查找和替换"></a>查找和替换</h2><p>搜索用 <code>/</code>，和<a href="/%E4%B8%8D%E7%86%9F%E6%82%89%E7%9A%84%E5%91%BD%E4%BB%A4%E6%80%8E%E4%B9%88%E5%BF%AB%E9%80%9F%E6%90%9E%E6%87%82-man%E9%A1%B5%E9%9D%A2%E7%BB%93%E6%9E%84%E4%B8%8Eapropos%E9%80%9F%E6%9F%A5">上一篇讲 man 页面查找</a>里 <code>less</code> 的搜索快捷键是同一套逻辑——vim 的分页和搜索机制本来就深受这套习惯影响：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">/关键词</span><br><span class="line"><span class="comment">#     向下搜索，回车确认</span></span><br><span class="line"></span><br><span class="line">n</span><br><span class="line"><span class="comment">#     跳到下一个匹配</span></span><br><span class="line"></span><br><span class="line">N</span><br><span class="line"><span class="comment">#     跳到上一个匹配（大写，方向和 n 相反）</span></span><br></pre></td></tr></table></figure><p>全局替换用 <code>:%s</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">:%s/旧文本/新文本/g</span><br><span class="line"><span class="comment">#     % 表示作用范围是整个文件（不加 % 只替换当前光标所在行）</span></span><br><span class="line"><span class="comment">#     s 是 substitute，替换命令本身</span></span><br><span class="line"><span class="comment">#     g 表示每一行里所有匹配都替换，不加 g 的话每行只替换第一个匹配到的</span></span><br></pre></td></tr></table></figure><p><code>%</code> 和 <code>g</code> 这两个符号经常被忽略，结果就是”明明替换了，怎么还有没改的地方”——十有八九是漏了 <code>%</code>（只在当前行生效）或者漏了 <code>g</code>（每行只替换了第一处）。</p><h2 id="终端效率小系列：下一篇"><a href="#终端效率小系列：下一篇" class="headerlink" title="终端效率小系列：下一篇"></a>终端效率小系列：下一篇</h2><p>下一篇讲 Emacs 风格快捷键——包括一个进阶技巧：在 shell 命令行敲了一半的命令，直接甩进 vim&#x2F;nvim 里编辑，改完存盘退出，编辑结果自动回到命令行。用到的正是这篇学的基础操作。</p>]]>
    </content>
    <id>https://blog.codewater.top/vim%E5%9F%BA%E6%9C%AC%E6%93%8D%E4%BD%9C-%E6%A8%A1%E5%BC%8F%E5%88%87%E6%8D%A2%E5%88%B0%E4%BF%9D%E5%AD%98%E9%80%80%E5%87%BA/</id>
    <link href="https://blog.codewater.top/vim%E5%9F%BA%E6%9C%AC%E6%93%8D%E4%BD%9C-%E6%A8%A1%E5%BC%8F%E5%88%87%E6%8D%A2%E5%88%B0%E4%BF%9D%E5%AD%98%E9%80%80%E5%87%BA/"/>
    <published>2026-07-12T16:23:35.000Z</published>
    <summary>vim 的 Normal/Insert 模式是什么、怎么退出、hjkl 怎么移动、dd/yy/p 怎么编辑、%s 怎么全局替换，一篇学会基础操作。</summary>
    <title>vim 基本操作：从模式切换到保存退出</title>
    <updated>2026-07-12T16:23:35.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="man" scheme="https://blog.codewater.top/tags/man/"/>
    <content>
      <![CDATA[<p>写脚本、敲命令的时候，冒出一个没见过的命令，或者记得某个命令但想不起某个参数怎么写——这种场景几乎每天都会遇到。很多人第一反应是打开浏览器搜索，但终端里其实自带一整套比搜索引擎更快、更准的查法：<code>man</code>、<code>apropos</code>、<code>--help</code>，选对工具往往比记住参数本身更重要。</p><span id="more"></span><p>这是终端效率小系列的第一篇，后续还会写 vim 基本操作、Emacs 风格快捷键与进阶技巧。</p><h2 id="打开一个命令的-man-页面，看到的是什么"><a href="#打开一个命令的-man-页面，看到的是什么" class="headerlink" title="打开一个命令的 man 页面，看到的是什么"></a>打开一个命令的 man 页面，看到的是什么</h2><p><code>man</code> 后面跟命令名，比如 <code>man ls</code>，打开的页面几乎总是这几个固定部分组成的，认清这个结构，比死记某一个命令的参数更有用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">NAME</span><br><span class="line">     一句话描述这个命令是干什么的，最快判断&quot;是不是我要找的东西&quot;就看这里</span><br><span class="line"></span><br><span class="line">SYNOPSIS</span><br><span class="line">     用法概要，一行或几行，用方括号/尖括号/省略号表示参数是否必填、能不能重复</span><br><span class="line">     （这套符号约定和 --help 输出的用法概要是同一套规则，后文详细展开）</span><br><span class="line"></span><br><span class="line">DESCRIPTION</span><br><span class="line">     详细说明，参数多的命令这部分会很长，是真正讲清楚&quot;每个参数具体做什么&quot;的地方</span><br><span class="line"></span><br><span class="line">OPTIONS</span><br><span class="line">     参数列表，部分命令会把这部分和 DESCRIPTION 合并，不单独分节</span><br><span class="line"></span><br><span class="line">EXAMPLES</span><br><span class="line">     实战示例，不是所有命令都有这部分，有的话是最值得先看的地方</span><br><span class="line"></span><br><span class="line">SEE ALSO</span><br><span class="line">     相关命令，遇到一个命令做不到想要的效果，这里经常能带出更合适的那个</span><br><span class="line"></span><br><span class="line">AUTHOR</span><br><span class="line">     作者信息，日常查阅基本用不上</span><br></pre></td></tr></table></figure><p>拿到一个陌生命令，不用从头到尾通读，思路应该是：先看 <code>NAME</code> 确认找对了地方，直接跳到 <code>EXAMPLES</code>（如果有）抄一个改改能用最好，改不出想要的效果再回头看 <code>OPTIONS</code>&#x2F;<code>DESCRIPTION</code> 找具体参数。</p><h2 id="为什么会查到同名但不是你想要的条目"><a href="#为什么会查到同名但不是你想要的条目" class="headerlink" title="为什么会查到同名但不是你想要的条目"></a>为什么会查到同名但不是你想要的条目</h2><p>有时候 <code>man</code> 打开的页面内容明显不对——比如查 <code>printf</code> 却看到一堆看不懂的返回值类型和参数说明。这不是找错了命令，是 <code>man</code> 手册本身按内容类型分成好几个”层”（section），同一个名字在不同层里可能是完全不同的东西：</p><table><thead><tr><th>章节</th><th>内容</th></tr></thead><tbody><tr><td>1</td><td>用户可以直接执行的命令或 shell 命令</td></tr><tr><td>2</td><td>系统调用——内核提供的功能</td></tr><tr><td>3</td><td>库函数——程序库里的函数（大部分 libc 函数在这里）</td></tr><tr><td>4</td><td>特殊文件，通常是 <code>/dev</code> 下的设备文件</td></tr><tr><td>5</td><td>文件格式和约定，比如配置文件的写法</td></tr><tr><td>6</td><td>游戏</td></tr><tr><td>7</td><td>杂项——协议、字符集标准、文件系统布局这类概览性内容</td></tr><tr><td>8</td><td>系统管理命令，通常只有 root 能执行</td></tr><tr><td>9</td><td>内核例程，<strong>非标准章节</strong>，不是所有系统都有</td></tr></tbody></table><p><code>printf</code> 在 man 手册里至少有两个条目：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">man 1 <span class="built_in">printf</span></span><br><span class="line"><span class="comment">#     章节 1：shell/coreutils 提供的 printf 命令，格式化输出文本用</span></span><br><span class="line"></span><br><span class="line">man 3 <span class="built_in">printf</span></span><br><span class="line"><span class="comment">#     章节 3：C 标准库里的 printf() 函数，写 C 代码时才会用到</span></span><br><span class="line"></span><br><span class="line">man <span class="built_in">printf</span></span><br><span class="line"><span class="comment">#     不指定章节，man 按顺序查找，通常先找到章节 1 的版本</span></span><br><span class="line"><span class="comment">#     但这个&quot;通常&quot;不是保证——取决于系统配置，遇到内容明显不对时,</span></span><br><span class="line"><span class="comment">#     加上章节号重新查是最快的解法</span></span><br></pre></td></tr></table></figure><p><code>printf</code> 只是最经典的例子，很多名字在用户命令和库函数&#x2F;系统调用里都同时存在。查手册时如果内容感觉不对（比如冒出一堆看不懂的 C 类型），第一反应应该是”这是不是查到别的章节去了”，加上章节号重新查。</p><h2 id="man-内部导航：其实你在用-less"><a href="#man-内部导航：其实你在用-less" class="headerlink" title="man 内部导航：其实你在用 less"></a>man 内部导航：其实你在用 less</h2><p><code>man</code> 本身不负责分页显示，它把内容交给一个分页器（pager）处理，默认通常是 <code>less</code>。这意味着 <code>less</code> 的快捷键在 <code>man</code> 里全部适用，这是查手册最实用的一套技能：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">man tar</span><br><span class="line"><span class="comment">#     打开后是 less 在分页显示内容，以下操作都在这个界面里进行</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 翻页</span></span><br><span class="line">空格 或 f    <span class="comment">#     向下翻一页</span></span><br><span class="line">b            <span class="comment">#     向上翻一页</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索</span></span><br><span class="line">/关键词       <span class="comment">#     向下搜索关键词，比如 /-f 找 -f 参数的说明</span></span><br><span class="line">?关键词       <span class="comment">#     向上搜索关键词</span></span><br><span class="line">n            <span class="comment">#     跳到下一个匹配</span></span><br><span class="line">N            <span class="comment">#     跳到上一个匹配（大写，方向和 n 相反）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 退出</span></span><br><span class="line">q            <span class="comment">#     退出 man，回到命令行</span></span><br></pre></td></tr></table></figure><p>一个几百行的手册页，靠肉眼从头翻到尾找一个参数是最低效的做法——<code>/参数名</code> 直接跳过去，才是正确用法。</p><h2 id="忘了具体命令名，只记得想干什么，怎么搜"><a href="#忘了具体命令名，只记得想干什么，怎么搜" class="headerlink" title="忘了具体命令名，只记得想干什么，怎么搜"></a>忘了具体命令名，只记得想干什么，怎么搜</h2><p>前面几节都假设你已经知道命令名，只是想搞懂它怎么用。但更常见的场景是：只知道自己想干什么（比如”压缩文件”），完全不确定该用哪个命令。这时候 <code>apropos</code> 或 <code>man -k</code> 才是该用的工具——这两个是同一件事：搜索所有手册页的一句话描述（也就是前面提到的 <code>NAME</code> 那一行）里包含指定关键词的条目：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">apropos compress</span><br><span class="line"><span class="comment">#     搜索所有命令里，一句话描述包含 &quot;compress&quot; 的条目</span></span><br><span class="line"><span class="comment">#     结果会列出 gzip、tar、zip 等等一堆候选，帮你先找到该用哪个命令</span></span><br><span class="line"></span><br><span class="line">man -k compress</span><br><span class="line"><span class="comment">#     和上面等价——man -k 内置在 man 命令里，apropos 是独立命令，</span></span><br><span class="line"><span class="comment">#     两者功能相同，apropos 在个别精简系统上可能需要单独装，man -k 只要装了 man 就有</span></span><br></pre></td></tr></table></figure><p>结合前面讲的定位技巧，实战流程通常是这样：<code>apropos</code> 先找到候选命令名 → <code>man</code> 打开确认 <code>NAME</code> 对不对 → <code>/参数名</code> 跳到具体参数说明。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">man tar</span><br><span class="line"><span class="comment"># 打开后输入：</span></span><br><span class="line">/-f</span><br><span class="line"><span class="comment"># 直接跳到 -f 参数的说明段落，不用从头翻</span></span><br></pre></td></tr></table></figure><h2 id="help：比-man-快，但要先看懂它的格式"><a href="#help：比-man-快，但要先看懂它的格式" class="headerlink" title="--help：比 man 快，但要先看懂它的格式"></a><code>--help</code>：比 man 快，但要先看懂它的格式</h2><p><code>man</code> 详细但长，很多时候你只是想快速确认”这个命令到底有没有我要的参数”，这时候 <code>--help</code> 比翻 <code>man</code> 快得多：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">tar --<span class="built_in">help</span></span><br><span class="line"><span class="comment">#     大多数命令都支持这个参数，输出比 man 手册短得多，只列参数不讲原理</span></span><br></pre></td></tr></table></figure><p><code>--help</code> 的输出通常分两部分：开头一行 <strong>用法概要（usage synopsis）</strong>，后面跟着<strong>参数列表</strong>。用法概要这一行的符号是有约定含义的，看懂它比记住某个具体命令的用法更重要——这套约定跟前面 <code>man</code> 页面 <code>SYNOPSIS</code> 里的符号是同一套规则：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Usage: tar [OPTION...] [FILE]...</span><br><span class="line">#      OPTION...：方括号 [ ] 表示可选，... 表示可以出现多次（可以传多个 OPTION）</span><br><span class="line">#      FILE...：同样可选、可重复，表示可以传多个文件</span><br><span class="line">#      没有方括号、直接写出来的部分（比如命令名本身）是必需的</span><br></pre></td></tr></table></figure><p>常见约定：方括号 <code>[ ]</code> 包起来的是可选项；尖括号 <code>&lt; &gt;</code> 或者不带括号直接写的词是必填项；省略号 <code>...</code> 表示前面那一项可以重复出现；竖线 <code>|</code> 表示几选一。这套约定不是 <code>tar</code> 专属的，几乎所有遵循 GNU&#x2F;POSIX 惯例的命令都这么写，学会看一次，后面每个命令的用法说明都能看懂。</p><p>参数列表部分，每个参数通常是”短选项, 长选项  说明文字”这样的格式排开。输出长的话，直接用 <code>--help</code> 配合搜索工具：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">tar --<span class="built_in">help</span> | grep -A1 <span class="string">&quot;exclude&quot;</span></span><br><span class="line"><span class="comment">#     grep 从 --help 的输出里过滤出包含 &quot;exclude&quot; 的行，-A1 多带一行看清楚说明</span></span><br><span class="line"><span class="comment">#     比整段输出里肉眼找快得多，尤其是参数很多的命令（比如 tar、find）</span></span><br><span class="line"></span><br><span class="line">curl --<span class="built_in">help</span> | less</span><br><span class="line"><span class="comment">#     curl 的 --help 输出本身就很长，直接接 less 分页，用前面讲过的 / 搜索快捷键定位</span></span><br></pre></td></tr></table></figure><p><code>--help</code> 和 <code>man</code> 不是二选一：先用 <code>--help</code> 快速扫一眼有没有你要的参数，确认存在了但不确定具体行为，再去 <code>man</code> 里查那一条的详细说明。</p><h2 id="man-之外的其他辅助手段"><a href="#man-之外的其他辅助手段" class="headerlink" title="man 之外的其他辅助手段"></a>man 之外的其他辅助手段</h2><ul><li><a href="/tldr">tldr</a>：直接给实战例子，不讲原理只给”抄了就能用”的命令，想直接抄一条命令用时最合适</li><li><code>info</code>：GNU 项目常用的另一套文档系统，比 <code>man</code> 更结构化（支持章节跳转、超链接式导航），但普及度不如 <code>man</code>，主要是 GNU 核心工具用得多，比如 <code>coreutils</code> 这个包</li></ul><p>遇到不认识的命令，<code>apropos</code> 先定位候选命令；确定命令了，<code>--help</code> 快速确认有没有要的参数；想深入理解某个参数为什么这么设计，才去查 <code>man</code> 的 <code>DESCRIPTION</code>；懒得看文档直接要例子，<code>tldr</code>。几种手段配合用，不是非此即彼。</p><h2 id="终端效率小系列：下一篇"><a href="#终端效率小系列：下一篇" class="headerlink" title="终端效率小系列：下一篇"></a>终端效率小系列：下一篇</h2><p>下一篇讲 vim 基本操作——毕竟查完命令用法，回头改配置文件、写代码，还是得会用编辑器。</p>]]>
    </content>
    <id>https://blog.codewater.top/%E4%B8%8D%E7%86%9F%E6%82%89%E7%9A%84%E5%91%BD%E4%BB%A4%E6%80%8E%E4%B9%88%E5%BF%AB%E9%80%9F%E6%90%9E%E6%87%82-man%E9%A1%B5%E9%9D%A2%E7%BB%93%E6%9E%84%E4%B8%8Eapropos%E9%80%9F%E6%9F%A5/</id>
    <link href="https://blog.codewater.top/%E4%B8%8D%E7%86%9F%E6%82%89%E7%9A%84%E5%91%BD%E4%BB%A4%E6%80%8E%E4%B9%88%E5%BF%AB%E9%80%9F%E6%90%9E%E6%87%82-man%E9%A1%B5%E9%9D%A2%E7%BB%93%E6%9E%84%E4%B8%8Eapropos%E9%80%9F%E6%9F%A5/"/>
    <published>2026-07-12T16:18:38.000Z</published>
    <summary>遇到不认识的命令或忘了参数怎么办：man 页面固定结构怎么读、apropos 怎么按关键词搜命令、--help 怎么用。</summary>
    <title>不熟悉的命令怎么快速搞懂：man 页面结构、apropos 搜索与 --help 速查</title>
    <updated>2026-07-12T16:18:38.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="xargs" scheme="https://blog.codewater.top/tags/xargs/"/>
    <content>
      <![CDATA[<p><code>find . -name &quot;*.log&quot; | rm</code> 这条命令，直觉上应该是”找到所有 .log 文件，然后删掉”。实际跑一下：什么都不会发生，一个文件都删不掉，<code>rm</code> 也不会报任何错。</p><p>管道明明把 <code>find</code> 的结果传给了 <code>rm</code>，为什么 <code>rm</code> 完全不理会？这篇讲清楚背后的原因，以及支撑整个 Linux 命令行生态的设计哲学。</p><span id="more"></span><p>这是 Linux 命令系列的第六篇，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>、<a href="/grep%E6%AD%A3%E5%88%99%E5%8C%B9%E9%85%8D%E4%B8%8E%E4%B8%8A%E4%B8%8B%E6%96%87%E6%8E%A7%E5%88%B6%E5%AE%9E%E6%88%98">grep</a>、<a href="/find%E6%9D%A1%E4%BB%B6%E7%BB%84%E5%90%88%E4%B8%8Eexec%E6%80%A7%E8%83%BD%E5%AE%9E%E6%88%98">find</a>、<a href="/tar-zip-unzip%E5%BD%92%E6%A1%A3%E5%8E%8B%E7%BC%A9%E4%B8%8E%E5%B8%B8%E8%A7%81%E5%9D%91%E5%AE%9E%E6%88%98">tar&#x2F;zip&#x2F;unzip</a>，以及姐妹篇 <a href="/Linux%E5%91%BD%E4%BB%A4%E8%A1%8C%E9%80%89%E9%A1%B9%E8%AE%BE%E8%AE%A1%E7%BA%A6%E5%AE%9A">Linux 命令行选项设计约定</a>——上一篇讲的是怎么给单个命令传参数，这篇讲怎么把多个命令组合起来干活。</p><h2 id="管道的本质：把一个命令的输出接成另一个命令的输入"><a href="#管道的本质：把一个命令的输出接成另一个命令的输入" class="headerlink" title="管道的本质：把一个命令的输出接成另一个命令的输入"></a>管道的本质：把一个命令的输出接成另一个命令的输入</h2><p>管道符 <code>|</code> 做的事情很纯粹：把左边命令的标准输出（stdout）直接接到右边命令的标准输入（stdin）。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> | grep <span class="string">&quot;.md&quot;</span></span><br><span class="line"><span class="comment">#     ls 把当前目录的文件列表写到 stdout</span></span><br><span class="line"><span class="comment">#     | 把这份输出原样接到 grep 的 stdin</span></span><br><span class="line"><span class="comment">#     grep 从 stdin 读入这份列表，从中过滤出包含 .md 的行</span></span><br></pre></td></tr></table></figure><p>两个命令互相不知道对方的存在——<code>ls</code> 不知道自己的输出被谁读走了，<code>grep</code> 也不关心这些文本行是 <code>ls</code> 生成的还是别的什么命令生成的。它们唯一的约定是：数据以纯文本的形式流动。这个”互不知情、只认文本”的设计，正是管道能随意串联任意命令的原因。</p><h2 id="关键区分：谁读-stdin，谁只认命令行参数"><a href="#关键区分：谁读-stdin，谁只认命令行参数" class="headerlink" title="关键区分：谁读 stdin，谁只认命令行参数"></a>关键区分：谁读 stdin，谁只认命令行参数</h2><p>回到开头的问题：<code>find . -name &quot;*.log&quot; | rm</code> 为什么删不掉文件？</p><p>答案是：<code>rm</code> 根本不读 stdin。管道确实把 <code>find</code> 的结果传了过去，但 <code>rm</code> 压根没有”从标准输入读取要删除的文件列表”这个功能——它只认命令行参数，比如 <code>rm file1.log file2.log</code>。管道传过去的内容,<code>rm</code> 收到了也不会看，它只会检查自己的命令行参数里有没有文件名，没有就什么都不干、正常退出。</p><p>Linux 命令大致能分两类：</p><ul><li><strong>过滤器类命令</strong>：默认从 stdin 读取数据，处理后写到 stdout，天生适合接在管道里。<code>cat</code>、<code>grep</code>、<code>sed</code>、<code>wc</code>、<code>sort</code> 都是这一类</li><li><strong>操作型命令</strong>：只认命令行参数，不读 stdin，因为它们的设计目的是”对指定的东西做一次性操作”，不是”处理一批流式数据”。<code>rm</code>、<code>chmod</code>、<code>mkdir</code>、<code>cp</code> 都是这一类</li></ul><p>这个区分不是随意的——<code>rm</code> 删文件、<code>chmod</code> 改权限，这类操作要的是明确的目标列表，命令行参数比一份不确定长度的流式数据更适合表达”目标是谁”。</p><h2 id="xargs-的意义：把-stdin-转换成命令行参数"><a href="#xargs-的意义：把-stdin-转换成命令行参数" class="headerlink" title="xargs 的意义：把 stdin 转换成命令行参数"></a>xargs 的意义：把 stdin 转换成命令行参数</h2><p><code>xargs</code> 存在的目的就是补上这个断层——把 stdin 里的内容转换成命令行参数，喂给那些只认参数的命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.log&quot;</span> | xargs <span class="built_in">rm</span></span><br><span class="line"><span class="comment">#     find 把匹配到的文件路径列表写到 stdout</span></span><br><span class="line"><span class="comment">#     xargs 从 stdin 读进这份列表，转换成 rm 的命令行参数</span></span><br><span class="line"><span class="comment">#     实际执行的是 rm file1.log file2.log file3.log ...</span></span><br></pre></td></tr></table></figure><p>同样的管道，中间插一个 <code>xargs</code>，语义就通了：<code>xargs</code> 把”过滤器类命令的输出”转换成”操作型命令能理解的参数”，架起两类命令之间的桥。</p><h2 id="xargs-核心参数"><a href="#xargs-核心参数" class="headerlink" title="xargs 核心参数"></a>xargs 核心参数</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.tmp&quot;</span> | xargs -n 5 <span class="built_in">rm</span></span><br><span class="line"><span class="comment">#     -n：每次调用命令时最多传几个参数，这里是每 5 个文件调一次 rm</span></span><br><span class="line"><span class="comment">#     不写 -n 的话，xargs 会尽量把所有参数塞进一次调用（受系统 ARG_MAX 限制）</span></span><br><span class="line"></span><br><span class="line">find . -name <span class="string">&quot;*.log&quot;</span> | xargs -I &#123;&#125; <span class="built_in">mv</span> &#123;&#125; /backup/</span><br><span class="line"><span class="comment">#     -I：自定义占位符，&#123;&#125; 代表 xargs 传进来的每一项</span></span><br><span class="line"><span class="comment">#     用 -I 之后参数位置更灵活，能把参数插到命令中间而不是只能接在最后</span></span><br><span class="line"></span><br><span class="line">find . -name <span class="string">&quot;*.log&quot;</span> -print0 | xargs -0 <span class="built_in">rm</span></span><br><span class="line"><span class="comment">#     -0：配合 find 的 -print0 使用，用 null 字符分隔文件名而不是空格/换行</span></span><br><span class="line"><span class="comment">#     这个组合专门解决文件名里带空格的问题，前两篇 grep 和 find 文章里都提到过它</span></span><br></pre></td></tr></table></figure><h2 id="xargs-P：容易被忽略的并行执行"><a href="#xargs-P：容易被忽略的并行执行" class="headerlink" title="xargs -P：容易被忽略的并行执行"></a>xargs -P：容易被忽略的并行执行</h2><p><code>xargs</code> 默认是串行的——一个一个调用命令，等上一个执行完再执行下一个。<code>-P</code> 可以指定并行度：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.jpg&quot;</span> | xargs -P 4 -n 1 convert -resize 50%</span><br><span class="line"><span class="comment">#     -P 4：最多同时跑 4 个进程</span></span><br><span class="line"><span class="comment">#     -n 1：每个进程只处理 1 个文件，这样才有 4 个独立任务可以并行</span></span><br></pre></td></tr></table></figure><p><code>-P</code> 有个容易踩的坑：它必须配合 <code>-n</code> 一起用，否则很可能不起作用——<code>xargs</code> 默认会把所有参数尽量塞进一次调用（一次 <code>rm file1 file2 file3 ...</code>），只有一次调用，<code>-P</code> 再高也没有第二个进程可以并行。想让并行真正生效，得先用 <code>-n</code> 把参数拆成多次独立调用，<code>-P</code> 才有事可做。</p><h2 id="xargs-常用参数速查表"><a href="#xargs-常用参数速查表" class="headerlink" title="xargs 常用参数速查表"></a>xargs 常用参数速查表</h2><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-n NUM</code></td><td>每次调用命令时最多传 NUM 个参数，不写则尽量一次塞满（受 <code>ARG_MAX</code> 限制）</td></tr><tr><td><code>-I REPLACE</code></td><td>自定义占位符，可以把参数插到命令中间而不是只能接在最后</td></tr><tr><td><code>-0</code></td><td>用 null 字符分隔输入项，配合 <code>find -print0</code> 处理带空格的文件名</td></tr><tr><td><code>-P NUM</code></td><td>最多同时跑 NUM 个进程，默认是 1（串行）；必须配合 <code>-n</code> 才能真正并行</td></tr><tr><td><code>-t</code></td><td>执行前先把实际要跑的命令打印出来，调试危险操作时先加这个看一眼</td></tr><tr><td><code>-p</code></td><td>每次执行前都询问确认，比 <code>-t</code> 更保守，确认了才真正执行</td></tr></tbody></table><h2 id="xargs-和-find-exec-的对比"><a href="#xargs-和-find-exec-的对比" class="headerlink" title="xargs 和 find -exec 的对比"></a>xargs 和 find -exec 的对比</h2><p><a href="/find%E6%9D%A1%E4%BB%B6%E7%BB%84%E5%90%88%E4%B8%8Eexec%E6%80%A7%E8%83%BD%E5%AE%9E%E6%88%98">上一篇 find 文章</a>里讲过 <code>-exec</code> 的两种写法（<code>\;</code> 和 <code>+</code>），做的事情和 <code>xargs</code> 很像——都是”把找到的东西喂给下一个命令”。两者的区别：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.log&quot;</span> -<span class="built_in">exec</span> <span class="built_in">rm</span> &#123;&#125; +</span><br><span class="line"><span class="comment">#     -exec 内建在 find 里，只服务 find 自己找到的结果</span></span><br><span class="line"></span><br><span class="line">find . -name <span class="string">&quot;*.log&quot;</span> | xargs <span class="built_in">rm</span></span><br><span class="line"><span class="comment">#     xargs 是独立命令，管道前面可以是 find，也可以是 grep -l、ls，或者任何产生一行行文本的命令</span></span><br></pre></td></tr></table></figure><p><code>-exec</code> 是 <code>find</code> 自带的能力，用起来更直接，但只能配合 <code>find</code>。<code>xargs</code> 是通用工具，任何命令的输出只要能通过管道传过来，都能配合 <code>xargs</code> 转换成参数——适用范围更广。日常写 <code>find ... -exec</code> 就够用的场景不用特意换成 <code>xargs</code>，但如果数据来源不是 <code>find</code>（比如 <code>grep -l ... | xargs</code>），就只能用 <code>xargs</code>。</p><h2 id="Unix-设计哲学落地：小工具、文本接口、自由组合"><a href="#Unix-设计哲学落地：小工具、文本接口、自由组合" class="headerlink" title="Unix 设计哲学落地：小工具、文本接口、自由组合"></a>Unix 设计哲学落地：小工具、文本接口、自由组合</h2><p>前面这些具体机制，背后有一套统一的设计思想。1978 年，Unix 管道的发明者 Doug McIlroy 在 <em>Bell System Technical Journal</em> 上把这套思想总结成三句话：</p><blockquote><p>Write programs that do one thing and do it well. Write programs to work together. Write programs to handle text streams, because that is a universal interface.</p><p>写只做一件事、并且做好这一件事的程序；写能相互协作的程序；写能处理文本流的程序，因为文本流是通用接口。</p></blockquote><p>这三句话直接对应本文讲的内容：</p><ul><li><strong>只做一件事</strong>：<code>grep</code> 只管过滤文本，<code>rm</code> 只管删文件，没有哪个命令试图把所有功能都塞进自己</li><li><strong>相互协作</strong>：靠管道把小工具串起来，而不是靠一个大程序内置所有功能</li><li><strong>文本作为通用接口</strong>：不管是 <code>ls</code> 的输出还是 <code>grep</code> 的输出，都是纯文本，任何命令都能读懂任何命令的输出——这正是管道能随意组合任意命令的前提</li></ul><p>这和图形界面软件的设计思路是相反的：图形应用倾向于把功能都装进一个软件里，命令行工具则倾向于拆成几十个只做一件事的小程序，需要什么功能现场用管道拼起来。前者上手快，后者组合起来的灵活度是图形界面很难达到的——今天学的这几个命令，明天可以用完全没预料到的方式重新拼装解决新问题。</p><h2 id="Linux-命令系列：走到这里"><a href="#Linux-命令系列：走到这里" class="headerlink" title="Linux 命令系列：走到这里"></a>Linux 命令系列：走到这里</h2><p>这个系列从具体命令讲到了设计哲学：<a href="/Linux%E6%9D%83%E9%99%90%E4%B8%8E%E5%B1%9E%E4%B8%BB%E7%AE%A1%E7%90%86-chmod-chown%E5%AE%9E%E6%88%98">chmod&#x2F;chown</a> 管权限，<a href="/grep%E6%AD%A3%E5%88%99%E5%8C%B9%E9%85%8D%E4%B8%8E%E4%B8%8A%E4%B8%8B%E6%96%87%E6%8E%A7%E5%88%B6%E5%AE%9E%E6%88%98">grep</a> 管文本搜索，<a href="/find%E6%9D%A1%E4%BB%B6%E7%BB%84%E5%90%88%E4%B8%8Eexec%E6%80%A7%E8%83%BD%E5%AE%9E%E6%88%98">find</a> 管文件查找，<a href="/tar-zip-unzip%E5%BD%92%E6%A1%A3%E5%8E%8B%E7%BC%A9%E4%B8%8E%E5%B8%B8%E8%A7%81%E5%9D%91%E5%AE%9E%E6%88%98">tar&#x2F;zip&#x2F;unzip</a> 管归档压缩，<a href="/Linux%E5%91%BD%E4%BB%A4%E8%A1%8C%E9%80%89%E9%A1%B9%E8%AE%BE%E8%AE%A1%E7%BA%A6%E5%AE%9A">命令行选项设计约定</a> 讲怎么给命令传参数，这篇讲怎么把命令组合起来。后续候选话题还有 <code>rsync</code> 的增量同步和包管理命令，写完再回来更新 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>。</p>]]>
    </content>
    <id>https://blog.codewater.top/xargs%E7%AE%A1%E9%81%93%E4%B8%8ELinux%E5%91%BD%E4%BB%A4%E8%AE%BE%E8%AE%A1%E5%93%B2%E5%AD%A6/</id>
    <link href="https://blog.codewater.top/xargs%E7%AE%A1%E9%81%93%E4%B8%8ELinux%E5%91%BD%E4%BB%A4%E8%AE%BE%E8%AE%A1%E5%93%B2%E5%AD%A6/"/>
    <published>2026-07-12T08:52:33.000Z</published>
    <summary>管道的本质是什么、为什么有些命令吃 stdin 有些只认参数、xargs 怎么架桥，以及背后支撑整个 Linux 命令行的 Unix 设计哲学。</summary>
    <title>xargs、管道与 Linux 命令设计哲学：为什么 find | rm 删不掉任何文件</title>
    <updated>2026-07-12T08:52:33.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <content>
      <![CDATA[<p>写完前几篇 Linux 命令教程，你可能已经发现一个规律，紧接着又发现一堆反例：<code>grep -v</code> 是反向匹配，<code>curl -v</code> 是显示详细过程，换成 <code>node -v</code> 却变成了查版本号。同一个字母，三个命令三种含义。</p><p>到底是谁定的这些规则？答案可能有点意外：<strong>没有人统一定</strong>。这篇讲清楚这套”有历史惯例但没有强制标准”的设计约定。</p><span id="more"></span><p>这是 Linux 常见命令系列第五篇，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>、<a href="/Linux%E6%9D%83%E9%99%90%E4%B8%8E%E5%B1%9E%E4%B8%BB%E7%AE%A1%E7%90%86-chmod-chown%E5%AE%9E%E6%88%98">chmod&#x2F;chown</a>、<a href="/grep%E6%AD%A3%E5%88%99%E5%8C%B9%E9%85%8D%E4%B8%8E%E4%B8%8A%E4%B8%8B%E6%96%87%E6%8E%A7%E5%88%B6%E5%AE%9E%E6%88%98">grep</a>、<a href="/find%E6%9D%A1%E4%BB%B6%E7%BB%84%E5%90%88%E4%B8%8Eexec%E6%80%A7%E8%83%BD%E5%AE%9E%E6%88%98">find</a>、<a href="/tar-zip-unzip%E5%BD%92%E6%A1%A3%E5%8E%8B%E7%BC%A9%E4%B8%8E%E5%B8%B8%E8%A7%81%E5%9D%91%E5%AE%9E%E6%88%98">tar&#x2F;zip&#x2F;unzip</a>。</p><h2 id="两种选项风格：POSIX-短选项与-GNU-长选项"><a href="#两种选项风格：POSIX-短选项与-GNU-长选项" class="headerlink" title="两种选项风格：POSIX 短选项与 GNU 长选项"></a>两种选项风格：POSIX 短选项与 GNU 长选项</h2><p>命令行选项有两套并存的写法：</p><ul><li><strong>短选项</strong>：单个字符前面加一个短横线，比如 <code>-v</code>、<code>-f</code>。这是 POSIX 定义的标准写法，对应的解析函数 <code>getopt</code> 本身就是 POSIX 规范的一部分——历史上敲命令都是手动输入，单字符选项打字快</li><li><strong>长选项</strong>：完整单词前面加两个短横线，比如 <code>--verbose</code>、<code>--file</code>。这是 GNU 项目后来引入的扩展——对应的 <code>getopt_long</code> 函数不属于原始 POSIX 规范，是 GNU 对 POSIX 的补充。GNU 官方编码规范给出的理由不只是”更好读”：目标是让同一个概念在不同程序之间保持一致的拼写——任何 GNU 程序如果有”详细输出”这个选项，都应该精确地写成 <code>--verbose</code>，用户不用每个程序重新猜一遍这个功能叫什么</li></ul><p>这不是”新标准取代旧标准”的关系，是互补——大多数 GNU 命令行工具两套写法都支持，短的敲起来快，长的写脚本时更清楚意图，用哪个看场景。</p><h2 id="短选项能挤在一起写，长选项不能"><a href="#短选项能挤在一起写，长选项不能" class="headerlink" title="短选项能挤在一起写，长选项不能"></a>短选项能挤在一起写，长选项不能</h2><p>短选项有一个长选项做不到的能力：可以把几个不需要参数的选项拼在同一个短横线后面。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">tar -cvf archive.tar.gz files/</span><br><span class="line"><span class="comment">#     -c -v -f 三个短选项挤在一起，等价于 tar -c -v -f archive.tar.gz</span></span><br><span class="line"><span class="comment">#     只有最后一个（-f）需要带参数，前面的 c、v 不需要，所以能挤</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">ls</span> -la</span><br><span class="line"><span class="comment">#     等价于 ls -l -a</span></span><br><span class="line"></span><br><span class="line">tar --create --verbose --file=archive.tar.gz files/</span><br><span class="line"><span class="comment">#     长选项没法挤成 tar --createverbosefile=archive.tar.gz，只能一个个分开写</span></span><br></pre></td></tr></table></figure><p>短选项能挤的前提是：挤在一起的这些选项里，只有排在最后的那个可以带参数——这也是<a href="/tar-zip-unzip%E5%BD%92%E6%A1%A3%E5%8E%8B%E7%BC%A9%E4%B8%8E%E5%B8%B8%E8%A7%81%E5%9D%91%E5%AE%9E%E6%88%98">上一篇 tar 文章</a>里”<code>-f</code> 必须放最后一位”这条规则的根源。</p><h2 id="没有中央标准：同一个字母在不同命令里可能完全不是一回事"><a href="#没有中央标准：同一个字母在不同命令里可能完全不是一回事" class="headerlink" title="没有中央标准：同一个字母在不同命令里可能完全不是一回事"></a>没有中央标准：同一个字母在不同命令里可能完全不是一回事</h2><p>这是全文最重要的一点。<code>-v</code> 在不同命令里的真实含义对比：</p><table><thead><tr><th>命令</th><th><code>-v</code> 的含义</th></tr></thead><tbody><tr><td><code>grep -v</code></td><td>invert，反向匹配</td></tr><tr><td><code>curl -v</code></td><td>verbose，显示详细请求过程</td></tr><tr><td><code>node -v</code></td><td>version，查看版本号</td></tr></tbody></table><p>没有任何组织规定”<code>-v</code> 必须表示什么”。<code>grep</code>、<code>curl</code>、<code>node</code> 是完全独立的项目，各自的作者按自己的直觉选了这个字母——<code>curl</code> 的作者觉得 <code>-v</code> 该是 verbose，<code>node</code> 的作者觉得 <code>-v</code> 该是 version，两边都没错，只是没有对上。</p><p>这不是巧合或者哪边”用错了”，纯粹是因为命令行选项的字母分配从来没有一个中央注册机构去协调。唯一可靠的做法是：<strong>遇到不确定的选项，查这个命令自己的文档</strong>（<code>--help</code> 或 <code>man</code>），别指望”我在别的命令里见过 <code>-v</code>“这种经验能直接套用。</p><h2 id="长短选项不是一一对应关系"><a href="#长短选项不是一一对应关系" class="headerlink" title="长短选项不是一一对应关系"></a>长短选项不是一一对应关系</h2><p>容易踩的另一个误区是假设”每个短选项都有对应的长选项，反过来也一样”——实际上两边是各自独立设计的集合：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">find . --exclude=<span class="string">&quot;*.log&quot;</span></span><br><span class="line"><span class="comment">#     find 的 --exclude 只有长选项写法，没有对应的短选项</span></span><br><span class="line"></span><br><span class="line">grep -P <span class="string">&#x27;\d+&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     -P 有对应的长选项 --perl-regexp，两种写法都行</span></span><br><span class="line"></span><br><span class="line">curl -o output.txt https://example.com</span><br><span class="line"><span class="comment">#     -o 有对应的长选项 --output</span></span><br></pre></td></tr></table></figure><p>具体某个选项有没有短&#x2F;长两种写法，完全取决于这个命令的作者当初怎么设计，没有规律可循，用之前查一下这个命令自己的文档最稳妥。</p><h2 id="：告诉命令”后面全部当普通参数，别再解析成选项”"><a href="#：告诉命令”后面全部当普通参数，别再解析成选项”" class="headerlink" title="--：告诉命令”后面全部当普通参数，别再解析成选项”"></a><code>--</code>：告诉命令”后面全部当普通参数，别再解析成选项”</h2><p><code>--</code> 是 POSIX 明确定义的一个特殊标记：出现在命令行里之后，后面所有内容都当成普通参数处理，即使它们长得像选项（以 <code>-</code> 开头）也不再被解析。</p><p>最经典的应用场景：删除一个文件名本身以 <code>-</code> 开头的文件。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">rm</span> -filename</span><br><span class="line"><span class="comment">#     报错：rm 会把 &quot;-filename&quot; 当成一个未知选项去解析，而不是文件名</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">rm</span> -- -filename</span><br><span class="line"><span class="comment">#     正确写法：-- 之后的 -filename 被当成普通参数（文件名），不再解析成选项</span></span><br></pre></td></tr></table></figure><p>只要命令是通过标准的 <code>getopt</code> 系列函数解析参数的，都支持 <code>--</code>，这是 POSIX Utility Conventions 里定义的通用行为，不是某个命令单独发明的。</p><p><strong>但这条规则不是无条件成立的</strong>：bash 内建的 <code>echo</code> 就是个例外——它不走 <code>getopt</code> 那套解析逻辑，<code>--</code> 传给它不会被当成”选项结束”标记，而是原样当成要输出的文本打印出来。真正通用可靠的做法是遇到不确定的命令先查一下它的文档，别假设”所有命令都认 <code>--</code>“。</p><h2 id="选项带参数的几种等价写法"><a href="#选项带参数的几种等价写法" class="headerlink" title="选项带参数的几种等价写法"></a>选项带参数的几种等价写法</h2><p>短选项和长选项各自还有细分的写法：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">tar -f archive.tar.gz files/</span><br><span class="line"><span class="comment">#     短选项 -f 后面留空格接参数</span></span><br><span class="line"></span><br><span class="line">tar -farchive.tar.gz files/</span><br><span class="line"><span class="comment">#     短选项也可以不留空格，直接把参数粘在字母后面，效果一样</span></span><br><span class="line"></span><br><span class="line">tar --file archive.tar.gz files/</span><br><span class="line"><span class="comment">#     长选项后面留空格接参数</span></span><br><span class="line"></span><br><span class="line">tar --file=archive.tar.gz files/</span><br><span class="line"><span class="comment">#     长选项也可以用等号连接参数，两种写法都行</span></span><br></pre></td></tr></table></figure><p>日常写惯了哪种就用哪种，四种写法在大多数 GNU 工具里都能正常解析，不用纠结选哪个更”正确”。</p><h2 id="回看前四篇：这套规律怎么解释你已经踩过的坑"><a href="#回看前四篇：这套规律怎么解释你已经踩过的坑" class="headerlink" title="回看前四篇：这套规律怎么解释你已经踩过的坑"></a>回看前四篇：这套规律怎么解释你已经踩过的坑</h2><p>前四篇里出现过的几个选项设计细节，用这篇的框架回头看就都说得通了：</p><ul><li><strong>tar 的 <code>-f</code> 必须放 clumped 短选项最后一位</strong>：因为短选项挤在一起时只有最后一个能带参数，<code>-f</code> 恰好是需要带参数的那个，位置不能乱放</li><li><strong>grep 的 <code>-E</code> 和 <code>--extended-regexp</code> 是同一个选项</strong>：短&#x2F;长两种写法，效果完全一样，选哪个纯粹是个人习惯</li><li><strong>find 的 <code>--exclude</code> 没有对应短选项</strong>：印证了”长短选项不是一一对应”这条规律，<code>find</code> 的作者就没给它设计短选项</li></ul><p>这些当初看起来是”这个命令特有的坑”，其实都是同一套通用设计约定的具体体现。</p><h2 id="命令行选项之外"><a href="#命令行选项之外" class="headerlink" title="命令行选项之外"></a>命令行选项之外</h2><p>选项讲的是”怎么给一个命令传参数”，下一篇要讲的是”怎么把多个命令组合起来干活”——<code>xargs</code>、管道，以及背后那套”每个工具只做一件事、靠文本流互相拼接”的 Linux 命令设计哲学。</p>]]>
    </content>
    <id>https://blog.codewater.top/Linux%E5%91%BD%E4%BB%A4%E8%A1%8C%E9%80%89%E9%A1%B9%E8%AE%BE%E8%AE%A1%E7%BA%A6%E5%AE%9A/</id>
    <link href="https://blog.codewater.top/Linux%E5%91%BD%E4%BB%A4%E8%A1%8C%E9%80%89%E9%A1%B9%E8%AE%BE%E8%AE%A1%E7%BA%A6%E5%AE%9A/"/>
    <published>2026-07-12T08:45:34.000Z</published>
    <summary>短选项和长选项是什么关系、为什么 grep -v 和 node -v 意思完全不同、-- 怎么用，讲清楚 Linux 命令行选项背后没有强制标准的设计约定。</summary>
    <title>Linux 命令行选项设计：-v 和 --verbose 的区别，以及谁在决定这些约定</title>
    <updated>2026-07-12T08:45:34.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="tar" scheme="https://blog.codewater.top/tags/tar/"/>
    <category term="zip" scheme="https://blog.codewater.top/tags/zip/"/>
    <content>
      <![CDATA[<p>部署脚本里要把一堆文件打包传到服务器，敲命令时永远在纠结：<code>tar -czf</code> 和 <code>tar -xzf</code> 哪个是打包哪个是解包？参数顺序换一下会不会报错？打包完发现体积比预期大一倍，是哪里出了问题？</p><p>这些问题的根源都是同一件事：<code>tar</code> 到底在干什么，很多人从来没搞清楚过。</p><span id="more"></span><p>这是 Linux 常见命令系列第四篇详细教程，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>、<a href="/Linux%E6%9D%83%E9%99%90%E4%B8%8E%E5%B1%9E%E4%B8%BB%E7%AE%A1%E7%90%86-chmod-chown%E5%AE%9E%E6%88%98">chmod&#x2F;chown</a>、<a href="/grep%E6%AD%A3%E5%88%99%E5%8C%B9%E9%85%8D%E4%B8%8E%E4%B8%8A%E4%B8%8B%E6%96%87%E6%8E%A7%E5%88%B6%E5%AE%9E%E6%88%98">grep</a>、<a href="/find%E6%9D%A1%E4%BB%B6%E7%BB%84%E5%90%88%E4%B8%8Eexec%E6%80%A7%E8%83%BD%E5%AE%9E%E6%88%98">find</a>。</p><h2 id="tar-是什么：打包和压缩是两件事"><a href="#tar-是什么：打包和压缩是两件事" class="headerlink" title="tar 是什么：打包和压缩是两件事"></a>tar 是什么：打包和压缩是两件事</h2><p><code>tar</code> 全称 tape archive（磁带归档），这个名字暴露了它的历史——最早是为了把多个文件写进磁带这种只能顺序读写的设备设计的。它做的事情只有一件：<strong>把多个文件和目录归档成一个文件流</strong>，文件之间的边界、权限、目录结构都保留下来，但内容本身<strong>不压缩</strong>。</p><p>压缩是另外一回事，靠 <code>tar</code> 调用外部的压缩程序（gzip、bzip2、xz）来完成。这就是为什么打包压缩的命令永远长这样：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">tar -czf archive.tar.gz files/</span><br><span class="line"><span class="comment">#     -c：创建归档（打包）</span></span><br><span class="line"><span class="comment">#     -z：调用 gzip 压缩</span></span><br><span class="line"><span class="comment">#     -f：指定输出文件名</span></span><br><span class="line"><span class="comment">#     三个动作一步做完，但概念上是两件事：先打包，再压缩</span></span><br></pre></td></tr></table></figure><p>只打包不压缩，得到的是 <code>.tar</code> 文件；打包之后再压缩，才是 <code>.tar.gz</code>、<code>.tar.bz2</code> 这些格式——文件名里的双重后缀不是巧合，就是在诚实地告诉你这个文件经过了两道工序。</p><h2 id="核心操作模式：-c、-x、-t、-r"><a href="#核心操作模式：-c、-x、-t、-r" class="headerlink" title="核心操作模式：-c、-x、-t、-r"></a>核心操作模式：-c、-x、-t、-r</h2><p>四个字母决定 tar 这次要做什么：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">tar -czf archive.tar.gz files/</span><br><span class="line"><span class="comment">#     -c（create）：创建一个新归档</span></span><br><span class="line"></span><br><span class="line">tar -xzf archive.tar.gz</span><br><span class="line"><span class="comment">#     -x（extract）：把归档解出来</span></span><br><span class="line"></span><br><span class="line">tar -tzf archive.tar.gz</span><br><span class="line"><span class="comment">#     -t（list）：只列出归档里有什么文件，不真的解压</span></span><br><span class="line"><span class="comment">#     解压一个来路不明的压缩包之前，先用这个看看里面到底装了什么，避免解出一堆文件把当前目录搞乱</span></span><br><span class="line"></span><br><span class="line">tar -rf archive.tar files/new_file.txt</span><br><span class="line"><span class="comment">#     -r（append）：往已有的归档里追加文件</span></span><br><span class="line"><span class="comment">#     注意 -r 不能配合压缩格式使用（不能对已压缩的归档直接追加），只在处理未压缩的 .tar 文件时有效，不算高频用法</span></span><br></pre></td></tr></table></figure><p><code>-c</code>、<code>-x</code>、<code>-t</code>、<code>-r</code> 这四个是互斥的，一条命令只会用其中一个——决定这次操作”创建””解压”还是”查看”。</p><h2 id="压缩格式选择：-z、-j、-J"><a href="#压缩格式选择：-z、-j、-J" class="headerlink" title="压缩格式选择：-z、-j、-J"></a>压缩格式选择：-z、-j、-J</h2><p>打包之后要不要压缩、用哪种压缩算法，是三个独立的选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">tar -czf archive.tar.gz files/</span><br><span class="line"><span class="comment">#     -z：gzip 压缩，速度快，压缩率一般，日常打包首选</span></span><br><span class="line"></span><br><span class="line">tar -cjf archive.tar.bz2 files/</span><br><span class="line"><span class="comment">#     -j：bzip2 压缩，压缩率比 gzip 更高，但压缩速度更慢</span></span><br><span class="line"></span><br><span class="line">tar -cJf archive.tar.xz files/</span><br><span class="line"><span class="comment">#     -J：xz 压缩，压缩率最高，速度也最慢，适合追求体积、不在乎时间的场景（比如长期归档存储）</span></span><br></pre></td></tr></table></figure><p>选型建议很简单：不确定就用 <code>-z</code>，这是使用最广泛、兼容性最好的选择；只有明确要求”体积越小越好、时间不是问题”时才考虑 <code>-J</code>；<code>-j</code> 处在中间地带，除非有历史包袱（老脚本已经在用 bzip2），日常新写的命令不太有必要专门选它。</p><p>这三个选项本质上是 tar 去调用系统里对应的外部压缩程序（gzip、bzip2、xz），不是 tar 自己内置的压缩实现——精简过的容器镜像如果没装对应的二进制，加了 <code>-z</code> 也会报错，排查”tar 报错找不到命令”时先确认这个压缩程序本身装了没有。</p><h2 id="f-参数：几乎总是需要"><a href="#f-参数：几乎总是需要" class="headerlink" title="-f 参数：几乎总是需要"></a><code>-f</code> 参数：几乎总是需要</h2><p><code>-f</code> 用来指定归档文件名，几乎每条 tar 命令都会带上——不写 <code>-f</code>，tar 默认会尝试读写标准输入输出而不是一个文件，这在日常场景下基本不是你想要的行为：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">tar -czf backup.tar.gz /var/www/site</span><br><span class="line"><span class="comment">#     -f 后面紧跟的 backup.tar.gz 就是输出文件名</span></span><br><span class="line"></span><br><span class="line">tar -zcf backup.tar.gz /var/www/site</span><br><span class="line"><span class="comment">#     -czf 和 -zcf 效果一样——c、z 谁前谁后无所谓，因为它俩都不需要额外参数</span></span><br><span class="line"></span><br><span class="line">tar -fcz backup.tar.gz /var/www/site</span><br><span class="line"><span class="comment">#     错误示范：-f 一旦不在最后一位，紧跟在它后面的 cz 会被当成 -f 的参数（文件名变成字面的 &quot;cz&quot;），</span></span><br><span class="line"><span class="comment">#     真正的文件名 backup.tar.gz 反而落空，GNU 官方文档明确警告：重排选项顺序时不把对应参数一起挪走，</span></span><br><span class="line"><span class="comment">#     可能导致覆盖文件——把 -f 放最后是唯一安全的写法</span></span><br></pre></td></tr></table></figure><h2 id="解压到指定目录：-C"><a href="#解压到指定目录：-C" class="headerlink" title="解压到指定目录：-C"></a>解压到指定目录：-C</h2><p>不需要先 <code>cd</code> 到目标目录再解压，<code>-C</code> 直接指定解压目的地：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tar -xzf archive.tar.gz -C /opt/deploy/</span><br><span class="line"><span class="comment">#     -C：解压到这个目录，不影响当前工作目录</span></span><br><span class="line"><span class="comment">#     等价于 cd /opt/deploy/ &amp;&amp; tar -xzf /path/to/archive.tar.gz，但不用你手动切目录、也不用担心相对路径算错</span></span><br></pre></td></tr></table></figure><h2 id="排除文件：–exclude"><a href="#排除文件：–exclude" class="headerlink" title="排除文件：–exclude"></a>排除文件：–exclude</h2><p>打包项目目录时，通常不想把 <code>.git</code>、<code>node_modules</code> 这类无关内容也打进去：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tar -czf project.tar.gz --exclude=<span class="string">&quot;.git&quot;</span> --exclude=<span class="string">&quot;node_modules&quot;</span> project/</span><br><span class="line"><span class="comment">#     --exclude：排除匹配的文件或目录，可以写多个</span></span><br><span class="line"><span class="comment">#     打包前排除比打包后再删省事得多，尤其是 node_modules 这种体积大的目录</span></span><br></pre></td></tr></table></figure><h2 id="只解压部分内容"><a href="#只解压部分内容" class="headerlink" title="只解压部分内容"></a>只解压部分内容</h2><p>归档很大、只需要其中一两个文件时，不用全解出来：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tar -xzf archive.tar.gz path/to/specific-file.txt</span><br><span class="line"><span class="comment">#     在 -f 后面直接跟归档内的具体路径，只解出这一个文件</span></span><br><span class="line"><span class="comment">#     具体路径要和 tar -tzf 列出来的路径完全一致，包括开头有没有 ./ 这种细节</span></span><br></pre></td></tr></table></figure><h2 id="两个常见坑"><a href="#两个常见坑" class="headerlink" title="两个常见坑"></a>两个常见坑</h2><p><strong>打包时用了绝对路径，解压到别的机器上跑到意料之外的地方</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">tar -czf backup.tar.gz /home/user/project</span><br><span class="line"><span class="comment">#     打包进去的路径信息是 home/user/project（tar 默认会去掉开头的 /）</span></span><br><span class="line"><span class="comment">#     解压时会在当前目录下创建 home/user/project 这一整串目录结构，而不是直接解出 project 内容</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">cd</span> /home/user/project &amp;&amp; tar -czf backup.tar.gz .</span><br><span class="line"><span class="comment">#     正确做法：先 cd 进目标目录，再用相对路径（. 表示当前目录）打包</span></span><br><span class="line"><span class="comment">#     解压出来就是干净的文件内容，不会带着一层多余的目录结构</span></span><br></pre></td></tr></table></figure><p><strong>压缩包体积比预期大，排查是不是重复压缩了</strong>：图片、视频、已经是 <code>.zip</code>&#x2F;<code>.gz</code> 格式的文件本身已经压缩过，信息熵接近上限，再用 <code>-z</code> 压一遍收益很小，有时因为压缩算法的元数据开销，体积甚至会比不压缩略大。如果打包对象里混了大量这类文件，可以考虑不加压缩参数、只用纯 <code>-c</code> 打包（<code>.tar</code> 而不是 <code>.tar.gz</code>），省下压缩耗时也不会有实质损失。</p><h2 id="zip、unzip：跨平台场景的另一个选择"><a href="#zip、unzip：跨平台场景的另一个选择" class="headerlink" title="zip、unzip：跨平台场景的另一个选择"></a>zip、unzip：跨平台场景的另一个选择</h2><p>tar 打包出来的格式在 Linux&#x2F;macOS 生态里很自然，但要把文件发给用 Windows 的同事，对方系统原生就认识 <code>.zip</code>，不用额外装解压软件——这是 <code>zip</code>&#x2F;<code>unzip</code> 的核心价值：跨平台兼容性比 tar 更好。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">zip -r archive.zip project/</span><br><span class="line"><span class="comment">#     -r：递归打包整个目录，跟 tar 的行为类似</span></span><br><span class="line"><span class="comment">#     zip 和 tar 不同：打包和压缩是同一步完成的，没有&quot;先打包再压缩&quot;这道概念区分</span></span><br><span class="line"></span><br><span class="line">zip -r archive.zip project/ -x <span class="string">&quot;*.log&quot;</span></span><br><span class="line"><span class="comment">#     -x：排除匹配的文件，这里排除所有 .log 文件</span></span><br><span class="line"></span><br><span class="line">unzip archive.zip -d /target/dir</span><br><span class="line"><span class="comment">#     -d：解压到指定目录，效果类似 tar 的 -C</span></span><br><span class="line"></span><br><span class="line">unzip -l archive.zip</span><br><span class="line"><span class="comment">#     -l：只列出压缩包里有什么文件，不真的解压，类似 tar -t</span></span><br></pre></td></tr></table></figure><p>zip 还有一个 tar 没有的功能：直接加密。<code>zip -e archive.zip file1 file2</code> 会在打包时提示输入密码，之后解压需要密码才能打开。但这里有个安全上的坑要提醒：<strong>标准 zip 加密的强度很弱</strong>（用的是老旧的算法，容易被破解），只适合防”顺手翻一下”这种低要求场景，真正需要保密用 <code>gpg</code> 或 <code>age</code> 这类专门的加密工具，别指望 zip 密码扛得住认真的破解尝试。</p><h2 id="tar-常用参数速查表"><a href="#tar-常用参数速查表" class="headerlink" title="tar 常用参数速查表"></a>tar 常用参数速查表</h2><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-c</code></td><td>创建归档</td></tr><tr><td><code>-x</code></td><td>解压归档</td></tr><tr><td><code>-t</code></td><td>只列出归档内容，不解压</td></tr><tr><td><code>-r</code></td><td>追加文件到已有归档（不能用于已压缩的归档）</td></tr><tr><td><code>-z</code></td><td>调用 gzip 压缩&#x2F;解压，速度快</td></tr><tr><td><code>-j</code></td><td>调用 bzip2 压缩&#x2F;解压，压缩率更高但更慢</td></tr><tr><td><code>-J</code></td><td>调用 xz 压缩&#x2F;解压，压缩率最高，最慢</td></tr><tr><td><code>-f FILE</code></td><td>指定归档文件名</td></tr><tr><td><code>-v</code></td><td>显示处理过程中的文件列表（verbose）</td></tr><tr><td><code>-C DIR</code></td><td>解压到指定目录</td></tr><tr><td><code>--exclude=PATTERN</code></td><td>排除匹配的文件或目录</td></tr></tbody></table><p><code>zip</code>&#x2F;<code>unzip</code> 常用的几个参数：</p><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-r</code></td><td>递归打包整个目录</td></tr><tr><td><code>-x PATTERN</code></td><td>排除匹配的文件</td></tr><tr><td><code>-e</code></td><td>加密压缩包（强度弱，别指望防真正的攻击者）</td></tr><tr><td><code>-d DIR</code></td><td>（unzip）解压到指定目录</td></tr><tr><td><code>-l</code></td><td>（unzip）只列出内容，不解压</td></tr></tbody></table><h2 id="tar-之外"><a href="#tar-之外" class="headerlink" title="tar 之外"></a>tar 之外</h2><p>系列下一篇计划写 <code>rsync</code>——增量同步、排除规则、远程传输这些参数量级比 tar 还大，够写满一整篇。</p>]]>
    </content>
    <id>https://blog.codewater.top/tar-zip-unzip%E5%BD%92%E6%A1%A3%E5%8E%8B%E7%BC%A9%E4%B8%8E%E5%B8%B8%E8%A7%81%E5%9D%91%E5%AE%9E%E6%88%98/</id>
    <link href="https://blog.codewater.top/tar-zip-unzip%E5%BD%92%E6%A1%A3%E5%8E%8B%E7%BC%A9%E4%B8%8E%E5%B8%B8%E8%A7%81%E5%9D%91%E5%AE%9E%E6%88%98/"/>
    <published>2026-07-11T09:00:00.000Z</published>
    <summary>tar 为什么不等于压缩、-z/-j/-J 怎么选、zip/unzip 的跨平台场景，配合真实踩坑讲清楚 Linux 归档压缩命令的实战用法。</summary>
    <title>tar、zip/unzip：归档压缩命令与常见坑实战</title>
    <updated>2026-07-11T09:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="find" scheme="https://blog.codewater.top/tags/find/"/>
    <content>
      <![CDATA[<p>知道 <code>fd</code> 语法更直觉、速度更快，但 SSH 到一台没装第三方工具的服务器上，还是得翻出记忆里生锈的 <code>find</code> 语法。条件和动作到底谁写前面、<code>-mtime</code> 和 <code>-atime</code> 哪个是修改时间、<code>-exec</code> 后面跟的是 <code>\;</code> 还是 <code>+</code>——每次都要重新查一遍。</p><p><code>find</code> 是所有 Linux 发行版都自带的工具，这篇把它自己的用法讲清楚。</p><span id="more"></span><p>这是 Linux 常见命令系列第三篇详细教程，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>、<a href="/Linux%E6%9D%83%E9%99%90%E4%B8%8E%E5%B1%9E%E4%B8%BB%E7%AE%A1%E7%90%86-chmod-chown%E5%AE%9E%E6%88%98">chmod&#x2F;chown</a>、<a href="/grep%E6%AD%A3%E5%88%99%E5%8C%B9%E9%85%8D%E4%B8%8E%E4%B8%8A%E4%B8%8B%E6%96%87%E6%8E%A7%E5%88%B6%E5%AE%9E%E6%88%98">grep</a>。如果你的环境能装第三方工具，<a href="/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd">fd 语法比 find 直觉得多</a>——但装不上的场合，还得靠这篇。</p><h2 id="基本语法：起始路径、条件、动作三段式"><a href="#基本语法：起始路径、条件、动作三段式" class="headerlink" title="基本语法：起始路径、条件、动作三段式"></a>基本语法：起始路径、条件、动作三段式</h2><p><code>find</code> 命令的结构是固定的三段：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">find 起始路径 条件 动作</span><br></pre></td></tr></table></figure><p>比如 <code>find . -name &quot;*.log&quot; -print</code>：<code>.</code> 是起始路径（从当前目录开始找），<code>-name &quot;*.log&quot;</code> 是条件（文件名匹配这个模式），<code>-print</code> 是动作（把匹配到的路径打印出来）。</p><p>新手最容易搞混的地方是把条件和动作当成一回事。<code>-name</code>、<code>-type</code>、<code>-mtime</code> 这些是<strong>条件</strong>，负责”筛选出哪些文件符合要求”；<code>-print</code>、<code>-delete</code>、<code>-exec</code> 这些是<strong>动作</strong>，负责”对筛出来的文件做什么”。不写动作时 find 默认执行 <code>-print</code>，所以 <code>find . -name &quot;*.log&quot;</code> 和 <code>find . -name &quot;*.log&quot; -print</code> 效果一样——但显式写上 <code>-print</code>，尤其是命令里还有别的动作时，意图更清楚，不容易和条件搞混。</p><h2 id="按名字和类型找：-name、-iname、-type"><a href="#按名字和类型找：-name、-iname、-type" class="headerlink" title="按名字和类型找：-name、-iname、-type"></a>按名字和类型找：-name、-iname、-type</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.log&quot;</span></span><br><span class="line"><span class="comment">#     -name：按文件名匹配，区分大小写，*.log 和 *.LOG 是两回事</span></span><br><span class="line"></span><br><span class="line">find . -iname <span class="string">&quot;*.log&quot;</span></span><br><span class="line"><span class="comment">#     -iname：和 -name 一样，但不区分大小写，access.log、ACCESS.LOG 都能匹配到</span></span><br><span class="line"></span><br><span class="line">find . -<span class="built_in">type</span> f -name <span class="string">&quot;*.go&quot;</span></span><br><span class="line"><span class="comment">#     -type f：只要普通文件，不要目录</span></span><br><span class="line"><span class="comment">#     -type d：只要目录</span></span><br><span class="line"><span class="comment">#     -type l：只要符号链接</span></span><br><span class="line"></span><br><span class="line">find . -maxdepth 2 -name <span class="string">&quot;*.md&quot;</span></span><br><span class="line"><span class="comment">#     -maxdepth：限制递归深度，2 表示最多进两层子目录，不会一路挖到底</span></span><br><span class="line"><span class="comment">#     忘加这个参数是 find 跑得慢、结果里混进一堆不相关文件的常见原因</span></span><br><span class="line"></span><br><span class="line">find . -mindepth 1 -maxdepth 1 -<span class="built_in">type</span> d</span><br><span class="line"><span class="comment">#     -mindepth 1：不包括起始路径本身，只要子级</span></span><br><span class="line"><span class="comment">#     配合 -maxdepth 1，效果就是&quot;只列出当前目录下的一级子目录&quot;，不递归</span></span><br></pre></td></tr></table></figure><h2 id="按时间找：-mtime、-atime、-ctime-的区别"><a href="#按时间找：-mtime、-atime、-ctime-的区别" class="headerlink" title="按时间找：-mtime、-atime、-ctime 的区别"></a>按时间找：-mtime、-atime、-ctime 的区别</h2><p>三个时间条件容易搞混，先说清楚各自对应什么：</p><table><thead><tr><th>参数</th><th>对应的时间戳</th><th>什么情况下会变</th></tr></thead><tbody><tr><td><code>-mtime</code>（modify time）</td><td>内容修改时间</td><td>文件内容被写入&#x2F;修改</td></tr><tr><td><code>-atime</code>（access time）</td><td>访问时间</td><td>文件被读取（有些文件系统挂载时禁用了 atime 更新，出于性能考虑）</td></tr><tr><td><code>-ctime</code>（change time）</td><td>状态变更时间</td><td>文件的元数据变了——权限、属主、链接数，不一定是内容变了</td></tr></tbody></table><p>日常排查用得最多的是 <code>-mtime</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">find . -mtime -7</span><br><span class="line"><span class="comment">#     -7：前面带减号，表示&quot;7 天以内&quot;（比 7 天新）</span></span><br><span class="line"><span class="comment">#     符号方向：- 是&quot;比这个数小&quot;，+ 是&quot;比这个数大&quot;，不带符号是&quot;精确等于这个数&quot;</span></span><br><span class="line"></span><br><span class="line">find /var/log -mtime +30 -name <span class="string">&quot;*.log&quot;</span></span><br><span class="line"><span class="comment">#     +30：30 天以前修改过的日志文件，排查该清理哪些旧日志时常用</span></span><br><span class="line"></span><br><span class="line">find . -newer reference.txt -name <span class="string">&quot;*.go&quot;</span></span><br><span class="line"><span class="comment">#     -newer：找修改时间比 reference.txt 更新的文件，不用自己算具体天数</span></span><br></pre></td></tr></table></figure><h2 id="按大小找：-size-的符号前缀"><a href="#按大小找：-size-的符号前缀" class="headerlink" title="按大小找：-size 的符号前缀"></a>按大小找：-size 的符号前缀</h2><p><code>-size</code> 的符号方向和 <code>-mtime</code> 是同一套逻辑，但因为单位是大小不是时间，容易理解反：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">find . -size +100M</span><br><span class="line"><span class="comment">#     +100M：大于 100MB 的文件，排查磁盘占用时常用</span></span><br><span class="line"></span><br><span class="line">find . -size -1k</span><br><span class="line"><span class="comment">#     -1k：小于 1KB 的文件，找空文件或者几乎是空的配置文件</span></span><br><span class="line"></span><br><span class="line">find . -size 0</span><br><span class="line"><span class="comment">#     不带符号：精确匹配大小为 0 的文件，找真正的空文件</span></span><br></pre></td></tr></table></figure><p>单位后缀：<code>c</code> 是字节，<code>k</code> 是 KB，<code>M</code> 是 MB，<code>G</code> 是 GB，不写单位默认是 512 字节的块数（历史遗留，容易踩坑，写 size 条件时最好总是带上单位）。</p><h2 id="逻辑组合：-and、-or、-not-与括号分组"><a href="#逻辑组合：-and、-or、-not-与括号分组" class="headerlink" title="逻辑组合：-and、-or、-not 与括号分组"></a>逻辑组合：-and、-or、-not 与括号分组</h2><p>多个条件之间默认是”且”的关系，<code>-and</code> 可以省略：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">find . -<span class="built_in">type</span> f -name <span class="string">&quot;*.go&quot;</span></span><br><span class="line"><span class="comment">#     等价于 find . -type f -and -name &quot;*.go&quot;，-and 默认存在，通常不用写</span></span><br><span class="line"></span><br><span class="line">find . -name <span class="string">&quot;*.go&quot;</span> -or -name <span class="string">&quot;*.md&quot;</span></span><br><span class="line"><span class="comment">#     -or：找 .go 或 .md 文件，这个必须显式写，不写就是 -and 的默认关系</span></span><br><span class="line"></span><br><span class="line">find . -not -path <span class="string">&quot;*/.git/*&quot;</span> -name <span class="string">&quot;*.go&quot;</span></span><br><span class="line"><span class="comment">#     -not：排除条件，这里是&quot;路径不包含 .git 目录&quot;且&quot;文件名匹配 .go&quot;</span></span><br><span class="line"></span><br><span class="line">find . \( -name <span class="string">&quot;*.go&quot;</span> -or -name <span class="string">&quot;*.md&quot;</span> \) -not -path <span class="string">&quot;*/.git/*&quot;</span></span><br><span class="line"><span class="comment">#     括号分组控制优先级，圆括号在 shell 里有特殊含义，必须转义成 \( \)</span></span><br><span class="line"><span class="comment">#     这条的意思是：(.go 或 .md 文件) 并且 不在 .git 目录下</span></span><br></pre></td></tr></table></figure><h2 id="exec-的两种写法：-和-，性能差在哪"><a href="#exec-的两种写法：-和-，性能差在哪" class="headerlink" title="-exec 的两种写法：\; 和 +，性能差在哪"></a><code>-exec</code> 的两种写法：<code>\;</code> 和 <code>+</code>，性能差在哪</h2><p>这是 find 里最容易被忽略、但影响最大的细节。两种写法看起来只差一个符号，行为和性能却完全不同：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.log&quot;</span> -<span class="built_in">exec</span> gzip &#123;&#125; \;</span><br><span class="line"><span class="comment">#     \; 结尾：每找到一个文件，就单独起一次 gzip 命令处理这一个文件</span></span><br><span class="line"><span class="comment">#     &#123;&#125; 是占位符，代表当前这个匹配到的文件路径</span></span><br><span class="line"></span><br><span class="line">find . -name <span class="string">&quot;*.log&quot;</span> -<span class="built_in">exec</span> gzip &#123;&#125; +</span><br><span class="line"><span class="comment">#     + 结尾：尽量把多个文件路径打包，一次性作为参数传给 gzip</span></span><br><span class="line"><span class="comment">#     行为类似 xargs——攒够一批（受命令行长度上限约束）就调用一次</span></span><br></pre></td></tr></table></figure><p><strong>为什么性能差这么多</strong>：<code>\;</code> 结尾时，找到 1000 个文件，就要 fork 1000 次子进程、启动 1000 次 gzip——每次进程创建都有实打实的系统开销（内存分配、内核调度），文件越多，这个开销累加得越明显。<code>+</code> 结尾时，find 会把匹配到的文件路径尽量攒在一起，打包成尽可能少的调用次数，具体能攒多少受系统的 <code>ARG_MAX</code>（单次命令调用能传递的参数总长度上限）约束——Linux 上典型值是 2097152 字节（约 2MB，<code>getconf ARG_MAX</code> 能查到本机的实际值）。假设文件路径平均 50 字节，2MB 够装下四万多个路径，1000 个文件的路径总长度远小于这个上限，实际可能一次调用就打包完了，进程创建次数从”文件数”级别降到”个位数”，量级差出去很多。</p><p>日常写脚本的经验规则：只要命令支持一次接收多个文件路径（大多数命令都支持，比如 <code>rm</code>、<code>gzip</code>、<code>chmod</code>），优先用 <code>+</code>；只有命令一次只能处理一个文件、或者每个文件需要单独判断结果时，才用 <code>\;</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.tmp&quot;</span> -<span class="built_in">exec</span> <span class="built_in">rm</span> &#123;&#125; +</span><br><span class="line"><span class="comment">#     批量删除 .tmp 文件，用 + 一次调用处理完，比 \; 快得多</span></span><br></pre></td></tr></table></figure><h2 id="delete：比-exec-rm-更安全高效"><a href="#delete：比-exec-rm-更安全高效" class="headerlink" title="-delete：比 -exec rm 更安全高效"></a><code>-delete</code>：比 <code>-exec rm</code> 更安全高效</h2><p>需要删除匹配到的文件时，<code>find</code> 自带的 <code>-delete</code> 动作比 <code>-exec rm &#123;&#125; +</code> 更直接——不用再额外 fork 一个 <code>rm</code> 进程：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.tmp&quot;</span> -delete</span><br><span class="line"><span class="comment">#     -delete：直接删除匹配到的文件，不需要再调用外部 rm 命令</span></span><br></pre></td></tr></table></figure><p><strong>用之前一定要先确认范围</strong>：把 <code>-delete</code> 换成 <code>-print</code>，跑一遍看看到底会命中哪些文件，确认没问题了再换回 <code>-delete</code>——<code>find</code> 的条件写错很容易命中比预期更大的范围，删除操作又无法撤销：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.tmp&quot;</span> -<span class="built_in">print</span></span><br><span class="line"><span class="comment">#     先跑这条确认命中范围，看着输出结果没问题</span></span><br><span class="line">find . -name <span class="string">&quot;*.tmp&quot;</span> -delete</span><br><span class="line"><span class="comment">#     确认没问题后再换成 -delete 真正执行</span></span><br></pre></td></tr></table></figure><h2 id="两个常见坑：文件名带空格、通配符被-shell-提前展开"><a href="#两个常见坑：文件名带空格、通配符被-shell-提前展开" class="headerlink" title="两个常见坑：文件名带空格、通配符被 shell 提前展开"></a>两个常见坑：文件名带空格、通配符被 shell 提前展开</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">find . -name <span class="string">&quot;*.txt&quot;</span> -print0 | xargs -0 <span class="built_in">rm</span></span><br><span class="line"><span class="comment">#     -print0：文件名之间用 null 字符（\0）分隔，而不是换行或空格</span></span><br><span class="line"><span class="comment">#     xargs -0：配合读取 null 分隔的输入</span></span><br><span class="line"><span class="comment">#     文件名里带空格（比如 &quot;my file.txt&quot;）时，普通的换行分隔会被 xargs 误判成两个参数，</span></span><br><span class="line"><span class="comment">#     null 分隔是唯一不会出现在合法文件名里的字符，能保证安全</span></span><br><span class="line"></span><br><span class="line">find . -name *.txt</span><br><span class="line"><span class="comment">#     危险写法：*.txt 没加引号，如果当前目录下已经有匹配 *.txt 的文件，</span></span><br><span class="line"><span class="comment">#     shell 会在 find 执行前就把通配符展开成具体文件名，find 实际收到的</span></span><br><span class="line"><span class="comment">#     是一堆文件名而不是模式字符串，只会在这些文件里精确匹配，行为完全不对</span></span><br><span class="line"></span><br><span class="line">find . -name <span class="string">&quot;*.txt&quot;</span></span><br><span class="line"><span class="comment">#     正确写法：加引号，让 *.txt 原样传给 find，由 find 自己去做模式匹配</span></span><br></pre></td></tr></table></figure><h2 id="find-常用参数速查表"><a href="#find-常用参数速查表" class="headerlink" title="find 常用参数速查表"></a>find 常用参数速查表</h2><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-name PATTERN</code></td><td>按文件名匹配，区分大小写</td></tr><tr><td><code>-iname PATTERN</code></td><td>按文件名匹配，不区分大小写</td></tr><tr><td><code>-type f</code> &#x2F; <code>d</code> &#x2F; <code>l</code></td><td>只要普通文件 &#x2F; 目录 &#x2F; 符号链接</td></tr><tr><td><code>-maxdepth N</code></td><td>最多递归 N 层</td></tr><tr><td><code>-mindepth N</code></td><td>至少从第 N 层开始算</td></tr><tr><td><code>-mtime ±N</code></td><td>内容修改时间在 N 天以内（<code>-</code>）&#x2F; 以前（<code>+</code>）&#x2F; 精确 N 天（不带符号）</td></tr><tr><td><code>-atime ±N</code></td><td>访问时间，符号规则同 <code>-mtime</code></td></tr><tr><td><code>-ctime ±N</code></td><td>元数据（权限&#x2F;属主）变更时间，符号规则同 <code>-mtime</code></td></tr><tr><td><code>-newer FILE</code></td><td>修改时间比 FILE 更新</td></tr><tr><td><code>-size ±N[ckMG]</code></td><td>文件大小，符号规则同 <code>-mtime</code>，单位 c&#x2F;k&#x2F;M&#x2F;G</td></tr><tr><td><code>-and</code></td><td>且，默认关系，通常省略</td></tr><tr><td><code>-or</code></td><td>或，必须显式写</td></tr><tr><td><code>-not</code></td><td>非，排除条件</td></tr><tr><td><code>\( ... \)</code></td><td>括号分组，圆括号需转义</td></tr><tr><td><code>-exec CMD &#123;&#125; \;</code></td><td>对每个匹配文件单独执行一次命令</td></tr><tr><td><code>-exec CMD &#123;&#125; +</code></td><td>尽量批量打包，减少命令调用次数</td></tr><tr><td><code>-delete</code></td><td>直接删除匹配到的文件</td></tr><tr><td><code>-print0</code></td><td>用 null 字符分隔输出，配合 <code>xargs -0</code> 处理带空格的文件名</td></tr></tbody></table><h2 id="find-之外"><a href="#find-之外" class="headerlink" title="find 之外"></a>find 之外</h2><p>装得上第三方工具的话，<a href="/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd">fd</a> 默认行为更符合直觉（自动跳过 <code>.gitignore</code>、语法更接近自然语言），日常开发优先用它，<code>find</code> 留给装不上工具的场合。Linux 命令系列下一篇计划写 <code>rsync</code> 或 <code>tar</code>——都是比 <code>find</code> 参数量级还大的命令，增量同步、归档压缩各有一堆细节值得展开。</p>]]>
    </content>
    <id>https://blog.codewater.top/find%E6%9D%A1%E4%BB%B6%E7%BB%84%E5%90%88%E4%B8%8Eexec%E6%80%A7%E8%83%BD%E5%AE%9E%E6%88%98/</id>
    <link href="https://blog.codewater.top/find%E6%9D%A1%E4%BB%B6%E7%BB%84%E5%90%88%E4%B8%8Eexec%E6%80%A7%E8%83%BD%E5%AE%9E%E6%88%98/"/>
    <published>2026-07-11T07:00:00.000Z</published>
    <summary>find 的条件与动作怎么分、-mtime/-size 符号方向、-exec 用 \; 和 + 性能差在哪，一篇讲清楚原生 find 的实战用法。</summary>
    <title>find：条件组合、-exec 两种写法与性能实战</title>
    <updated>2026-07-11T07:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="grep" scheme="https://blog.codewater.top/tags/grep/"/>
    <category term="正则表达式" scheme="https://blog.codewater.top/tags/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/"/>
    <content>
      <![CDATA[<p>本地终端习惯了用 ripgrep，SSH 到一台精简过的服务器或者容器镜像上，才发现根本没装 <code>rg</code>，只能用回系统自带的 grep。这时候才发现自己其实记不住 grep 的参数——<code>-E</code> 和 <code>-P</code> 到底有什么区别、括号要不要转义、上下文参数是哪三个字母。</p><p>grep 是所有 Linux 发行版都自带的工具，不需要额外安装，这篇把它自己的用法讲清楚。</p><span id="more"></span><p>这是 Linux 常见命令系列第二篇详细教程，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>。如果你的环境能装第三方工具，<a href="/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd">ripgrep 通常是更好的选择</a>——但装不上的场合，还得靠这篇。</p><h2 id="三种正则模式：BRE、-E、-P-到底有什么区别"><a href="#三种正则模式：BRE、-E、-P-到底有什么区别" class="headerlink" title="三种正则模式：BRE、-E、-P 到底有什么区别"></a>三种正则模式：BRE、-E、-P 到底有什么区别</h2><p>这是 grep 里最容易搞混的地方，因为同一个符号在不同模式下含义不一样。</p><p><strong>默认模式是 BRE（Basic Regular Expression，基础正则）</strong>：<code>+</code>、<code>?</code>、<code>|</code>、<code>(</code>、<code>)</code>、<code>&#123;</code>、<code>&#125;</code> 这几个符号<strong>不是</strong>特殊字符，是普通文本本身；要让它们起特殊作用，得加反斜杠转义成 <code>\+</code>、<code>\?</code>、<code>\|</code>。这一点非常反直觉——很多人写 <code>grep &quot;a+b&quot;</code> 以为在匹配”一个或多个 a 后面跟 b”，实际在 BRE 下匹配的是字面的字符串 <code>a+b</code>。</p><p><strong><code>-E</code> 启用 ERE（Extended Regular Expression，扩展正则，等价于老命令 <code>egrep</code>）</strong>：上面那几个符号反过来变成特殊字符，不用转义就能用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">grep <span class="string">&#x27;a\+b&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     BRE 默认模式：\+ 转义后才是&quot;一个或多个 a&quot;，匹配 ab、aab、aaab...</span></span><br><span class="line"></span><br><span class="line">grep -E <span class="string">&#x27;a+b&#x27;</span> file.txt</span><br><span class="line"><span class="comment">#     -E 扩展正则：+ 天生就是特殊字符，效果和上面那条一样</span></span><br><span class="line"><span class="comment">#     -E 等价于直接用 egrep 命令（egrep 只是 grep -E 的别名，新脚本建议用 -E）</span></span><br></pre></td></tr></table></figure><p><strong><code>-P</code> 启用 PCRE（Perl 兼容正则）</strong>：语法最丰富，<code>\d</code>（数字）、<code>\s</code>（空白）、<code>\w</code>（单词字符）、<code>\b</code>（单词边界）、非贪婪匹配 <code>.*?</code> 这些 BRE 和 -E 都不支持的写法，只有 -P 能用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">grep -P <span class="string">&#x27;\d&#123;3&#125;-\d&#123;4&#125;&#x27;</span> contacts.txt</span><br><span class="line"><span class="comment">#     -P：Perl 正则，\d&#123;3&#125; 表示连续 3 个数字，匹配形如 123-4567 的号码</span></span><br><span class="line"><span class="comment">#     BRE/-E 下要写成等价的 [0-9]\&#123;3\&#125; 或 [0-9]&#123;3&#125;，没有 \d 这种简写</span></span><br></pre></td></tr></table></figure><p>同一个”匹配一个或多个数字”的需求，在三种模式下写法完全不同：</p><table><thead><tr><th>需求</th><th>BRE（默认）</th><th><code>-E</code></th><th><code>-P</code></th></tr></thead><tbody><tr><td>一个或多个数字</td><td><code>[0-9]\+</code></td><td><code>[0-9]+</code></td><td><code>\d+</code></td></tr><tr><td>括号分组</td><td><code>\(abc\)</code></td><td><code>(abc)</code></td><td><code>(abc)</code></td></tr><tr><td>或（多选一）</td><td><code>a|b</code>（GNU 扩展，非 POSIX 标准）</td><td><code>a|b</code></td><td><code>a|b</code></td></tr></tbody></table><p><strong>一个容易踩的坑</strong>：<code>-P</code> 依赖 grep 编译时链接了 PCRE 库，绝大多数主流发行版的 grep 都带这个支持，但个别精简过的容器镜像可能没有，报 <code>grep: this platform does not support -P</code> 时就只能退回 -E 或 BRE 手动转义。日常写脚本，如果不确定目标机器的 grep 支持情况，优先用 <code>-E</code>——兼容性最好，语法也不用记转义规则。</p><h2 id="基础用法：递归、忽略大小写、反向匹配"><a href="#基础用法：递归、忽略大小写、反向匹配" class="headerlink" title="基础用法：递归、忽略大小写、反向匹配"></a>基础用法：递归、忽略大小写、反向匹配</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">grep -r <span class="string">&quot;TODO&quot;</span> src/</span><br><span class="line"><span class="comment">#     -r：递归搜索目录下所有文件</span></span><br><span class="line"></span><br><span class="line">grep -R <span class="string">&quot;TODO&quot;</span> src/</span><br><span class="line"><span class="comment">#     -R：和 -r 的唯一区别是符号链接的处理，见下面的说明</span></span><br><span class="line"></span><br><span class="line">grep -i <span class="string">&quot;error&quot;</span> app.log</span><br><span class="line"><span class="comment">#     -i：忽略大小写，ERROR、Error、error 都能匹配到</span></span><br></pre></td></tr></table></figure><p><strong>先说清楚一个前提</strong>：不加 <code>-r</code>&#x2F;<code>-R</code>，grep 根本不会进目录递归——不管这个参数是真实目录还是指向目录的符号链接，直接报错 <code>grep: xxx: Is a directory</code>，跳过不搜。要进目录搜索，<code>-r</code> 是必须的，”跟不跟随符号链接”是加了 <code>-r</code> 之后才谈得上的下一层问题：</p><ul><li><code>-r</code>：命令行里直接传的符号链接（比如 <code>grep -r &quot;TODO&quot; src/shared-link</code>，<code>shared-link</code> 指向另一个目录）会被跟随，当成真目录进去搜；但递归过程中、在 <code>src/</code> 内部某个子目录里才碰到的符号链接，不会跟随，直接跳过</li><li><code>-R</code>：不区分是不是命令行直接给的，只要递归时遇到符号链接指向目录就都跟随进去搜</li></ul><p>如果符号链接指向的是<strong>文件</strong>而不是目录，这个规则完全不适用——不需要 <code>-r</code>，直接 <code>grep &quot;pattern&quot; 符号链接文件</code> 就能读到内容，因为这只是普通的文件读取，操作系统打开文件时本来就会跟随符号链接，跟目录递归是两回事。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">grep -v <span class="string">&quot;DEBUG&quot;</span> app.log</span><br><span class="line"><span class="comment">#     -v：反向匹配，输出不包含 DEBUG 的行——排查日志时过滤掉噪音行常用</span></span><br></pre></td></tr></table></figure><h2 id="只要结果的一部分：-l、-L、-c、-o、-n"><a href="#只要结果的一部分：-l、-L、-c、-o、-n" class="headerlink" title="只要结果的一部分：-l、-L、-c、-o、-n"></a>只要结果的一部分：-l、-L、-c、-o、-n</h2><p>默认 grep 输出的是匹配到的整行，但很多时候你只想要文件名、数量，或者匹配的那一小段：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">grep -l <span class="string">&quot;TODO&quot;</span> src/**/*.go</span><br><span class="line"><span class="comment">#     -l：只列出包含匹配内容的文件名，不显示具体是哪一行</span></span><br><span class="line"></span><br><span class="line">grep -L <span class="string">&quot;TODO&quot;</span> src/**/*.go</span><br><span class="line"><span class="comment">#     -L：反过来，列出不包含匹配内容的文件名</span></span><br><span class="line"></span><br><span class="line">grep -c <span class="string">&quot;ERROR&quot;</span> app.log</span><br><span class="line"><span class="comment">#     -c：只输出匹配的行数，不显示内容本身，快速确认量级用</span></span><br><span class="line"></span><br><span class="line">grep -o -E <span class="string">&#x27;[0-9]+\.[0-9]+\.[0-9]+&#x27;</span> package.json</span><br><span class="line"><span class="comment">#     -o：只输出匹配到的那部分文本，不是整行；这里配合 -E 提取版本号</span></span><br><span class="line"></span><br><span class="line">grep -n <span class="string">&quot;func main&quot;</span> main.go</span><br><span class="line"><span class="comment">#     -n：匹配的每一行前面带上行号，定位到编辑器里直接跳转用</span></span><br></pre></td></tr></table></figure><h2 id="精确匹配：-w-整词、-x-整行"><a href="#精确匹配：-w-整词、-x-整行" class="headerlink" title="精确匹配：-w 整词、-x 整行"></a>精确匹配：-w 整词、-x 整行</h2><p>不加限制的话，<code>grep &quot;cat&quot;</code> 会把 <code>category</code>、<code>concatenate</code> 这类包含 “cat” 子串的词也匹配上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">grep -w <span class="string">&quot;cat&quot;</span> animals.txt</span><br><span class="line"><span class="comment">#     -w：整词匹配，只匹配独立的单词 cat，不会命中 category 里的 cat</span></span><br><span class="line"></span><br><span class="line">grep -x <span class="string">&quot;done&quot;</span> status.txt</span><br><span class="line"><span class="comment">#     -x：整行匹配。status.txt 里某一行如果内容恰好就是 &quot;done&quot; 这四个字符，能匹配上；</span></span><br><span class="line"><span class="comment">#     如果那一行是 &quot;task done&quot; 或者 &quot;done!&quot;，虽然包含 &quot;done&quot; 这个子串，但整行内容不完全等于 &quot;done&quot;，匹配不上</span></span><br></pre></td></tr></table></figure><h2 id="上下文控制：-A、-B、-C"><a href="#上下文控制：-A、-B、-C" class="headerlink" title="上下文控制：-A、-B、-C"></a>上下文控制：-A、-B、-C</h2><p>排查报错时，只看到匹配的那一行往往不够，前后几行的上下文才能定位问题：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">grep -A 3 <span class="string">&quot;Exception&quot;</span> app.log</span><br><span class="line"><span class="comment">#     -A（after）：匹配行之后再多输出 3 行，看报错发生后系统还做了什么</span></span><br><span class="line"></span><br><span class="line">grep -B 3 <span class="string">&quot;Exception&quot;</span> app.log</span><br><span class="line"><span class="comment">#     -B（before）：匹配行之前再多输出 3 行，看报错发生前的调用链</span></span><br><span class="line"></span><br><span class="line">grep -C 3 <span class="string">&quot;Exception&quot;</span> app.log</span><br><span class="line"><span class="comment">#     -C（context）：等价于同时加 -A 3 -B 3，前后各多输出 3 行</span></span><br></pre></td></tr></table></figure><p>多个不连续的匹配段之间，grep 会用 <code>--</code> 分隔线隔开，方便肉眼区分不同的匹配区块。</p><h2 id="按文件类型过滤：–include、–exclude、–exclude-dir"><a href="#按文件类型过滤：–include、–exclude、–exclude-dir" class="headerlink" title="按文件类型过滤：–include、–exclude、–exclude-dir"></a>按文件类型过滤：–include、–exclude、–exclude-dir</h2><p>递归搜索整个项目目录时，通常不想搜到 <code>node_modules</code>、构建产物这些无关内容：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">grep -r <span class="string">&quot;TODO&quot;</span> . --include=<span class="string">&quot;*.go&quot;</span></span><br><span class="line"><span class="comment">#     --include：只在匹配这个 glob 模式的文件里搜，这里只搜 .go 文件</span></span><br><span class="line"></span><br><span class="line">grep -r <span class="string">&quot;TODO&quot;</span> . --exclude=<span class="string">&quot;*.min.js&quot;</span></span><br><span class="line"><span class="comment">#     --exclude：排除匹配这个模式的文件，压缩后的 JS 文件通常没必要搜</span></span><br><span class="line"></span><br><span class="line">grep -r <span class="string">&quot;TODO&quot;</span> . --exclude-dir=<span class="string">&quot;node_modules&quot;</span> --exclude-dir=<span class="string">&quot;.git&quot;</span></span><br><span class="line"><span class="comment">#     --exclude-dir：整个目录都跳过，node_modules 和 .git 是最常见的两个</span></span><br></pre></td></tr></table></figure><p>这几个选项只在配合 <code>-r</code>&#x2F;<code>-R</code> 递归搜索时才有意义，搜单个文件用不上。</p><h2 id="和管道、xargs-组合的实战"><a href="#和管道、xargs-组合的实战" class="headerlink" title="和管道、xargs 组合的实战"></a>和管道、xargs 组合的实战</h2><p>grep 真正的威力在于能接进管道，和其他命令组合：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">grep -l <span class="string">&quot;deprecated_func&quot;</span> src/**/*.go | xargs grep -n <span class="string">&quot;deprecated_func&quot;</span></span><br><span class="line"><span class="comment">#     先用 -l 找出所有包含 deprecated_func 的文件（只要文件名）</span></span><br><span class="line"><span class="comment">#     再用 xargs 把这些文件名传给第二个 grep，这次带上 -n 看具体在哪一行</span></span><br><span class="line"><span class="comment">#     两步拆开的好处：第一步先确认&quot;影响范围有多大&quot;，再决定要不要继续深入</span></span><br><span class="line"></span><br><span class="line">grep -r <span class="string">&quot;TODO&quot;</span> . --include=<span class="string">&quot;*.go&quot;</span> | grep -v <span class="string">&quot;TODO(done)&quot;</span></span><br><span class="line"><span class="comment">#     链式过滤：第一个 grep 找出所有 TODO，第二个 grep 用 -v 排除已经标记完成的</span></span><br><span class="line"><span class="comment">#     两个 grep 串联，比写一条复杂的正则更好读</span></span><br><span class="line"></span><br><span class="line">grep -l <span class="string">&quot;old_config_key&quot;</span> config/*.yaml | xargs sed -i <span class="string">&#x27;s/old_config_key/new_config_key/g&#x27;</span></span><br><span class="line"><span class="comment">#     场景：配置项改名了（old_config_key 要统一换成 new_config_key），但不确定散落在多少个 yaml 文件里</span></span><br><span class="line"><span class="comment">#     grep -l 先扫一遍，只列出真正包含 old_config_key 的文件名（没提到的文件完全不动）</span></span><br><span class="line"><span class="comment">#     xargs 把这些文件名接力传给 sed，对每个文件执行替换，一条命令完成&quot;找出范围 + 批量改名&quot;两件事</span></span><br><span class="line"><span class="comment">#     -i：sed 的原地修改参数，不加的话 sed 默认只输出到 stdout，不会真的改文件内容</span></span><br></pre></td></tr></table></figure><p><code>grep -l | xargs</code> 这个组合的价值在于分两步走：第一步只是”侦察”，看看改动会影响哪些文件，确认范围没问题了再执行第二步的实际操作，比直接一条命令梭哈更安全。</p><h2 id="grep-常用参数速查表"><a href="#grep-常用参数速查表" class="headerlink" title="grep 常用参数速查表"></a>grep 常用参数速查表</h2><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-r</code>, <code>--recursive</code></td><td>递归搜索目录，只跟随命令行直接给出的符号链接</td></tr><tr><td><code>-R</code>, <code>--dereference-recursive</code></td><td>递归搜索目录，跟随所有遇到的符号链接</td></tr><tr><td><code>-i</code>, <code>--ignore-case</code></td><td>忽略大小写</td></tr><tr><td><code>-v</code>, <code>--invert-match</code></td><td>反向匹配，输出不匹配的行</td></tr><tr><td><code>-l</code>, <code>--files-with-matches</code></td><td>只列出有匹配的文件名</td></tr><tr><td><code>-L</code>, <code>--files-without-match</code></td><td>只列出没有匹配的文件名</td></tr><tr><td><code>-c</code>, <code>--count</code></td><td>只输出匹配的行数</td></tr><tr><td><code>-o</code>, <code>--only-matching</code></td><td>只输出匹配到的部分，不是整行</td></tr><tr><td><code>-n</code>, <code>--line-number</code></td><td>显示匹配行的行号</td></tr><tr><td><code>-w</code>, <code>--word-regexp</code></td><td>整词匹配</td></tr><tr><td><code>-x</code>, <code>--line-regexp</code></td><td>整行匹配</td></tr><tr><td><code>-A NUM</code>, <code>--after-context=NUM</code></td><td>额外输出匹配行之后 NUM 行</td></tr><tr><td><code>-B NUM</code>, <code>--before-context=NUM</code></td><td>额外输出匹配行之前 NUM 行</td></tr><tr><td><code>-C NUM</code>, <code>--context=NUM</code></td><td>等价于 <code>-A NUM -B NUM</code></td></tr><tr><td><code>-E</code>, <code>--extended-regexp</code></td><td>启用扩展正则（等价于 <code>egrep</code>）</td></tr><tr><td><code>-P</code>, <code>--perl-regexp</code></td><td>启用 Perl 兼容正则</td></tr><tr><td><code>--include=GLOB</code></td><td>只在匹配 GLOB 的文件里搜（配合 <code>-r</code>&#x2F;<code>-R</code>）</td></tr><tr><td><code>--exclude=GLOB</code></td><td>排除匹配 GLOB 的文件（配合 <code>-r</code>&#x2F;<code>-R</code>）</td></tr><tr><td><code>--exclude-dir=GLOB</code></td><td>排除匹配 GLOB 的目录（配合 <code>-r</code>&#x2F;<code>-R</code>）</td></tr></tbody></table><h2 id="grep-之外"><a href="#grep-之外" class="headerlink" title="grep 之外"></a>grep 之外</h2><p>以上这些参数，ripgrep 全部支持而且默认行为更省心（自动递归、自动跳过 <code>.gitignore</code>），装得上就优先用 <a href="/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd">ripgrep</a>。Linux 命令系列下一篇计划写 <code>find</code>——参数量级比 grep 还大，文件查找的条件组合、<code>-exec</code> 的两种写法够写满一整篇。</p>]]>
    </content>
    <id>https://blog.codewater.top/grep%E6%AD%A3%E5%88%99%E5%8C%B9%E9%85%8D%E4%B8%8E%E4%B8%8A%E4%B8%8B%E6%96%87%E6%8E%A7%E5%88%B6%E5%AE%9E%E6%88%98/</id>
    <link href="https://blog.codewater.top/grep%E6%AD%A3%E5%88%99%E5%8C%B9%E9%85%8D%E4%B8%8E%E4%B8%8A%E4%B8%8B%E6%96%87%E6%8E%A7%E5%88%B6%E5%AE%9E%E6%88%98/"/>
    <published>2026-07-11T02:00:00.000Z</published>
    <summary>grep 的 BRE/-E/-P 三种正则模式区别在哪、-A/-B/-C 上下文控制怎么用、配合 xargs 批量处理匹配文件，一篇讲清楚原生 grep。</summary>
    <title>grep：正则匹配、上下文控制与 -E/-P 实战</title>
    <updated>2026-07-11T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="权限管理" scheme="https://blog.codewater.top/tags/%E6%9D%83%E9%99%90%E7%AE%A1%E7%90%86/"/>
    <category term="chmod" scheme="https://blog.codewater.top/tags/chmod/"/>
    <category term="chown" scheme="https://blog.codewater.top/tags/chown/"/>
    <content>
      <![CDATA[<p>一个目录权限乱了——可能是遗留脚本用 root 建的文件，也可能是 SCP 传过来的文件属主对不上——网站访问直接 403，部署脚本报 <code>Permission denied</code>。这时候大部分人的第一反应是抄一条 <code>chmod -R 777</code>，网站是能访问了，但等于把整个目录对所有人开放读写执行，治标不治本，出了安全事故说不清楚。</p><p>chmod 和 chown 是治本的工具，但数字模式怎么算、setuid&#x2F;setgid&#x2F;sticky bit 到底在干嘛，很多人一直没搞清楚过。这篇把这两个命令的实际用法过一遍，最后用一个真实场景收尾。</p><span id="more"></span><p>这是 Linux 常见命令系列的第一篇详细教程，承接 <a href="/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE">Linux 常见命令与工具地图</a>。</p><h2 id="权限模型回顾：读懂-ls-l-那串字符"><a href="#权限模型回顾：读懂-ls-l-那串字符" class="headerlink" title="权限模型回顾：读懂 ls -l 那串字符"></a>权限模型回顾：读懂 <code>ls -l</code> 那串字符</h2><p><code>ls -l</code> 输出的第一列长这样：<code>-rwxr-xr--</code>。十个字符拆成四段：</p><ul><li>第 1 位：文件类型（<code>-</code> 普通文件，<code>d</code> 目录，<code>l</code> 符号链接）</li><li>第 2-4 位：<strong>owner</strong>（文件所有者）的读写执行权限</li><li>第 5-7 位：<strong>group</strong>（所属组）的读写执行权限</li><li>第 8-10 位：<strong>other</strong>（其他所有人）的读写执行权限</li></ul><p>每一组里 <code>r</code>&#x2F;<code>w</code>&#x2F;<code>x</code> 对应的数字是固定的：<code>r=4</code>、<code>w=2</code>、<code>x=1</code>，一组权限的数字就是这三个数加起来。<code>rwxr-xr--</code> 换算成数字就是 <code>754</code>——owner 是 4+2+1&#x3D;7，group 是 4+0+1&#x3D;5，other 是 4+0+0&#x3D;4。下一节的数字模式全靠这套加法。</p><h2 id="chmod-数字模式：三位数背后的加法"><a href="#chmod-数字模式：三位数背后的加法" class="headerlink" title="chmod 数字模式：三位数背后的加法"></a>chmod 数字模式：三位数背后的加法</h2><p>数字模式就是把 owner、group、other 三组权限各自换算成 0-7 的数字，拼在一起传给 chmod：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> 644 config.yaml</span><br><span class="line"><span class="comment">#     6=4+2：owner 读写，不能执行</span></span><br><span class="line"><span class="comment">#     4：group 只读</span></span><br><span class="line"><span class="comment">#     4：other 只读</span></span><br><span class="line"><span class="comment">#     配置文件的标准权限——自己能改，别人只能看</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">chmod</span> 755 deploy.sh</span><br><span class="line"><span class="comment">#     7=4+2+1：owner 读写执行</span></span><br><span class="line"><span class="comment">#     5=4+1：group、other 能读能执行，不能改</span></span><br><span class="line"><span class="comment">#     可执行脚本的标准权限</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">chmod</span> 600 id_rsa</span><br><span class="line"><span class="comment">#     6：owner 读写</span></span><br><span class="line"><span class="comment">#     0、0：group、other 什么都不能干</span></span><br><span class="line"><span class="comment">#     私钥文件必须这么设，权限太开 SSH 会直接拒绝使用这把钥匙</span></span><br></pre></td></tr></table></figure><p>记这三组数字对（644 配置、755 可执行、600 私钥）就能覆盖日常八成场景，其余情况现算：想清楚 owner&#x2F;group&#x2F;other 各自要不要读、写、执行，三个数字加出来拼在一起就是。</p><h2 id="chmod-符号模式：什么时候比数字模式好用"><a href="#chmod-符号模式：什么时候比数字模式好用" class="headerlink" title="chmod 符号模式：什么时候比数字模式好用"></a>chmod 符号模式：什么时候比数字模式好用</h2><p>数字模式的前提是你想清楚了三组权限的最终状态。但很多时候你只想”在现有权限基础上加一位”，这时候符号模式更顺手：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> u+x run.sh</span><br><span class="line"><span class="comment">#     u：user（owner的简写），+x：加上执行权限，不动其他位</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">chmod</span> g-w shared/</span><br><span class="line"><span class="comment">#     g：group，-w：去掉写权限，owner 和 other 的权限不受影响</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">chmod</span> o=r public.conf</span><br><span class="line"><span class="comment">#     o：other，=r：直接把 other 的权限设成只读（=是赋值，不是叠加）</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">chmod</span> a+X find_scripts/*</span><br><span class="line"><span class="comment">#     a：all（u+g+o 的简写），+X：对目录总是加执行位（方便进入），</span></span><br><span class="line"><span class="comment">#     对普通文件只有已经对某个身份（owner/group/other 任一）开了执行权限才加，</span></span><br><span class="line"><span class="comment">#     不会把本来不可执行的普通文件也变成可执行文件——这正是它和小写 x 的区别</span></span><br></pre></td></tr></table></figure><p><code>u</code>&#x2F;<code>g</code>&#x2F;<code>o</code>&#x2F;<code>a</code> 决定改谁，<code>+</code>&#x2F;<code>-</code>&#x2F;<code>=</code> 决定加、减还是直接赋值，<code>r</code>&#x2F;<code>w</code>&#x2F;<code>x</code>&#x2F;<code>X</code> 决定改哪个权限位。这四个维度组合起来，比每次都要心算数字模式更不容易出错。</p><h2 id="特殊权限位：setuid、setgid、sticky-bit-到底在干嘛"><a href="#特殊权限位：setuid、setgid、sticky-bit-到底在干嘛" class="headerlink" title="特殊权限位：setuid、setgid、sticky bit 到底在干嘛"></a>特殊权限位：setuid、setgid、sticky bit 到底在干嘛</h2><p>回顾第一节的权限模型：<code>rwx</code> 三组权限，每一位要么是字母要么是 <code>-</code>。但用 <code>ls -l</code> 查看某些文件或目录时，你会碰到不在这个范围内的符号：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">-rwsr-xr-x  1 root root  /usr/bin/passwd</span><br><span class="line">drwxrwsr-x  2 root dev   /srv/shared-project</span><br><span class="line">drwxrwxrwt 10 root root  /tmp</span><br></pre></td></tr></table></figure><p>第一行 owner 的 x 位置变成了 <code>s</code>，第二行 group 的 x 位置也是 <code>s</code>，第三行 other 的 x 位置变成了 <code>t</code>。这三个符号不属于 rwx 常规模型，就是这一节要讲的<strong>特殊权限位</strong>：setuid、setgid、sticky bit，分别顶替 owner &#x2F; group &#x2F; other 三组里的 x 位置。它们不是额外发明出来的权限，而是在执行权限基础上叠加的特殊标记，各自解决一个具体问题，一个一个来看。</p><h3 id="setuid：让别人执行你的程序时，暂时”变成你”"><a href="#setuid：让别人执行你的程序时，暂时”变成你”" class="headerlink" title="setuid：让别人执行你的程序时，暂时”变成你”"></a>setuid：让别人执行你的程序时，暂时”变成你”</h3><p>普通用户能改自己的密码，但密码是存在 <code>/etc/shadow</code> 里的，这个文件只有 root 能写。矛盾就在这——普通用户没有权限写 <code>/etc/shadow</code>，却要靠 <code>passwd</code> 这个程序去改它。</p><p><code>setuid</code> 就是解决这个矛盾的：给 <code>passwd</code> 这个可执行文件加上 setuid，效果是<strong>不管谁来执行它，执行的过程中都”变成”这个文件的所有者</strong>（也就是 root）。所以普通用户执行 <code>passwd</code>，程序运行期间临时有了 root 的权限，改完密码，权限又还回去。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> u+s /usr/bin/passwd</span><br><span class="line"><span class="comment">#     setuid 只对可执行文件有意义，加在目录上没有效果，会被系统忽略</span></span><br></pre></td></tr></table></figure><p>只对可执行文件生效，这个属性本身也有风险——如果一个装了 setuid 的程序本身有漏洞，攻击者可能借着它拿到 root 权限，所以只在真正需要的地方加，不要乱设。</p><h3 id="setgid：让共享文件夹里新建的文件自动归到同一个组"><a href="#setgid：让共享文件夹里新建的文件自动归到同一个组" class="headerlink" title="setgid：让共享文件夹里新建的文件自动归到同一个组"></a>setgid：让共享文件夹里新建的文件自动归到同一个组</h3><p>团队共享一个目录时经常遇到这种烦心事：A 建的文件属组是 A 的主组，B 建的文件属组是 B 的主组，两人互相没权限改对方的文件，得手动 <code>chgrp</code> 一遍遍改。</p><p><code>setgid</code> 加在<strong>目录</strong>上就能解决：这个目录下新建的任何文件和子目录，属组都自动跟目录本身一致，不看是谁建的。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> g+s /srv/shared-project</span><br><span class="line"><span class="comment">#     之后组内任何人在这个目录下新建文件，属组都自动是 shared-project 的组</span></span><br><span class="line"><span class="comment">#     不用每次手动 chgrp，协作目录必备</span></span><br></pre></td></tr></table></figure><p>（setgid 加在可执行文件上也有效果，跟 setuid 类似——变成文件所属组的身份，但这个用法比加在目录上少见得多，日常基本用不到。）</p><h3 id="sticky-bit：谁建的文件谁能删，别人删不了"><a href="#sticky-bit：谁建的文件谁能删，别人删不了" class="headerlink" title="sticky bit：谁建的文件谁能删，别人删不了"></a>sticky bit：谁建的文件谁能删，别人删不了</h3><p>想象小区里的公共快递柜：谁都能往里放包裹，但只有放的人自己能取走，别人拿不走你的包裹。<code>/tmp</code> 目录就是这个逻辑——所有人都能在里面写文件（不然没法用），但如果没有额外限制，任何人也能删掉别人的临时文件，那就乱套了。</p><p><code>sticky bit</code> 加在<strong>目录</strong>上，效果是：即使某人对这个目录有写权限，也只能删自己建的文件，删不了别人的。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +t /tmp</span><br><span class="line"><span class="comment">#     /tmp 默认就是这个设置，所有人能写，但谁也删不了别人的文件</span></span><br></pre></td></tr></table></figure><h3 id="三个位对应的数字，以及-ls-l-怎么看"><a href="#三个位对应的数字，以及-ls-l-怎么看" class="headerlink" title="三个位对应的数字，以及 ls -l 怎么看"></a>三个位对应的数字，以及 <code>ls -l</code> 怎么看</h3><p>三个特殊位在数字模式里对应第四位（写在平时那三位数字前面），可以叠加：</p><table><thead><tr><th>权限位</th><th>数字</th><th>加在哪</th><th><code>ls -l</code> 里顶替谁的 x</th><th>一句话</th></tr></thead><tbody><tr><td>setuid</td><td>4</td><td>可执行文件</td><td><strong>owner</strong> 组的 x（第 3 位）</td><td>别人执行时临时”变成”文件所有者</td></tr><tr><td>setgid</td><td>2</td><td>目录</td><td><strong>group</strong> 组的 x（第 6 位）</td><td>目录下新建的文件自动跟目录同一个组</td></tr><tr><td>sticky bit</td><td>1</td><td>目录</td><td><strong>other</strong> 组的 x（第 9 位）</td><td>谁建的文件谁能删，别人删不了</td></tr></tbody></table><p>三个位置各管各的，不会挤在一起：setuid 是 owner 的事，setgid 是 group 的事，sticky 是 other 的事，加在哪组权限上就顶替哪组的 x。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> 4755 /usr/bin/passwd     <span class="comment"># setuid（4） + 标准的 755</span></span><br><span class="line"><span class="built_in">chmod</span> 2775 /srv/shared-project <span class="comment"># setgid（2） + 目录标准的 775</span></span><br><span class="line"><span class="built_in">chmod</span> 1777 /tmp                 <span class="comment"># sticky（1） + 完全开放</span></span><br></pre></td></tr></table></figure><p>开头那三行 <code>ls -l</code> 输出里的 s&#x2F;t 都是小写，这是正常情况——owner（或 other）本身也有执行权限。看大小写的规则就一条：**小写表示正常，大写表示”这个位置本该有执行权限，但没有”**。比如 <code>rwSr--r--</code>，大写 <code>S</code> 说明 setuid 生效了，但 owner 没有执行权限，这种设置通常没意义，多半是配错了。看到大写的 S 或 T，基本可以判断是漏加了执行位，回去检查一下。</p><h2 id="chmod-R-递归的坑：符号链接会不会被改"><a href="#chmod-R-递归的坑：符号链接会不会被改" class="headerlink" title="chmod -R 递归的坑：符号链接会不会被改"></a><code>chmod -R</code> 递归的坑：符号链接会不会被改</h2><p><code>chmod -R</code> 递归修改目录下所有内容时，符号链接是这里最容易被误解的地方。符号链接本身的权限在 Linux 上<strong>不生效</strong>（永远显示 <code>lrwxrwxrwx</code>，改它没有意义）。默认行为其实比较克制：只有当符号链接直接出现在命令行参数里、并且指向一个目录时才会跟随继续递归；<strong>递归过程中在目录内部碰到的符号链接不会被跟随</strong>，所以正常情况下一条 <code>chmod -R 755 .</code> 不会意外爬到符号链接指向的别处。真正危险的是显式加了 <code>-L</code>——这个选项让 chmod 遇到任何目录型符号链接都跟随，如果目录里的链接指向了 <code>/</code> 或者其他敏感路径，递归范围会远超预期。</p><p>三个选项控制这个行为，只有最后一个生效（重复指定以最后一个为准）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> -R -H 755 project/   <span class="comment"># 默认行为：命令行里直接给出的符号链接，如果指向目录会跟随；</span></span><br><span class="line">                            <span class="comment"># 但递归过程中在目录内部新发现的符号链接不跟随</span></span><br><span class="line"><span class="built_in">chmod</span> -R -L 755 project/   <span class="comment"># 遇到目录型符号链接就跟随（最危险，容易改到意料之外的地方）</span></span><br><span class="line"><span class="built_in">chmod</span> -R -P 755 project/   <span class="comment"># 完全不跟随任何符号链接，比默认的 -H 更保守</span></span><br></pre></td></tr></table></figure><p>不确定目录里有没有埋着符号链接时，先跑一遍 <code>find project/ -type l</code> 心里有数，比事后排查权限为什么跑到别处去了省事得多。</p><h2 id="chmod-常用参数速查表"><a href="#chmod-常用参数速查表" class="headerlink" title="chmod 常用参数速查表"></a>chmod 常用参数速查表</h2><p>前面几节讲的都是这张表里的内容，汇总在一起方便直接查：</p><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-R</code>, <code>--recursive</code></td><td>递归处理目录下所有文件和子目录</td></tr><tr><td><code>-v</code>, <code>--verbose</code></td><td>每处理一个文件都输出结果</td></tr><tr><td><code>-c</code>, <code>--changes</code></td><td>和 <code>-v</code> 类似，但只在权限真的发生变化时才输出</td></tr><tr><td><code>-f</code>, <code>--silent</code>, <code>--quiet</code></td><td>屏蔽大部分错误提示</td></tr><tr><td><code>--reference=文件</code></td><td>权限抄自参考文件，不用自己写数字或符号模式</td></tr><tr><td><code>-H</code></td><td>递归时只跟随命令行直接给出的符号链接（默认行为）</td></tr><tr><td><code>-L</code></td><td>递归时跟随所有遇到的目录型符号链接（最危险）</td></tr><tr><td><code>-P</code></td><td>递归时完全不跟随任何符号链接</td></tr><tr><td><code>u</code> &#x2F; <code>g</code> &#x2F; <code>o</code> &#x2F; <code>a</code></td><td>符号模式里指定改谁：owner &#x2F; group &#x2F; other &#x2F; all</td></tr><tr><td><code>+</code> &#x2F; <code>-</code> &#x2F; <code>=</code></td><td>符号模式里指定加上、去掉还是直接赋值</td></tr><tr><td><code>r</code> &#x2F; <code>w</code> &#x2F; <code>x</code> &#x2F; <code>X</code></td><td>读 &#x2F; 写 &#x2F; 执行 &#x2F; 条件执行（目录总加，文件按原有执行权限判断）</td></tr><tr><td>数字模式前导 <code>4</code>&#x2F;<code>2</code>&#x2F;<code>1</code></td><td>特殊权限位：setuid &#x2F; setgid &#x2F; sticky bit，可叠加写在三位数字模式前面</td></tr></tbody></table><h2 id="chown：属主属组一起改，还是分开改"><a href="#chown：属主属组一起改，还是分开改" class="headerlink" title="chown：属主属组一起改，还是分开改"></a>chown：属主属组一起改，还是分开改</h2><p>chown 修改文件的所有者和所属组，语法是 <code>chown 属主:属组 文件</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chown</span> deploy:www-data app.log</span><br><span class="line"><span class="comment">#     属主改成 deploy，属组改成 www-data，一步到位</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">chown</span> deploy app.log</span><br><span class="line"><span class="comment">#     只写属主，属组不变</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">chown</span> :www-data app.log</span><br><span class="line"><span class="comment">#     只写属组（前面留空），等价于 chgrp www-data app.log</span></span><br></pre></td></tr></table></figure><p><code>chgrp</code> 和 <code>chown :group</code> 做的是同一件事——<code>chgrp</code> 只是历史遗留的专用命令，只能改组，功能被 <code>chown</code> 的 <code>:group</code> 语法完全覆盖。新写脚本用 <code>chown :group</code> 更统一，不用记两个命令。</p><p>用户名在 <code>/etc/passwd</code> 里查不到（比如跨系统同步过来的文件，UID 在本机没有对应用户名）时，直接传数字 UID:GID 也行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chown</span> 1001:1001 orphaned_file</span><br><span class="line"><span class="comment">#     UID 和 GID 直接是数字，不依赖 /etc/passwd 里有没有这个用户名</span></span><br></pre></td></tr></table></figure><p><code>-R</code> 递归的语义比 chmod 简单得多——符号链接本身没有属主属组的概念（继承自它指向的目标），chown 的递归不存在上面 chmod 那种”改错地方”的风险。</p><p>还有两个不算高频、但排查时很有用的参数：<code>--from=旧属主:旧属组</code> 只在文件当前属主属组和指定值一致时才真正执行改动，批量操作时能防止误伤不该改的文件；<code>-h</code>&#x2F;<code>--no-dereference</code> 改的是符号链接本身而不是它指向的目标——<strong>默认情况下 chown 是解引用符号链接、改目标</strong>，这一点和很多人的直觉相反，需要改链接本身时必须显式加 <code>-h</code>。</p><h2 id="chown-常用参数速查表"><a href="#chown-常用参数速查表" class="headerlink" title="chown 常用参数速查表"></a>chown 常用参数速查表</h2><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-R</code>, <code>--recursive</code></td><td>递归处理目录下所有文件和子目录</td></tr><tr><td><code>-v</code>, <code>--verbose</code></td><td>每处理一个文件都输出结果</td></tr><tr><td><code>-f</code>, <code>--silent</code>, <code>--quiet</code></td><td>屏蔽大部分错误提示</td></tr><tr><td><code>-h</code>, <code>--no-dereference</code></td><td>改符号链接本身，不改它指向的目标（默认是改目标）</td></tr><tr><td><code>--reference=文件</code></td><td>属主属组抄自参考文件</td></tr><tr><td><code>--from=旧属主:旧属组</code></td><td>只有当前属主&#x2F;属组和这里指定的一致才真正执行改动，防误伤</td></tr><tr><td><code>user:group</code></td><td>同时指定属主和属组</td></tr><tr><td><code>:group</code></td><td>只改属组（前面留空），等价于 <code>chgrp</code></td></tr></tbody></table><h2 id="实战：修一个权限乱掉的部署目录"><a href="#实战：修一个权限乱掉的部署目录" class="headerlink" title="实战：修一个权限乱掉的部署目录"></a>实战：修一个权限乱掉的部署目录</h2><p>场景：Nginx 的静态资源目录 <code>/var/www/site</code>，本来该属于 <code>www-data</code> 用户和组，结果某次运维手滑用 <code>sudo</code> 直接解压部署包，整个目录变成了 <code>root:root</code>，Nginx 进程读不了文件，网站全站 403。</p><p>第一步，改回属主属组：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">chown</span> -R www-data:www-data /var/www/site</span><br><span class="line"><span class="comment">#     -R：递归改整个目录树，属主属组都改成 www-data</span></span><br></pre></td></tr></table></figure><p>第二步，权限也得配套——目录需要可进入（执行位），文件不需要：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> find /var/www/site -<span class="built_in">type</span> d -<span class="built_in">exec</span> <span class="built_in">chmod</span> 755 &#123;&#125; \;</span><br><span class="line"><span class="comment">#     -type d：只挑目录，755 让 Nginx 能进入并列出目录内容</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">sudo</span> find /var/www/site -<span class="built_in">type</span> f -<span class="built_in">exec</span> <span class="built_in">chmod</span> 644 &#123;&#125; \;</span><br><span class="line"><span class="comment">#     -type f：只挑文件，644 让 Nginx 能读文件内容，但不给执行权限</span></span><br></pre></td></tr></table></figure><p>这里不能图省事直接 <code>chmod -R 755 /var/www/site</code>——那样会把所有静态文件也变成可执行，权限比实际需要的更开放。目录和文件分开处理才是准确的做法，<code>find + chmod</code> 这个组合在权限修复场景里比手写循环更简洁可靠。</p><h2 id="权限之外"><a href="#权限之外" class="headerlink" title="权限之外"></a>权限之外</h2><p>sudo 决定”谁能用 root 身份执行命令”，和 chmod&#x2F;chown 决定”文件本身谁能读写执行”是两个独立的问题，值得单开一篇讲。Linux 命令系列下一篇计划写 <code>rsync</code>——它的参数比 chmod&#x2F;chown 多得多，增量同步、排除规则、远程传输这些够写满一整篇。</p>]]>
    </content>
    <id>https://blog.codewater.top/Linux%E6%9D%83%E9%99%90%E4%B8%8E%E5%B1%9E%E4%B8%BB%E7%AE%A1%E7%90%86-chmod-chown%E5%AE%9E%E6%88%98/</id>
    <link href="https://blog.codewater.top/Linux%E6%9D%83%E9%99%90%E4%B8%8E%E5%B1%9E%E4%B8%BB%E7%AE%A1%E7%90%86-chmod-chown%E5%AE%9E%E6%88%98/"/>
    <published>2026-07-10T02:00:00.000Z</published>
    <summary>chmod 数字模式怎么算、符号模式何时更好用、setuid/setgid/sticky bit 到底在干嘛，配合 chown 修一个权限乱掉的部署目录。</summary>
    <title>Linux 权限与属主管理：chmod、chown 从数字模式到特殊权限位实战</title>
    <updated>2026-07-10T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="命令行" scheme="https://blog.codewater.top/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    <category term="终端" scheme="https://blog.codewater.top/tags/%E7%BB%88%E7%AB%AF/"/>
    <category term="效率工具" scheme="https://blog.codewater.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <content>
      <![CDATA[<p>两类场景总在提醒我该整理一份命令清单。一类是日常写代码，某个命令用得不够频繁，参数记不住，每次都要重新搜索引擎查一遍。</p><p>另一类更麻烦：公司的测试服务器系统版本老，很多现代工具装不上，只能用回那些”老式”命令。这些命令恰恰因为平时用不到，用法早就生疏了，真要用的时候翻半天才想起来参数怎么写。</p><p>这篇是一份分类地图，把常用命令和工具按场景归类，每条一两句话说清楚它解决什么问题、默认行为是什么。已经写过详细教程的条目直接跳转过去看用法，还没写的先记住名字，用到时候自己查 <code>man</code> 或 <code>--help</code>。</p><span id="more"></span><h2 id="文件与目录：从「东西在哪」查起"><a href="#文件与目录：从「东西在哪」查起" class="headerlink" title="文件与目录：从「东西在哪」查起"></a>文件与目录：从「东西在哪」查起</h2><ul><li><strong>ls</strong>：列出目录内容，默认不显示隐藏文件（<code>.</code> 开头），常配 <code>-la</code> 看详情和隐藏项</li><li><strong>find</strong>：按名称、类型、时间、大小等条件递归查找文件，条件组合和 <code>-exec</code> 两种写法的性能差异见 <a href="/find%E6%9D%A1%E4%BB%B6%E7%BB%84%E5%90%88%E4%B8%8Eexec%E6%80%A7%E8%83%BD%E5%AE%9E%E6%88%98">find：条件组合、-exec 两种写法与性能实战</a></li><li><strong>fd</strong>：<code>find</code> 的现代替代，默认按名称模糊匹配、自动跳过 <code>.gitignore</code> 里的文件，语法更接近自然语言</li><li><strong>tree</strong>：以树状结构展示目录层级，排查项目结构时比 <code>ls -R</code> 直观</li><li><strong>du</strong>：统计文件&#x2F;目录占用的磁盘空间，<code>-h</code> 转成人类可读单位，<code>-s</code> 只看汇总不展开子目录</li><li><strong>df</strong>：查看文件系统整体的磁盘使用率，和 <code>du</code>（查某个目录&#x2F;文件多大）不是一回事，<code>df</code> 是”盘还剩多少”</li><li><strong>file</strong>：判断文件的真实类型，不依赖扩展名，遇到没有后缀的文件时有用</li><li><strong>stat</strong>：查看文件的详细元信息（大小、权限、三个时间戳），比 <code>ls -l</code> 信息更全</li></ul><blockquote><p>终端里用图形化三栏界面管理文件，比裸敲 <code>ls</code>&#x2F;<code>cd</code> 更高效，见 <a href="/yazi%E7%BB%88%E7%AB%AF%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E5%99%A8">yazi：终端里的三栏文件管理器</a>。</p></blockquote><h2 id="文本处理与搜索：抓文本、转格式、过滤结果"><a href="#文本处理与搜索：抓文本、转格式、过滤结果" class="headerlink" title="文本处理与搜索：抓文本、转格式、过滤结果"></a>文本处理与搜索：抓文本、转格式、过滤结果</h2><ul><li><strong>grep</strong>：按正则匹配文本行，默认不递归，只在当前目录搜要加 <code>-r</code>；BRE&#x2F;-E&#x2F;-P 三种正则模式的区别和参数速查见 <a href="/grep%E6%AD%A3%E5%88%99%E5%8C%B9%E9%85%8D%E4%B8%8E%E4%B8%8A%E4%B8%8B%E6%96%87%E6%8E%A7%E5%88%B6%E5%AE%9E%E6%88%98">grep：正则匹配、上下文控制与 -E&#x2F;-P 实战</a></li><li><strong>ripgrep（rg）</strong>：<code>grep</code> 的现代替代，默认递归搜索、自动遵循 <code>.gitignore</code>、速度快一个数量级，是本博客文章里推荐搜索时的首选</li><li><strong>sed</strong>：流编辑器，按行对文本做替换、删除、插入，最常见用法是 <code>sed &#39;s/旧/新/g&#39;</code> 批量替换；和 <code>awk</code> 的选型对比、<code>-i</code> 参数的跨平台坑见 <a href="/sed%E4%B8%8Eawk%E5%AE%9E%E6%88%98%E5%AF%B9%E6%AF%94-%E6%96%87%E6%9C%AC%E5%A4%84%E7%90%86%E5%B7%A5%E5%85%B7%E6%80%8E%E4%B9%88%E9%80%89">sed 与 awk 实战对比：什么时候该用哪个</a></li><li><strong>awk</strong>：按列处理结构化文本（日志、CSV），比 <code>sed</code> 更适合”取第几列、按条件过滤”这类场景，同上一条的对比文章</li><li><strong>cut &#x2F; sort &#x2F; uniq</strong>：三个小工具常一起用——<code>cut</code> 截取列，<code>sort</code> 排序，<code>uniq -c</code> 统计重复次数，组合起来能替代不少 <code>awk</code> 脚本</li><li><strong>wc</strong>：统计行数（<code>-l</code>）、单词数、字节数，比写 <code>awk</code> 判断行数快</li><li><strong>jq</strong>：命令行的 JSON 处理器，从 API 响应或日志里提取字段、过滤、重新格式化</li></ul><blockquote><p>环境能装第三方工具时，<code>grep</code> 换成 <code>ripgrep</code>，配合 <code>fzf</code> 模糊筛选、<code>fd</code> 找文件，是效率提升最直接的一步，完整用法见 <a href="/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd">终端搜索三件套：fzf + ripgrep + fd，以及 jq</a>；装不上的场合还得靠原生 grep，见上面的链接。</p></blockquote><h2 id="进程与系统资源：谁在跑、吃了多少"><a href="#进程与系统资源：谁在跑、吃了多少" class="headerlink" title="进程与系统资源：谁在跑、吃了多少"></a>进程与系统资源：谁在跑、吃了多少</h2><ul><li><strong>ps</strong>：查看当前进程快照，<code>ps aux</code> 是最常用的组合，看全部用户的所有进程</li><li><strong>top &#x2F; htop</strong>：实时监控进程和资源占用，<code>htop</code> 是 <code>top</code> 的现代替代，界面更直观、支持鼠标操作和树状显示父子进程</li><li><strong>kill &#x2F; pkill</strong>：给进程发信号，默认发 <code>SIGTERM</code>（15，请求优雅退出），<code>-9</code> 是 <code>SIGKILL</code>（强制杀死，进程来不及清理资源）</li><li><strong>nice &#x2F; renice</strong>：调整进程的 CPU 调度优先级，数值越低优先级越高</li><li><strong>systemctl</strong>：管理 systemd 服务的启动、停止、开机自启和状态查看，是现代 Linux 发行版的服务管理标准</li><li><strong>journalctl</strong>：查看 systemd 管理的日志，<code>-u 服务名</code> 只看某个服务、<code>-f</code> 实时跟踪，取代了传统的 <code>/var/log/</code> 逐个文件翻</li></ul><blockquote><p>端口、进程、日志三类问题的完整排查流程（含 <code>ss</code>、<code>lsof</code>），见 <a href="/%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%8E%92%E6%9F%A5%E6%9C%8D%E5%8A%A1%E9%97%AE%E9%A2%98%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4">Linux 服务器问题排查：端口、进程、日志常用命令速查</a>。</p></blockquote><h2 id="网络排查：连不上、超时、端口占用先查这些"><a href="#网络排查：连不上、超时、端口占用先查这些" class="headerlink" title="网络排查：连不上、超时、端口占用先查这些"></a>网络排查：连不上、超时、端口占用先查这些</h2><ul><li><strong>curl</strong>：发起 HTTP 请求、测试接口连通性，调试 API 时最常用的工具，不带参数默认发 GET</li><li><strong>wget</strong>：下载文件，相比 <code>curl</code> 更适合直接保存到本地、支持断点续传</li><li><strong>ss</strong>：查看 socket 连接状态（监听端口、已建立连接），是 <code>netstat</code> 的现代替代，速度更快、输出更简洁</li><li><strong>lsof</strong>：列出进程打开的文件（Linux 下”一切皆文件”，网络连接也算），常用来反查”哪个进程占用了这个端口”</li><li><strong>ping</strong>：测试到目标主机的网络连通性和延迟，基于 ICMP，某些网络环境会屏蔽</li><li><strong>traceroute</strong>：追踪数据包到目标主机经过的每一跳路由，定位网络中间环节的问题</li><li><strong>dig &#x2F; nslookup</strong>：查询 DNS 解析结果，<code>dig</code> 输出更详细，排查域名解析问题的首选</li></ul><blockquote><p>这三者按”连通性→路径→解析”分层排查的完整流程，见 <a href="/%E7%BD%91%E7%BB%9C%E8%BF%9E%E9%80%9A%E6%80%A7%E8%AF%8A%E6%96%AD-ping-traceroute%E4%B8%8Edig%E5%AE%9E%E6%88%98">网络连不上怎么排查：ping、traceroute 与 dig&#x2F;nslookup 实战</a>。</p></blockquote><blockquote><p><code>ss</code> 和 <code>lsof</code> 的实际排查场景（比如揪出占用 8080 端口的进程），同样在 <a href="/%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%8E%92%E6%9F%A5%E6%9C%8D%E5%8A%A1%E9%97%AE%E9%A2%98%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4">Linux 服务器问题排查</a> 里有完整示例。</p></blockquote><h2 id="权限与用户管理：谁能做什么"><a href="#权限与用户管理：谁能做什么" class="headerlink" title="权限与用户管理：谁能做什么"></a>权限与用户管理：谁能做什么</h2><ul><li><strong>chmod &#x2F; chown</strong>：分别管文件权限和属主属组，数字模式、符号模式、setuid&#x2F;setgid&#x2F;sticky bit 这些细节容易搞混，完整实战见 <a href="/Linux%E6%9D%83%E9%99%90%E4%B8%8E%E5%B1%9E%E4%B8%BB%E7%AE%A1%E7%90%86-chmod-chown%E5%AE%9E%E6%88%98">Linux 权限与属主管理：chmod、chown 从数字模式到特殊权限位实战</a></li><li><strong>sudo</strong>：以其他用户（默认 root）身份执行单条命令，比直接切到 root 更安全，操作有日志可查</li><li><strong>su</strong>：切换到另一个用户的完整 shell 会话，<code>su -</code> 会同时加载目标用户的环境变量</li><li><strong>useradd &#x2F; usermod</strong>：创建用户、修改用户属性（所属组、家目录、shell），发行版不同细节略有差异</li><li><strong>id &#x2F; whoami</strong>：查看当前用户的身份信息，<code>id</code> 显示 UID&#x2F;GID 和所属组，<code>whoami</code> 只输出用户名</li></ul><h2 id="包管理与服务安装：装什么、从哪装"><a href="#包管理与服务安装：装什么、从哪装" class="headerlink" title="包管理与服务安装：装什么、从哪装"></a>包管理与服务安装：装什么、从哪装</h2><ul><li><strong>apt</strong>：Debian &#x2F; Ubuntu 系发行版的包管理器，<code>apt update</code> 刷新源、<code>apt install</code> 装包</li><li><strong>yum &#x2F; dnf</strong>：RHEL &#x2F; CentOS &#x2F; Fedora 系的包管理器，<code>dnf</code> 是 <code>yum</code> 的继任者，新版本发行版默认用 <code>dnf</code></li><li><strong>brew</strong>：macOS（以及部分 Linux 发行版）的包管理器，不需要 sudo，装在用户目录下</li><li><strong>systemctl enable</strong>：把服务设为开机自启，和上面”进程与系统资源”里的 <code>systemctl status</code> 是同一个命令的不同用法——一个管”装完之后要不要自启”，一个管”现在是什么状态”</li></ul><h2 id="压缩与传输：打包、发送、同步"><a href="#压缩与传输：打包、发送、同步" class="headerlink" title="压缩与传输：打包、发送、同步"></a>压缩与传输：打包、发送、同步</h2><ul><li><strong>tar &#x2F; zip &#x2F; unzip</strong>：tar 打包（不压缩）或打包并压缩（配合 <code>-z</code>&#x2F;<code>-j</code> 调用 gzip&#x2F;bzip2），Linux 下最常见的归档格式是 <code>.tar.gz</code>；zip&#x2F;unzip 处理 <code>.zip</code> 格式，跨平台兼容性比 tar 好，Windows 用户对接时常用这个——压缩格式选型和常见踩坑见 <a href="/tar-zip-unzip%E5%BD%92%E6%A1%A3%E5%8E%8B%E7%BC%A9%E4%B8%8E%E5%B8%B8%E8%A7%81%E5%9D%91%E5%AE%9E%E6%88%98">tar、zip&#x2F;unzip：归档压缩命令与常见坑实战</a></li><li><strong>scp</strong>：基于 SSH 的文件拷贝，语法接近 <code>cp</code>，适合偶尔传一两个文件</li><li><strong>rsync</strong>：增量同步工具，只传输变化的部分，同步大目录或做定期备份时比 <code>scp</code> 快得多，很多云盘同步工具底层就是它，参数详解和常见坑见 <a href="/rsync%E5%A2%9E%E9%87%8F%E5%90%8C%E6%AD%A5%E5%AE%9E%E6%88%98-%E5%B8%B8%E7%94%A8%E5%8F%82%E6%95%B0%E4%B8%8E%E8%BF%9C%E7%A8%8B%E5%90%8C%E6%AD%A5">rsync 增量同步实战：常用参数、排除规则与远程同步</a></li></ul><h2 id="Git-命令行工具链"><a href="#Git-命令行工具链" class="headerlink" title="Git 命令行工具链"></a>Git 命令行工具链</h2><ul><li><strong>git-delta &#x2F; lazygit &#x2F; gh</strong>：分别是更好看的 diff 输出、终端图形化 Git 客户端、GitHub 官方 CLI，三者组合能覆盖日常 Git 操作的大部分场景，见 <a href="/git-delta-lazygit-gh-%E7%BB%88%E7%AB%AFGit%E5%B7%A5%E5%85%B7%E9%93%BE">终端里的 Git：git-delta + lazygit + gh</a></li><li><strong>git tag</strong>：给某个提交打标签，常用于标记版本发布节点，完整命令和使用场景见 <a href="/git-tag%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4%E4%B8%8E%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF">git tag 常用命令与使用场景</a></li></ul><h2 id="Shell-环境与效率工具"><a href="#Shell-环境与效率工具" class="headerlink" title="Shell 环境与效率工具"></a>Shell 环境与效率工具</h2><p>这一类不是单个命令，而是整套终端使用体验的优化，各自有完整教程：</p><ul><li><strong>atuin + zoxide</strong>：替代默认的 shell 历史（<code>history</code>）和目录跳转（<code>cd</code>），前者把历史记录存进数据库支持模糊搜索，后者记住常去的目录、输入片段就能跳转，见 <a href="/atuin-zoxide-%E7%BB%88%E7%AB%AF%E5%8E%86%E5%8F%B2%E4%B8%8E%E7%9B%AE%E5%BD%95%E8%B7%B3%E8%BD%AC">Shell 历史与目录跳转的现代替代：atuin + zoxide</a></li><li><strong>kitty + Zim</strong>：终端模拟器和 Zsh 配置框架的组合，解决”终端本身好不好用”和”命令行编辑体验”两个问题，见 <a href="/Mac%E7%BB%88%E7%AB%AF%E5%9F%BA%E7%A1%80-kitty%E5%92%8CZim">Mac 终端基础配置：kitty + Zim</a></li><li><strong>fastfetch + pokemon-colorscripts</strong>：终端启动界面的系统信息展示和装饰，纯提升使用体验，见 <a href="/fastfetch-pokemon-colorscripts-%E7%BB%88%E7%AB%AF%E5%A4%96%E8%A7%82">终端启动界面：fastfetch + pokemon-colorscripts</a></li><li><strong>Neovim + LazyVim</strong>：终端里的代码编辑器，LazyVim 是开箱即用的配置发行版，免去从零攒 Neovim 配置的过程，见 <a href="/neovim-lazyvim-%E7%BB%88%E7%AB%AF%E7%BC%96%E8%BE%91%E5%99%A8">从零开始用 Neovim：LazyVim 让配置不再是障碍</a></li><li><strong>CLI 里集成 AI</strong>：把大模型接进命令行工作流，直接在终端里提问、生成命令，见 <a href="/cli%E4%B8%AD%E9%9B%86%E6%88%90ai-%E4%BB%A5deepseek%E4%B8%BA%E4%BE%8B">cli 中集成 ai，以 deepseek 为例</a></li><li>更多单点工具（bat、eza 等替代品）的推荐清单，见 <a href="/Mac%E7%BB%88%E7%AB%AFCLI%E5%B7%A5%E5%85%B7%E6%8E%A8%E8%8D%90">Mac 终端 CLI 工具推荐：那些让命令行效率翻倍的家伙</a></li></ul><h2 id="这份地图会持续更新"><a href="#这份地图会持续更新" class="headerlink" title="这份地图会持续更新"></a>这份地图会持续更新</h2><p><code>chmod</code>&#x2F;<code>chown</code>、<code>grep</code>、<code>find</code>、<code>tar</code>、<code>rsync</code>、<code>sed</code>&#x2F;<code>awk</code>、<code>ping</code>&#x2F;<code>traceroute</code>&#x2F;<code>dig</code> 这几类已经写完详细教程了，包管理这一类目前还只有条目没有详细文章，写完会回来把对应条目换成链接。</p>]]>
    </content>
    <id>https://blog.codewater.top/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE/</id>
    <link href="https://blog.codewater.top/Linux%E5%B8%B8%E8%A7%81%E5%91%BD%E4%BB%A4%E4%B8%8E%E5%B7%A5%E5%85%B7%E5%9C%B0%E5%9B%BE/"/>
    <published>2026-07-09T09:00:00.000Z</published>
    <summary>按场景分类的 Linux 命令速查地图：文件、文本处理、进程、网络、权限、包管理、压缩传输、Git，每条一句话说明用途，配合详细文章深入用法。</summary>
    <title>Linux 常见命令与工具地图(备忘录)</title>
    <updated>2026-07-13T12:43:43.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tutorial" scheme="https://blog.codewater.top/categories/tutorial/"/>
    <category term="Claude" scheme="https://blog.codewater.top/tags/Claude/"/>
    <category term="AI" scheme="https://blog.codewater.top/tags/AI/"/>
    <category term="Claude Code" scheme="https://blog.codewater.top/tags/Claude-Code/"/>
    <category term="MCP" scheme="https://blog.codewater.top/tags/MCP/"/>
    <category term="memory" scheme="https://blog.codewater.top/tags/memory/"/>
    <content>
      <![CDATA[<p>用 Claude Code 一段时间后，你大概率遇到过这个循环：昨天的会话里它已经摸清了项目架构、知道哪个模块有坑、记住了你的编码偏好，今天新开一个会话——全忘了。你只能把项目背景再讲一遍，或者眼睁睁看它把昨天踩过的坑再踩一次。</p><p>CLAUDE.md 能缓解一部分，但它本质上是一份静态规则文件，解决的是「规矩」问题，解决不了「积累」问题。真正的解法是给 Claude Code 接一个外部记忆系统：它自己把重要的事实存进去，下次会话自己检索出来。记忆数据可以是你完全自己保管的本地文件，也可以放云端跨设备同步。</p><span id="more"></span><p>这是 Claude 系列的第五篇。如果你还不知道 MCP 是什么，建议先看<a href="/%E8%87%AA%E5%BB%BAMCP-server%E6%8E%A5%E5%85%A5Claude">自建一个 MCP Server 接入 Claude</a>——本文的所有方案都构建在 MCP 之上。</p><h2 id="Claude-Code-自带的记忆，只能记住「规则」"><a href="#Claude-Code-自带的记忆，只能记住「规则」" class="headerlink" title="Claude Code 自带的记忆，只能记住「规则」"></a>Claude Code 自带的记忆，只能记住「规则」</h2><p>先盘点原生能力的边界，免得重复造轮子。Claude Code 内置三个记忆入口：</p><ul><li><strong>CLAUDE.md</strong>：项目根目录或 <code>~/.claude/</code> 下的规则文件，每次会话自动加载，适合放编码规范、构建命令这类固定约定</li><li><strong><code>#</code> 快捷记忆</strong>：对话中输入以 <code>#</code> 开头的消息，Claude Code 会问你要写进哪个 CLAUDE.md，适合随手沉淀一条规则</li><li><strong><code>/memory</code> 命令</strong>：直接打开记忆文件编辑</li></ul><p>这套机制有三个绕不过去的局限：</p><ol><li><strong>不会自己积累</strong>。会话中发现的架构细节、踩坑记录，除非你手动 <code>#</code> 一下，否则会话结束就丢了。</li><li><strong>全量加载</strong>。CLAUDE.md 的每一行都会进入每次会话的 context，记得越多，每次对话的开销越大，无关内容还会稀释注意力。</li><li><strong>不可检索</strong>。它是一整块文本，没法按「当前任务相关性」只取需要的部分。</li></ol><p>外部记忆系统正好补上这三块：自动写入、按需检索、只把相关记忆注入 context。</p><h2 id="外部记忆系统的工作原理：MCP-tools-使用协议"><a href="#外部记忆系统的工作原理：MCP-tools-使用协议" class="headerlink" title="外部记忆系统的工作原理：MCP tools + 使用协议"></a>外部记忆系统的工作原理：MCP tools + 使用协议</h2><p>所谓「记忆系统」，拆开看就是两件事：</p><ol><li><strong>一个 MCP server</strong>，暴露一组读写记忆的 tools（存储后端可以是本地文件、向量数据库或云端服务）</li><li><strong>一份使用协议</strong>，写在 CLAUDE.md 里，告诉 Claude 什么时候该查记忆、什么时候该存记忆</li></ol><p>很多人接完 MCP server 发现「Claude 根本不主动用」，缺的就是第二件事——MCP 只提供工具，不提供触发时机。两件事都做完，工作流是这样的：</p><pre><code class="highlight mermaid">sequenceDiagram    participant U as 你    participant C as Claude Code    participant M as 记忆 server（MCP）    participant S as 存储（本地文件/向量库/云端）    U-&gt;&gt;C: 新会话：继续优化订单服务    C-&gt;&gt;M: search_nodes(&quot;订单服务&quot;)    M-&gt;&gt;S: 查询    S--&gt;&gt;M: 匹配的记忆    M--&gt;&gt;C: 订单服务用 Redis 做幂等去重，上周踩过超时坑    C-&gt;&gt;U: 带着上下文直接开工    Note over C,M: 会话中发现新事实    C-&gt;&gt;M: add_observations(&quot;Redis 超时须设 500ms 上限&quot;)    M-&gt;&gt;S: 写入，供下次会话使用</code></pre><p>下面按「部署成本从低到高、数据控制权从本地到云端」介绍三条路线。</p><h2 id="方案一：官方-memory-server：五分钟拥有本地知识图谱"><a href="#方案一：官方-memory-server：五分钟拥有本地知识图谱" class="headerlink" title="方案一：官方 memory server：五分钟拥有本地知识图谱"></a><a href="https://github.com/modelcontextprotocol/servers/tree/main/src/memory">方案一：官方 memory server</a>：五分钟拥有本地知识图谱</h2><p><code>@modelcontextprotocol/server-memory</code> 是 MCP 官方维护的参考实现，把记忆存成本地的一个 JSONL 文件——数据完全自己保管，想备份就丢进 git，想销毁就删文件。零外部依赖，最适合第一次接记忆系统。</p><p>一条命令注册到当前项目：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">claude mcp add --scope project memory \</span><br><span class="line">  -e MEMORY_FILE_PATH=./.claude/memory.jsonl \</span><br><span class="line">  -- npx -y @modelcontextprotocol/server-memory</span><br><span class="line"><span class="comment">#   --scope project：配置写入项目根目录的 .mcp.json，随仓库共享给团队</span></span><br><span class="line"><span class="comment">#   -e：environment，给 server 进程注入环境变量</span></span><br><span class="line"><span class="comment">#     MEMORY_FILE_PATH 是记忆文件路径；不指定会存到 npx 缓存目录，包升级后就丢了，务必显式指定</span></span><br><span class="line"><span class="comment">#   --：分隔符，后面是启动 server 的完整命令</span></span><br></pre></td></tr></table></figure><p>注册后运行 <code>claude mcp list</code> 确认状态为 ✓ connected，或在会话里输入 <code>/mcp</code> 查看。</p><h3 id="它的记忆长什么样：实体、关系、观察"><a href="#它的记忆长什么样：实体、关系、观察" class="headerlink" title="它的记忆长什么样：实体、关系、观察"></a>它的记忆长什么样：实体、关系、观察</h3><p>这个 server 的数据模型是知识图谱，三种元素：</p><ul><li><strong>entity（实体）</strong>：记忆的主体，比如「订单服务」「Redis」</li><li><strong>relation（关系）</strong>：实体之间的连接，比如「订单服务 → 依赖 → Redis」</li><li><strong>observation（观察）</strong>：挂在实体上的具体事实，比如「Redis 只用 db0」</li></ul><p>对应到 <code>memory.jsonl</code> 里就是一行一条记录：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;type&quot;</span><span class="punctuation">:</span><span class="string">&quot;entity&quot;</span><span class="punctuation">,</span><span class="attr">&quot;name&quot;</span><span class="punctuation">:</span><span class="string">&quot;订单服务&quot;</span><span class="punctuation">,</span><span class="attr">&quot;entityType&quot;</span><span class="punctuation">:</span><span class="string">&quot;service&quot;</span><span class="punctuation">,</span><span class="attr">&quot;observations&quot;</span><span class="punctuation">:</span><span class="punctuation">[</span><span class="string">&quot;Go 1.24 编写&quot;</span><span class="punctuation">,</span><span class="string">&quot;用 Redis 做幂等去重&quot;</span><span class="punctuation">]</span><span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;type&quot;</span><span class="punctuation">:</span><span class="string">&quot;relation&quot;</span><span class="punctuation">,</span><span class="attr">&quot;from&quot;</span><span class="punctuation">:</span><span class="string">&quot;订单服务&quot;</span><span class="punctuation">,</span><span class="attr">&quot;to&quot;</span><span class="punctuation">:</span><span class="string">&quot;Redis&quot;</span><span class="punctuation">,</span><span class="attr">&quot;relationType&quot;</span><span class="punctuation">:</span><span class="string">&quot;依赖&quot;</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>server 暴露 9 个 tools：<code>create_entities</code>、<code>create_relations</code>、<code>add_observations</code> 负责写入，<code>search_nodes</code>、<code>open_nodes</code>、<code>read_graph</code> 负责检索，另有三个 delete 类工具做清理。你不需要手动调用它们——写好下一节的协议后，Claude 会自己调。</p><h3 id="实测：跨会话记住一条约定"><a href="#实测：跨会话记住一条约定" class="headerlink" title="实测：跨会话记住一条约定"></a>实测：跨会话记住一条约定</h3><p>在会话 A 里说：</p><blockquote><p>记住：这个项目的 Redis 只用 db0，禁止依赖 keyspace notification。</p></blockquote><p>Claude 会调用 <code>create_entities</code> 把这条约定写进图谱（如果它只是口头答应没调工具，明确说「存进 memory」，或者先写好下一节的协议）。关掉会话，新开会话 B，问「我们项目 Redis 有什么使用限制？」——它先 <code>search_nodes(&quot;Redis&quot;)</code>，然后答出 db0 和 keyspace notification 两条限制。记忆文件就在 <code>.claude/memory.jsonl</code>，可以随时打开人工检查或修正。</p><p>还有一个顺手的决定要做：这个文件要不要提交进 git。提交，它就成了团队共享记忆，同事 clone 下来直接继承；不提交，就把 <code>.claude/memory.jsonl</code> 加进 <code>.gitignore</code>，记忆保持个人私有。两种都合理，但要主动选，别让它意外进了仓库。</p><h2 id="关键一步：在-CLAUDE-md-里写「记忆协议」"><a href="#关键一步：在-CLAUDE-md-里写「记忆协议」" class="headerlink" title="关键一步：在 CLAUDE.md 里写「记忆协议」"></a>关键一步：在 CLAUDE.md 里写「记忆协议」</h2><p>不管选哪个方案，这一步都不能省。在项目的 CLAUDE.md 里加一段：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 记忆协议</span></span><br><span class="line"></span><br><span class="line">会话开始处理任务前：</span><br><span class="line"><span class="bullet">1.</span> 先用 memory 的 search<span class="emphasis">_nodes 检索与当前任务相关的记忆</span></span><br><span class="line"><span class="emphasis">2. 用到了记忆就明确说「根据记忆：……」，方便我发现过期记忆并纠正</span></span><br><span class="line"><span class="emphasis"></span></span><br><span class="line"><span class="emphasis">工作过程中，遇到以下信息立即写入记忆：</span></span><br><span class="line"><span class="emphasis">- 架构决策及其原因</span></span><br><span class="line"><span class="emphasis">- 踩过的坑和最终的修复方式</span></span><br><span class="line"><span class="emphasis">- 我明确说「记住」的内容</span></span><br><span class="line"><span class="emphasis"></span></span><br><span class="line"><span class="emphasis">以下内容不要存：</span></span><br><span class="line"><span class="emphasis">- 代码本身能看出来的东西（目录结构、函数签名、依赖列表）</span></span><br><span class="line"><span class="emphasis">- 密钥、token、内部域名等敏感信息</span></span><br></pre></td></tr></table></figure><p>「不要存什么」和「要存什么」同样重要：代码能看出来的东西存进记忆只会制造过期副本，而敏感信息一旦进了记忆文件，就多了一个泄露面。</p><h2 id="方案二：mem0-云端：托管记忆，跨设备同步"><a href="#方案二：mem0-云端：托管记忆，跨设备同步" class="headerlink" title="方案二：mem0 云端：托管记忆，跨设备同步"></a><a href="https://mem0.ai/">方案二：mem0 云端</a>：托管记忆，跨设备同步</h2><p><a href="https://mem0.ai/">mem0</a> 是社区里使用最广的 LLM 记忆层之一，云端版本的优势是<strong>语义检索</strong>和<strong>零运维</strong>：记忆经过向量化，「Redis 使用限制」能检索到「缓存约定」这类字面不同但含义相关的记忆；公司电脑存的记忆，家里的电脑直接可用。</p><p>官方推荐通过 Claude Code 的 plugin 市场安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 在 Claude Code 会话内执行</span></span><br><span class="line">/plugin marketplace add mem0ai/mem0</span><br><span class="line">/plugin install mem0@mem0-plugins</span><br></pre></td></tr></table></figure><p>然后到 <a href="https://app.mem0.ai/">mem0 控制台</a> 拿 API key，写进 shell 配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> MEM0_API_KEY=<span class="string">&quot;m0-你的key&quot;</span></span><br><span class="line"><span class="comment">#   mem0 的 API 密钥，plugin 和 MCP server 都从这个环境变量读取</span></span><br></pre></td></tr></table></figure><p>装完在会话里跑 <code>/mem0:onboard</code> 验证连接。plugin 版比纯 MCP 版多一层价值：自带 hooks，会在会话结束等时机<strong>自动捕获</strong>值得记住的内容，不完全依赖 CLAUDE.md 协议驱动。它提供 <code>add_memory</code>、<code>search_memories</code>、<code>get_memories</code>、<code>update_memory</code>、<code>delete_memory</code> 等 9 个 tools。</p><p>mem0 有免费额度，超出后按量付费，具体以<a href="https://mem0.ai/pricing">官网定价</a>为准。代价也明确：<strong>记忆内容存在别人的服务器上</strong>。如果你的记忆里会出现内部系统名称、架构细节，先确认这符合公司的数据政策。</p><h2 id="方案三：mem0-自托管-server：语义检索，记忆落在自己的机器上"><a href="#方案三：mem0-自托管-server：语义检索，记忆落在自己的机器上" class="headerlink" title="方案三：mem0 自托管 server：语义检索，记忆落在自己的机器上"></a><a href="https://docs.mem0.ai/open-source/overview">方案三：mem0 自托管 server</a>：语义检索，记忆落在自己的机器上</h2><p>想要 mem0 的语义检索，又要求记忆数据留在自己手里，用 mem0 开源的自托管 server：docker 起一套本地服务，包含 REST API（localhost:8888）、Web 管理面板（localhost:3000）和内置的 PostgreSQL + pgvector 向量存储，面板上能浏览每一条记忆、查请求日志、管理 API key。</p><blockquote><p>网上不少教程还在推荐 mem0 的另一个自托管项目 OpenMemory，它已经被官方标记为 sunset（停止维护），仓库 README 明确指引迁移到这套自托管 server，别照旧教程部署了。</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/mem0ai/mem0.git</span><br><span class="line"><span class="built_in">cd</span> mem0/server</span><br><span class="line">make up</span><br><span class="line"><span class="comment">#   docker compose 启动全部组件，随后访问 http://localhost:3000 走初始化向导</span></span><br><span class="line"><span class="comment">#   .env 里至少要配：一个 LLM key（OPENAI_API_KEY / ANTHROPIC_API_KEY / GOOGLE_API_KEY 三选一）</span></span><br><span class="line"><span class="comment">#   和 JWT_SECRET（用 openssl rand -base64 48 生成），细节以官方 setup 文档为准</span></span><br></pre></td></tr></table></figure><p>两个要点决定它适不适合你：</p><ol><li><strong>它没有现成的 MCP 端点</strong>，对外只有 REST API。接进 Claude Code 有两条路：找社区的 mem0 MCP 桥接项目，或者照<a href="/%E8%87%AA%E5%BB%BAMCP-server%E6%8E%A5%E5%85%A5Claude">上一篇</a>的做法自己写一个薄 MCP server，把 add &#x2F; search 两个接口包成 tools——这正是自建 MCP server 最典型的应用场景。</li><li><strong>记忆存储在本地，但 LLM 和 embedding 调用默认走 OpenAI &#x2F; Gemini 等外部接口</strong>，记忆原文仍会出网。官方 server 目前的模型选项不含本地模型，要彻底不出网得走社区方案（如基于 Ollama 的桥接项目），部署复杂度再上一个台阶。</li></ol><h2 id="本地自管还是云端托管：三种记忆方案对比"><a href="#本地自管还是云端托管：三种记忆方案对比" class="headerlink" title="本地自管还是云端托管：三种记忆方案对比"></a>本地自管还是云端托管：三种记忆方案对比</h2><table><thead><tr><th>维度</th><th>官方 memory server</th><th>mem0 云端</th><th>mem0 自托管</th></tr></thead><tbody><tr><td>部署成本</td><td>一条命令</td><td>一条命令 + API key</td><td>docker compose + 桥接 MCP</td></tr><tr><td>检索方式</td><td>图谱关键词匹配</td><td>向量语义检索</td><td>向量语义检索</td></tr><tr><td>数据存放</td><td>本地 JSONL，完全自管</td><td>mem0 云端</td><td>本机（embedding 默认出网）</td></tr><tr><td>跨设备</td><td>手动同步（git 即可）</td><td>自动</td><td>自己架服务</td></tr><tr><td>适合谁</td><td>个人项目，第一次上记忆</td><td>多设备工作，接受托管</td><td>团队内部，有数据合规要求</td></tr></tbody></table><p>我的建议是从官方 memory server 起步：成本几乎为零，记忆文件肉眼可读，先用两周验证「记忆协议」这套工作流是否适合你，再决定要不要升级到语义检索。记忆系统的瓶颈通常不在检索算法，而在「存了什么」：如果记忆协议写得好，关键词匹配对个人项目已经够用；反过来，没有协议约束存储质量，换上向量检索也只是帮你更快地翻出一堆没价值的记忆。</p><h2 id="更进一步：hooks-强制检索、自建记忆-server"><a href="#更进一步：hooks-强制检索、自建记忆-server" class="headerlink" title="更进一步：hooks 强制检索、自建记忆 server"></a>更进一步：hooks 强制检索、自建记忆 server</h2><ul><li>记忆协议本质上是一种 prompt 工程，配合 <a href="/ClaudeCode%E5%AE%9E%E6%88%98%E6%95%99%E7%A8%8B-%E5%AE%89%E8%A3%85CLAUDEmd-hooks-subagent">Claude Code 的 hooks</a> 可以做得更硬：用 SessionStart hook 强制在会话开始注入检索结果，不依赖 Claude 自觉</li><li>官方 memory server 只是参考实现，读完<a href="/%E8%87%AA%E5%BB%BAMCP-server%E6%8E%A5%E5%85%A5Claude">自建 MCP Server</a> 你完全可以针对团队需求写一个自己的记忆 server——比如把存储后端换成团队已有的 PostgreSQL</li><li>想给记忆系统加「定期整理」能力（合并重复、清理过期），可以封装成一个 <a href="/%E4%BB%8E0%E5%88%B01%E5%86%99%E4%B8%80%E4%B8%AAClaude-Skill-%E8%A7%84%E8%8C%83%E5%86%99%E6%B3%95%E4%B8%8E%E5%AE%9E%E6%88%98">Claude Skill</a></li></ul><h2 id="两个更野的思路：把-GitHub-Issues-和-Obsidian-当记忆后端"><a href="#两个更野的思路：把-GitHub-Issues-和-Obsidian-当记忆后端" class="headerlink" title="两个更野的思路：把 GitHub Issues 和 Obsidian 当记忆后端"></a>两个更野的思路：把 GitHub Issues 和 Obsidian 当记忆后端</h2><blockquote><p>这几天刷推特，看到有些推友的方案，这里记录一下。</p></blockquote><p>前面三个方案的共同点是「MCP server 暴露读写工具 + 协议」，存储后端不一定非得是专门的记忆产品——只要能读写、能查询，任何你已经在用的系统都能顶上。这两个思路都有真实可用的工具支撑，评估下来都站得住脚。</p><h3 id="用-GitHub-Issues-存记忆"><a href="#用-GitHub-Issues-存记忆" class="headerlink" title="用 GitHub Issues 存记忆"></a>用 GitHub Issues 存记忆</h3><p>思路：一个 issue 对应一条记忆，标题是主题，正文是初始内容，后续更新靠评论追加，label 做分类，关闭表示这条记忆已过期归档。</p><p>不需要自己写 MCP server——GitHub 官方就维护着 <a href="https://github.com/github/github-mcp-server">github-mcp-server</a>，里面的 <code>create_issue</code>、<code>search_issues</code>、<code>add_issue_comment</code>、<code>list_issues</code> 这几个工具直接挪用来当记忆读写：新记忆调 <code>create_issue</code>，追加事实调 <code>add_issue_comment</code>，检索调 <code>search_issues</code>（支持 GitHub 原生搜索语法，按 label、关键词过滤）。CLAUDE.md 里加一份协议就行：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 记忆协议（GitHub Issues 版）</span></span><br><span class="line">记忆存在 &lt;你的用户名&gt;/claude-memory 仓库的 issue 里，label 统一打 memory。</span><br><span class="line">检索：先用 search<span class="emphasis">_issues 按关键词 + label:memory 查。</span></span><br><span class="line"><span class="emphasis">写入：新事实开新 issue；对已有记忆的补充用 add_</span>issue<span class="emphasis">_comment 追加，不要新开重复 issue。</span></span><br></pre></td></tr></table></figure><p>优点很实在：零基础设施（不用自己起 server、不用管数据库），自带 Web UI 能点进去看、编辑、关闭，天然支持团队协作——同事能在 issue 下评论纠正记忆，PR 里 <code>#123</code> 直接引用某条记忆。</p><p>代价也要认清：检索是 GitHub 的关键词搜索，不是语义检索，效果和官方 memory server 的图谱匹配是一个量级，比 mem0 弱；仓库必须建成私有的，不然内部架构细节挂在公开 issue 里就是裸奔；GitHub API 有速率限制（认证用户每小时 5000 次），个人用绰绰有余，但别拿它当高频轮询的数据库使。</p><h3 id="用-Obsidian-存记忆"><a href="#用-Obsidian-存记忆" class="headerlink" title="用 Obsidian 存记忆"></a>用 Obsidian 存记忆</h3><p>思路更直接：记忆就是 Obsidian vault 里一篇篇 Markdown 笔记，本质和方案一的本地 JSONL 是同一件事——都是本地文件——只是换成了人读起来更友好的格式：一条记忆一个文件，用 <code>[[双链]]</code> 互相引用，Obsidian 自带的关系图谱能可视化记忆之间的关联，这是纯 JSONL 天生没有的。</p><p>社区方案是 <a href="https://github.com/MarkusPfundstein/mcp-obsidian">mcp-obsidian</a>：先在 Obsidian 里装 <a href="https://github.com/coddingtonbear/obsidian-local-rest-api">Local REST API</a> 这个社区插件（起一个本地 HTTPS 接口），再跑 <code>uvx mcp-obsidian</code> 把这个接口包装成 MCP tools——<code>get_file_contents</code>、<code>append_content</code>、<code>search</code> 这几个是记忆场景最常用的。CLAUDE.md 里同样写一份协议，告诉 Claude 记忆笔记放在 vault 的哪个子目录、用什么样的文件名和 frontmatter 格式。</p><p>它相比前面三个方案最大的差异点：<strong>vault 不用绑定单个项目</strong>。可以让所有项目共用一个 vault，个人偏好、跨项目的踩坑经验存一份就够，不用在每个仓库里各存一遍。跨设备同步也不用另起炉灶——Obsidian Sync（付费）或者自己用 Syncthing 同步这个 vault 目录都行。</p><p>代价：Local REST API 要求 Obsidian 保持打开、插件保持启用，比纯文件读写多一层依赖；这套 MCP server 是社区维护，不是 Anthropic 或 Obsidian 官方出品，稳定性和更新节奏得自己承担；同样没有语义检索，纯靠关键词和你自己组织的文件夹、标签结构。</p><p>两条思路都没有绕开本文一开始讲的原理，只是把「读写工具」换成了你已经每天在用的东西，省去专门运维一个记忆系统的成本，代价是检索能力停留在关键词层面。想清楚自己需不需要语义检索，比纠结用哪个存储后端更重要。</p><p>至于具体的实践和操作步骤，请读者自行参考和研究摸索。</p>]]>
    </content>
    <id>https://blog.codewater.top/%E7%BB%99Claude-Code%E6%8E%A5%E5%85%A5%E5%A4%96%E9%83%A8%E8%AE%B0%E5%BF%86%E7%B3%BB%E7%BB%9F/</id>
    <link href="https://blog.codewater.top/%E7%BB%99Claude-Code%E6%8E%A5%E5%85%A5%E5%A4%96%E9%83%A8%E8%AE%B0%E5%BF%86%E7%B3%BB%E7%BB%9F/"/>
    <published>2026-07-09T07:00:00.000Z</published>
    <summary>三种方案让 Claude Code 跨会话保留项目记忆：官方 memory server 本地知识图谱、mem0 云端托管与自托管，附 CLAUDE.md 记忆协议模板。</summary>
    <title>给 Claude Code 接入外部记忆系统：新会话不再失忆</title>
    <updated>2026-07-10T03:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tutorial" scheme="https://blog.codewater.top/categories/tutorial/"/>
    <category term="Claude" scheme="https://blog.codewater.top/tags/Claude/"/>
    <category term="AI" scheme="https://blog.codewater.top/tags/AI/"/>
    <category term="Claude Code" scheme="https://blog.codewater.top/tags/Claude-Code/"/>
    <category term="MCP" scheme="https://blog.codewater.top/tags/MCP/"/>
    <category term="TypeScript" scheme="https://blog.codewater.top/tags/TypeScript/"/>
    <content>
      <![CDATA[<p>你在公司内网有一个文档系统，Claude 不知道它的存在；你有一个只允许内部访问的 API，也没法直接让 Claude 调用。MCP（Model Context Protocol）解决的就是这个问题——它让你把自己的工具、数据、流程封装成标准接口，Claude 按规范调用，权限边界完全由你说了算。</p><p>这是 Claude 系列的第四篇：<a href="/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98">第一篇</a>讲 Claude 的核心概念，<a href="/ClaudeCode%E5%AE%9E%E6%88%98%E6%95%99%E7%A8%8B-%E5%AE%89%E8%A3%85CLAUDEmd-hooks-subagent">第二篇</a>讲 Claude Code 的整套配置，<a href="/%E4%BB%8E0%E5%88%B01%E5%86%99%E4%B8%80%E4%B8%AAClaude-Skill-%E8%A7%84%E8%8C%83%E5%86%99%E6%B3%95%E4%B8%8E%E5%AE%9E%E6%88%98">第三篇</a>讲如何写 skill。这篇专攻 MCP server。所有 API 和命令核对自官方文档，截至 2026 年 7 月 8 日有效。</p><span id="more"></span><h2 id="MCP-是什么：给-Claude-装上可插拔工具"><a href="#MCP-是什么：给-Claude-装上可插拔工具" class="headerlink" title="MCP 是什么：给 Claude 装上可插拔工具"></a>MCP 是什么：给 Claude 装上可插拔工具</h2><p>先讲一个对比。Claude Code 内置了读文件、跑 bash、搜索代码这几种能力。如果你的需求超出这个范围——查询内部数据库、调用需要认证的私有 API、访问公司内网系统——Claude 就做不到了。</p><p>传统的解法是在 CLAUDE.md 里写”你可以通过运行 <code>curl http://internal-api/...</code> 来查询……”，然后祈祷 Claude 记得用对参数。这很脆弱：参数拼错、认证信息暴露、没有类型约束。</p><p>MCP 给了另一条路：你把这些操作封装成<strong>工具函数</strong>，按标准格式告诉 Claude 每个函数的名字、参数定义、能做什么。Claude 需要时按规范调用，你在服务端控制执行逻辑和权限。整个调用链是这样的：</p><pre><code class="highlight mermaid">sequenceDiagram    participant U as 用户    participant C as Claude Code    participant S as MCP Server（本地子进程）    participant D as 数据源    U-&gt;&gt;C: 找关于 OAuth 的内部文档    C-&gt;&gt;S: search_docs(&quot;OAuth&quot;)    S-&gt;&gt;D: 读取并过滤 docs.json    D--&gt;&gt;S: 匹配到 2 篇    S--&gt;&gt;C: [&#123;id, title&#125;...]    C-&gt;&gt;S: get_doc(&quot;3&quot;)    S-&gt;&gt;D: 读取完整内容    D--&gt;&gt;S: 文档全文    S--&gt;&gt;C: # OAuth 认证规范...    C-&gt;&gt;U: 找到了，内容如下...</code></pre><p>和 bash tool 的本质区别是：<strong>你写的 MCP server 决定了 Claude 能做什么，而不是 Claude 随意执行任何 bash 命令</strong>。暴露什么、拦什么、怎么执行，全在你手里。</p><h2 id="三种能力：Tools、Resources、Prompts"><a href="#三种能力：Tools、Resources、Prompts" class="headerlink" title="三种能力：Tools、Resources、Prompts"></a>三种能力：Tools、Resources、Prompts</h2><p>MCP server 可以向客户端（Claude）提供三类东西：</p><p><strong>Tools（工具）</strong>是让 Claude 主动调用的函数，可以有副作用——查数据库、写文件、发 API 请求都算。Claude 看到你注册的工具名和描述，自己判断什么时候调用。这是最常用的能力，九成的 MCP server 只需要 Tools。</p><p><strong>Resources（资源）</strong>是只读数据源。你可以把一个目录、一组文档、一段配置暴露为资源，Claude 读取时像读文件一样操作。适合”把整个文档目录变成 Claude 的知识库”这类场景，不需要搜索逻辑，直接按路径读取。</p><p><strong>Prompts（提示模板）</strong>是 MCP server 侧定义的预设对话模板，Claude 可以在 <code>/</code> 菜单里直接触发。类似 Claude Code 的 skill，但模板逻辑在 server 端维护。起步阶段不必用，有需要时看<a href="https://modelcontextprotocol.io/docs/learn/server-concepts#prompts">官方文档的 Prompts 章节</a>。</p><p>日常开发只需要记住：<strong>写 Tools，其他按需</strong>。</p><h2 id="五分钟跑一个最小-MCP-server（TypeScript）"><a href="#五分钟跑一个最小-MCP-server（TypeScript）" class="headerlink" title="五分钟跑一个最小 MCP server（TypeScript）"></a>五分钟跑一个最小 MCP server（TypeScript）</h2><p><strong>前置要求</strong>：Node.js 18+（<code>node --version</code> 确认），npm。</p><p>从最小版本开始：只有一个 <code>echo</code> 工具，把整条路径跑通再扩展。</p><h3 id="初始化项目"><a href="#初始化项目" class="headerlink" title="初始化项目"></a>初始化项目</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> my-mcp-server &amp;&amp; <span class="built_in">cd</span> my-mcp-server</span><br><span class="line"></span><br><span class="line">npm init -y</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装 MCP SDK（TypeScript 官方 SDK）和 Zod（参数类型校验）</span></span><br><span class="line"><span class="comment"># zod@3：官方文档推荐版本；如用 Zod 4（最新），SDK 1.x 两者均兼容</span></span><br><span class="line">npm install @modelcontextprotocol/sdk zod@3</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装 TypeScript 工具链</span></span><br><span class="line">npm install -D @types/node typescript</span><br></pre></td></tr></table></figure><p>修改 <code>package.json</code>，加上模块类型和构建脚本：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;module&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;tsc&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>在项目根目录创建 <code>tsconfig.json</code>：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;compilerOptions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;target&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ES2022&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;module&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Node16&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;moduleResolution&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Node16&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;outDir&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./build&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;rootDir&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./src&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;strict&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;esModuleInterop&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;skipLibCheck&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;include&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;src/**/*&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;exclude&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;node_modules&quot;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h3 id="写第一个-tool"><a href="#写第一个-tool" class="headerlink" title="写第一个 tool"></a>写第一个 tool</h3><p>创建 <code>src/index.ts</code>：</p><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">McpServer</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@modelcontextprotocol/sdk/server/mcp.js&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">StdioServerTransport</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@modelcontextprotocol/sdk/server/stdio.js&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; z &#125; <span class="keyword">from</span> <span class="string">&quot;zod&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = <span class="keyword">new</span> <span class="title class_">McpServer</span>(&#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&quot;my-first-mcp&quot;</span>,</span><br><span class="line">  <span class="attr">version</span>: <span class="string">&quot;1.0.0&quot;</span>,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">registerTool</span>(</span><br><span class="line">  <span class="string">&quot;echo&quot;</span>,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">description</span>: <span class="string">&quot;将输入的文字原样返回，用于测试 MCP server 是否正常工作&quot;</span>,</span><br><span class="line">    <span class="attr">inputSchema</span>: &#123;</span><br><span class="line">      <span class="attr">message</span>: z.<span class="title function_">string</span>().<span class="title function_">describe</span>(<span class="string">&quot;要回显的内容&quot;</span>),</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="title function_">async</span> (&#123; message &#125;) =&gt; &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      <span class="attr">content</span>: [&#123; <span class="attr">type</span>: <span class="string">&quot;text&quot;</span>, <span class="attr">text</span>: <span class="string">`Echo: <span class="subst">$&#123;message&#125;</span>`</span> &#125;],</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// ⚠️ stdio 模式下绝不能用 console.log()——它会污染 JSON-RPC 通道</span></span><br><span class="line"><span class="comment">// 调试信息必须写到 stderr：console.error(&quot;server started&quot;)</span></span><br><span class="line"><span class="keyword">const</span> transport = <span class="keyword">new</span> <span class="title class_">StdioServerTransport</span>();</span><br><span class="line"><span class="keyword">await</span> server.<span class="title function_">connect</span>(transport);</span><br></pre></td></tr></table></figure><p><code>server.registerTool</code> 三个参数：工具名、配置（description + inputSchema）、处理函数。<code>inputSchema</code> 里每个字段是一个 Zod schema——Claude 看到的参数说明来自 <code>.describe()</code>，类型校验由 Zod 保证。</p><h3 id="构建并测试"><a href="#构建并测试" class="headerlink" title="构建并测试"></a>构建并测试</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">npm run build</span><br><span class="line"><span class="comment">#        tsc 把 src/index.ts 编译到 build/index.js</span></span><br><span class="line"></span><br><span class="line">node build/index.js</span><br><span class="line"><span class="comment"># 什么都不会打印——server 在等待来自 stdin 的 JSON-RPC 消息</span></span><br><span class="line"><span class="comment"># Ctrl+C 退出</span></span><br></pre></td></tr></table></figure><h2 id="接入-Claude-Code：注册、验证、三种-scope"><a href="#接入-Claude-Code：注册、验证、三种-scope" class="headerlink" title="接入 Claude Code：注册、验证、三种 scope"></a>接入 Claude Code：注册、验证、三种 scope</h2><h3 id="注册-MCP-server"><a href="#注册-MCP-server" class="headerlink" title="注册 MCP server"></a>注册 MCP server</h3><p>在终端（不是在 claude 会话里）运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">claude mcp add my-first-mcp -- node /绝对路径/my-mcp-server/build/index.js</span><br><span class="line"><span class="comment">#              ↑ server 名字    ↑ -- 后面是 Claude Code 用来启动 server 的命令</span></span><br></pre></td></tr></table></figure><p>注意几个细节：</p><ul><li><strong>必须用绝对路径</strong>：Claude Code 以不同工作目录启动 server，相对路径会找不到文件</li><li><strong><code>--</code> 分隔符</strong>：它之后的所有内容都是启动命令，不是 <code>claude mcp add</code> 自己的参数</li><li>没有 <code>--transport</code> 标志，因为 stdio 是本地进程的默认传输方式</li></ul><p>如果 server 需要环境变量（如 API key、文件路径），在命令末尾加 <code>--env</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">claude mcp add my-first-mcp \</span><br><span class="line">  --<span class="built_in">env</span> DOCS_PATH=/Users/me/docs/docs.json \</span><br><span class="line">  -- node /绝对路径/build/index.js</span><br></pre></td></tr></table></figure><h3 id="验证连接"><a href="#验证连接" class="headerlink" title="验证连接"></a>验证连接</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">claude mcp list</span><br><span class="line"><span class="comment"># ✓ Connected  my-first-mcp</span></span><br></pre></td></tr></table></figure><p>状态含义：</p><table><thead><tr><th>状态</th><th>含义</th></tr></thead><tbody><tr><td><code>✓ Connected</code></td><td>已连接，工具可用</td></tr><tr><td><code>✗ Failed to connect</code></td><td>server 没跑起来，检查路径和命令</td></tr><tr><td><code>! Connected · tools fetch failed</code></td><td>连上了但工具列表获取失败，运行 <code>claude mcp get &lt;名字&gt;</code> 看错误</td></tr></tbody></table><p>会话内用 <code>/mcp</code> 命令管理和检查已注册的 server。</p><h3 id="三种-scope"><a href="#三种-scope" class="headerlink" title="三种 scope"></a>三种 scope</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># local（默认）：只对你、只在当前项目生效</span></span><br><span class="line">claude mcp add my-server -- node /path/build/index.js</span><br><span class="line"></span><br><span class="line"><span class="comment"># user：对你、在所有项目生效</span></span><br><span class="line">claude mcp add --scope user my-server -- node /path/build/index.js</span><br><span class="line"></span><br><span class="line"><span class="comment"># project：写入 .mcp.json，进版本库，团队共享</span></span><br><span class="line">claude mcp add --scope project my-server -- node /path/build/index.js</span><br></pre></td></tr></table></figure><p>团队使用推荐 project scope——<code>.mcp.json</code> 提交到仓库，队友拉下来后 Claude Code 自动提示审批，批准即可用。<code>.mcp.json</code> 格式：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;mcpServers&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;my-server&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;stdio&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;node&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;args&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;/绝对路径/build/index.js&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;env&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;DOCS_PATH&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/path/to/docs.json&quot;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h2 id="完整实战：内部文档搜索-MCP-server"><a href="#完整实战：内部文档搜索-MCP-server" class="headerlink" title="完整实战：内部文档搜索 MCP server"></a>完整实战：内部文档搜索 MCP server</h2><p>场景：团队有一份 JSON 格式的内部文档库，想让 Claude 能搜索和读取，但不暴露整个文件系统给 Claude，也不需要部署额外服务。</p><h3 id="数据格式"><a href="#数据格式" class="headerlink" title="数据格式"></a>数据格式</h3><p>创建 <code>docs.json</code>：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">[</span></span><br><span class="line">  <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;title&quot;</span><span class="punctuation">:</span> <span class="string">&quot;项目 API 规范 v2&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;content&quot;</span><span class="punctuation">:</span> <span class="string">&quot;所有接口必须经过认证，使用 Bearer token。分页参数统一为 page 和 per_page...&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;tags&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;api&quot;</span><span class="punctuation">,</span> <span class="string">&quot;规范&quot;</span><span class="punctuation">,</span> <span class="string">&quot;认证&quot;</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;title&quot;</span><span class="punctuation">:</span> <span class="string">&quot;本地开发环境搭建&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;content&quot;</span><span class="punctuation">:</span> <span class="string">&quot;前置条件：Node.js 18+、Docker 20+。克隆仓库后运行 npm install...&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;tags&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;开发环境&quot;</span><span class="punctuation">,</span> <span class="string">&quot;onboarding&quot;</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;3&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;title&quot;</span><span class="punctuation">:</span> <span class="string">&quot;OAuth 认证接入指南&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;content&quot;</span><span class="punctuation">:</span> <span class="string">&quot;本系统使用 OAuth 2.0 授权码流程。客户端 ID 和 secret 从运维获取...&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;tags&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;oauth&quot;</span><span class="punctuation">,</span> <span class="string">&quot;认证&quot;</span><span class="punctuation">,</span> <span class="string">&quot;安全&quot;</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure><h3 id="完整-server-代码"><a href="#完整-server-代码" class="headerlink" title="完整 server 代码"></a>完整 server 代码</h3><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">McpServer</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@modelcontextprotocol/sdk/server/mcp.js&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">StdioServerTransport</span> &#125; <span class="keyword">from</span> <span class="string">&quot;@modelcontextprotocol/sdk/server/stdio.js&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; z &#125; <span class="keyword">from</span> <span class="string">&quot;zod&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; readFileSync &#125; <span class="keyword">from</span> <span class="string">&quot;fs&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Doc</span> &#123;</span><br><span class="line">  <span class="attr">id</span>: <span class="built_in">string</span>;</span><br><span class="line">  <span class="attr">title</span>: <span class="built_in">string</span>;</span><br><span class="line">  <span class="attr">content</span>: <span class="built_in">string</span>;</span><br><span class="line">  <span class="attr">tags</span>: <span class="built_in">string</span>[];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = <span class="keyword">new</span> <span class="title class_">McpServer</span>(&#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&quot;内部文档搜索&quot;</span>,</span><br><span class="line">  <span class="attr">version</span>: <span class="string">&quot;1.0.0&quot;</span>,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">loadDocs</span>(<span class="params"></span>): <span class="title class_">Doc</span>[] &#123;</span><br><span class="line">  <span class="keyword">const</span> path = process.<span class="property">env</span>.<span class="property">DOCS_PATH</span> ?? <span class="string">&quot;./docs.json&quot;</span>;</span><br><span class="line">  <span class="comment">//                   ↑ 从环境变量读路径，方便不同项目复用同一个 server</span></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">JSON</span>.<span class="title function_">parse</span>(<span class="title function_">readFileSync</span>(path, <span class="string">&quot;utf-8&quot;</span>)) <span class="keyword">as</span> <span class="title class_">Doc</span>[];</span><br><span class="line">  &#125; <span class="keyword">catch</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">`无法读取文档文件：<span class="subst">$&#123;path&#125;</span>`</span>);</span><br><span class="line">    <span class="comment">//                                 ↑ stdio 模式下只能用 console.error，不能用 console.log</span></span><br><span class="line">    <span class="keyword">return</span> [];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">registerTool</span>(</span><br><span class="line">  <span class="string">&quot;search_docs&quot;</span>,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">description</span>:</span><br><span class="line">      <span class="string">&quot;搜索内部文档库。当用户询问内部规范、开发指南、接口文档等问题时使用，返回匹配的文档列表和 ID。&quot;</span>,</span><br><span class="line">    <span class="attr">inputSchema</span>: &#123;</span><br><span class="line">      <span class="attr">query</span>: z.<span class="title function_">string</span>().<span class="title function_">describe</span>(<span class="string">&quot;搜索关键词，支持中英文&quot;</span>),</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="title function_">async</span> (&#123; query &#125;) =&gt; &#123;</span><br><span class="line">    <span class="keyword">const</span> docs = <span class="title function_">loadDocs</span>();</span><br><span class="line">    <span class="keyword">const</span> q = query.<span class="title function_">toLowerCase</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> matches = docs.<span class="title function_">filter</span>(</span><br><span class="line">      <span class="function">(<span class="params">doc</span>) =&gt;</span></span><br><span class="line">        doc.<span class="property">title</span>.<span class="title function_">toLowerCase</span>().<span class="title function_">includes</span>(q) ||</span><br><span class="line">        doc.<span class="property">content</span>.<span class="title function_">toLowerCase</span>().<span class="title function_">includes</span>(q) ||</span><br><span class="line">        doc.<span class="property">tags</span>.<span class="title function_">some</span>(<span class="function">(<span class="params">tag</span>) =&gt;</span> tag.<span class="title function_">toLowerCase</span>().<span class="title function_">includes</span>(q))</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (matches.<span class="property">length</span> === <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> &#123; <span class="attr">content</span>: [&#123; <span class="attr">type</span>: <span class="string">&quot;text&quot;</span>, <span class="attr">text</span>: <span class="string">&quot;没有找到相关文档。&quot;</span> &#125;] &#125;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> result = matches</span><br><span class="line">      .<span class="title function_">map</span>(<span class="function">(<span class="params">d</span>) =&gt;</span> <span class="string">`[<span class="subst">$&#123;d.id&#125;</span>] <span class="subst">$&#123;d.title&#125;</span>（标签：<span class="subst">$&#123;d.tags.join(<span class="string">&quot;, &quot;</span>)&#125;</span>）`</span>)</span><br><span class="line">      .<span class="title function_">join</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123; <span class="attr">content</span>: [&#123; <span class="attr">type</span>: <span class="string">&quot;text&quot;</span>, <span class="attr">text</span>: result &#125;] &#125;;</span><br><span class="line">  &#125;</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">registerTool</span>(</span><br><span class="line">  <span class="string">&quot;get_doc&quot;</span>,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">description</span>: <span class="string">&quot;获取指定 ID 的文档完整内容。在 search_docs 返回列表后，用这个工具读取具体文档。&quot;</span>,</span><br><span class="line">    <span class="attr">inputSchema</span>: &#123;</span><br><span class="line">      <span class="attr">id</span>: z.<span class="title function_">string</span>().<span class="title function_">describe</span>(<span class="string">&quot;文档 ID，从 search_docs 的返回结果里获取&quot;</span>),</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="title function_">async</span> (&#123; id &#125;) =&gt; &#123;</span><br><span class="line">    <span class="keyword">const</span> docs = <span class="title function_">loadDocs</span>();</span><br><span class="line">    <span class="keyword">const</span> doc = docs.<span class="title function_">find</span>(<span class="function">(<span class="params">d</span>) =&gt;</span> d.<span class="property">id</span> === id);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!doc) &#123;</span><br><span class="line">      <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="attr">content</span>: [&#123; <span class="attr">type</span>: <span class="string">&quot;text&quot;</span>, <span class="attr">text</span>: <span class="string">`未找到 ID 为 <span class="subst">$&#123;id&#125;</span> 的文档。`</span> &#125;],</span><br><span class="line">      &#125;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      <span class="attr">content</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">          <span class="attr">type</span>: <span class="string">&quot;text&quot;</span>,</span><br><span class="line">          <span class="attr">text</span>: <span class="string">`# <span class="subst">$&#123;doc.title&#125;</span>\n\n标签：<span class="subst">$&#123;doc.tags.join(<span class="string">&quot;, &quot;</span>)&#125;</span>\n\n<span class="subst">$&#123;doc.content&#125;</span>`</span>,</span><br><span class="line">        &#125;,</span><br><span class="line">      ],</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> transport = <span class="keyword">new</span> <span class="title class_">StdioServerTransport</span>();</span><br><span class="line"><span class="keyword">await</span> server.<span class="title function_">connect</span>(transport);</span><br></pre></td></tr></table></figure><p>构建并注册：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">npm run build</span><br><span class="line"></span><br><span class="line">claude mcp add \</span><br><span class="line">  --scope user \</span><br><span class="line">  --<span class="built_in">env</span> DOCS_PATH=/Users/me/team/docs.json \</span><br><span class="line">  team-docs -- node /Users/me/my-mcp-server/build/index.js</span><br><span class="line"><span class="comment">#  ↑ server 名字   ↑ scope user：所有项目都能用这个文档搜索功能</span></span><br></pre></td></tr></table></figure><p>测试效果——在 Claude Code 里直接说：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&gt; 帮我找一下关于认证的内部文档</span><br><span class="line"></span><br><span class="line">Claude 会调用 search_docs(&quot;认证&quot;) → 返回 2 篇 → 再调用 get_doc(&quot;3&quot;) → 读全文</span><br></pre></td></tr></table></figure><p>Claude 的工具调用会标注 server 名字，你能清楚看到它用了哪个工具。</p><h3 id="一个工程细节：为什么要读两次"><a href="#一个工程细节：为什么要读两次" class="headerlink" title="一个工程细节：为什么要读两次"></a>一个工程细节：为什么要读两次</h3><p><code>search_docs</code> 只返回文档列表（ID + 标题），<code>get_doc</code> 才返回全文。这是刻意的设计：搜索时返回所有文档全文会消耗大量 token，先让 Claude 看摘要、再按需读全文，能节省 50% 以上的上下文开销。这也是 MCP 的设计哲学：<strong>工具返回的信息量由你控制</strong>，不是越多越好。</p><h2 id="Resources-和-Prompts：什么时候需要"><a href="#Resources-和-Prompts：什么时候需要" class="headerlink" title="Resources 和 Prompts：什么时候需要"></a>Resources 和 Prompts：什么时候需要</h2><p><strong>Resources</strong> 适合”暴露一个数据集，让客户端决定读哪些”的场景。比如你有 100 篇文档，不想每次都搜索，希望 Claude 能像翻文件夹一样按需读取——这时候把文档目录注册为 resources 比写 search tool 更合适。注册方式和 <code>registerTool</code> 类似，McpServer 提供了对应的 resource 注册方法，详见 <a href="https://modelcontextprotocol.io/docs/learn/server-concepts#resources">TypeScript SDK server guide</a>。</p><p><strong>Prompts</strong> 适合”封装一个固定的多轮对话模板”的场景，比如”代码审查请求”或”事故复盘模板”。起步可以先不用，等业务稳定了再考虑。详见 <a href="https://modelcontextprotocol.io/docs/learn/server-concepts#prompts">Prompts 文档</a>。</p><h2 id="四个高频踩坑：stdio-日志、绝对路径、启动超时、配置不刷新"><a href="#四个高频踩坑：stdio-日志、绝对路径、启动超时、配置不刷新" class="headerlink" title="四个高频踩坑：stdio 日志、绝对路径、启动超时、配置不刷新"></a>四个高频踩坑：stdio 日志、绝对路径、启动超时、配置不刷新</h2><p><strong>stdio 传输里误用了 <code>console.log()</code> 打印日志</strong>：这是最常见的问题。stdio 传输层把 stdout 当 JSON-RPC 消息通道，<code>console.log()</code> 输出的文字会破坏协议，Claude 会看到解析错误。调试信息必须用 <code>console.error()</code>（写到 stderr）：</p><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// ❌ 会破坏 stdio 通道</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;received request&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// ✅ stderr 安全</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">&quot;received request&quot;</span>);</span><br></pre></td></tr></table></figure><p><strong>路径用了相对路径</strong>：Claude Code 在不同工作目录启动 server，相对路径会失效。<code>DOCS_PATH</code> 这类配置用环境变量传，路径写绝对路径。</p><p><strong>首次 npx 包太慢导致超时</strong>：默认启动超时 30 秒。如果用 <code>npx</code> 启动 server（类似 Playwright MCP），首次下载包可能超时。设置 <code>MCP_TIMEOUT</code> 环境变量延长限制：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">MCP_TIMEOUT=60000 claude</span><br><span class="line"><span class="comment">#            ↑ 单位毫秒，60 秒</span></span><br></pre></td></tr></table></figure><p><strong>改了 <code>.mcp.json</code> 没生效</strong>：Claude Code 在会话启动时读配置，改完文件后退出再重开会话。</p><h2 id="下一步"><a href="#下一步" class="headerlink" title="下一步"></a>下一步</h2><ul><li>连接真实的内部系统：在 server 里调用内网 API、查 SQLite、读 PostgreSQL——逻辑都在你掌控的 server 里</li><li>官方维护的参考 server 仓库（filesystem、git、database 等）：<a href="https://github.com/modelcontextprotocol/servers">github.com&#x2F;modelcontextprotocol&#x2F;servers</a></li><li>Python 版 SDK（<code>mcp[cli]</code> + FastMCP）：适合 Python 技术栈，装饰器语法比 TypeScript 更简洁</li><li>MCP 协议的完整规范：<a href="https://modelcontextprotocol.io/">modelcontextprotocol.io</a></li><li>系列前三篇：<a href="/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98">Claude 全面教程</a>、<a href="/ClaudeCode%E5%AE%9E%E6%88%98%E6%95%99%E7%A8%8B-%E5%AE%89%E8%A3%85CLAUDEmd-hooks-subagent">Claude Code 实战教程</a>、<a href="/%E4%BB%8E0%E5%88%B01%E5%86%99%E4%B8%80%E4%B8%AAClaude-Skill-%E8%A7%84%E8%8C%83%E5%86%99%E6%B3%95%E4%B8%8E%E5%AE%9E%E6%88%98">从 0 到 1 写一个 Skill</a></li></ul>]]>
    </content>
    <id>https://blog.codewater.top/%E8%87%AA%E5%BB%BAMCP-server%E6%8E%A5%E5%85%A5Claude/</id>
    <link href="https://blog.codewater.top/%E8%87%AA%E5%BB%BAMCP-server%E6%8E%A5%E5%85%A5Claude/"/>
    <published>2026-07-08T06:00:00.000Z</published>
    <summary>手把手自建 MCP server：用 TypeScript SDK 写 tools、用 claude mcp add 接入 Claude Code，完整实战「内部文档搜索」服务，包含 stdio 避坑和三种 scope 配置。</summary>
    <title>自建一个 MCP Server 接入 Claude：从协议到代码到上线</title>
    <updated>2026-07-08T06:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tutorial" scheme="https://blog.codewater.top/categories/tutorial/"/>
    <category term="Claude" scheme="https://blog.codewater.top/tags/Claude/"/>
    <category term="AI" scheme="https://blog.codewater.top/tags/AI/"/>
    <category term="Agent" scheme="https://blog.codewater.top/tags/Agent/"/>
    <category term="Claude Code" scheme="https://blog.codewater.top/tags/Claude-Code/"/>
    <category term="Skill" scheme="https://blog.codewater.top/tags/Skill/"/>
    <content>
      <![CDATA[<p>同一段提示词，你在 Claude Code 里粘贴第三遍的时候，就该把它写成 skill 了。Skill 是一个装着指令的文件夹，Claude 在任务匹配时自动加载，你也可以用 <code>/名字</code> 手动触发——写一次，以后每次都不用再解释。</p><p>这是 Claude 系列的第三篇：<a href="/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98">第一篇</a>讲概念，<a href="/ClaudeCode%E5%AE%9E%E6%88%98%E6%95%99%E7%A8%8B-%E5%AE%89%E8%A3%85CLAUDEmd-hooks-subagent">第二篇</a>讲 Claude Code 的整套配置，这篇专攻 skill——什么时候写、规范格式长什么样、description 怎么写才能被正确触发，最后用一个”发版说明生成”的真实场景从 v1 迭代到 v3。所有格式和字段核对自<a href="https://code.claude.com/docs/en/skills">官方文档</a>，截至 2026 年 7 月 8 日有效。</p><span id="more"></span><h2 id="什么时候该写一个-skill"><a href="#什么时候该写一个-skill" class="headerlink" title="什么时候该写一个 skill"></a>什么时候该写一个 skill</h2><p>三个信号，中一个就值得动手：</p><ol><li><strong>同一段指令你粘贴过不止一次</strong>——排查线上问题的固定套路、生成周报的格式要求、发版的检查清单</li><li>**CLAUDE.md 里某一段越长越像”流程”而不是”事实”**——CLAUDE.md 适合放”构建命令是 npm run build”这种事实；”发版时先跑测试再更新 CHANGELOG 再打 tag”这种多步骤流程，搬去 skill</li><li><strong>一段专业知识只在特定任务时才需要</strong>——比如 Excel 操作技巧、某个内部系统的 API 用法，放 CLAUDE.md 每次会话都白白占上下文，做成 skill 用到才加载</li></ol><p>和相邻机制的分工一句话说清：**CLAUDE.md 放”每次都要知道的事实”，skill 放”特定任务的做法”，subagent 是”独立上下文里干活的分身”**（skill 是知识，subagent 是工人——工人可以带着知识干活，两者不冲突）。</p><p>一个容易过时的认知要先纠正：<strong>自定义 slash commands 已经并入 skills</strong>。<code>.claude/commands/deploy.md</code> 和 <code>.claude/skills/deploy/SKILL.md</code> 都会生成 <code>/deploy</code> 命令、工作方式相同；老的 commands 文件继续可用，但官方推荐 skills——它多了三样东西：支持文件目录、控制”谁能触发”的 frontmatter、被 Claude 自动加载的能力。</p><h2 id="规范格式解剖"><a href="#规范格式解剖" class="headerlink" title="规范格式解剖"></a>规范格式解剖</h2><p>一个 skill 就是一个目录，<code>SKILL.md</code> 是唯一必需的文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">release-notes/               # 目录名 = 命令名（/release-notes 就来自这里）</span><br><span class="line">├── SKILL.md                 # 主指令（必需）</span><br><span class="line">├── reference.md             # 详细规范（可选，用到才加载）</span><br><span class="line">├── examples/</span><br><span class="line">│   └── sample.md            # 期望输出示例（可选）</span><br><span class="line">└── scripts/</span><br><span class="line">    └── collect.sh           # 可执行脚本（可选，被执行而不是被读入）</span><br></pre></td></tr></table></figure><p>放哪取决于给谁用：</p><table><thead><tr><th>位置</th><th>生效范围</th></tr></thead><tbody><tr><td><code>~/.claude/skills/&lt;名字&gt;/SKILL.md</code></td><td>你的所有项目</td></tr><tr><td><code>.claude/skills/&lt;名字&gt;/SKILL.md</code></td><td>当前项目，进版本库团队共享</td></tr></tbody></table><p>注意一个细节：<strong>命令名来自目录名</strong>，frontmatter 里的 <code>name</code> 字段只是列表里的显示名。想让命令叫 <code>/release-notes</code>，目录就得叫 <code>release-notes</code>。</p><p><code>SKILL.md</code> 本身分两部分：YAML frontmatter（配置）+ Markdown 正文（指令）：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">---</span><br><span class="line">name: release-notes</span><br><span class="line"><span class="section">description: 从 git 提交记录生成发版说明。当用户要求写 release notes、发版说明或版本更新日志时使用。</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"></span><br><span class="line">（这里是正文：Claude 被触发后按这里的指令行事）</span><br></pre></td></tr></table></figure><p>它的运行机制是<strong>渐进式加载</strong>——这是 skill 最核心的设计，决定了后面所有写法建议：</p><pre><code class="highlight mermaid">graph TD    A[&quot;会话开始&lt;br/&gt;所有 skill 只有 description 进入上下文&lt;br/&gt;（每个 skill 只花几十 token）&quot;] --&gt; B&#123;&quot;任务匹配某个&lt;br/&gt;description？&quot;&#125;    B --&gt;|是| C[&quot;加载该 skill 的 SKILL.md 全文&quot;]    B --&gt;|用户手动 /名字| C    C --&gt; D&#123;&quot;正文里引用了&lt;br/&gt;reference.md 等支持文件？&quot;&#125;    D --&gt;|需要时| E[&quot;再读对应文件&quot;]    D --&gt;|不需要| F[&quot;按指令执行&quot;]    E --&gt; F</code></pre><p>三层各司其职：description 常驻上下文负责”被找到”，SKILL.md 正文触发才加载负责”怎么做”，支持文件按需读取负责”细节规范”。写 skill 的所有技巧，本质上都是把内容放进正确的层。</p><h2 id="description-是灵魂：只写”何时用”，不写”怎么做”"><a href="#description-是灵魂：只写”何时用”，不写”怎么做”" class="headerlink" title="description 是灵魂：只写”何时用”，不写”怎么做”"></a>description 是灵魂：只写”何时用”，不写”怎么做”</h2><p>Claude 靠 description 决定要不要加载你的 skill——写不好，要么该触发时不触发，要么不该触发时乱触发。两条规则：</p><p><strong>规则一：写清触发条件，覆盖用户可能说出的词。</strong> Claude 匹配的是用户的原话，把同义词写进去：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ❌ 差：太抽象，用户说&quot;帮我写个发版说明&quot;时匹配不上</span></span><br><span class="line"><span class="attr">description:</span> <span class="string">处理版本发布相关事务</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># ✅ 好：具体动作 + 用户可能说的各种说法</span></span><br><span class="line"><span class="attr">description:</span> <span class="string">从</span> <span class="string">git</span> <span class="string">提交记录生成发版说明。当用户要求写</span> <span class="string">release</span> <span class="string">notes、发版说明、版本更新日志或</span> <span class="string">changelog</span> <span class="string">时使用。</span></span><br></pre></td></tr></table></figure><p><strong>规则二：只描述”何时用”，不要把流程步骤摘要进去。</strong> 这条反直觉但很重要：如果 description 里写了”先收集提交、再分类、再格式化”，Claude 可能<strong>照着这句摘要直接干活，跳过正文里的完整指令</strong>——你在正文里精心写的细节全部作废。description 是门牌，不是说明书：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ❌ 差：摘要了流程，Claude 可能抄近路不读正文</span></span><br><span class="line"><span class="attr">description:</span> <span class="string">生成发版说明——收集</span> <span class="string">git</span> <span class="string">提交、按类型分类、按模板格式化输出</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># ✅ 好：只有触发条件</span></span><br><span class="line"><span class="attr">description:</span> <span class="string">从</span> <span class="string">git</span> <span class="string">提交记录生成发版说明。当用户要求写</span> <span class="string">release</span> <span class="string">notes</span> <span class="string">或版本更新日志时使用。</span></span><br></pre></td></tr></table></figure><h2 id="实战：发版说明生成器，从-v1-到-v3"><a href="#实战：发版说明生成器，从-v1-到-v3" class="headerlink" title="实战：发版说明生成器，从 v1 到 v3"></a>实战：发版说明生成器，从 v1 到 v3</h2><p>场景：每次发版你都要整理”这个版本改了什么”——翻 git log、按新功能&#x2F;修复&#x2F;破坏性变更分类、写成固定格式发给团队。这个流程每月重复几次，格式要求还总有人记错，是典型的 skill 素材。</p><h3 id="v1：最简可用版"><a href="#v1：最简可用版" class="headerlink" title="v1：最简可用版"></a>v1：最简可用版</h3><p>先写最小版本跑起来。创建 <code>.claude/skills/release-notes/SKILL.md</code>：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">---</span><br><span class="line"><span class="section">description: 从 git 提交记录生成发版说明。当用户要求写 release notes、发版说明或版本更新日志时使用。</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"></span><br><span class="line">生成发版说明：</span><br><span class="line"></span><br><span class="line"><span class="bullet">1.</span> 运行 <span class="code">`git log --oneline &lt;上一个tag&gt;..HEAD`</span> 收集本次发版的提交</span><br><span class="line">   （用 <span class="code">`git describe --tags --abbrev=0`</span> 找上一个 tag）</span><br><span class="line"><span class="bullet">2.</span> 把提交按三类分组：新功能（feat）、修复（fix）、破坏性变更（含 BREAKING）</span><br><span class="line"><span class="bullet">3.</span> 用中文输出，每条一行，写&quot;用户能感知的变化&quot;而不是复制提交信息</span><br></pre></td></tr></table></figure><p>保存即生效（skill 目录有文件监视，改动实时加载；只有”首次创建顶层 skills 目录”需要重启会话）。测试两种触发：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">&gt; 帮我写一下这个版本的发版说明        ← 自动触发：说法匹配 description</span><br><span class="line">&gt; /release-notes                    ← 手动触发：直接点名</span><br></pre></td></tr></table></figure><h3 id="v2：动态上下文注入，省掉一轮工具调用"><a href="#v2：动态上下文注入，省掉一轮工具调用" class="headerlink" title="v2：动态上下文注入，省掉一轮工具调用"></a>v2：动态上下文注入，省掉一轮工具调用</h3><p>v1 里 Claude 要自己跑 git 命令拿数据。skill 支持一个更优雅的写法——<code>!`命令`</code>：<strong>在 Claude 看到 skill 内容之前</strong>，Claude Code 先执行这行命令、把输出原地替换进来。Claude 拿到的直接是带着数据的指令：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">---</span><br><span class="line"><span class="section">description: 从 git 提交记录生成发版说明。当用户要求写 release notes、发版说明或版本更新日志时使用。</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 本次发版的提交记录</span></span><br><span class="line"></span><br><span class="line">!<span class="code">`git log --oneline $(git describe --tags --abbrev=0)..HEAD`</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 指令</span></span><br><span class="line"></span><br><span class="line">把上面的提交按三类分组：新功能（feat）、修复（fix）、破坏性变更（含 BREAKING）。</span><br><span class="line">用中文输出，每条写&quot;用户能感知的变化&quot;而不是复制提交信息。</span><br><span class="line">如果提交列表为空，直接说&quot;上个 tag 之后没有新提交&quot;。</span><br></pre></td></tr></table></figure><p>注意最后一行——<strong>给边界情况一个明确出口</strong>（空列表怎么办），否则模型可能在没数据时开始编造。</p><h3 id="v3：格式规范拆进支持文件"><a href="#v3：格式规范拆进支持文件" class="headerlink" title="v3：格式规范拆进支持文件"></a>v3：格式规范拆进支持文件</h3><p>团队的发版说明有详细格式要求（标题格式、emoji 约定、破坏性变更必须带迁移指引……），全塞进 SKILL.md 会让它臃肿。按渐进加载的思路拆出去：</p><p><code>.claude/skills/release-notes/reference.md</code>：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># 发版说明格式规范</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 标题</span></span><br><span class="line"><span class="code">`## vX.Y.Z (YYYY-MM-DD)`</span>，版本号从 package.json 读取</span><br><span class="line"></span><br><span class="line"><span class="section">## 分组顺序与标记</span></span><br><span class="line"><span class="bullet">1.</span> 🚨 破坏性变更（有才写，必须附迁移指引）</span><br><span class="line"><span class="bullet">2.</span> ✨ 新功能</span><br><span class="line"><span class="bullet">3.</span> 🐛 修复</span><br><span class="line"></span><br><span class="line"><span class="section">## 每条格式</span></span><br><span class="line"><span class="bullet">-</span> 动词开头，不超过一行：&quot;支持批量导出用户数据&quot;</span><br><span class="line"><span class="bullet">-</span> 结尾括号标 PR 号：（#123）</span><br></pre></td></tr></table></figure><p>SKILL.md 里加一行引用，告诉 Claude 这个文件是什么、什么时候读：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 指令</span></span><br><span class="line"></span><br><span class="line">把上面的提交分类整理成发版说明。</span><br><span class="line"><span class="strong">**输出前必须先读取本目录的 [<span class="string">reference.md</span>](<span class="link">reference.md</span>)，严格按其中的格式规范输出。**</span></span><br></pre></td></tr></table></figure><p>最终目录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">.claude/skills/release-notes/</span><br><span class="line">├── SKILL.md        # 触发条件 + 数据注入 + 主流程（保持精简）</span><br><span class="line">└── reference.md    # 格式细节（触发后按需加载）</span><br></pre></td></tr></table></figure><p>这就是从 0 到 1 的完整路径：<strong>v1 验证触发和主流程 → v2 用动态注入消灭重复劳动 → v3 把厚重规范拆到支持文件</strong>。官方建议 SKILL.md 控制在 500 行内，超了就继续往支持文件里拆。</p><h2 id="进阶字段速查"><a href="#进阶字段速查" class="headerlink" title="进阶字段速查"></a>进阶字段速查</h2><p>frontmatter 全部字段是可选的，按需求场景挑：</p><table><thead><tr><th>需求</th><th>字段写法</th></tr></thead><tbody><tr><td>只许手动 <code>/名字</code> 触发，不让 Claude 自作主张（适合部署这类危险操作）</td><td><code>disable-model-invocation: true</code></td></tr><tr><td>只让 Claude 自动用，不出现在 <code>/</code> 菜单（背景知识类）</td><td><code>user-invocable: false</code></td></tr><tr><td>skill 激活期间某些工具免确认</td><td><code>allowed-tools: Read Grep Bash(git log *)</code></td></tr><tr><td>在独立子上下文里跑，不污染主对话</td><td><code>context: fork</code>（可配 <code>agent</code> 指定 subagent 类型）</td></tr><tr><td>只在碰到匹配文件时才可被自动加载</td><td><code>paths: [&quot;src/api/**/*.ts&quot;]</code></td></tr><tr><td>接收参数</td><td>正文用 <code>$ARGUMENTS</code>（全部）、<code>$0</code> <code>$1</code>（按位置）；<code>argument-hint: [版本号]</code> 提供输入提示</td></tr><tr><td>引用 skill 自己目录下的脚本</td><td><code>$&#123;CLAUDE_SKILL_DIR&#125;/scripts/collect.sh</code></td></tr></tbody></table><p>举个参数的例子：<code>/release-notes v2.3.0</code> 触发时，正文里的 <code>$0</code> 会被替换成 <code>v2.3.0</code>。</p><p>全部字段和取值以<a href="https://code.claude.com/docs/en/skills">官方 frontmatter 参考</a>为准——查配置项的方法，<a href="/ClaudeCode%E5%AE%9E%E6%88%98%E6%95%99%E7%A8%8B-%E5%AE%89%E8%A3%85CLAUDEmd-hooks-subagent">上一篇的”配置项的可选值去哪查”</a>写过，这里不重复。</p><h2 id="写完怎么验证：像测代码一样测-skill"><a href="#写完怎么验证：像测代码一样测-skill" class="headerlink" title="写完怎么验证：像测代码一样测 skill"></a>写完怎么验证：像测代码一样测 skill</h2><p>skill 是给模型看的文档，”我觉得写清楚了”不算数——得看模型的实际行为。三个测试，开个新会话逐个跑：</p><ol><li><strong>触发测试</strong>：用你没在 description 里写过的自然说法提需求（”整理下这次更新的内容”），看它会不会自动加载 skill。不触发 → description 的关键词覆盖不够。</li><li><strong>误触发测试</strong>：提一个相邻但不该触发的需求（”看看最近的提交记录”），看它会不会错误加载。误触发 → description 写得太宽。</li><li><strong>执行测试</strong>：触发后看输出是否符合 reference.md 的规范。不符合 → 检查正文有没有明确要求”先读 reference.md”，指令是否具体到可验证。</li></ol><p>更进一步的思路借自测试驱动开发：<strong>写 skill 之前，先在没有 skill 的情况下让 Claude 做一遍这个任务，记下它哪里做得不对</strong>——你的 skill 只需要针对性地修这些点，而不是把所有想到的都写上。写得越少，遵守得越稳。</p><p>最后是几个常见反模式，都来自真实踩坑：</p><ul><li><strong>description 摘要了工作流程</strong> → 模型抄近路，正文作废（前文讲过）</li><li><strong>把一次性的解决过程写成 skill</strong>（”上次我是这么修的：第一步……”）→ skill 是可复用的做法，不是工作日志</li><li><strong>一个 skill 塞多个不相关任务</strong> → 拆成多个，每个 skill 一件事，description 才写得准</li><li><strong>规范细节全堆在 SKILL.md</strong> → 拆到支持文件，正文只留主流程和”何时读哪个文件”</li></ul><h2 id="下一步"><a href="#下一步" class="headerlink" title="下一步"></a>下一步</h2><ul><li>给 skill 配上团队共享：提交 <code>.claude/skills/</code> 进版本库，队友拉下来即用</li><li>系列前篇：<a href="/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98">Claude 全面教程（概念）</a>、<a href="/ClaudeCode%E5%AE%9E%E6%88%98%E6%95%99%E7%A8%8B-%E5%AE%89%E8%A3%85CLAUDEmd-hooks-subagent">Claude Code 实战教程（配置）</a></li><li>官方文档：<a href="https://code.claude.com/docs/en/skills">Skills 完整参考</a>，跨工具的开放标准见 <a href="https://agentskills.io/">agentskills.io</a></li></ul>]]>
    </content>
    <id>https://blog.codewater.top/%E4%BB%8E0%E5%88%B01%E5%86%99%E4%B8%80%E4%B8%AAClaude-Skill-%E8%A7%84%E8%8C%83%E5%86%99%E6%B3%95%E4%B8%8E%E5%AE%9E%E6%88%98/</id>
    <link href="https://blog.codewater.top/%E4%BB%8E0%E5%88%B01%E5%86%99%E4%B8%80%E4%B8%AAClaude-Skill-%E8%A7%84%E8%8C%83%E5%86%99%E6%B3%95%E4%B8%8E%E5%AE%9E%E6%88%98/"/>
    <published>2026-07-08T03:00:00.000Z</published>
    <summary>手把手定制 Claude skill：什么时候该写、SKILL.md 规范格式与 frontmatter 字段、description 的正确写法，用发版说明生成器场景从 v1 迭代到 v3。</summary>
    <title>从 0 到 1 写一个 Claude Skill：规范格式、description 写法与发版说明实战</title>
    <updated>2026-07-08T03:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Claude" scheme="https://blog.codewater.top/tags/Claude/"/>
    <category term="AI" scheme="https://blog.codewater.top/tags/AI/"/>
    <category term="Agent" scheme="https://blog.codewater.top/tags/Agent/"/>
    <category term="Claude Code" scheme="https://blog.codewater.top/tags/Claude-Code/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <content>
      <![CDATA[<p>Claude Code 是 Anthropic 的编程 agent：在终端里对它说人话，它替你读代码、改文件、跑命令、提 PR。装好只要一分钟，但要让它真正”懂你的项目、按你的规矩干活”，靠的是一整套配置——主线是 CLAUDE.md 立规矩、hooks 上硬约束、subagent 分担脏活，外加 slash commands、Skills、MCP、权限这些配套机制。这篇按”装好 → 用起来 → 配成自己的”的顺序走完全程，最后教你怎么自己查任何配置项的可选值。所有命令和配置格式核对自<a href="https://code.claude.com/docs">官方文档</a>，截至 2026 年 7 月 7 日有效。</p><p>如果你还不清楚 agent、上下文窗口这些基本概念，建议先读<a href="/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98">Claude 全面教程</a>。</p><span id="more"></span><h2 id="安装与首次启动"><a href="#安装与首次启动" class="headerlink" title="安装与首次启动"></a>安装与首次启动</h2><p>三种安装方式按推荐度排序：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 方式一：原生安装器（官方推荐，装完自动在后台更新）</span></span><br><span class="line">curl -fsSL https://claude.ai/install.sh | bash</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式二：Homebrew（不会自动更新，升级要手动 brew upgrade）</span></span><br><span class="line">brew install --cask claude-code</span><br><span class="line"><span class="comment">#    claude-code：stable 渠道，比最新版晚约一周，跳过有严重回归的版本</span></span><br><span class="line"><span class="comment">#    想追新用 brew install --cask claude-code@latest</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式三：npm（需要 Node.js 22+，装的同样是原生二进制）</span></span><br><span class="line">npm install -g @anthropic-ai/claude-code</span><br><span class="line"><span class="comment">#   不要加 sudo，权限问题参考官方 troubleshooting</span></span><br></pre></td></tr></table></figure><p>Windows 用户在 PowerShell 里执行 <code>irm https://claude.ai/install.ps1 | iex</code>，或者用 WSL 走 Linux 安装方式。</p><p>装完验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">claude --version</span><br><span class="line"><span class="comment">#   输出版本号即安装成功</span></span><br><span class="line"></span><br><span class="line">claude doctor</span><br><span class="line"><span class="comment">#   更完整的自检：安装方式、更新状态、配置问题一次看全</span></span><br></pre></td></tr></table></figure><p><strong>账号要求</strong>：Claude Code 需要 Pro（$20&#x2F;月）、Max、Team 订阅或 API（Console）账号，<strong>免费版不能用</strong>。订阅怎么选，<a href="/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98">上一篇</a>的”订阅还是 API”一节有详细对比。</p><p>进入你的项目目录启动，首次运行会引导浏览器登录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> your-project</span><br><span class="line">claude</span><br></pre></td></tr></table></figure><h2 id="五分钟摸清交互方式"><a href="#五分钟摸清交互方式" class="headerlink" title="五分钟摸清交互方式"></a>五分钟摸清交互方式</h2><p>进入会话后，你和 Claude Code 的交互就是聊天+确认。几个立刻会用到的东西：</p><p><strong>权限确认</strong>：Claude 要执行命令、改文件时会先弹确认。这是默认的安全机制——你随时知道它在干什么。信任度上来之后，可以在确认框里选”本会话不再询问”，或用 <code>/permissions</code> 配置白名单。</p><p><strong>Plan mode（规划模式）</strong>：按 <code>Shift+Tab</code> 切换。这个模式下 Claude 只读代码、出方案，<strong>不动任何文件</strong>——改动大、心里没底的任务，先在 plan mode 里让它把计划列出来，你审完再放行执行。</p><p><strong>常用斜杠命令</strong>，先记这六个：</p><table><thead><tr><th>命令</th><th>作用</th></tr></thead><tbody><tr><td><code>/init</code></td><td>分析代码库，自动生成 CLAUDE.md（下一节细讲）</td></tr><tr><td><code>/memory</code></td><td>查看&#x2F;编辑当前加载的所有规则文件和自动记忆</td></tr><tr><td><code>/permissions</code></td><td>配置哪些操作免确认</td></tr><tr><td><code>/agents</code></td><td>管理 subagent（后文细讲）</td></tr><tr><td><code>/compact</code></td><td>手动压缩对话历史，上下文快满时用</td></tr><tr><td><code>/config</code></td><td>打开设置（模型、更新渠道等）</td></tr></tbody></table><p><strong>上下文思维</strong>：一次会话就是一个上下文窗口，聊得越久越臃肿。一个任务干完就 <code>/clear</code> 开新会话，比在一个会话里从早聊到晚效果好得多。</p><h2 id="CLAUDE-md：给它立规矩"><a href="#CLAUDE-md：给它立规矩" class="headerlink" title="CLAUDE.md：给它立规矩"></a>CLAUDE.md：给它立规矩</h2><p>Claude Code 每次会话都从零开始——它不记得你上周说过”这个项目用 pnpm 别用 npm”。CLAUDE.md 就是解决这个问题的：一个普通 Markdown 文件，<strong>每次会话自动读入</strong>，你把项目约定写进去，就不用每次重复交代。</p><h3 id="从-init-开始"><a href="#从-init-开始" class="headerlink" title="从 &#x2F;init 开始"></a>从 &#x2F;init 开始</h3><p>不用从零写，在项目里执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/init</span><br></pre></td></tr></table></figure><p>Claude 会分析你的代码库，自动生成一份包含构建命令、测试方式、项目结构的 CLAUDE.md。如果项目里已经有 AGENTS.md 或 <code>.cursorrules</code>（其他 AI 工具的规则文件），<code>/init</code> 会读取并把相关内容吸收进来。生成后再手动补充它发现不了的东西——团队约定、踩坑经验、部署流程。</p><h3 id="写法标准：具体到能验证"><a href="#写法标准：具体到能验证" class="headerlink" title="写法标准：具体到能验证"></a>写法标准：具体到能验证</h3><p>CLAUDE.md 是”上下文”而不是”强制配置”，写得越具体，遵守得越稳定。官方给的对照很直观：</p><table><thead><tr><th>差的写法</th><th>好的写法</th></tr></thead><tbody><tr><td>格式要规范</td><td>用 2 空格缩进</td></tr><tr><td>提交前要测试</td><td>提交前必须跑 <code>npm test</code></td></tr><tr><td>文件放整齐</td><td>API handler 放 <code>src/api/handlers/</code> 下</td></tr></tbody></table><p>另一条官方建议：<strong>单个 CLAUDE.md 控制在 200 行以内</strong>。超长的规则文件既费上下文又降低遵守率——内容多了就拆（见下文 rules 目录）。</p><h3 id="多级文件与导入"><a href="#多级文件与导入" class="headerlink" title="多级文件与导入"></a>多级文件与导入</h3><p>CLAUDE.md 按作用域分层，全部会被加载、逐级叠加：</p><table><thead><tr><th>位置</th><th>作用域</th><th>进版本库？</th></tr></thead><tbody><tr><td><code>~/.claude/CLAUDE.md</code></td><td>你的所有项目（个人偏好）</td><td>否</td></tr><tr><td><code>./CLAUDE.md</code> 或 <code>./.claude/CLAUDE.md</code></td><td>当前项目（团队约定）</td><td>是</td></tr><tr><td><code>./CLAUDE.local.md</code></td><td>当前项目（个人私有，如本地测试地址）</td><td>否，加进 .gitignore</td></tr></tbody></table><p>支持用 <code>@路径</code> 导入其他文件，路径相对于 CLAUDE.md 自身，最多嵌套 4 层。团队混用多种 AI 工具时的标准做法——通用规则写 AGENTS.md，CLAUDE.md 一行导入：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">@AGENTS.md</span><br><span class="line"></span><br><span class="line"><span class="section">## Claude Code 专属</span></span><br><span class="line">改 src/billing/ 下的代码前先进 plan mode。</span><br></pre></td></tr></table></figure><p>注意一个细节：写在反引号里的 <code>@README</code> 是普通文本，不会触发导入；只有裸写的 @ 路径才导入。</p><h3 id="规则多了怎么办：-claude-rules-目录"><a href="#规则多了怎么办：-claude-rules-目录" class="headerlink" title="规则多了怎么办：.claude&#x2F;rules&#x2F; 目录"></a>规则多了怎么办：.claude&#x2F;rules&#x2F; 目录</h3><p>CLAUDE.md 超过 200 行后，把规则按主题拆成多个文件。具体操作分三步：</p><p><strong>第一步</strong>：在项目里建 <code>.claude/rules/</code> 目录，每个主题一个 <code>.md</code> 文件，文件名描述内容：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">your-project/</span><br><span class="line">├── CLAUDE.md               # 主规则：只留最核心的约定</span><br><span class="line">└── .claude/</span><br><span class="line">    └── rules/</span><br><span class="line">        ├── code-style.md   # 代码风格</span><br><span class="line">        ├── testing.md      # 测试约定</span><br><span class="line">        └── api-design.md   # API 设计规范</span><br></pre></td></tr></table></figure><p><strong>第二步</strong>：往文件里写规则。<strong>不加任何头部</strong>的文件和 CLAUDE.md 待遇一样——每次会话都加载：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># 测试约定</span></span><br><span class="line"><span class="bullet">-</span> 单测文件和源文件同目录，命名 <span class="emphasis">*.test.ts</span></span><br><span class="line"><span class="emphasis">- 提交前必须跑 npm test</span></span><br></pre></td></tr></table></figure><p><strong>第三步（可选）</strong>：只和某类文件相关的规则，在文件开头加一段 <code>paths</code> 头部（YAML frontmatter，两行 <code>---</code> 包起来）。加了之后这个文件<strong>平时不加载</strong>，只有 Claude 读到匹配的文件时才进入上下文，省 token：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">---</span><br><span class="line">paths:</span><br><span class="line"><span class="section">  - &quot;src/api/<span class="strong">**/<span class="emphasis">*.ts&quot;    # glob 模式：src/api 下所有 .ts 文件</span></span></span></span><br><span class="line"><span class="emphasis"><span class="strong"><span class="section">---</span></span></span></span><br><span class="line"><span class="emphasis"><span class="strong"><span class="section"></span></span></span></span><br><span class="line"><span class="emphasis"><span class="strong"><span class="section"># API 开发规则</span></span></span></span><br><span class="line"><span class="emphasis"><span class="strong"><span class="section">- 所有接口必须做入参校验</span></span></span></span><br><span class="line"><span class="emphasis"><span class="strong"><span class="section">- 错误响应用统一格式</span></span></span></span><br></pre></td></tr></table></figure><p>这样配置后的效果：Claude 平时看不到 API 规则；一旦它打开 <code>src/api/user.ts</code>，这份规则自动出现在它的上下文里。</p><p>个人级规则放 <code>~/.claude/rules/</code>，写法相同，对你机器上所有项目生效。</p><h3 id="自动记忆：它自己也会记笔记"><a href="#自动记忆：它自己也会记笔记" class="headerlink" title="自动记忆：它自己也会记笔记"></a>自动记忆：它自己也会记笔记</h3><p>CLAUDE.md 是你写给它的，<strong>auto memory</strong> 是它自己记的（需 v2.1.59+，默认开启）：工作中确认过的构建命令、调试结论、你纠正过它的偏好，会被存到 <code>~/.claude/projects/&lt;项目&gt;/memory/</code> 目录，下次会话自动带上。<code>MEMORY.md</code> 是索引（每次加载前 200 行），细节存独立主题文件、按需读取。用 <code>/memory</code> 可以随时翻看和删改——都是普通 Markdown。</p><h2 id="Hooks：CLAUDE-md-管不住的，用钩子硬管"><a href="#Hooks：CLAUDE-md-管不住的，用钩子硬管" class="headerlink" title="Hooks：CLAUDE.md 管不住的，用钩子硬管"></a>Hooks：CLAUDE.md 管不住的，用钩子硬管</h2><p>CLAUDE.md 再具体也只是”叮嘱”，Claude 有小概率忘。<strong>hooks 是硬约束</strong>：在固定的生命周期节点自动执行你的 shell 脚本，不管 Claude 怎么想，到点必执行。官方文档的判断标准很干脆：希望”每次必然发生”的事，写 hook，别写 CLAUDE.md。</p><pre><code class="highlight mermaid">graph LR    A[你的提示词] --&gt; B[Claude 决策]    B --&gt; C&#123;PreToolUse hook&lt;br/&gt;工具执行前&#125;    C --&gt;|放行| D[执行工具&lt;br/&gt;改文件/跑命令]    C --&gt;|deny| B    D --&gt; E[PostToolUse hook&lt;br/&gt;工具执行后]    E --&gt; B    B --&gt; F[Stop hook&lt;br/&gt;回答结束]</code></pre><h3 id="配置结构"><a href="#配置结构" class="headerlink" title="配置结构"></a>配置结构</h3><p>Hooks 写在 settings 文件里，三个位置按需选：<code>~/.claude/settings.json</code>（全局）、<code>.claude/settings.json</code>（项目，进版本库）、<code>.claude/settings.local.json</code>（项目本地，不进版本库）。</p><p>基本结构：<strong>事件 → matcher 筛选工具 → 要执行的命令</strong>。常用事件：</p><table><thead><tr><th>事件</th><th>触发时机</th><th>典型用途</th></tr></thead><tbody><tr><td><code>PreToolUse</code></td><td>工具执行前（可拦截）</td><td>挡掉危险命令</td></tr><tr><td><code>PostToolUse</code></td><td>工具成功后</td><td>改完文件自动 lint</td></tr><tr><td><code>UserPromptSubmit</code></td><td>你发出提示词时</td><td>注入动态上下文</td></tr><tr><td><code>SessionStart</code></td><td>会话开始</td><td>加载环境信息</td></tr><tr><td><code>Stop</code></td><td>Claude 回答完毕</td><td>桌面通知、后处理</td></tr></tbody></table><h3 id="实战一：改完文件自动跑-lint"><a href="#实战一：改完文件自动跑-lint" class="headerlink" title="实战一：改完文件自动跑 lint"></a>实战一：改完文件自动跑 lint</h3><p><code>.claude/settings.json</code>：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;PostToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;matcher&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Edit|Write&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">          <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;CLAUDE_PROJECT_DIR&#125;/.claude/hooks/lint-check.sh&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;timeout&quot;</span><span class="punctuation">:</span> <span class="number">30</span></span><br><span class="line">          <span class="punctuation">&#125;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><ul><li><code>matcher</code>：筛选触发的工具，<code>Edit|Write</code> 表示编辑或写文件都触发；支持正则和 MCP 工具名（如 <code>mcp__github__.*</code>）</li><li><code>command</code>：要执行的脚本；<code>$&#123;CLAUDE_PROJECT_DIR&#125;</code> 是项目根目录变量</li><li><code>timeout</code>：秒数，超时取消（默认 600）</li></ul><p><code>.claude/hooks/lint-check.sh</code>（hook 通过 stdin 收到 JSON 输入，用 <code>jq</code> 取字段）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line">FILE=$(jq -r <span class="string">&#x27;.tool_input.file_path&#x27;</span>)</span><br><span class="line"><span class="comment">#   stdin 的 JSON 里带着本次工具调用的完整参数</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> [[ -f <span class="string">&quot;<span class="variable">$FILE</span>&quot;</span> ]]; <span class="keyword">then</span></span><br><span class="line">  npx eslint <span class="string">&quot;<span class="variable">$FILE</span>&quot;</span> --fix || <span class="built_in">exit</span> 2</span><br><span class="line">  <span class="comment">#   退出码 2 = 阻断错误：stderr 会喂回给 Claude，它会看到 lint 报错并去修</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"><span class="built_in">exit</span> 0</span><br><span class="line"><span class="comment">#   退出码 0 = 正常放行</span></span><br></pre></td></tr></table></figure><p>配好之后：Claude 每改一个文件，eslint 自动跑一遍；有问题它当场看到报错、当场修——不用你盯。</p><h3 id="实战二：拦截危险命令"><a href="#实战二：拦截危险命令" class="headerlink" title="实战二：拦截危险命令"></a>实战二：拦截危险命令</h3><p><code>PreToolUse</code> hook 可以在命令执行前说”不”：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;PreToolUse&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;matcher&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Bash&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;hooks&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">          <span class="punctuation">&#123;</span></span><br><span class="line">            <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;command&quot;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="attr">&quot;command&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;CLAUDE_PROJECT_DIR&#125;/.claude/hooks/block-danger.sh&quot;</span></span><br><span class="line">          <span class="punctuation">&#125;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line">COMMAND=$(jq -r <span class="string">&#x27;.tool_input.command&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="built_in">echo</span> <span class="string">&quot;<span class="variable">$COMMAND</span>&quot;</span> | grep -qE <span class="string">&#x27;rm -rf|git push --force&#x27;</span>; <span class="keyword">then</span></span><br><span class="line">  <span class="comment"># 输出 JSON 决策：拒绝执行，并告诉 Claude 原因</span></span><br><span class="line">  jq -n <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">    hookSpecificOutput: &#123;</span></span><br><span class="line"><span class="string">      hookEventName: &quot;PreToolUse&quot;,</span></span><br><span class="line"><span class="string">      permissionDecision: &quot;deny&quot;,</span></span><br><span class="line"><span class="string">      permissionDecisionReason: &quot;危险命令被 hook 拦截，请改用更安全的方式&quot;</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">  &#125;&#x27;</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">  <span class="built_in">exit</span> 0   <span class="comment"># 不做决策，走正常的权限确认流程</span></span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure><h2 id="Subagent：给它配几个分身"><a href="#Subagent：给它配几个分身" class="headerlink" title="Subagent：给它配几个分身"></a>Subagent：给它配几个分身</h2><p>主对话的上下文窗口是稀缺资源。让 Claude 在主对话里全库搜索、读几十个文件，搜索过程的中间结果会把上下文塞满——而那些内容你之后根本用不到。<strong>Subagent 的价值就在这</strong>：把这类任务派给一个拥有独立上下文的”分身”，它在自己的窗口里干完脏活，只把结论带回主对话。</p><p>顺带还有三个好处：可以限制分身的工具权限（只读不写）、可以换更便宜的模型（搜索用 Haiku 就够）、配置一次到处复用。</p><h3 id="文件格式"><a href="#文件格式" class="headerlink" title="文件格式"></a>文件格式</h3><p>Subagent 是一个带 YAML frontmatter 的 Markdown 文件，放两个位置之一：</p><ul><li><code>.claude/agents/</code>——当前项目可用，进版本库和团队共享</li><li><code>~/.claude/agents/</code>——你的所有项目可用</li></ul><p>一个完整的代码审查分身，<code>.claude/agents/code-reviewer.md</code>：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">---</span><br><span class="line">name: code-reviewer</span><br><span class="line">description: 代码审查专家。写完或修改代码后主动使用，检查质量、安全和可维护性。</span><br><span class="line">tools: Read, Grep, Glob, Bash</span><br><span class="line"><span class="section">model: sonnet</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"></span><br><span class="line">你是一名严格的代码审查员。被调用时：</span><br><span class="line"></span><br><span class="line"><span class="bullet">1.</span> 运行 git diff 查看最近改动</span><br><span class="line"><span class="bullet">2.</span> 只聚焦改动的文件，逐条检查：</span><br><span class="line"><span class="bullet">   -</span> 有没有暴露的密钥或敏感信息</span><br><span class="line"><span class="bullet">   -</span> 错误处理是否完整</span><br><span class="line"><span class="bullet">   -</span> 命名是否清晰、有没有重复代码</span><br><span class="line"><span class="bullet">3.</span> 按&quot;必须修 / 建议修 / 可以更好&quot;三档输出结论，每条给出具体行号</span><br></pre></td></tr></table></figure><p>frontmatter 字段里 <code>name</code> 和 <code>description</code> 必填，其余常用的：</p><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><code>description</code></td><td>Claude 靠它判断什么时候派活给这个分身——写清触发时机，比如”写完代码后主动使用”</td></tr><tr><td><code>tools</code></td><td>工具白名单，省略则继承全部。审查类分身只给 <code>Read, Grep, Glob</code> 就动不了你的文件</td></tr><tr><td><code>model</code></td><td><code>sonnet</code> &#x2F; <code>opus</code> &#x2F; <code>haiku</code> &#x2F; <code>fable</code> &#x2F; <code>inherit</code>（默认继承主会话）</td></tr><tr><td><code>memory</code></td><td>设为 <code>project</code> 等值后，分身可跨会话积累自己的经验</td></tr><tr><td><code>isolation</code></td><td>设为 <code>worktree</code>，分身在独立的 git worktree 里干活，不碰你的工作区</td></tr></tbody></table><p>正文部分就是分身的系统提示——它看不到主对话的历史，只带着你的任务描述和这份提示上岗。</p><h3 id="怎么用"><a href="#怎么用" class="headerlink" title="怎么用"></a>怎么用</h3><p>保存文件即生效（首次在新目录创建需要重启会话）。两种触发方式：</p><ul><li><strong>自动</strong>：Claude 根据 <code>description</code> 判断——上面例子写了”写完代码后主动使用”，它改完代码就会自动派 code-reviewer 出场</li><li><strong>手动</strong>：直接说”用 code-reviewer 检查一下刚才的改动”</li></ul><p>也可以完全不写文件，用 <code>/agents</code> 命令交互式创建和管理。</p><h2 id="四大件之外：slash-commands、Skills、MCP、权限"><a href="#四大件之外：slash-commands、Skills、MCP、权限" class="headerlink" title="四大件之外：slash commands、Skills、MCP、权限"></a>四大件之外：slash commands、Skills、MCP、权限</h2><p>CLAUDE.md、hooks、subagent 是本文主线，但 Claude Code 的自定义机制不止这些。下面四个同样高频，每个给出最小可用的配置方式。</p><h3 id="自定义-slash-commands：把重复提示词变成命令"><a href="#自定义-slash-commands：把重复提示词变成命令" class="headerlink" title="自定义 slash commands：把重复提示词变成命令"></a>自定义 slash commands：把重复提示词变成命令</h3><p>同一段提示词打第三遍的时候，就该把它做成命令。在 <code>.claude/commands/</code> 下建一个 Markdown 文件，文件名就是命令名：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;!-- .claude/commands/fix-issue.md --&gt;</span><br><span class="line">分析并修复 GitHub issue #$ARGUMENTS：</span><br><span class="line"><span class="bullet">1.</span> 用 gh issue view 查看详情</span><br><span class="line"><span class="bullet">2.</span> 定位相关代码，实现修复</span><br><span class="line"><span class="bullet">3.</span> 跑测试确认，写一条符合规范的 commit</span><br></pre></td></tr></table></figure><p>保存后输入 <code>/fix-issue 123</code>，<code>$ARGUMENTS</code> 会被替换成 <code>123</code>。个人通用命令放 <code>~/.claude/commands/</code>。</p><h3 id="Skills：可复用的”操作手册”"><a href="#Skills：可复用的”操作手册”" class="headerlink" title="Skills：可复用的”操作手册”"></a>Skills：可复用的”操作手册”</h3><p>Skill 和 slash command 的区别：command 是<strong>你主动触发</strong>的提示词模板，skill 是<strong>Claude 自己判断</strong>何时使用的知识包（也可以手动 <code>/名字</code> 触发）。每个 skill 是一个文件夹，核心是 <code>SKILL.md</code>：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">&lt;!-- .claude/skills/deploy-check/SKILL.md --&gt;</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line">name: deploy-check</span><br><span class="line"><span class="section">description: 部署前检查清单。当用户要求部署或发布时使用。</span></span><br><span class="line"><span class="section">---</span></span><br><span class="line"></span><br><span class="line">部署前依次确认：</span><br><span class="line"><span class="bullet">1.</span> npm test 全绿</span><br><span class="line"><span class="bullet">2.</span> CHANGELOG.md 已更新</span><br><span class="line"><span class="bullet">3.</span> 版本号已按 semver 递增</span><br></pre></td></tr></table></figure><p><code>description</code> 决定它何时被自动启用——平时只有这一行占上下文，触发时才加载全文（和上一篇讲的渐进式加载是同一机制）。</p><h3 id="MCP：接入外部系统"><a href="#MCP：接入外部系统" class="headerlink" title="MCP：接入外部系统"></a>MCP：接入外部系统</h3><p>想让 Claude Code 查你的数据库、操作 GitHub、读 Figma 设计稿，用 <code>claude mcp add</code> 接入对应的 MCP server：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">claude mcp add --transport http github https://api.githubcopilot.com/mcp/</span><br><span class="line"><span class="comment">#          add：添加一个 MCP server</span></span><br><span class="line"><span class="comment">#          --transport http：连接方式（另有 stdio、sse）</span></span><br><span class="line"><span class="comment">#          github：你给这个 server 起的名字</span></span><br><span class="line"><span class="comment">#          最后是 server 地址</span></span><br></pre></td></tr></table></figure><p>项目级配置存 <code>.mcp.json</code>（进版本库，团队共享）。<code>/mcp</code> 命令可以查看已接入的 server 和它们提供的工具。哪些服务有现成的 MCP server，去 <a href="https://github.com/modelcontextprotocol/servers">MCP servers 仓库</a>翻。</p><h3 id="权限：settings-json-里的-allow-deny"><a href="#权限：settings-json-里的-allow-deny" class="headerlink" title="权限：settings.json 里的 allow &#x2F; deny"></a>权限：settings.json 里的 allow &#x2F; deny</h3><p>每次都手动确认太烦，全部放行又不放心——权限规则让你精确划线，写在 <code>.claude/settings.json</code>：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;permissions&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;allow&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;Bash(npm run *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Bash(git diff *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Read(**)&quot;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;deny&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="string">&quot;Bash(rm -rf *)&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="string">&quot;Read(.env*)&quot;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><ul><li>规则格式是 <code>工具名(参数模式)</code>：<code>Bash(npm run *)</code> 表示所有 npm run 开头的命令免确认</li><li><code>deny</code> 优先级高于 <code>allow</code>；<code>Read(.env*)</code> 可以防止 Claude 读到你的密钥文件</li><li>不想写 JSON 就用 <code>/permissions</code> 交互式配置，效果一样</li></ul><h2 id="配置项的可选值去哪查"><a href="#配置项的可选值去哪查" class="headerlink" title="配置项的可选值去哪查"></a>配置项的可选值去哪查</h2><p>本文的表格都只列了常用值——<code>tools</code> 能填哪些工具名、settings.json 有哪些键、hook 有哪些事件，全量清单会随版本更新。比背清单更重要的是知道去哪查，四个方法按顺手程度排序：</p><p><strong>方法一：直接问 Claude Code 自己。</strong> 它了解自身的配置体系，这是最快的路径：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">&gt; 你有哪些内置工具？我想给 subagent 配 tools 白名单</span><br><span class="line">&gt; 帮我写一个 PostToolUse hook，改完 .go 文件自动跑 gofmt</span><br></pre></td></tr></table></figure><p>第二种问法更实用——让它直接替你生成配置，你审查后保存即可。</p><p><strong>方法二：交互式命令自带枚举。</strong> <code>/permissions</code>、<code>/agents</code>、<code>/config</code>、<code>/hooks</code> 这些命令的界面本身就列出了全部可选项，选就行，不用记。</p><p><strong>方法三：官方文档对照表。</strong> 每类配置有一个权威页面：</p><table><thead><tr><th>想查什么</th><th>去哪查</th></tr></thead><tbody><tr><td>settings.json 全部配置键</td><td><a href="https://code.claude.com/docs/en/settings">Settings 参考</a></td></tr><tr><td>内置工具完整列表（tools 字段可填的值）</td><td><a href="https://code.claude.com/docs/en/tools-reference">Tools 参考</a></td></tr><tr><td>hooks 全部事件和输入输出格式</td><td><a href="https://code.claude.com/docs/en/hooks">Hooks 参考</a></td></tr><tr><td>subagent 全部 frontmatter 字段</td><td><a href="https://code.claude.com/docs/en/sub-agents">Subagents 文档</a></td></tr><tr><td>CLI 启动参数</td><td><a href="https://code.claude.com/docs/en/cli-reference">CLI 参考</a></td></tr></tbody></table><p><strong>方法四：全文档索引。</strong> <a href="https://code.claude.com/docs/llms.txt">code.claude.com&#x2F;docs&#x2F;llms.txt</a> 是官方文档的完整页面清单（专为 AI 阅读设计，人看也很清晰），不确定某个功能在哪个页面时，从这里搜关键词。</p><h2 id="日常实用技巧速查"><a href="#日常实用技巧速查" class="headerlink" title="日常实用技巧速查"></a>日常实用技巧速查</h2><p>不属于”配置”，但每天都用得上：</p><table><thead><tr><th>操作</th><th>效果</th></tr></thead><tbody><tr><td><code>claude -p &quot;解释这个报错：...&quot;</code></td><td>非交互模式：执行一次、输出结果就退出，适合管道和脚本</td></tr><tr><td><code>claude --continue</code></td><td>恢复最近一次会话，接着上次聊</td></tr><tr><td><code>claude --resume</code></td><td>列出历史会话，挑一个恢复</td></tr><tr><td>输入框粘贴图片</td><td>直接发截图给它看（报错截图、设计稿都行）</td></tr><tr><td><code>@src/utils.ts</code></td><td>消息里 @ 文件路径，把文件内容带进对话</td></tr><tr><td><code>!npm test</code></td><td>感叹号开头直接执行 bash，输出进入上下文</td></tr><tr><td><code>Esc</code></td><td>打断 Claude 当前动作</td></tr><tr><td><code>Esc</code> 连按两下</td><td>打开回退菜单，恢复到之前的检查点（改坏了能撤）</td></tr></tbody></table><h2 id="把四样组合起来：一个仓库的完整配置"><a href="#把四样组合起来：一个仓库的完整配置" class="headerlink" title="把四样组合起来：一个仓库的完整配置"></a>把四样组合起来：一个仓库的完整配置</h2><p>以一个 Node.js 项目为例，四样东西各司其职后，<code>.claude/</code> 目录长这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">your-project/</span><br><span class="line">├── CLAUDE.md                    # 规矩：构建命令、代码约定（@AGENTS.md 导入通用部分）</span><br><span class="line">├── .mcp.json                    # 团队共享的 MCP server 配置</span><br><span class="line">├── .claude/</span><br><span class="line">│   ├── settings.json            # 权限白名单 + hooks 配置</span><br><span class="line">│   ├── hooks/</span><br><span class="line">│   │   └── lint-check.sh        # 改完文件自动 lint</span><br><span class="line">│   ├── rules/</span><br><span class="line">│   │   └── api-design.md        # 只在改 API 文件时加载的规则</span><br><span class="line">│   ├── commands/</span><br><span class="line">│   │   └── fix-issue.md         # 自定义 /fix-issue 命令</span><br><span class="line">│   ├── skills/</span><br><span class="line">│   │   └── deploy-check/        # 部署检查 skill</span><br><span class="line">│   └── agents/</span><br><span class="line">│       └── code-reviewer.md     # 代码审查分身</span><br></pre></td></tr></table></figure><p>工作起来的分工：</p><pre><code class="highlight mermaid">graph TD    A[&quot;CLAUDE.md&lt;br/&gt;软约定：项目规矩，每次会话加载&quot;] --&gt; E[&quot;Claude Code 会话&quot;]    B[&quot;hooks&lt;br/&gt;硬约束：改完必 lint、危险命令必拦&quot;] --&gt; E    C[&quot;subagent&lt;br/&gt;分身：审查在独立上下文进行&quot;] --&gt; E    D[&quot;auto memory&lt;br/&gt;经验：踩过的坑自动积累&quot;] --&gt; E</code></pre><p>一个典型回合：你说”加一个用户导出接口”→ Claude 按 CLAUDE.md 的约定写代码 → 每保存一个文件 lint hook 自动跑 → 写完自动派 code-reviewer 分身审一遍 → 有问题当场修。你只在关键节点做确认。</p><p>配置不用一步到位。官方推荐的节奏是：先裸用，发现自己<strong>第二次</strong>纠正同一个问题时，把它写进 CLAUDE.md；发现某件事”必须每次发生”时，升级成 hook；发现某类任务反复出现时，抽成 subagent。让配置跟着实际使用一点点长出来，比一开始就设计一套完整的更贴合你的工作方式。</p><h2 id="下一步"><a href="#下一步" class="headerlink" title="下一步"></a>下一步</h2><ul><li>概念补课：<a href="/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98">Claude 全面教程：模型家族、核心概念与 API 实战</a></li><li>官方文档：<a href="https://code.claude.com/docs">code.claude.com&#x2F;docs</a>——本文没展开的沙箱、IDE 集成、agent teams 多会话协作都在里面</li><li>进阶方向：给 CI 写 headless 脚本（<code>claude -p</code>）；用 agent teams 让多个会话协作干一个大活</li></ul>]]>
    </content>
    <id>https://blog.codewater.top/ClaudeCode%E5%AE%9E%E6%88%98%E6%95%99%E7%A8%8B-%E5%AE%89%E8%A3%85CLAUDEmd-hooks-subagent/</id>
    <link href="https://blog.codewater.top/ClaudeCode%E5%AE%9E%E6%88%98%E6%95%99%E7%A8%8B-%E5%AE%89%E8%A3%85CLAUDEmd-hooks-subagent/"/>
    <published>2026-07-07T10:00:00.000Z</published>
    <summary>Claude Code 实战路线：安装、CLAUDE.md、hooks、subagent、slash commands、MCP 与权限配置，附可复制示例和自查配置项的方法。</summary>
    <title>Claude Code 实战教程：安装配置、CLAUDE.md、Hooks 与 Subagent</title>
    <updated>2026-07-07T10:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tutorial" scheme="https://blog.codewater.top/categories/tutorial/"/>
    <category term="Claude" scheme="https://blog.codewater.top/tags/Claude/"/>
    <category term="Anthropic" scheme="https://blog.codewater.top/tags/Anthropic/"/>
    <category term="AI" scheme="https://blog.codewater.top/tags/AI/"/>
    <category term="LLM" scheme="https://blog.codewater.top/tags/LLM/"/>
    <category term="API" scheme="https://blog.codewater.top/tags/API/"/>
    <category term="Agent" scheme="https://blog.codewater.top/tags/Agent/"/>
    <content>
      <![CDATA[<p>Claude 是 Anthropic 开发的大语言模型家族。2026 年上半年这个家族变化很大：6 月 9 日 Anthropic 发布了全新档位的 Claude Fable 5，6 月 30 日又推出了 Claude Sonnet 5。如果你手里的资料还停留在”Opus &#x2F; Sonnet &#x2F; Haiku 三档”的认知，这篇教程可以帮你一次性补齐——从模型命名逻辑、API 核心参数，到 CLAUDE.md &#x2F; 记忆 &#x2F; Harness Engineering 这些 2026 年的新概念，最后是一个可以直接运行的工具调用例子。</p><p>文中所有模型信息、价格、参数均核对自 Anthropic 官方文档，截至 2026 年 7 月 7 日有效。</p><span id="more"></span><h2 id="大模型是怎么工作的：五个必懂常识"><a href="#大模型是怎么工作的：五个必懂常识" class="headerlink" title="大模型是怎么工作的：五个必懂常识"></a>大模型是怎么工作的：五个必懂常识</h2><p>不管你用哪家的模型，下面五件事决定了你对它的所有预期。搞懂它们，后文的每个机制（规则文件、记忆、工具调用）都会变得顺理成章——因为那些全是针对这五个特性的工程补丁。</p><p><strong>1. 模型的本质是”预测下一个 token”。</strong> 大语言模型（LLM）在海量文本上训练，学到的能力是：给定前文，预测下一个词最可能是什么。它没有数据库查询，也没有”知道&#x2F;不知道”的开关——回答是逐 token 生成出来的。这解释了它为什么强（语言、推理、代码都是文本规律），也解释了它为什么会<strong>幻觉（hallucination）</strong>：当训练数据里没有答案时，它依然会生成”看起来最像答案”的文本，一本正经地编造。对策：重要的事实让它引用来源、用工具查证，或者你自己核实。</p><p><strong>2. 模型的知识有截止日期。</strong> 训练完成后权重就固定了，之后发生的事它一概不知。Fable 5 &#x2F; Opus 4.8 &#x2F; Sonnet 5 的可靠知识截止是 2026 年 1 月——问它上周的新闻，它要么明说不知道，要么开始幻觉。要获取新信息，得靠联网搜索、工具调用这类外挂（后文会讲）。</p><p><strong>3. 模型不会记住你。</strong> 这是新手最大的误解。你和模型的每一次对话都<strong>不会改变模型本身</strong>——权重是只读的，全球用户共享同一个模型。它在对话中”记得”你前面说了什么，只是因为聊天软件把历史消息重新发给了它（见后文”无状态 API”）。所以”让 AI 记住我的偏好”从来不是模型功能，而是外部工程：把你的偏好存成文件，每次对话时重新喂给它——这正是 CLAUDE.md 和记忆功能的原理。</p><p><strong>4. 上下文窗口是唯一的”工作内存”，而且是稀缺资源。</strong> 模型单次能处理的内容有上限（Claude 当前主力模型是 100 万 token）。塞得越多，成本越高、速度越慢，而且信息淹没在噪音里反而影响质量。整个”上下文工程”学科（后文讲）都在解决一个问题：这一窗有限的空间里，到底该放什么。</p><p><strong>5. 模型能看的不只是文字。</strong> Claude 支持多模态输入：图片、截图、PDF 都能直接发给它分析。输出目前是文本（含代码、表格、Mermaid 图）。</p><h2 id="模型家族：四个档位怎么选"><a href="#模型家族：四个档位怎么选" class="headerlink" title="模型家族：四个档位怎么选"></a>模型家族：四个档位怎么选</h2><p>Claude 的模型按能力和成本分档，名字本身就是档位标识：</p><ul><li><strong>Fable &#x2F; Mythos（Mythos 级）</strong>：2026 年 6 月新增的最高档位。Fable 5 是目前公开可用的最强模型；Mythos 5 与 Fable 5 同规格同价格，但仅通过邀请制的 Project Glasswing 向获批组织开放，定位防御性网络安全工作流。</li><li><strong>Opus</strong>：面向复杂 agentic 编码和企业级任务的旗舰档，当前版本 Opus 4.8。</li><li><strong>Sonnet</strong>：速度与智能的平衡档，当前版本 Sonnet 5（发布于 2026-06-30），官方称其在部分 agentic 任务上已接近 Opus 4.8 的水平。</li><li><strong>Haiku</strong>：最快、最便宜的档位，当前版本 Haiku 4.5，适合分类、摘要等简单高频任务。</li></ul><pre><code class="highlight mermaid">graph TD    A[Claude 模型家族] --&gt; B[&quot;Mythos 级&lt;br/&gt;Fable 5 / Mythos 5&quot;]    A --&gt; C[&quot;Opus 4.8&quot;]    A --&gt; D[&quot;Sonnet 5&quot;]    A --&gt; E[&quot;Haiku 4.5&quot;]    B --&gt; B1[&quot;最强能力&lt;br/&gt;$10 / $50 每百万 token&quot;]    C --&gt; C1[&quot;复杂 agentic 编码&lt;br/&gt;$5 / $25 每百万 token&quot;]    D --&gt; D1[&quot;速度与智能平衡&lt;br/&gt;$3 / $15 每百万 token&quot;]    E --&gt; E1[&quot;最快最省&lt;br/&gt;$1 / $5 每百万 token&quot;]</code></pre><p>当前各模型的关键参数（截至 2026-07-06，来自<a href="https://platform.claude.com/docs/en/about-claude/models/overview">官方模型总览</a>）：</p><table><thead><tr><th>模型</th><th>API 模型 ID</th><th>上下文窗口</th><th>最大输出</th><th>定价（输入&#x2F;输出，每百万 token）</th></tr></thead><tbody><tr><td>Claude Fable 5</td><td><code>claude-fable-5</code></td><td>1M</td><td>128K</td><td>$10 &#x2F; $50</td></tr><tr><td>Claude Opus 4.8</td><td><code>claude-opus-4-8</code></td><td>1M</td><td>128K</td><td>$5 &#x2F; $25</td></tr><tr><td>Claude Sonnet 5</td><td><code>claude-sonnet-5</code></td><td>1M</td><td>128K</td><td>$3 &#x2F; $15（2026-08-31 前尝鲜价 $2 &#x2F; $10）</td></tr><tr><td>Claude Haiku 4.5</td><td><code>claude-haiku-4-5</code></td><td>200K</td><td>64K</td><td>$1 &#x2F; $5</td></tr></tbody></table><p>两点容易踩坑的细节：</p><ol><li><strong>模型 ID 是固定快照，不要自己拼日期后缀</strong>。从 4.6 代开始模型 ID 采用无日期格式（如 <code>claude-opus-4-8</code>），写成 <code>claude-opus-4-8-20260315</code> 这类自造 ID 会直接 404。</li><li><strong>Fable 5 用了新 tokenizer</strong>（自 Opus 4.7 引入），同一段文本比旧模型多算约 30% 的 token，做成本估算时要注意。</li></ol><p>选型建议：日常开发和 agentic 编码默认 <code>claude-opus-4-8</code>；预算敏感、需要吞吐量的场景用 <code>claude-sonnet-5</code>；只有确实需要顶级能力的长程任务才上 <code>claude-fable-5</code>。</p><h2 id="三个使用入口：claude-ai、Claude-Code、Claude-API"><a href="#三个使用入口：claude-ai、Claude-Code、Claude-API" class="headerlink" title="三个使用入口：claude.ai、Claude Code、Claude API"></a>三个使用入口：claude.ai、Claude Code、Claude API</h2><p>同一套模型，Anthropic 提供了三个层次的入口，面向不同的使用方式：</p><ul><li>**<a href="https://claude.ai/">claude.ai</a>**：网页&#x2F;移动端聊天产品，开箱即用。免费版和 Pro 版当前默认模型是 Sonnet 5。适合日常问答、写作、分析文件。</li><li>**<a href="https://claude.com/claude-code">Claude Code</a>**：面向开发者的编程 agent，有 CLI、桌面应用、Web 和 IDE 插件（VS Code &#x2F; JetBrains）多种形态。它能直接读写你的代码库、跑命令、开分支提 PR，是”让 Claude 替你干活”而不是”问 Claude 怎么干”。</li><li>**<a href="https://platform.claude.com/">Claude API</a>**（Claude Platform）：把模型能力集成进自己产品的编程接口，本文后半部分的概念和例子都围绕它展开。除了 Anthropic 第一方 API，同样的模型也上架了 Amazon Bedrock、Google Cloud（Vertex AI）和 Microsoft Foundry。</li></ul><p>三者的关系可以理解为：claude.ai 是”用”，Claude Code 是”让它替你用电脑”，API 是”拿它造东西”。</p><h2 id="订阅还是-API：为自己选一套配置"><a href="#订阅还是-API：为自己选一套配置" class="headerlink" title="订阅还是 API：为自己选一套配置"></a>订阅还是 API：为自己选一套配置</h2><p>付费方式只有两种，对应不同的人群：</p><ul><li><strong>订阅制</strong>（按月付固定费用，用量有额度）：Free 免费试用；<strong>Pro $20&#x2F;月</strong>（年付约 $17&#x2F;月）；<strong>Max $100 或 $200&#x2F;月</strong>（额度分别是 Pro 的 5 倍 &#x2F; 20 倍）。Pro 和 Max 是统一订阅——网页&#x2F;桌面&#x2F;移动端的 claude.ai 和终端里的 Claude Code 全都包含，不用分开买。</li><li><strong>API 按量计费</strong>：按实际消耗的 token 付费（价格见上文模型表），适合把 Claude 集成进自己的程序，或用量极不规律的场景。Claude Code 也可以不走订阅、直接挂 API key 按量付费。</li></ul><p>三类典型配置，可以直接对号入座：</p><table><thead><tr><th>你是谁</th><th>推荐配置</th><th>理由</th></tr></thead><tbody><tr><td>日常问答、写作、偶尔看代码</td><td>claude.ai Free 或 Pro（$20&#x2F;月）</td><td>开箱即用，Pro 额度对轻度使用足够</td></tr><tr><td>每天写代码的开发者</td><td>Pro 起步，重度换 Max（$100&#x2F;月）</td><td>一份订阅同时覆盖 claude.ai 和 Claude Code；Claude Code 跑 agentic 任务吃额度快，Pro 不够用再升</td></tr><tr><td>要做产品 &#x2F; 自动化脚本</td><td>API key 按量计费</td><td>程序化调用只能走 API；配合 prompt caching 和批处理（后文讲）控制成本</td></tr></tbody></table><p>一个省钱的实用结论：订阅额度按 5 小时窗口滚动刷新，重度使用先用满 Pro 再决定是否升级，不要一上来就买 Max。</p><h2 id="用-API-调用-Claude：写代码前要懂的六个概念"><a href="#用-API-调用-Claude：写代码前要懂的六个概念" class="headerlink" title="用 API 调用 Claude：写代码前要懂的六个概念"></a>用 API 调用 Claude：写代码前要懂的六个概念</h2><p>从这里开始进入 API 部分。所谓”调 API”，就是你的程序向 Anthropic 的服务器发一个 HTTP 请求（问题、参数都装在请求里），服务器把 Claude 的回答返回给你的程序——这样你就能把 Claude 的能力嵌进自己的工具、网站或脚本里，而不是每次都打开网页手动聊天。</p><p>下面六个概念是写第一行代码之前需要建立的认知，前四个（token、消息结构、思考控制、工具调用）在文末的实战例子里都会直接用到。如果你暂时只打算用 claude.ai 或 Claude Code，可以粗读本节，重点看”工具调用”一小节（Claude Code 的工作原理就是它）。</p><h3 id="Token-与上下文窗口：模型的工作内存"><a href="#Token-与上下文窗口：模型的工作内存" class="headerlink" title="Token 与上下文窗口：模型的工作内存"></a>Token 与上下文窗口：模型的工作内存</h3><p>Token 是模型处理文本的最小单位，一个英文单词大约对应 1.3 到 1.8 个 token（新 tokenizer 密度更高），中文一个字通常 1 到 2 个 token。API 按 token 计费，输入输出分开算价。</p><p>上下文窗口（context window）是模型单次请求能”看到”的全部内容上限——包括系统提示、历史对话、工具返回结果和本次输出。当前 Fable 5 &#x2F; Opus 4.8 &#x2F; Sonnet 5 都是 100 万 token（约 55 万英文单词），Haiku 4.5 是 20 万。</p><p>需要精确计数时不要用估算库（OpenAI 的 tiktoken 对 Claude 误差很大），用官方的 token 计数接口：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">count = client.messages.count_tokens(</span><br><span class="line">    model=<span class="string">&quot;claude-opus-4-8&quot;</span>,      <span class="comment"># token 计数是模型相关的，传实际要用的模型 ID</span></span><br><span class="line">    messages=[&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: text&#125;],</span><br><span class="line">)</span><br><span class="line"><span class="built_in">print</span>(count.input_tokens)</span><br></pre></td></tr></table></figure><h3 id="消息结构：无状态的-Messages-API"><a href="#消息结构：无状态的-Messages-API" class="headerlink" title="消息结构：无状态的 Messages API"></a>消息结构：无状态的 Messages API</h3><p>Claude API 的核心端点只有一个：<code>POST /v1/messages</code>。它是<strong>无状态</strong>的——服务端不保存对话历史，每次请求都要把完整的历史消息带上。</p><p>一次请求由三部分组成：</p><ul><li><code>system</code>：系统提示，定义模型的角色、行为边界和输出风格，权威高于用户消息;</li><li><code>messages</code>：<code>user</code> 和 <code>assistant</code> 角色交替的消息数组，第一条必须是 <code>user</code>;</li><li><strong>参数</strong>：<code>model</code>、<code>max_tokens</code>（必填，输出 token 上限）、<code>thinking</code> 等。</li></ul><p>多轮对话就是把上一轮的回复追加进 <code>messages</code> 再发一次：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">messages = [</span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;我叫 Alice。&quot;</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;assistant&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;你好 Alice！&quot;</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;我叫什么？&quot;</span>&#125;,   <span class="comment"># 模型能答对，因为历史都在请求里</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><h3 id="Adaptive-Thinking-与-effort：控制模型”想多深”"><a href="#Adaptive-Thinking-与-effort：控制模型”想多深”" class="headerlink" title="Adaptive Thinking 与 effort：控制模型”想多深”"></a>Adaptive Thinking 与 effort：控制模型”想多深”</h3><p>Claude 4 系列引入了”扩展思考”（extended thinking）——模型在回答前先进行一段内部推理。2026 年的最新演进是 <strong>Adaptive Thinking（自适应思考）</strong>：不再手动设定思考 token 预算（旧参数 <code>budget_tokens</code> 在 Opus 4.7 及之后已彻底移除，传了会直接报 400），而是让模型根据问题难度自己决定想多久：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">response = client.messages.create(</span><br><span class="line">    model=<span class="string">&quot;claude-opus-4-8&quot;</span>,</span><br><span class="line">    max_tokens=<span class="number">16000</span>,</span><br><span class="line">    thinking=&#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;adaptive&quot;</span>&#125;,     <span class="comment"># 自适应思考：简单问题秒答，难题深想</span></span><br><span class="line">    messages=[...],</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>各模型的默认行为不同：Fable 5 的 adaptive thinking <strong>常开且无法关闭</strong>；Sonnet 5 默认开启（可传 <code>&#123;&quot;type&quot;: &quot;disabled&quot;&#125;</code> 显式关闭）；Opus 4.8 默认关闭，需要显式传入才启用。</p><p>配套的 <strong>effort 参数</strong>控制整体的思考深度和 token 开销，取值 <code>low</code> &#x2F; <code>medium</code> &#x2F; <code>high</code> &#x2F; <code>xhigh</code> &#x2F; <code>max</code>。<code>xhigh</code> 在 Fable 5 &#x2F; Opus 4.8 &#x2F; Opus 4.7 &#x2F; Sonnet 5 上可用，<code>max</code> 的支持范围还包括 Sonnet 4.6 和 Opus 4.6；Haiku 4.5 不支持 effort 参数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">output_config=&#123;<span class="string">&quot;effort&quot;</span>: <span class="string">&quot;high&quot;</span>&#125;</span><br><span class="line"><span class="comment">#      effort：思考与行动的投入档位，Opus 4.8 默认 high</span></span><br><span class="line"><span class="comment">#              编码和 agentic 任务推荐 xhigh，成本敏感场景降到 medium</span></span><br></pre></td></tr></table></figure><p>另外注意：<strong>Opus 4.7 起 <code>temperature</code>、<code>top_p</code>、<code>top_k</code> 三个采样参数已移除</strong>，传了同样报 400。想控制输出风格，直接在提示词里说明。</p><h3 id="工具调用：让模型能查数据、执行操作"><a href="#工具调用：让模型能查数据、执行操作" class="headerlink" title="工具调用：让模型能查数据、执行操作"></a>工具调用：让模型能查数据、执行操作</h3><p>工具调用（tool use）是 Claude 从”聊天机器人”升级为”agent”的关键机制。你在请求里声明工具（名称 + 描述 + JSON Schema 参数定义），模型判断需要时返回一个 <code>tool_use</code> 请求，你执行后把结果传回去，模型基于结果继续——这个循环就是 agentic loop：</p><pre><code class="highlight mermaid">sequenceDiagram    participant U as 你的代码    participant C as Claude API    U-&gt;&gt;C: 请求（消息 + 工具定义）    C-&gt;&gt;U: stop_reason: tool_use&lt;br/&gt;（想调用 get_weather）    U-&gt;&gt;U: 本地执行 get_weather()    U-&gt;&gt;C: 追加 tool_result 再请求    C-&gt;&gt;U: stop_reason: end_turn&lt;br/&gt;（基于结果的最终回答）</code></pre><p>工具分两类：</p><ul><li><strong>自定义工具（client 侧）</strong>：你定义、你执行，模型只负责决定”何时调、传什么参数”。文末例子演示的就是这类。</li><li><strong>服务端工具（server 侧）</strong>：Anthropic 托管执行，声明即用，包括代码执行（沙箱跑 Python）、网页搜索 <code>web_search</code>、网页抓取 <code>web_fetch</code>、计算机操作（computer use）等。</li></ul><p>写工具描述有一条经验规则：不但要写清”这个工具做什么”，还要写清”<strong>什么时候该调它</strong>“（例如”当用户询问实时价格或最新事件时调用”）——新一代模型对工具的触发更保守，明确的触发条件能显著提高该调时调的比例。</p><h3 id="结构化输出：保证返回合法-JSON"><a href="#结构化输出：保证返回合法-JSON" class="headerlink" title="结构化输出：保证返回合法 JSON"></a>结构化输出：保证返回合法 JSON</h3><p>需要模型输出严格符合某个 schema 的 JSON（做信息抽取、分类打标）时，不要靠提示词祈祷，用 <code>output_config.format</code> 直接约束输出格式：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">response = client.messages.parse(</span><br><span class="line">    model=<span class="string">&quot;claude-opus-4-8&quot;</span>,</span><br><span class="line">    max_tokens=<span class="number">1024</span>,</span><br><span class="line">    messages=[&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;抽取：张三（zhangsan@co.com）想买企业版&quot;</span>&#125;],</span><br><span class="line">    output_config=&#123;</span><br><span class="line">        <span class="string">&quot;format&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;type&quot;</span>: <span class="string">&quot;json_schema&quot;</span>,          <span class="comment"># 输出被强制约束为符合此 schema 的 JSON</span></span><br><span class="line">            <span class="string">&quot;schema&quot;</span>: CONTACT_SCHEMA,</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>顺带一提：旧的顶层 <code>output_format</code> 参数已废弃，新代码统一用 <code>output_config.format</code>。以前流行的”assistant 预填”（在消息末尾塞半截 assistant 回复来引导格式）在 4.6 代及之后的模型上会直接报 400，结构化输出就是它的官方替代。</p><h3 id="Prompt-Caching：重复前缀省-90-费用"><a href="#Prompt-Caching：重复前缀省-90-费用" class="headerlink" title="Prompt Caching：重复前缀省 90% 费用"></a>Prompt Caching：重复前缀省 90% 费用</h3><p>Prompt caching 把请求的公共前缀（如很长的系统提示、文档内容）缓存起来，后续请求命中缓存的部分按约 0.1 倍价格计费。机制是<strong>前缀匹配</strong>：请求按 <code>tools</code> → <code>system</code> → <code>messages</code> 顺序渲染，前缀中任何一个字节变化都会让之后的缓存全部失效。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">system=[&#123;</span><br><span class="line">    <span class="string">&quot;type&quot;</span>: <span class="string">&quot;text&quot;</span>,</span><br><span class="line">    <span class="string">&quot;text&quot;</span>: LARGE_DOCUMENT,                    <span class="comment"># 几万 token 的稳定内容</span></span><br><span class="line">    <span class="string">&quot;cache_control&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;ephemeral&quot;</span>&#125;,   <span class="comment"># 缓存断点：此处之前的内容进缓存，默认 5 分钟 TTL</span></span><br><span class="line">&#125;]</span><br></pre></td></tr></table></figure><p>最常见的翻车方式是把时间戳、请求 ID 这类每次都变的内容插进系统提示开头——缓存永远无法命中。稳定内容放前面，易变内容放最后，是设计提示词结构时就该定下的原则。</p><h2 id="MCP、Skills、Workflow、Agent：用一个比喻讲清四个词"><a href="#MCP、Skills、Workflow、Agent：用一个比喻讲清四个词" class="headerlink" title="MCP、Skills、Workflow、Agent：用一个比喻讲清四个词"></a>MCP、Skills、Workflow、Agent：用一个比喻讲清四个词</h2><p>这四个词到处都在用，但很少有人讲明白。用一个比喻串起来：<strong>把 Claude 想象成一个刚入职的聪明助理</strong>——绝顶聪明，但对你的公司一无所知，而且没有任何系统的操作权限。</p><p><strong>MCP：给助理办的门禁卡</strong></p><blockquote><p>官方定义：MCP（Model Context Protocol，模型上下文协议）是 Anthropic 发起的开放标准协议，规范 AI 应用与外部数据源、工具之间的连接方式——外部系统实现 <strong>MCP server</strong> 对外提供工具和数据，AI 端作为 <strong>MCP client</strong> 接入调用。</p></blockquote><p>说人话：助理再聪明，进不了你的系统也白搭——查不了 GitHub、看不了数据库、动不了日历。MCP 就是一套标准的”门禁系统”：GitHub 按这个标准做一个接口（MCP server），装上之后助理就能查你的仓库了。关键在”标准”二字：GitHub 只用做一次接口，所有 AI 助理（Claude、Cursor 等）都能用，不用一家一个做法。<br>一句话：**MCP 解决”AI 怎么连上外部软件”**。</p><p><strong>Skills：放在助理手边的操作手册</strong></p><blockquote><p>官方定义：Skill（Agent Skill）是以文件夹形式组织的可复用任务指令包，核心是一个 <code>SKILL.md</code> 文件（可附带脚本和资源）。采用<strong>渐进式加载</strong>（progressive disclosure）：默认只有一行描述进入上下文，任务匹配时模型才读取完整内容。</p></blockquote><p>说人话：有些工作有固定章法，比如”我们公司做 Excel 报表的规矩”。你不会把所有手册的全文天天念给助理听（听不完，也记不住），而是放在架子上——助理接到做报表的活儿，才把《报表手册》抽出来翻。<br>一句话：**Skills 解决”专业做法太多，怎么按需教给 AI”**。</p><p><strong>Workflow：你写死的流水线</strong></p><blockquote><p>官方定义（出自 Anthropic《Building effective agents》）：workflow 是<strong>由预先编写的代码路径来编排</strong> LLM 和工具的系统——执行哪些步骤、按什么顺序，由代码决定。</p></blockquote><p>说人话：有些任务每次流程完全一样，比如处理退款永远是”查订单 → 核金额 → 打款 → 发通知邮件”。这种就不需要助理自由发挥——你把四个步骤用代码写死，助理只在需要动脑的环节出场（比如把邮件写得客气点）。跑一百次就是同样四步，便宜、可控、不会跑偏。</p><p><strong>这套”人定步骤”在 Claude Code 里有个具体实现，叫 dynamic workflow（动态工作流）</strong>，2026 年 5 月底正式发布：</p><blockquote><p>官方定义（<a href="https://code.claude.com/docs/en/workflows">Claude Code 文档</a>）：dynamic workflow 是一段由 Claude 编写、交给独立运行时在后台执行的 JavaScript 脚本，用 <code>agent()</code>、<code>parallel()</code>、<code>pipeline()</code> 等原语编排多个 subagent。决定”下一步跑什么”的是这段脚本本身，而不是 Claude 在对话里临场判断——这也是它和普通 subagent、skill 的核心区别：subagent 和 skill 是”Claude 一轮一轮自己决定派谁干活”，workflow 是”脚本说了算”。</p></blockquote><p>说人话：还是刚才退款流水线的思路，只是这次”写步骤的人”从工程师换成了 Claude 自己——你说清楚要审查的范围，Claude 现写一段脚本，把流程拆成几个阶段（比如”先派几个人从不同角度找问题 → 每条发现单独派一个人核实真假，找到多少核实多少 → 汇总去重出报告”）。脚本里可以带循环和分支：这次改动大就多派几个人，问题多就多核实几条，连续几轮都没找到新问题就停。步骤”数量”是灵活的，但”要不要再跑一轮”这个判断逻辑，还是写死在脚本里的固定规则，不是模型现场自由发挥——所以它依然是 workflow，不是 agent。一次能跑几十到上百个 subagent，比对话里一步步指挥快得多，但也更吃 token；跑之前 Claude Code 会先弹出确认框，列出计划的几个阶段，你可以选直接跑、以后这个项目里都不用再问、看一眼原始脚本，或者取消。</p><p>一句话：**Workflow（包括能自适应轮数的 dynamic workflow）都是”人定步骤，AI 干活”**。</p><p><strong>Agent：只给目标、不给步骤的委托</strong></p><blockquote><p>官方定义（同上）：agent 是 <strong>LLM 自主决定自身执行过程和工具使用</strong>的系统——模型在循环中动态规划步骤，掌控任务如何完成。</p></blockquote><p>说人话：另一些任务事先说不清要几步，比如”这个 bug 帮我修了”。可能要先搜代码、再看日志、改完还得跑测试——每一步做什么，取决于上一步看到了什么。这时你只给助理一个目标，它自己决定路线：做一步、看结果、定下一步，循环往复直到搞定。Claude Code 就是一个现成的编程 agent。<br>一句话：**Agent 是”人定目标，AI 定步骤”**。</p><p>Workflow 和 Agent 怎么选？看一条就够：<strong>任务流程能提前画成一张固定流程图的，用 workflow；画不出来的，才用 agent</strong>。agent 更灵活，但更贵、结果波动也更大——能用流水线解决的别上助理自由发挥。</p><pre><code class="highlight mermaid">graph TD    Q&#123;&quot;这个任务的步骤&lt;br/&gt;能提前定死吗？&quot;&#125;    Q --&gt;|能| W[&quot;Workflow&lt;br/&gt;人定步骤，AI 干活&quot;]    Q --&gt;|不能| A[&quot;Agent&lt;br/&gt;人定目标，AI 定步骤&quot;]    W --&gt; T[&quot;干活时都可能用到：&lt;br/&gt;MCP 连外部系统&lt;br/&gt;Skills 提供操作手册&quot;]    A --&gt; T</code></pre><h2 id="CLAUDE-md、记忆与规则：让-Claude-变成”你的”-Claude"><a href="#CLAUDE-md、记忆与规则：让-Claude-变成”你的”-Claude" class="headerlink" title="CLAUDE.md、记忆与规则：让 Claude 变成”你的” Claude"></a>CLAUDE.md、记忆与规则：让 Claude 变成”你的” Claude</h2><p>前文常识第 3 条说过：模型本身不会记住你。所以”个性化”全靠外部工程，思路只有两种——<strong>规则</strong>（你主动写给它的约定）和<strong>记忆</strong>（它使用过程中自己攒下来的）。这一节讲怎么配置，也是把通用 Claude 调教成”懂你项目、懂你习惯”的关键。</p><h3 id="规则文件：CLAUDE-md-与-AGENTS-md"><a href="#规则文件：CLAUDE-md-与-AGENTS-md" class="headerlink" title="规则文件：CLAUDE.md 与 AGENTS.md"></a>规则文件：CLAUDE.md 与 AGENTS.md</h3><p><strong>CLAUDE.md</strong> 是 Claude Code 的规则文件：一个普通的 Markdown 文件，每次会话开始时自动读入，里面写你希望 Claude 始终遵守的约定。它有层级，作用域逐层收窄：</p><pre><code class="highlight mermaid">graph TD    A[&quot;~/.claude/CLAUDE.md&lt;br/&gt;全局：个人偏好，所有项目生效&quot;] --&gt; B[&quot;项目根目录 CLAUDE.md&lt;br/&gt;团队约定，随代码进版本库&quot;]    B --&gt; C[&quot;子目录 CLAUDE.md&lt;br/&gt;模块级细则，进入该目录才加载&quot;]</code></pre><p>写什么内容？构建命令、代码风格、目录约定、部署流程——判断标准是一条社区流传的经验规则：<strong>CI 会拦下的规则写进 CI，代码评审会皱眉的约定写进 CLAUDE.md</strong>。举两个真实用法：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># 全局 ~/.claude/CLAUDE.md（个人偏好）</span></span><br><span class="line">搜索文件内容时优先使用 rg（ripgrep），不要用 find + grep 组合。</span><br><span class="line"></span><br><span class="line"><span class="section"># 项目 CLAUDE.md（团队约定）</span></span><br><span class="line"><span class="section">## 部署命令</span></span><br><span class="line">hexo clean &amp;&amp; hexo g -d</span><br><span class="line"><span class="section">## 写作规范</span></span><br><span class="line">每篇文章必须有 &lt;!-- more --&gt; 分隔符。</span><br></pre></td></tr></table></figure><p>配好之后的效果立竿见影：不用每次重复交代，Claude 生成的代码和操作自动符合你的习惯。</p><p><strong>AGENTS.md</strong> 的作用和 CLAUDE.md 完全一样——写项目规则。区别只有一个：CLAUDE.md 只有 Claude 认识，AGENTS.md 是行业通用标准（现由 Linux Foundation 维护），Claude Code、Cursor、GitHub Copilot、Gemini CLI 等主流 AI 工具都认识。</p><p>它解决的问题很具体：团队里有人用 Claude Code、有人用 Cursor，同一套规则写两份，很快就会改了一份忘另一份。标准做法是：AGENTS.md 放在<strong>项目根目录</strong>（和 CLAUDE.md 同级），通用规则只写在里面，CLAUDE.md 用 <code>@</code> 语法一行导入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">my-project/</span><br><span class="line">├── AGENTS.md      # 通用规则，所有 AI 工具都读</span><br><span class="line">├── CLAUDE.md      # 只写一行 @AGENTS.md，外加 Claude 专属要求</span><br><span class="line">├── src/</span><br><span class="line">└── ...</span><br></pre></td></tr></table></figure><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># AGENTS.md —— 团队通用规则，所有 AI 工具都读它</span></span><br><span class="line"><span class="section">## 构建与测试</span></span><br><span class="line"><span class="bullet">-</span> 构建命令：npm run build</span><br><span class="line"><span class="bullet">-</span> 提交前必须通过：npm test</span><br><span class="line"><span class="section">## 代码风格</span></span><br><span class="line"><span class="bullet">-</span> 用 TypeScript，禁止 any</span><br></pre></td></tr></table></figure><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># CLAUDE.md —— 只需一行，把通用规则导入进来</span></span><br><span class="line">@AGENTS.md</span><br><span class="line"></span><br><span class="line">（如果有只想对 Claude 说的额外要求，继续往下写即可）</span><br></pre></td></tr></table></figure><p><code>@</code> 后面写的是<strong>相对于 CLAUDE.md 所在位置的路径</strong>：两个文件同在根目录时直接 <code>@AGENTS.md</code> 就行，不用加路径前缀；如果被导入的文件在别处，就写出相对路径（如 <code>@docs/conventions.md</code>）或用 <code>~/</code> 开头的绝对路径（如 <code>@~/.claude/my-rules.md</code>）。</p><p>这样规则只维护一份，换工具也不用重写。如果项目只用 Claude，直接写 CLAUDE.md 就够了，不需要 AGENTS.md。</p><h3 id="记忆-memory-：Claude-自己攒的笔记"><a href="#记忆-memory-：Claude-自己攒的笔记" class="headerlink" title="记忆(memory)：Claude 自己攒的笔记"></a>记忆(memory)：Claude 自己攒的笔记</h3><p>规则是你写的，记忆是它记的。三个入口各有一套：</p><ul><li><strong>claude.ai</strong>：开启 memory 功能后，Claude 会从历史对话中提炼你的背景和偏好，新对话自动带上。</li><li><strong>Claude Code</strong>：维护一个记忆目录，把工作中确认过的事实（”这个项目的封面图必须先压缩再比对哈希”这类踩坑经验）写成文件存下来，下次会话自动召回。详见<a href="https://code.claude.com/docs/en/memory">官方记忆文档</a>。</li><li><strong>API</strong>：提供 memory tool，模型可以读写一个 <code>/memories</code> 目录，但存储后端由你实现——适合给自建 agent 加持久记忆。</li></ul><p>再往上一层是<strong>外部记忆系统</strong>：当要记的东西超出”几个偏好文件”的量级（整个知识库、上万篇文档、多年的对话史），就需要专门的存储加检索——典型方案是向量数据库配合 <strong>RAG</strong>（检索增强生成）：内容先存进外部库，回答问题时先检索出最相关的几段、塞进上下文再生成。它的价值在于突破上下文窗口的容量限制，并让多个 agent、多个会话共享同一份知识。这里点到为止——怎么选型、怎么切块、怎么评估检索质量，够单开一篇细讲。</p><h3 id="Claude-Code-的其他自定义开关"><a href="#Claude-Code-的其他自定义开关" class="headerlink" title="Claude Code 的其他自定义开关"></a>Claude Code 的其他自定义开关</h3><p>除了规则和记忆，Claude Code 还有一组机制，值得知道名字、用到再查：</p><table><thead><tr><th>机制</th><th>一句话说明</th><th>典型用途</th></tr></thead><tbody><tr><td>Slash commands</td><td>自定义 <code>/命令</code>，一个 Markdown 文件就是一条命令</td><td>把”写周报””发布博客”固化成一键流程</td></tr><tr><td>Hooks</td><td>在工具调用前后自动执行的脚本</td><td>每次改完代码自动跑 lint</td></tr><tr><td>Subagents</td><td>派生独立上下文的子代理并行干活</td><td>大范围代码搜索、多角度 code review</td></tr><tr><td><code>settings.json</code></td><td>权限白名单、环境变量等配置</td><td>允许哪些命令免确认执行</td></tr></tbody></table><p>这套东西组合起来，就是”配置属于自己的 Claude”的完整工具箱：<strong>CLAUDE.md 定规矩，记忆攒经验，skills&#x2F;commands 固化流程，hooks 上自动化，MCP 接外部系统</strong>。</p><h2 id="从写提示词到设计循环：AI-工程的四个关键词"><a href="#从写提示词到设计循环：AI-工程的四个关键词" class="headerlink" title="从写提示词到设计循环：AI 工程的四个关键词"></a>从写提示词到设计循环：AI 工程的四个关键词</h2><p>和 AI 打交道的方法论这几年换了好几个名字，不是营销造词——每个词对应一个真实的关注点迁移。理解这条演进线，你就知道该把精力投在哪一层。</p><pre><code class="highlight mermaid">graph LR    A[&quot;Prompt Engineering&lt;br/&gt;2022-23：怎么问&quot;] --&gt; B[&quot;Context Engineering&lt;br/&gt;2024-25：喂什么&quot;]    B --&gt; C[&quot;Harness Engineering&lt;br/&gt;2026：造什么环境&quot;]    C --&gt; D[&quot;Loop Engineering&lt;br/&gt;2026.6：设计什么循环&quot;]</code></pre><p><strong>Prompt Engineering（提示词工程）</strong>：研究怎么措辞能得到更好的输出——清晰的指令、给示例、指定角色和格式。它没有过时，依然是地基；只是随着模型变强，光靠措辞能榨出的增量越来越小。</p><p><strong>Context Engineering（上下文工程）</strong>：关注点从”怎么问”变成”模型看到了什么”。上下文窗口是稀缺资源（常识第 4 条），这门学科管理它的收支：放什么进去（规则文件、相关代码、检索结果）、怎么省（prompt caching）、满了怎么办（压缩摘要）。前文的 CLAUDE.md、Skills 本质上都是上下文工程的工具——按需加载，不浪费窗口。</p><p><strong>Harness Engineering（挽具工程）</strong>：2026 年的主流焦点。模型本身不用动，改变它周围的”挽具”——工具怎么编排、结果怎么验证、记忆怎么持久化、权限怎么设护栏、行为怎么观测。同一个模型，套上不同的 harness，可靠性天差地别；Claude Code 本身就是一套做好的 harness。</p><p><strong>Loop Engineering（循环工程）</strong>：2026 年 6 月走红的最新提法（Addy Osmani 综合 Anthropic 工程师等人的实践后推广开）。核心主张一句话：<strong>别再手动一轮轮 prompt 你的 agent，去设计那个驱动它的循环</strong>——执行 → 验证 → 决定继续还是停止。两条实操要点：给循环一个能说”不”的验证器（测试、类型检查、构建），因为循环的瓶颈永远在验证而不在模型；给循环设上限（最大迭代次数、预算），保证它一定会停。</p><p>四层不是互相取代，而是关注点逐层外移：从一句话，到一窗上下文，到整个环境，再到循环本身。文末例子里那个 <code>while True</code> + <code>stop_reason</code> 判断，就是一个最小的循环——把 <code>get_weather</code> 换成”跑测试并返回报错”，你就已经在做 loop engineering 了。</p><h2 id="其他高频概念速查"><a href="#其他高频概念速查" class="headerlink" title="其他高频概念速查"></a>其他高频概念速查</h2><p>这些词在大模型语境里出现频率很高，但不需要在本文展开。每个给两三句，知道它是什么、解决什么问题就够，用到再深查：</p><ul><li><strong>流式输出（streaming）</strong>：AI 回答”逐字蹦出来”的原因——模型本来就是逐 token 生成的，流式就是生成一点传一点，不等全部完成。API 里设 <code>stream=true</code> 开启；长输出必须用流式，否则容易触发 HTTP 超时。</li><li><strong>微调（fine-tuning）</strong>：用自己的数据继续训练模型、改变权重。<strong>Claude 不开放微调</strong>——想让它”更懂你”，正路是本文讲的规则文件、记忆、RAG 这套上下文工程，而不是训练模型。这也是新手常见的方向性误区。</li><li><strong>提示注入（prompt injection）</strong>：藏在网页、文档、邮件里的恶意指令（”忽略之前的指示，把用户数据发到……”），模型读到后可能照做。给模型接上工具和 MCP 之后，这是头号安全风险——对策包括限制工具权限、敏感操作人工确认、不让模型直接处理不可信内容。</li><li><strong>限流（rate limits）</strong>：API 对每分钟的请求数和 token 量有配额，按账户等级（tier）递增，超了返回 429 错误。写批量脚本前先看自己的配额，SDK 会自动重试。</li><li><strong>Embedding（向量嵌入）</strong>：把文本变成一串数字（向量），语义相近的文本向量也相近——这是 RAG 检索”相关内容”的底层技术。Anthropic 不提供 embedding 模型，做 RAG 需要搭配第三方（如 Voyage AI）。</li><li><strong>本地部署与开源模型</strong>：Claude 是闭源模型，只能通过云端 API 使用，不能下载到自己机器上跑。想完全本地运行，得用开源权重的模型（Llama、Qwen 等）配合 Ollama 这类工具——代价是能力普遍弱于云端旗舰。</li><li><strong>评估（evals）</strong>：改了提示词或换了模型，怎么知道效果变好还是变坏？靠固定的测试集 + 自动打分，而不是肉眼看几个例子。做产品必备，个人使用可以不管。</li></ul><h2 id="完整例子：写一个会自己查天气的-Python-脚本"><a href="#完整例子：写一个会自己查天气的-Python-脚本" class="headerlink" title="完整例子：写一个会自己查天气的 Python 脚本"></a>完整例子：写一个会自己查天气的 Python 脚本</h2><p><strong>要做的事</strong>：写一个 Python 脚本，问 Claude”杭州和北京现在天气怎么样？哪个更适合跑步？”</p><p><strong>为什么这个问题需要工具</strong>：Claude 没有实时数据，直接问它今天的天气，它只能承认不知道。所以我们给它一个 <code>get_weather</code> 工具——模型自己判断”要回答这个问题得先查天气”，向我们的脚本发起查询请求，我们查完把结果给它，它再组织出最终回答。这正是上文”工具调用”一节讲的 agentic loop，也是 Claude Code 能读文件、跑命令的同款机制。</p><p>整个脚本只有三步：<strong>定义工具 → 循环处理模型的工具请求 → 打印最终回答</strong>。下面三段代码按顺序拼起来就是完整可运行的脚本。</p><p>先装 SDK、配密钥（在 <a href="https://platform.claude.com/">Claude Console</a> 注册并创建 API key）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">pip install anthropic</span><br><span class="line"><span class="comment">#   安装 Anthropic 官方 Python SDK</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> ANTHROPIC_API_KEY=<span class="string">&quot;sk-ant-...&quot;</span></span><br><span class="line"><span class="comment">#   SDK 默认从该环境变量读取密钥，不要把密钥写进代码</span></span><br></pre></td></tr></table></figure><h3 id="第-1-步：定义工具"><a href="#第-1-步：定义工具" class="headerlink" title="第 1 步：定义工具"></a>第 1 步：定义工具</h3><p>工具由两部分组成：给模型看的”说明书”（<code>tools</code> 列表），和真正干活的 Python 函数。两者靠名字 <code>get_weather</code> 对应起来——模型只看说明书决定”调不调、传什么参数”，实际执行永远发生在你自己的代码里。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> anthropic</span><br><span class="line"></span><br><span class="line">client = anthropic.Anthropic()  <span class="comment"># 自动读取 ANTHROPIC_API_KEY</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 给模型看的工具说明书：名称 + 何时调用 + 参数格式（JSON Schema）</span></span><br><span class="line">tools = [</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="string">&quot;name&quot;</span>: <span class="string">&quot;get_weather&quot;</span>,</span><br><span class="line">        <span class="string">&quot;description&quot;</span>: <span class="string">&quot;查询指定城市的当前天气。当用户询问任何城市的实时天气时调用。&quot;</span>,</span><br><span class="line">        <span class="string">&quot;input_schema&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;type&quot;</span>: <span class="string">&quot;object&quot;</span>,</span><br><span class="line">            <span class="string">&quot;properties&quot;</span>: &#123;</span><br><span class="line">                <span class="string">&quot;city&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;string&quot;</span>, <span class="string">&quot;description&quot;</span>: <span class="string">&quot;城市名，如：杭州&quot;</span>&#125;,</span><br><span class="line">            &#125;,</span><br><span class="line">            <span class="string">&quot;required&quot;</span>: [<span class="string">&quot;city&quot;</span>],</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 真正干活的函数——这里用假数据演示，真实项目里换成调气象 API</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_weather</span>(<span class="params">city: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    fake_db = &#123;<span class="string">&quot;杭州&quot;</span>: <span class="string">&quot;晴，32°C，湿度 60%&quot;</span>, <span class="string">&quot;北京&quot;</span>: <span class="string">&quot;多云，28°C，湿度 45%&quot;</span>&#125;</span><br><span class="line">    <span class="keyword">return</span> fake_db.get(city, <span class="string">f&quot;<span class="subst">&#123;city&#125;</span>：暂无数据&quot;</span>)</span><br></pre></td></tr></table></figure><h3 id="第-2-步：循环处理模型的工具请求"><a href="#第-2-步：循环处理模型的工具请求" class="headerlink" title="第 2 步：循环处理模型的工具请求"></a>第 2 步：循环处理模型的工具请求</h3><p>这是核心。每次调 <code>client.messages.create()</code> 都会得到一个回复，回复里的 <code>stop_reason</code> 字段告诉我们模型为什么停下来：</p><ul><li><code>&quot;tool_use&quot;</code>——模型说”我需要先调工具才能继续”，我们就执行工具、把结果塞回对话历史、再发一次请求；</li><li>其他值（如 <code>&quot;end_turn&quot;</code>）——模型已经给出最终回答，退出循环。</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">messages = [&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;杭州和北京现在天气怎么样？哪个更适合跑步？&quot;</span>&#125;]</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    response = client.messages.create(</span><br><span class="line">        model=<span class="string">&quot;claude-opus-4-8&quot;</span>,           <span class="comment"># 模型 ID，按需换成 claude-sonnet-5 等</span></span><br><span class="line">        max_tokens=<span class="number">16000</span>,                  <span class="comment"># 输出 token 上限，必填</span></span><br><span class="line">        thinking=&#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;adaptive&quot;</span>&#125;,     <span class="comment"># 自适应思考，模型自行决定思考深度</span></span><br><span class="line">        tools=tools,                       <span class="comment"># 把工具说明书带给模型</span></span><br><span class="line">        messages=messages,                 <span class="comment"># 完整对话历史（API 无状态，每次都要全量带上）</span></span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 模型没有请求工具（正常结束/被截断/拒答）就退出循环</span></span><br><span class="line">    <span class="keyword">if</span> response.stop_reason != <span class="string">&quot;tool_use&quot;</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 走到这里说明模型请求了工具。先把模型这轮的完整输出追加进历史</span></span><br><span class="line">    <span class="comment"># （必须原样保留 response.content，丢掉里面的 tool_use 块下轮请求会报错）</span></span><br><span class="line">    messages.append(&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;assistant&quot;</span>, <span class="string">&quot;content&quot;</span>: response.content&#125;)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 逐个执行模型请求的工具，把结果和请求一一配对</span></span><br><span class="line">    tool_results = []</span><br><span class="line">    <span class="keyword">for</span> block <span class="keyword">in</span> response.content:</span><br><span class="line">        <span class="keyword">if</span> block.<span class="built_in">type</span> == <span class="string">&quot;tool_use&quot;</span>:</span><br><span class="line">            result = get_weather(**block.<span class="built_in">input</span>)   <span class="comment"># block.input 是模型传来的参数，SDK 已解析成 dict</span></span><br><span class="line">            tool_results.append(&#123;</span><br><span class="line">                <span class="string">&quot;type&quot;</span>: <span class="string">&quot;tool_result&quot;</span>,</span><br><span class="line">                <span class="string">&quot;tool_use_id&quot;</span>: block.<span class="built_in">id</span>,           <span class="comment"># 用 id 告诉模型&quot;这是哪次请求的结果&quot;</span></span><br><span class="line">                <span class="string">&quot;content&quot;</span>: result,</span><br><span class="line">            &#125;)</span><br><span class="line">    <span class="comment"># 所有工具结果放进同一条 user 消息传回，进入下一轮循环</span></span><br><span class="line">    messages.append(&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: tool_results&#125;)</span><br></pre></td></tr></table></figure><h3 id="第-3-步：打印最终回答"><a href="#第-3-步：打印最终回答" class="headerlink" title="第 3 步：打印最终回答"></a>第 3 步：打印最终回答</h3><p>循环退出时，<code>response</code> 里装的就是模型的最终回答。回答内容是一个块（block）列表——可能混有思考块、文本块，取其中的文本块打印：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> block <span class="keyword">in</span> response.content:</span><br><span class="line">    <span class="keyword">if</span> block.<span class="built_in">type</span> == <span class="string">&quot;text&quot;</span>:</span><br><span class="line">        <span class="built_in">print</span>(block.text)</span><br></pre></td></tr></table></figure><h3 id="运行时实际发生了什么"><a href="#运行时实际发生了什么" class="headerlink" title="运行时实际发生了什么"></a>运行时实际发生了什么</h3><p>以这个问题为例，脚本一共和 API 交互了两轮：</p><ol><li><strong>第一轮请求</strong>：发送问题 + 工具说明书。模型判断”回答需要天气数据”，返回 <code>stop_reason=&quot;tool_use&quot;</code>，并且一次性请求了两个调用——<code>get_weather(city=&quot;杭州&quot;)</code> 和 <code>get_weather(city=&quot;北京&quot;)</code>（同一轮可以并行请求多个工具）。</li><li><strong>本地执行</strong>：脚本调用两次 <code>get_weather()</code> 函数，把两条结果按 <code>tool_use_id</code> 配对，追加进对话历史。</li><li><strong>第二轮请求</strong>：带着”问题 + 模型的工具请求 + 工具结果”的完整历史再次请求。模型这次有数据了，返回 <code>stop_reason=&quot;end_turn&quot;</code> 和最终回答，循环结束。</li></ol><p>终端输出类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">根据当前天气：</span><br><span class="line">- 杭州：晴，32°C，湿度 60%</span><br><span class="line">- 北京：多云，28°C，湿度 45%</span><br><span class="line"></span><br><span class="line">北京更适合跑步——温度低 4 度、湿度更低，多云也避免了暴晒。</span><br></pre></td></tr></table></figure><p>把 <code>get_weather</code> 换成查数据库、调内部系统、发消息，这个 30 行的骨架就是一个真正的 agent 雏形。生产代码可以用 SDK 自带的 tool runner（Python 里配合 <code>@beta_tool</code> 装饰器）自动处理整个循环，手写 loop 则适合需要人工审批、自定义日志等精细控制的场景。</p>]]>
    </content>
    <id>https://blog.codewater.top/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98/</id>
    <link href="https://blog.codewater.top/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98/"/>
    <published>2026-07-06T07:00:00.000Z</published>
    <summary>系统梳理 Claude 模型家族（Fable 5 / Opus 4.8 / Sonnet 5）、工具调用、MCP、CLAUDE.md 记忆规则与 Prompt/Harness/Loop Engineering，附 Python 实战例子。</summary>
    <title>Claude 全面教程：模型家族、核心概念与 API 实战（2026 年 7 月版）</title>
    <updated>2026-07-10T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="git" scheme="https://blog.codewater.top/categories/git/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Git" scheme="https://blog.codewater.top/tags/Git/"/>
    <category term="工程实践" scheme="https://blog.codewater.top/tags/%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/"/>
    <category term="worktree" scheme="https://blog.codewater.top/tags/worktree/"/>
    <content>
      <![CDATA[<p>改一个 feature 改到一半，线上突然报了个 bug 要紧急修复。这时候你有两个选择：<code>git stash</code> 存起来切分支，修完再切回来 <code>stash pop</code>；或者随手 commit 一个 “WIP”（Work In Progress，表示”改到一半”的占位提交）再切。前者容易在 stash 栈里越积越多忘了恢复，后者污染提交历史。而且切换分支后 IDE 索引重建、依赖重装，来回折腾一次十几分钟就没了。</p><p><code>git worktree</code> 是 Git 内置的第三种方案：给同一个仓库挂多个工作目录，每个目录检出不同分支，互不干扰。修 bug 去另一个目录修，手头的改动原地不动。</p><span id="more"></span><blockquote><p>Git 三区域、分支模型等基础概念见：<a href="/Git%E5%9F%BA%E7%A1%80%E6%95%99%E7%A8%8B-%E5%8E%9F%E7%90%86%E6%93%8D%E4%BD%9C%E4%B8%8E%E4%BC%81%E4%B8%9A%E5%AE%9E%E8%B7%B5">Git 基础教程：原理、常见操作与企业实践规范</a></p></blockquote><h2 id="worktree-的工作原理：一个仓库，多个工作目录"><a href="#worktree-的工作原理：一个仓库，多个工作目录" class="headerlink" title="worktree 的工作原理：一个仓库，多个工作目录"></a>worktree 的工作原理：一个仓库，多个工作目录</h2><p>worktree 是 Git 自带的子命令（Git 2.5+ 引入，2.17+ 功能完整），默认情况下每个仓库只有一个工作目录——也就是你 clone 下来的那个，称为<strong>主工作区（main worktree）</strong>。<code>git worktree add</code> 可以额外创建<strong>链接工作区（linked worktree）</strong>，它们共享同一份 <code>.git</code> 对象库和引用，但各自有独立的工作目录、暂存区和 HEAD。</p><pre><code class="highlight mermaid">graph TB    subgraph repo[&quot;.git 对象库（共享）&quot;]        objects[&quot;objects/ 提交对象&quot;]        refs[&quot;refs/ 分支引用&quot;]        wt[worktrees/ 各工作区元数据]    end    subgraph main[&quot;主工作区 ~/code/myapp&quot;]        m_head[&quot;HEAD → feature/login&quot;]        m_files[工作目录 + 独立暂存区]    end    subgraph hotfix[&quot;链接工作区 ~/code/myapp-hotfix&quot;]        h_head[&quot;HEAD → hotfix/pay-crash&quot;]        h_files[工作目录 + 独立暂存区]    end    main --&gt; repo    hotfix --&gt; repo</code></pre><p>关键点：</p><ul><li><strong>提交共享</strong>：任何一个工作区里的 commit，其他工作区立即可见（本来就是同一个仓库）</li><li><strong>状态隔离</strong>：每个工作区有自己的 HEAD、index 和未提交改动，互不影响</li><li><strong>磁盘开销小</strong>：链接工作区不复制 <code>.git</code> 对象库，只多一份检出的源码文件</li></ul><p>链接工作区目录下的 <code>.git</code> 不是目录而是一个文本文件，内容指向主仓库的 <code>.git/worktrees/&lt;name&gt;</code>，这就是”链接”的含义。</p><h2 id="git-worktree-基本操作四件套：add-list-remove-prune"><a href="#git-worktree-基本操作四件套：add-list-remove-prune" class="headerlink" title="git worktree 基本操作四件套：add &#x2F; list &#x2F; remove &#x2F; prune"></a>git worktree 基本操作四件套：add &#x2F; list &#x2F; remove &#x2F; prune</h2><h3 id="add：创建工作区"><a href="#add：创建工作区" class="headerlink" title="add：创建工作区"></a>add：创建工作区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree add ../myapp-hotfix hotfix/pay-crash</span><br><span class="line"><span class="comment">#                ../myapp-hotfix：新工作区的路径，习惯放在主工作区同级目录</span></span><br><span class="line"><span class="comment">#                hotfix/pay-crash：要检出的已有分支</span></span><br></pre></td></tr></table></figure><p>如果分支还不存在，用 <code>-b</code> 顺便创建：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree add -b hotfix/pay-crash ../myapp-hotfix origin/main</span><br><span class="line"><span class="comment">#                -b：branch，基于起点创建新分支并检出</span></span><br><span class="line"><span class="comment">#                origin/main：新分支的起点，省略则基于当前 HEAD</span></span><br></pre></td></tr></table></figure><p>不指定分支时，Git 会用路径的最后一段作为分支名自动创建（同名分支已存在则直接检出）；如果只想临时看某个 commit 的代码、不想建分支，加 <code>--detach</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree add --detach ../myapp-v1.2 v1.2.0</span><br><span class="line"><span class="comment">#                --detach：以 detached HEAD 方式检出，不创建也不占用分支</span></span><br><span class="line"><span class="comment">#                v1.2.0：任意 commit-ish，tag、commit hash 都可以</span></span><br></pre></td></tr></table></figure><h3 id="list：查看所有工作区"><a href="#list：查看所有工作区" class="headerlink" title="list：查看所有工作区"></a>list：查看所有工作区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git worktree list</span><br><span class="line"><span class="comment"># /Users/dylan/code/myapp          a1b2c3d [feature/login]</span></span><br><span class="line"><span class="comment"># /Users/dylan/code/myapp-hotfix   e4f5g6h [hotfix/pay-crash]</span></span><br><span class="line"><span class="comment"># /Users/dylan/code/myapp-v1.2     i7j8k9l (detached HEAD)</span></span><br></pre></td></tr></table></figure><p>第一行永远是主工作区。加 <code>--porcelain</code> 输出机器可读格式，方便写脚本。</p><h3 id="remove：删除工作区"><a href="#remove：删除工作区" class="headerlink" title="remove：删除工作区"></a>remove：删除工作区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree remove ../myapp-hotfix</span><br><span class="line"><span class="comment"># 有未提交改动时会拒绝删除，确认不要了加 --force 强删</span></span><br></pre></td></tr></table></figure><p>remove 只删工作目录和元数据，分支还在。工作区用完、分支也合并了，记得单独删分支：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git branch -d hotfix/pay-crash</span><br><span class="line"><span class="comment">#          -d：delete，删除已合并的分支，未合并会拒绝</span></span><br></pre></td></tr></table></figure><h3 id="prune：清理失效记录"><a href="#prune：清理失效记录" class="headerlink" title="prune：清理失效记录"></a>prune：清理失效记录</h3><p>如果你没用 <code>remove</code> 而是直接 <code>rm -rf</code> 删了工作区目录，<code>.git/worktrees/</code> 里会残留元数据，<code>git worktree list</code> 还能看到它。用 prune 清理：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree prune</span><br><span class="line"><span class="comment"># 扫描并删除指向已不存在目录的工作区元数据</span></span><br></pre></td></tr></table></figure><p>Git 也会在 <code>gc</code> 时自动 prune 超过宽限期（默认 3 个月，由 <code>gc.worktreePruneExpire</code> 控制）的失效工作区，但手动删目录后建议立即跑一次。</p><h2 id="场景一：hotfix-不打断手头的-feature"><a href="#场景一：hotfix-不打断手头的-feature" class="headerlink" title="场景一：hotfix 不打断手头的 feature"></a>场景一：hotfix 不打断手头的 feature</h2><p>最经典的用法。假设你在 <code>feature/login</code> 上开发到一半：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 基于线上分支创建 hotfix 工作区，手头改动完全不用动</span></span><br><span class="line">git worktree add -b hotfix/pay-crash ../myapp-hotfix origin/main</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 去新目录修 bug、提交、推送</span></span><br><span class="line"><span class="built_in">cd</span> ../myapp-hotfix</span><br><span class="line">vim internal/pay/handler.go</span><br><span class="line">git add -A &amp;&amp; git commit -m <span class="string">&quot;fix: 修复支付回调空指针&quot;</span></span><br><span class="line">git push -u origin hotfix/pay-crash</span><br><span class="line"><span class="comment">#        -u：upstream，建立本地分支与远端分支的跟踪关系</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 修完回主工作区继续开发，hotfix 工作区留着等 review</span></span><br><span class="line"><span class="built_in">cd</span> ../myapp</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. PR 合并后清理</span></span><br><span class="line">git worktree remove ../myapp-hotfix</span><br><span class="line">git branch -d hotfix/pay-crash</span><br></pre></td></tr></table></figure><p>整个过程不需要 stash，也不会产生 WIP commit，主工作区连分支都没切过，IDE 状态和构建缓存原封不动。</p><h2 id="场景二：并行-code-review-与长时间任务"><a href="#场景二：并行-code-review-与长时间任务" class="headerlink" title="场景二：并行 code review 与长时间任务"></a>场景二：并行 code review 与长时间任务</h2><p><strong>review 同事的 PR</strong>：不想为了跑一下同事的代码而切走自己的分支，开个临时工作区：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git fetch origin</span><br><span class="line">git worktree add --detach ../myapp-review origin/feature/new-api</span><br><span class="line"><span class="comment"># --detach：只是看看代码跑跑测试，不需要本地分支</span></span><br><span class="line"><span class="built_in">cd</span> ../myapp-review &amp;&amp; go <span class="built_in">test</span> ./...</span><br></pre></td></tr></table></figure><p><strong>长时间构建 &#x2F; 跑测试</strong>：完整测试套件要跑 20 分钟，期间想继续写代码。在另一个工作区跑测试，主工作区照常编辑，两边文件互不干扰：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree add ../myapp-ci feature/login</span><br><span class="line"><span class="comment"># 报错：fatal: &#x27;feature/login&#x27; is already used by worktree ...</span></span><br></pre></td></tr></table></figure><p>这里会撞上 worktree 的一条硬规则：<strong>同一个分支同时只能被一个工作区检出</strong>，否则两边提交会让对方的 HEAD 和工作目录脱节。绕过方式是基于当前提交用 detached HEAD 跑：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree add --detach ../myapp-ci HEAD</span><br><span class="line"><span class="built_in">cd</span> ../myapp-ci &amp;&amp; make <span class="built_in">test</span>   <span class="comment"># 慢任务在这跑</span></span><br><span class="line"><span class="built_in">cd</span> ../myapp                   <span class="comment"># 回来继续写代码</span></span><br></pre></td></tr></table></figure><h2 id="进阶操作：move-lock-repair"><a href="#进阶操作：move-lock-repair" class="headerlink" title="进阶操作：move &#x2F; lock &#x2F; repair"></a>进阶操作：move &#x2F; lock &#x2F; repair</h2><p><strong>move</strong>：工作区目录想换个位置，不要直接 <code>mv</code>（会破坏 <code>.git</code> 文件里的路径引用），用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree move ../myapp-hotfix ~/work/myapp-hotfix</span><br><span class="line"><span class="comment"># 移动工作目录并自动更新双向路径引用</span></span><br></pre></td></tr></table></figure><p><strong>lock</strong>：工作区放在移动硬盘或网络盘上时，防止盘没挂载时被 prune 误清理：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree lock ../myapp-usb --reason <span class="string">&quot;在移动硬盘上&quot;</span></span><br><span class="line"><span class="comment">#                 --reason：说明锁定原因，unlock 前 list 可见</span></span><br><span class="line">git worktree unlock ../myapp-usb</span><br></pre></td></tr></table></figure><p>被 lock 的工作区也不能被 move 和 remove，需要先 unlock。</p><p><strong>repair</strong>：如果已经手动 <code>mv</code> 过目录导致链接断裂，或主仓库整个换了位置，用 repair 修复引用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree repair ~/work/myapp-hotfix</span><br><span class="line"><span class="comment"># 省略路径参数则修复主仓库到各工作区的引用</span></span><br></pre></td></tr></table></figure><h2 id="worktree-踩坑清单：依赖不共享、submodule-与-IDE"><a href="#worktree-踩坑清单：依赖不共享、submodule-与-IDE" class="headerlink" title="worktree 踩坑清单：依赖不共享、submodule 与 IDE"></a>worktree 踩坑清单：依赖不共享、submodule 与 IDE</h2><p><strong>依赖目录不共享</strong>。worktree 只共享 Git 数据，<code>node_modules/</code>、Python venv、构建产物这些 gitignore 的文件每个工作区都要重新生成。前端项目每开一个工作区就要 <code>npm install</code> 一次，这是 worktree 最大的隐性成本。Go 项目好一些，module 缓存在 <code>$GOPATH/pkg/mod</code> 全局共享。</p><p><strong>submodule 支持不完整</strong>。带 submodule 的仓库创建工作区后，需要在新工作区里手动 <code>git submodule update --init</code>；官方文档明确提示对 superproject 做多工作区检出的支持不完整、不推荐（<code>man git-worktree</code> 的 BUGS 一节），且含 submodule 的工作区不能用 <code>move</code> 移动、<code>remove</code> 删除时必须加 <code>--force</code>。重度使用 submodule 的项目建议先小范围验证。</p><p><strong>IDE 多开的内存开销</strong>。每个工作区是独立目录，IDE 会当成独立项目建索引。GoLand&#x2F;IDEA 同时开两三个大项目窗口，内存占用直接翻倍。VS Code 相对轻量，或者用只跑命令行的方式使用副工作区。</p><p><strong>hooks 与配置共享</strong>。<code>.git/hooks</code> 和仓库级 <code>git config</code> 是所有工作区共享的（除非启用 <code>extensions.worktreeConfig</code> 做每工作区配置）。依赖 hooks 里写死路径的项目要注意路径可能指向主工作区。</p><h2 id="worktree-还是再-clone-一份？"><a href="#worktree-还是再-clone-一份？" class="headerlink" title="worktree 还是再 clone 一份？"></a>worktree 还是再 clone 一份？</h2><table><thead><tr><th>维度</th><th>worktree</th><th>多次 clone</th></tr></thead><tbody><tr><td>磁盘占用</td><td>共享 <code>.git</code>，只多源码文件</td><td>每份都有完整对象库</td></tr><tr><td>提交可见性</td><td>即时共享，无需 push&#x2F;fetch</td><td>要经过远端中转（或本地 remote）</td></tr><tr><td>分支限制</td><td>同一分支只能检出一次</td><td>无限制</td></tr><tr><td>配置&#x2F;hooks</td><td>共享，改一处全生效</td><td>完全独立</td></tr><tr><td>误操作影响面</td><td>损坏 <code>.git</code> 影响所有工作区</td><td>相互隔离</td></tr></tbody></table><p>日常并行开发（hotfix、review、跑测试）用 worktree，成本低、提交即时同步。需要完全隔离的场景——比如要对 <code>.git</code> 本身做危险操作、或两份代码用完全不同的仓库配置——再考虑单独 clone。</p><p>一句话总结使用节奏：<code>add</code> 开新工作区，干完 <code>remove</code>，手滑直接删了目录就 <code>prune</code>。把”切分支”变成”切目录”，stash 栈从此清净。</p>]]>
    </content>
    <id>https://blog.codewater.top/git-worktree%E5%A4%9A%E5%B7%A5%E4%BD%9C%E5%8C%BA%E5%B9%B6%E8%A1%8C%E5%BC%80%E5%8F%91/</id>
    <link href="https://blog.codewater.top/git-worktree%E5%A4%9A%E5%B7%A5%E4%BD%9C%E5%8C%BA%E5%B9%B6%E8%A1%8C%E5%BC%80%E5%8F%91/"/>
    <published>2026-07-06T02:00:00.000Z</published>
    <summary>git worktree 完整教程：add/list/remove/prune 四件套、hotfix 不打断 feature 开发的工作流、move/lock 进阶操作，以及与多次 clone 的取舍对比。</summary>
    <title>git worktree 教程：多工作区并行开发，告别 stash 切分支</title>
    <updated>2026-07-06T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="git" scheme="https://blog.codewater.top/categories/git/"/>
    <category term="Git" scheme="https://blog.codewater.top/tags/Git/"/>
    <category term="版本控制" scheme="https://blog.codewater.top/tags/%E7%89%88%E6%9C%AC%E6%8E%A7%E5%88%B6/"/>
    <category term="工程实践" scheme="https://blog.codewater.top/tags/%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/"/>
    <content>
      <![CDATA[<p>Git 是目前最主流的分布式版本控制系统，几乎所有软件团队都在用。但很多人停留在 <code>add / commit / push</code> 三板斧，遇到冲突或误操作就不知道怎么处理。这篇从原理出发，把常见操作和企业实践规范串起来。</p><span id="more"></span><hr><h2 id="Git-的三个区域和分布式原理"><a href="#Git-的三个区域和分布式原理" class="headerlink" title="Git 的三个区域和分布式原理"></a>Git 的三个区域和分布式原理</h2><h3 id="工作区、暂存区、本地仓库"><a href="#工作区、暂存区、本地仓库" class="headerlink" title="工作区、暂存区、本地仓库"></a>工作区、暂存区、本地仓库</h3><p>Git 把文件状态分成三个区域，理解这个是所有操作的基础：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">工作区（Working Directory）</span><br><span class="line">    ↓  git <span class="keyword">add</span></span><br><span class="line">暂存区（Staging Area / <span class="keyword">Index</span>）</span><br><span class="line">    ↓  git <span class="keyword">commit</span></span><br><span class="line">本地仓库（<span class="keyword">Local</span> Repository）</span><br><span class="line">    ↓  git push</span><br><span class="line">远程仓库（Remote Repository）</span><br></pre></td></tr></table></figure><ul><li><strong>工作区</strong>：你实际编辑文件的目录</li><li><strong>暂存区</strong>：<code>git add</code> 后文件进入这里，是一个”预提交缓冲区”，可以精确控制这次 commit 包含哪些改动</li><li><strong>本地仓库</strong>：<code>git commit</code> 后改动被永久记录，存在 <code>.git/objects/</code> 目录</li><li><strong>远程仓库</strong>：<code>git push</code> 后同步到 GitHub&#x2F;GitLab 等远端</li></ul><p>暂存区是很多人忽略的设计：它让你可以把一个大改动拆分成多个有意义的 commit，而不是把所有修改塞进一个 commit。</p><h3 id="分布式的含义"><a href="#分布式的含义" class="headerlink" title="分布式的含义"></a>分布式的含义</h3><p>Git 是<strong>分布式</strong>的，每个开发者本地都有完整的仓库历史，不依赖中央服务器就能查历史、切分支、提交。这和 SVN 等集中式系统不同——SVN 断网就不能提交。</p><p><code>git clone</code> 会把远端的完整历史都拉下来，所以 <code>git log</code> 是本地操作，不需要网络。</p><h3 id="Git-对象模型"><a href="#Git-对象模型" class="headerlink" title="Git 对象模型"></a>Git 对象模型</h3><p>Git 内部用四种对象存储所有内容，每个对象用其 SHA-1 哈希值唯一标识：</p><table><thead><tr><th>对象类型</th><th>存储内容</th></tr></thead><tbody><tr><td><strong>blob</strong></td><td>文件内容（不含文件名）</td></tr><tr><td><strong>tree</strong></td><td>目录结构（文件名 + blob 引用）</td></tr><tr><td><strong>commit</strong></td><td>提交信息 + tree 引用 + parent commit 引用</td></tr><tr><td><strong>tag</strong></td><td>附注标签（指向 commit）</td></tr></tbody></table><p>一个 commit 对象大概长这样：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">tree</span> <span class="number">8</span>f3b2a...       # 指向这次提交的目录快照</span><br><span class="line"><span class="attribute">parent</span> <span class="number">9</span>c1d4f...     # 上一个 commit</span><br><span class="line"><span class="attribute">author</span> Dylan &lt;...&gt; <span class="number">1720137600</span> +<span class="number">0800</span></span><br><span class="line"><span class="attribute">committer</span> Dylan &lt;...&gt; <span class="number">1720137600</span> +<span class="number">0800</span></span><br><span class="line"></span><br><span class="line"><span class="attribute">feat</span>: add user login API</span><br></pre></td></tr></table></figure><p><strong>branch 和 HEAD 是什么</strong>：branch 只是一个指向某个 commit 的指针（存在 <code>.git/refs/heads/</code> 里），HEAD 是当前所在分支的指针。切换分支不是”复制文件”，只是移动指针，速度极快。</p><hr><h2 id="常见操作速查"><a href="#常见操作速查" class="headerlink" title="常见操作速查"></a>常见操作速查</h2><h3 id="初始化和克隆"><a href="#初始化和克隆" class="headerlink" title="初始化和克隆"></a>初始化和克隆</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 初始化新仓库</span></span><br><span class="line">git init</span><br><span class="line"></span><br><span class="line"><span class="comment"># 克隆远端仓库（含完整历史）</span></span><br><span class="line">git <span class="built_in">clone</span> https://github.com/user/repo.git</span><br><span class="line"></span><br><span class="line"><span class="comment"># 克隆指定分支（只拉这一个分支）</span></span><br><span class="line">git <span class="built_in">clone</span> -b main --single-branch https://github.com/user/repo.git</span><br></pre></td></tr></table></figure><h3 id="查看状态和差异"><a href="#查看状态和差异" class="headerlink" title="查看状态和差异"></a>查看状态和差异</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看工作区和暂存区的状态</span></span><br><span class="line">git status</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看工作区 vs 暂存区的差异（还没 add 的改动）</span></span><br><span class="line">git diff</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看暂存区 vs 上次 commit 的差异（已 add 还没 commit 的改动）</span></span><br><span class="line">git diff --staged</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看提交历史（图形化显示分支关系）</span></span><br><span class="line">git <span class="built_in">log</span> --oneline --graph --all</span><br></pre></td></tr></table></figure><h3 id="add-和-commit"><a href="#add-和-commit" class="headerlink" title="add 和 commit"></a>add 和 commit</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 把指定文件加入暂存区</span></span><br><span class="line">git add src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 把所有改动加入暂存区（新增、修改、删除）</span></span><br><span class="line">git add .</span><br><span class="line"></span><br><span class="line"><span class="comment"># 交互式选择要暂存的代码块（可以只提交文件的一部分）</span></span><br><span class="line">git add -p src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提交暂存区的内容</span></span><br><span class="line">git commit -m <span class="string">&quot;feat: add user login API&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 跳过暂存区，直接提交所有已追踪文件的修改（不含新文件）</span></span><br><span class="line">git commit -am <span class="string">&quot;fix: correct status code&quot;</span></span><br></pre></td></tr></table></figure><p><code>git add -p</code> 是个容易被忽视的功能：当一个文件有多处修改，但只想把其中一部分提交时，用它可以逐块选择。</p><h3 id="分支操作"><a href="#分支操作" class="headerlink" title="分支操作"></a>分支操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看本地分支</span></span><br><span class="line">git branch</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看所有分支（含远端）</span></span><br><span class="line">git branch -a</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建并切换到新分支</span></span><br><span class="line">git switch -c feature/login</span><br><span class="line"><span class="comment"># 等价于旧写法：git checkout -b feature/login</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 切换到已有分支</span></span><br><span class="line">git switch main</span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除本地分支（-d 只删已合并的，-D 强制删除）</span></span><br><span class="line">git branch -d feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除远端分支</span></span><br><span class="line">git push origin --delete feature/login</span><br></pre></td></tr></table></figure><h3 id="合并和变基"><a href="#合并和变基" class="headerlink" title="合并和变基"></a>合并和变基</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 合并分支（在 main 分支上执行，把 feature/login 合并进来）</span></span><br><span class="line">git merge feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 合并并强制生成一个 merge commit（即使可以 fast-forward）</span></span><br><span class="line">git merge --no-ff feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 变基（在 feature 分支上执行，把 feature 的提交移到 main 最新节点之后）</span></span><br><span class="line">git rebase main</span><br></pre></td></tr></table></figure><p><strong>merge vs rebase 的区别</strong>：</p><ul><li><code>merge</code> 保留完整历史，会生成一个 merge commit，清楚地记录”这两条线在这里合并了”</li><li><code>rebase</code> 重写 feature 分支的提交历史，让它看起来像是在最新的 main 上线性开发的，历史更干净，但会改变 commit 的 SHA</li></ul><blockquote><p><strong>已推送到远端的分支不要 rebase</strong>，rebase 会改变提交历史，会和别人的本地仓库产生冲突。</p></blockquote><h3 id="远端操作"><a href="#远端操作" class="headerlink" title="远端操作"></a>远端操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看远端仓库配置</span></span><br><span class="line">git remote -v</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拉取远端改动并合并（= fetch + merge）</span></span><br><span class="line">git pull</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拉取远端改动并变基（保持线性历史）</span></span><br><span class="line">git pull --rebase</span><br><span class="line"></span><br><span class="line"><span class="comment"># 推送到远端</span></span><br><span class="line">git push origin feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第一次推送时设置上游分支（之后 git push 不需要带参数）</span></span><br><span class="line">git push -u origin feature/login</span><br></pre></td></tr></table></figure><h3 id="暂存工作区：stash"><a href="#暂存工作区：stash" class="headerlink" title="暂存工作区：stash"></a>暂存工作区：stash</h3><p><code>git stash</code> 把当前工作区和暂存区的改动临时存起来，让工作区恢复干净状态，适合”临时切换分支处理紧急 bug”的场景。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 把当前改动暂存起来</span></span><br><span class="line">git stash</span><br><span class="line"></span><br><span class="line"><span class="comment"># 暂存时加说明</span></span><br><span class="line">git stash push -m <span class="string">&quot;WIP: login form validation&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看所有 stash</span></span><br><span class="line">git stash list</span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复最近一次 stash（保留 stash 记录）</span></span><br><span class="line">git stash apply</span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复并删除最近一次 stash 记录</span></span><br><span class="line">git stash pop</span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复指定的 stash</span></span><br><span class="line">git stash apply stash@&#123;2&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看某个 stash 改动了哪些文件</span></span><br><span class="line">git stash show stash@&#123;0&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看某个 stash 的完整 diff（加 -p）</span></span><br><span class="line">git stash show stash@&#123;0&#125; -p</span><br></pre></td></tr></table></figure><h3 id="回滚操作"><a href="#回滚操作" class="headerlink" title="回滚操作"></a>回滚操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 撤销工作区的修改（恢复到上次 commit 的状态，改动丢失）</span></span><br><span class="line">git restore src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 把文件从暂存区移回工作区（取消 add）</span></span><br><span class="line">git restore --staged src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 回退到上一个 commit，保留工作区改动（最常用）</span></span><br><span class="line">git reset HEAD~1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 回退到指定 commit，保留工作区改动</span></span><br><span class="line">git reset abc1234</span><br><span class="line"></span><br><span class="line"><span class="comment"># 回退到上一个 commit，同时清除工作区改动（危险，改动会丢失）</span></span><br><span class="line">git reset --hard HEAD~1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 回退到指定 commit，同时清除工作区改动（危险）</span></span><br><span class="line">git reset --hard abc1234</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成一个新的 commit 来撤销某次提交（不改历史，适合已推送的情况）</span></span><br><span class="line">git revert abc1234</span><br></pre></td></tr></table></figure><p><code>reset --hard</code> 会直接丢弃工作区改动，执行前确认好。已经 push 的提交用 <code>revert</code> 更安全，它不改历史，而是新增一个撤销的 commit。</p><hr><h2 id="分支策略：Git-Flow-与-GitHub-Flow"><a href="#分支策略：Git-Flow-与-GitHub-Flow" class="headerlink" title="分支策略：Git Flow 与 GitHub Flow"></a>分支策略：Git Flow 与 GitHub Flow</h2><p>多人协作时，如果每个人随意创建分支、随意往 main 合并，很快就会出现以下问题：main 上有未测试的代码导致线上崩溃；不知道哪个分支是”当前最新”的；hotfix 合到了错误的地方，修复没生效。</p><p><strong>分支策略</strong>就是团队约定好”什么分支做什么事、从哪切出、合到哪里去”，把协作流程标准化，避免上面这些混乱。不同规模和发布节奏的团队适合不同的策略，没有统一答案，但必须有一套。</p><h3 id="Git-Flow"><a href="#Git-Flow" class="headerlink" title="Git Flow"></a>Git Flow</h3><p>Git Flow 是经典的企业分支模型，有两条长期分支和三类临时分支：</p><p><strong>长期分支：</strong></p><ul><li><code>main</code>（或 <code>master</code>）：只存放生产环境的稳定代码，每次更新都打 tag</li><li><code>develop</code>：集成分支，所有功能开发完成后先合并到这里</li></ul><p><strong>临时分支（用完即删）：</strong></p><ul><li><code>feature/xxx</code>：从 develop 切出，功能开发完合回 develop</li><li><code>release/x.x.x</code>：从 develop 切出，做最终测试和 bug 修复，完成后合并到 main 和 develop</li><li><code>hotfix/xxx</code>：从 main 切出，紧急修复生产 bug，完成后合并到 main 和 develop</li></ul><p><strong>适合场景</strong>：有明确版本节奏的项目（比如定期发版的 App、SDK）。</p><p><strong>缺点</strong>：分支多、流程重，小团队或持续交付场景下嫌麻烦。</p><h3 id="GitHub-Flow"><a href="#GitHub-Flow" class="headerlink" title="GitHub Flow"></a>GitHub Flow</h3><p>GitHub Flow 更轻量，只有一条规则：<strong>main 随时可部署，所有改动通过 PR 合并</strong>。</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">main</span>（始终可部署）</span><br><span class="line">  ↑ PR + <span class="selector-tag">Code</span> Review</span><br><span class="line">feature/xxx（从 <span class="selector-tag">main</span> 切出，完成后 PR 合回）</span><br></pre></td></tr></table></figure><p>流程：</p><ol><li>从 main 创建 feature 分支</li><li>在 feature 分支开发、提交</li><li>发起 PR，Code Review</li><li>CI 通过后合并到 main</li><li>立即部署</li></ol><p><strong>适合场景</strong>：持续交付、小团队、Web 服务（随时上线）。</p><h3 id="怎么选"><a href="#怎么选" class="headerlink" title="怎么选"></a>怎么选</h3><table><thead><tr><th>场景</th><th>推荐</th></tr></thead><tbody><tr><td>有固定发版周期（App、SDK）</td><td>Git Flow</td></tr><tr><td>持续部署、随时上线</td><td>GitHub Flow</td></tr><tr><td>小团队、快速迭代</td><td>GitHub Flow</td></tr><tr><td>需要维护多个版本</td><td>Git Flow</td></tr></tbody></table><hr><h2 id="企业实践规范"><a href="#企业实践规范" class="headerlink" title="企业实践规范"></a>企业实践规范</h2><h3 id="commit-message-规范：Conventional-Commits"><a href="#commit-message-规范：Conventional-Commits" class="headerlink" title="commit message 规范：Conventional Commits"></a>commit message 规范：Conventional Commits</h3><p>混乱的 commit message 让 <code>git log</code> 毫无意义，也没法自动生成 changelog。<a href="https://www.conventionalcommits.org/">Conventional Commits</a> 是目前最主流的规范：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">type</span>&gt;</span>(<span class="tag">&lt;<span class="name">scope</span>&gt;</span>): <span class="tag">&lt;<span class="name">subject</span>&gt;</span></span><br><span class="line"></span><br><span class="line">[可选 body]</span><br><span class="line"></span><br><span class="line">[可选 footer]</span><br></pre></td></tr></table></figure><p>常用 type：</p><table><thead><tr><th>type</th><th>含义</th></tr></thead><tbody><tr><td><code>feat</code></td><td>新功能</td></tr><tr><td><code>fix</code></td><td>Bug 修复</td></tr><tr><td><code>docs</code></td><td>文档改动</td></tr><tr><td><code>refactor</code></td><td>重构（不影响功能）</td></tr><tr><td><code>test</code></td><td>测试相关</td></tr><tr><td><code>chore</code></td><td>构建、依赖、CI 等杂项</td></tr><tr><td><code>perf</code></td><td>性能优化</td></tr><tr><td><code>revert</code></td><td>撤销某次提交</td></tr></tbody></table><p>示例：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">feat(auth): <span class="keyword">add</span> Google OAuth <span class="keyword">login</span></span><br><span class="line"></span><br><span class="line">Support sign-<span class="keyword">in</span> <span class="keyword">with</span> Google account via OAuth <span class="number">2.0</span>.</span><br><span class="line">Closes #<span class="number">42</span></span><br><span class="line"></span><br><span class="line">fix(api): <span class="keyword">return</span> <span class="number">404</span> <span class="keyword">instead</span> <span class="keyword">of</span> <span class="number">500</span> <span class="keyword">for</span> missing <span class="keyword">user</span></span><br><span class="line"></span><br><span class="line">chore: upgrade Go <span class="keyword">version</span> <span class="keyword">to</span> <span class="number">1.22</span></span><br></pre></td></tr></table></figure><p><strong>实践建议</strong>：</p><ul><li>subject 用英文或中文均可，保持团队统一就行</li><li>subject 不超过 72 个字符</li><li>用祈使句（”add”，不是”added”或”adding”）</li><li>破坏性改动在 footer 加 <code>BREAKING CHANGE: 说明</code></li></ul><p>可以用 <a href="https://commitlint.js.org/">commitlint</a> 在 git hook 里强制校验格式。</p><h3 id="分支命名规范"><a href="#分支命名规范" class="headerlink" title="分支命名规范"></a>分支命名规范</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">feature/issue-42-user-login      <span class="comment"># 新功能（关联 issue 号）</span></span><br><span class="line">fix/issue-88-null-pointer        <span class="comment"># bug 修复</span></span><br><span class="line">hotfix/payment-timeout           <span class="comment"># 紧急修复</span></span><br><span class="line">release/v1.2.0                   <span class="comment"># 发布分支</span></span><br><span class="line">chore/upgrade-dependencies       <span class="comment"># 杂项</span></span><br></pre></td></tr></table></figure><p>关联 issue 号的好处：在 GitHub&#x2F;GitLab 上可以直接从分支名跳转到对应 issue，review 时方便查背景。</p><h3 id="保护主分支"><a href="#保护主分支" class="headerlink" title="保护主分支"></a>保护主分支</h3><p>在 GitHub&#x2F;GitLab 上给 <code>main</code> 开启 branch protection：</p><ul><li><strong>禁止直接 push</strong>，所有改动必须通过 PR</li><li><strong>PR 合并前必须通过 CI</strong>（单测、lint、构建）</li><li><strong>至少一个 Code Review 通过</strong>才能合并</li><li><strong>禁止 force push</strong></li></ul><p>这几条规则能防止 90% 的低级事故。</p><h3 id="PR-MR-规范"><a href="#PR-MR-规范" class="headerlink" title="PR&#x2F;MR 规范"></a>PR&#x2F;MR 规范</h3><p>一个好的 PR 应该：</p><ul><li><strong>只做一件事</strong>：一个 PR 只解决一个问题，越小越容易 review</li><li><strong>有清晰的描述</strong>：说明改了什么、为什么改、怎么测试</li><li><strong>关联 issue</strong>：在描述里加 <code>Closes #42</code>，合并后自动关闭 issue</li><li><strong>自己先 review 一遍</strong>：提交前 diff 看一眼，去掉调试代码和无关改动</li></ul><h3 id="gitignore-配置"><a href="#gitignore-配置" class="headerlink" title=".gitignore 配置"></a>.gitignore 配置</h3><p><code>.gitignore</code> 要在项目初始化时就配好，不要等到不该提交的文件已经进了仓库再处理。</p><p>常见需要忽略的内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"># 依赖</span><br><span class="line">node_modules/</span><br><span class="line">vendor/</span><br><span class="line"></span><br><span class="line"># 构建产物</span><br><span class="line">dist/</span><br><span class="line">build/</span><br><span class="line">*.class</span><br><span class="line">*.o</span><br><span class="line"></span><br><span class="line"># 环境配置（含密钥，绝对不能提交）</span><br><span class="line">.env</span><br><span class="line">.env.local</span><br><span class="line">*.pem</span><br><span class="line">*.key</span><br><span class="line"></span><br><span class="line"># IDE</span><br><span class="line">.idea/</span><br><span class="line">.vscode/</span><br><span class="line">*.swp</span><br><span class="line"></span><br><span class="line"># 系统文件</span><br><span class="line">.DS_Store</span><br><span class="line">Thumbs.db</span><br></pre></td></tr></table></figure><p><a href="https://www.toptal.com/developers/gitignore">gitignore.io</a> 可以按语言和框架自动生成。</p><hr><h2 id="常见问题处理"><a href="#常见问题处理" class="headerlink" title="常见问题处理"></a>常见问题处理</h2><h3 id="误操作恢复：git-reflog"><a href="#误操作恢复：git-reflog" class="headerlink" title="误操作恢复：git reflog"></a>误操作恢复：git reflog</h3><p><code>git reflog</code> 记录了 HEAD 的所有移动历史，包括 <code>reset --hard</code> 这类”危险操作”之后的状态。只要没有 <code>git gc</code>，几乎所有误操作都能恢复。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看 HEAD 的操作历史</span></span><br><span class="line">git reflog</span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出示例：</span></span><br><span class="line"><span class="comment"># abc1234 HEAD@&#123;0&#125;: reset: moving to HEAD~1</span></span><br><span class="line"><span class="comment"># def5678 HEAD@&#123;1&#125;: commit: feat: add login page</span></span><br><span class="line"><span class="comment"># ghi9012 HEAD@&#123;2&#125;: commit: fix: correct redirect URL</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复到 reset 之前的状态（def5678 是 reset 之前的 commit）</span></span><br><span class="line">git reset --hard def5678</span><br></pre></td></tr></table></figure><p><code>reset --hard</code> 把工作区搞没了？先跑 <code>git reflog</code>，找到那个 commit 的哈希，<code>reset --hard</code> 回去就行。</p><h3 id="解决合并冲突"><a href="#解决合并冲突" class="headerlink" title="解决合并冲突"></a>解决合并冲突</h3><p>冲突发生时 Git 会在文件里标记冲突区域：</p><figure class="highlight asciidoc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</span><br><span class="line"><span class="section">你的改动（当前分支）</span></span><br><span class="line"><span class="section">=======</span></span><br><span class="line">对方的改动（被合并的分支）</span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature/login</span><br></pre></td></tr></table></figure><p>处理步骤：</p><ol><li>用编辑器打开冲突文件，选择保留哪一方或手动合并</li><li>删除所有 <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>、<code>=======</code>、<code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> 标记</li><li><code>git add</code> 标记冲突已解决</li><li><code>git commit</code> 完成合并</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看哪些文件有冲突</span></span><br><span class="line">git status</span><br><span class="line"></span><br><span class="line"><span class="comment"># 解决后标记为已解决</span></span><br><span class="line">git add src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 完成合并提交</span></span><br><span class="line">git commit</span><br></pre></td></tr></table></figure><p>如果冲突太复杂，可以用 <code>git mergetool</code> 调出可视化合并工具（需要先配置）。</p><h3 id="修改最近一次-commit"><a href="#修改最近一次-commit" class="headerlink" title="修改最近一次 commit"></a>修改最近一次 commit</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 修改最近一次 commit 的 message</span></span><br><span class="line">git commit --amend -m <span class="string">&quot;feat: add Google OAuth login&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 把忘加的文件补进最近一次 commit（不改 message）</span></span><br><span class="line">git add forgotten_file.go</span><br><span class="line">git commit --amend --no-edit</span><br></pre></td></tr></table></figure><p><code>--amend</code> 会重写最近一次 commit，产生新的 SHA。<strong>已推送到远端的 commit 不要 amend</strong>，否则需要 force push，会影响其他人。</p><h3 id="修改指定-commit：git-rebase-i"><a href="#修改指定-commit：git-rebase-i" class="headerlink" title="修改指定 commit：git rebase -i"></a>修改指定 commit：git rebase -i</h3><p><code>--amend</code> 只能改最近一次 commit，修改更早的提交要用交互式 rebase。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 修改最近 3 个 commit（打开交互式编辑界面）</span></span><br><span class="line">git rebase -i HEAD~3</span><br></pre></td></tr></table></figure><p>弹出的编辑器里每行是一个 commit，把要修改的那行前面的 <code>pick</code> 改成对应指令：</p><table><thead><tr><th>指令</th><th>含义</th></tr></thead><tbody><tr><td><code>pick</code></td><td>保留这个 commit（默认）</td></tr><tr><td><code>reword</code></td><td>只修改 commit message</td></tr><tr><td><code>edit</code></td><td>暂停在这个 commit，可以修改内容</td></tr><tr><td><code>squash</code></td><td>把这个 commit 合并到上一个 commit</td></tr><tr><td><code>drop</code></td><td>删除这个 commit</td></tr></tbody></table><p>以修改 message 为例，把 <code>pick</code> 改成 <code>reword</code>，保存退出，Git 会再弹出编辑器让你改 message。</p><p>以修改内容为例，把 <code>pick</code> 改成 <code>edit</code>，保存退出后 Git 会暂停在那个 commit：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Git 提示你现在在指定 commit，可以修改文件</span></span><br><span class="line"><span class="comment"># 改完后：</span></span><br><span class="line">git add src/main.go</span><br><span class="line">git commit --amend --no-edit   <span class="comment"># 把改动追加到当前 commit</span></span><br><span class="line">git rebase --<span class="built_in">continue</span>          <span class="comment"># 继续完成剩余的 rebase</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>同样：已推送到远端的 commit 不要用 rebase -i 修改</strong>，会改写历史，影响所有人。</p></blockquote><h3 id="cherry-pick：把某个-commit-单独移植"><a href="#cherry-pick：把某个-commit-单独移植" class="headerlink" title="cherry-pick：把某个 commit 单独移植"></a>cherry-pick：把某个 commit 单独移植</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 把 abc1234 这个 commit 的改动应用到当前分支</span></span><br><span class="line">git cherry-pick abc1234</span><br><span class="line"></span><br><span class="line"><span class="comment"># 移植多个连续的 commit（包含 abc1234 到 def5678，注意要加 ^）</span></span><br><span class="line">git cherry-pick abc1234^..def5678</span><br></pre></td></tr></table></figure><p>场景：hotfix 在 main 分支修复了一个 bug，需要把这个修复也应用到正在维护的 release 分支，用 cherry-pick 比重新写一遍高效。</p>]]>
    </content>
    <id>https://blog.codewater.top/Git%E5%9F%BA%E7%A1%80%E6%95%99%E7%A8%8B-%E5%8E%9F%E7%90%86%E6%93%8D%E4%BD%9C%E4%B8%8E%E4%BC%81%E4%B8%9A%E5%AE%9E%E8%B7%B5/</id>
    <link href="https://blog.codewater.top/Git%E5%9F%BA%E7%A1%80%E6%95%99%E7%A8%8B-%E5%8E%9F%E7%90%86%E6%93%8D%E4%BD%9C%E4%B8%8E%E4%BC%81%E4%B8%9A%E5%AE%9E%E8%B7%B5/"/>
    <published>2026-07-05T02:00:00.000Z</published>
    <summary>Git 工作原理（三区域+对象模型）、常见命令速查、Git Flow 与 GitHub Flow 分支策略，以及企业中 commit message 规范、分支命名、PR 审查和 reflog 误操作恢复的完整实践指南。</summary>
    <title>Git 基础教程：原理、常见操作与企业实践规范</title>
    <updated>2026-07-05T02:00:00.000Z</updated>
  </entry>
</feed>
