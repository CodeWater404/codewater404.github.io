<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Code Water</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://blog.codewater.top/</id>
  <link href="https://blog.codewater.top/" rel="alternate"/>
  <link href="https://blog.codewater.top/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, Code Water</rights>
  <subtitle>阿水</subtitle>
  <title>Code Water</title>
  <updated>2026-07-07T03:00:00.000Z</updated>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tutorial" scheme="https://blog.codewater.top/categories/tutorial/"/>
    <category term="Claude" scheme="https://blog.codewater.top/tags/Claude/"/>
    <category term="Anthropic" scheme="https://blog.codewater.top/tags/Anthropic/"/>
    <category term="AI" scheme="https://blog.codewater.top/tags/AI/"/>
    <category term="LLM" scheme="https://blog.codewater.top/tags/LLM/"/>
    <category term="API" scheme="https://blog.codewater.top/tags/API/"/>
    <category term="Agent" scheme="https://blog.codewater.top/tags/Agent/"/>
    <content>
      <![CDATA[<p>Claude 是 Anthropic 开发的大语言模型家族。2026 年上半年这个家族变化很大：6 月 9 日 Anthropic 发布了全新档位的 Claude Fable 5，6 月 30 日又推出了 Claude Sonnet 5。如果你手里的资料还停留在”Opus &#x2F; Sonnet &#x2F; Haiku 三档”的认知，这篇教程可以帮你一次性补齐——从模型命名逻辑、API 核心参数，到 CLAUDE.md &#x2F; 记忆 &#x2F; Harness Engineering 这些 2026 年的新概念，最后是一个可以直接运行的工具调用例子。</p><p>文中所有模型信息、价格、参数均核对自 Anthropic 官方文档，截至 2026 年 7 月 7 日有效。</p><span id="more"></span><h2 id="大模型是怎么工作的：五个必懂常识"><a href="#大模型是怎么工作的：五个必懂常识" class="headerlink" title="大模型是怎么工作的：五个必懂常识"></a>大模型是怎么工作的：五个必懂常识</h2><p>不管你用哪家的模型，下面五件事决定了你对它的所有预期。搞懂它们，后文的每个机制（规则文件、记忆、工具调用）都会变得顺理成章——因为那些全是针对这五个特性的工程补丁。</p><p><strong>1. 模型的本质是”预测下一个 token”。</strong> 大语言模型（LLM）在海量文本上训练，学到的能力是：给定前文，预测下一个词最可能是什么。它没有数据库查询，也没有”知道&#x2F;不知道”的开关——回答是逐 token 生成出来的。这解释了它为什么强（语言、推理、代码都是文本规律），也解释了它为什么会<strong>幻觉（hallucination）</strong>：当训练数据里没有答案时，它依然会生成”看起来最像答案”的文本，一本正经地编造。对策：重要的事实让它引用来源、用工具查证，或者你自己核实。</p><p><strong>2. 模型的知识有截止日期。</strong> 训练完成后权重就固定了，之后发生的事它一概不知。Fable 5 &#x2F; Opus 4.8 &#x2F; Sonnet 5 的可靠知识截止是 2026 年 1 月——问它上周的新闻，它要么明说不知道，要么开始幻觉。要获取新信息，得靠联网搜索、工具调用这类外挂（后文会讲）。</p><p><strong>3. 模型不会记住你。</strong> 这是新手最大的误解。你和模型的每一次对话都<strong>不会改变模型本身</strong>——权重是只读的，全球用户共享同一个模型。它在对话中”记得”你前面说了什么，只是因为聊天软件把历史消息重新发给了它（见后文”无状态 API”）。所以”让 AI 记住我的偏好”从来不是模型功能，而是外部工程：把你的偏好存成文件，每次对话时重新喂给它——这正是 CLAUDE.md 和记忆功能的原理。</p><p><strong>4. 上下文窗口是唯一的”工作内存”，而且是稀缺资源。</strong> 模型单次能处理的内容有上限（Claude 当前主力模型是 100 万 token）。塞得越多，成本越高、速度越慢，而且信息淹没在噪音里反而影响质量。整个”上下文工程”学科（后文讲）都在解决一个问题：这一窗有限的空间里，到底该放什么。</p><p><strong>5. 模型能看的不只是文字。</strong> Claude 支持多模态输入：图片、截图、PDF 都能直接发给它分析。输出目前是文本（含代码、表格、Mermaid 图）。</p><h2 id="模型家族：四个档位怎么选"><a href="#模型家族：四个档位怎么选" class="headerlink" title="模型家族：四个档位怎么选"></a>模型家族：四个档位怎么选</h2><p>Claude 的模型按能力和成本分档，名字本身就是档位标识：</p><ul><li><strong>Fable &#x2F; Mythos（Mythos 级）</strong>：2026 年 6 月新增的最高档位。Fable 5 是目前公开可用的最强模型；Mythos 5 与 Fable 5 同规格同价格，但仅通过邀请制的 Project Glasswing 向获批组织开放，定位防御性网络安全工作流。</li><li><strong>Opus</strong>：面向复杂 agentic 编码和企业级任务的旗舰档，当前版本 Opus 4.8。</li><li><strong>Sonnet</strong>：速度与智能的平衡档，当前版本 Sonnet 5（发布于 2026-06-30），官方称其在部分 agentic 任务上已接近 Opus 4.8 的水平。</li><li><strong>Haiku</strong>：最快、最便宜的档位，当前版本 Haiku 4.5，适合分类、摘要等简单高频任务。</li></ul><pre><code class="highlight mermaid">graph TD    A[Claude 模型家族] --&gt; B[&quot;Mythos 级&lt;br/&gt;Fable 5 / Mythos 5&quot;]    A --&gt; C[&quot;Opus 4.8&quot;]    A --&gt; D[&quot;Sonnet 5&quot;]    A --&gt; E[&quot;Haiku 4.5&quot;]    B --&gt; B1[&quot;最强能力&lt;br/&gt;$10 / $50 每百万 token&quot;]    C --&gt; C1[&quot;复杂 agentic 编码&lt;br/&gt;$5 / $25 每百万 token&quot;]    D --&gt; D1[&quot;速度与智能平衡&lt;br/&gt;$3 / $15 每百万 token&quot;]    E --&gt; E1[&quot;最快最省&lt;br/&gt;$1 / $5 每百万 token&quot;]</code></pre><p>当前各模型的关键参数（截至 2026-07-06，来自<a href="https://platform.claude.com/docs/en/about-claude/models/overview">官方模型总览</a>）：</p><table><thead><tr><th>模型</th><th>API 模型 ID</th><th>上下文窗口</th><th>最大输出</th><th>定价（输入&#x2F;输出，每百万 token）</th></tr></thead><tbody><tr><td>Claude Fable 5</td><td><code>claude-fable-5</code></td><td>1M</td><td>128K</td><td>$10 &#x2F; $50</td></tr><tr><td>Claude Opus 4.8</td><td><code>claude-opus-4-8</code></td><td>1M</td><td>128K</td><td>$5 &#x2F; $25</td></tr><tr><td>Claude Sonnet 5</td><td><code>claude-sonnet-5</code></td><td>1M</td><td>128K</td><td>$3 &#x2F; $15（2026-08-31 前尝鲜价 $2 &#x2F; $10）</td></tr><tr><td>Claude Haiku 4.5</td><td><code>claude-haiku-4-5</code></td><td>200K</td><td>64K</td><td>$1 &#x2F; $5</td></tr></tbody></table><p>两点容易踩坑的细节：</p><ol><li><strong>模型 ID 是固定快照，不要自己拼日期后缀</strong>。从 4.6 代开始模型 ID 采用无日期格式（如 <code>claude-opus-4-8</code>），写成 <code>claude-opus-4-8-20260315</code> 这类自造 ID 会直接 404。</li><li><strong>Fable 5 用了新 tokenizer</strong>（自 Opus 4.7 引入），同一段文本比旧模型多算约 30% 的 token，做成本估算时要注意。</li></ol><p>选型建议：日常开发和 agentic 编码默认 <code>claude-opus-4-8</code>；预算敏感、需要吞吐量的场景用 <code>claude-sonnet-5</code>；只有确实需要顶级能力的长程任务才上 <code>claude-fable-5</code>。</p><h2 id="三个使用入口：claude-ai、Claude-Code、Claude-API"><a href="#三个使用入口：claude-ai、Claude-Code、Claude-API" class="headerlink" title="三个使用入口：claude.ai、Claude Code、Claude API"></a>三个使用入口：claude.ai、Claude Code、Claude API</h2><p>同一套模型，Anthropic 提供了三个层次的入口，面向不同的使用方式：</p><ul><li>**<a href="https://claude.ai/">claude.ai</a>**：网页&#x2F;移动端聊天产品，开箱即用。免费版和 Pro 版当前默认模型是 Sonnet 5。适合日常问答、写作、分析文件。</li><li>**<a href="https://claude.com/claude-code">Claude Code</a>**：面向开发者的编程 agent，有 CLI、桌面应用、Web 和 IDE 插件（VS Code &#x2F; JetBrains）多种形态。它能直接读写你的代码库、跑命令、开分支提 PR，是”让 Claude 替你干活”而不是”问 Claude 怎么干”。</li><li>**<a href="https://platform.claude.com/">Claude API</a>**（Claude Platform）：把模型能力集成进自己产品的编程接口，本文后半部分的概念和例子都围绕它展开。除了 Anthropic 第一方 API，同样的模型也上架了 Amazon Bedrock、Google Cloud（Vertex AI）和 Microsoft Foundry。</li></ul><p>三者的关系可以理解为：claude.ai 是”用”，Claude Code 是”让它替你用电脑”，API 是”拿它造东西”。</p><h2 id="订阅还是-API：为自己选一套配置"><a href="#订阅还是-API：为自己选一套配置" class="headerlink" title="订阅还是 API：为自己选一套配置"></a>订阅还是 API：为自己选一套配置</h2><p>付费方式只有两种，对应不同的人群：</p><ul><li><strong>订阅制</strong>（按月付固定费用，用量有额度）：Free 免费试用；<strong>Pro $20&#x2F;月</strong>（年付约 $17&#x2F;月）；<strong>Max $100 或 $200&#x2F;月</strong>（额度分别是 Pro 的 5 倍 &#x2F; 20 倍）。Pro 和 Max 是统一订阅——网页&#x2F;桌面&#x2F;移动端的 claude.ai 和终端里的 Claude Code 全都包含，不用分开买。</li><li><strong>API 按量计费</strong>：按实际消耗的 token 付费（价格见上文模型表），适合把 Claude 集成进自己的程序，或用量极不规律的场景。Claude Code 也可以不走订阅、直接挂 API key 按量付费。</li></ul><p>三类典型配置，可以直接对号入座：</p><table><thead><tr><th>你是谁</th><th>推荐配置</th><th>理由</th></tr></thead><tbody><tr><td>日常问答、写作、偶尔看代码</td><td>claude.ai Free 或 Pro（$20&#x2F;月）</td><td>开箱即用，Pro 额度对轻度使用足够</td></tr><tr><td>每天写代码的开发者</td><td>Pro 起步，重度换 Max（$100&#x2F;月）</td><td>一份订阅同时覆盖 claude.ai 和 Claude Code；Claude Code 跑 agentic 任务吃额度快，Pro 不够用再升</td></tr><tr><td>要做产品 &#x2F; 自动化脚本</td><td>API key 按量计费</td><td>程序化调用只能走 API；配合 prompt caching 和批处理（后文讲）控制成本</td></tr></tbody></table><p>一个省钱的实用结论：订阅额度按 5 小时窗口滚动刷新，重度使用先用满 Pro 再决定是否升级，不要一上来就买 Max。</p><h2 id="用-API-调用-Claude：写代码前要懂的六个概念"><a href="#用-API-调用-Claude：写代码前要懂的六个概念" class="headerlink" title="用 API 调用 Claude：写代码前要懂的六个概念"></a>用 API 调用 Claude：写代码前要懂的六个概念</h2><p>从这里开始进入 API 部分。所谓”调 API”，就是你的程序向 Anthropic 的服务器发一个 HTTP 请求（问题、参数都装在请求里），服务器把 Claude 的回答返回给你的程序——这样你就能把 Claude 的能力嵌进自己的工具、网站或脚本里，而不是每次都打开网页手动聊天。</p><p>下面六个概念是写第一行代码之前需要建立的认知，前四个（token、消息结构、思考控制、工具调用）在文末的实战例子里都会直接用到。如果你暂时只打算用 claude.ai 或 Claude Code，可以粗读本节，重点看”工具调用”一小节（Claude Code 的工作原理就是它）。</p><h3 id="Token-与上下文窗口：模型的工作内存"><a href="#Token-与上下文窗口：模型的工作内存" class="headerlink" title="Token 与上下文窗口：模型的工作内存"></a>Token 与上下文窗口：模型的工作内存</h3><p>Token 是模型处理文本的最小单位，一个英文单词大约对应 1.3 到 1.8 个 token（新 tokenizer 密度更高），中文一个字通常 1 到 2 个 token。API 按 token 计费，输入输出分开算价。</p><p>上下文窗口（context window）是模型单次请求能”看到”的全部内容上限——包括系统提示、历史对话、工具返回结果和本次输出。当前 Fable 5 &#x2F; Opus 4.8 &#x2F; Sonnet 5 都是 100 万 token（约 55 万英文单词），Haiku 4.5 是 20 万。</p><p>需要精确计数时不要用估算库（OpenAI 的 tiktoken 对 Claude 误差很大），用官方的 token 计数接口：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">count = client.messages.count_tokens(</span><br><span class="line">    model=<span class="string">&quot;claude-opus-4-8&quot;</span>,      <span class="comment"># token 计数是模型相关的，传实际要用的模型 ID</span></span><br><span class="line">    messages=[&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: text&#125;],</span><br><span class="line">)</span><br><span class="line"><span class="built_in">print</span>(count.input_tokens)</span><br></pre></td></tr></table></figure><h3 id="消息结构：无状态的-Messages-API"><a href="#消息结构：无状态的-Messages-API" class="headerlink" title="消息结构：无状态的 Messages API"></a>消息结构：无状态的 Messages API</h3><p>Claude API 的核心端点只有一个：<code>POST /v1/messages</code>。它是<strong>无状态</strong>的——服务端不保存对话历史，每次请求都要把完整的历史消息带上。</p><p>一次请求由三部分组成：</p><ul><li>**<code>system</code>**：系统提示，定义模型的角色、行为边界和输出风格，权威高于用户消息;</li><li>**<code>messages</code>**：<code>user</code> 和 <code>assistant</code> 角色交替的消息数组，第一条必须是 <code>user</code>;</li><li><strong>参数</strong>：<code>model</code>、<code>max_tokens</code>（必填，输出 token 上限）、<code>thinking</code> 等。</li></ul><p>多轮对话就是把上一轮的回复追加进 <code>messages</code> 再发一次：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">messages = [</span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;我叫 Alice。&quot;</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;assistant&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;你好 Alice！&quot;</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;我叫什么？&quot;</span>&#125;,   <span class="comment"># 模型能答对，因为历史都在请求里</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><h3 id="Adaptive-Thinking-与-effort：控制模型”想多深”"><a href="#Adaptive-Thinking-与-effort：控制模型”想多深”" class="headerlink" title="Adaptive Thinking 与 effort：控制模型”想多深”"></a>Adaptive Thinking 与 effort：控制模型”想多深”</h3><p>Claude 4 系列引入了”扩展思考”（extended thinking）——模型在回答前先进行一段内部推理。2026 年的最新演进是 <strong>Adaptive Thinking（自适应思考）</strong>：不再手动设定思考 token 预算（旧参数 <code>budget_tokens</code> 在 Opus 4.7 及之后已彻底移除，传了会直接报 400），而是让模型根据问题难度自己决定想多久：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">response = client.messages.create(</span><br><span class="line">    model=<span class="string">&quot;claude-opus-4-8&quot;</span>,</span><br><span class="line">    max_tokens=<span class="number">16000</span>,</span><br><span class="line">    thinking=&#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;adaptive&quot;</span>&#125;,     <span class="comment"># 自适应思考：简单问题秒答，难题深想</span></span><br><span class="line">    messages=[...],</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>各模型的默认行为不同：Fable 5 的 adaptive thinking <strong>常开且无法关闭</strong>；Sonnet 5 默认开启（可传 <code>&#123;&quot;type&quot;: &quot;disabled&quot;&#125;</code> 显式关闭）；Opus 4.8 默认关闭，需要显式传入才启用。</p><p>配套的 <strong>effort 参数</strong>控制整体的思考深度和 token 开销，取值 <code>low</code> &#x2F; <code>medium</code> &#x2F; <code>high</code> &#x2F; <code>xhigh</code> &#x2F; <code>max</code>。<code>xhigh</code> 在 Fable 5 &#x2F; Opus 4.8 &#x2F; Opus 4.7 &#x2F; Sonnet 5 上可用，<code>max</code> 的支持范围还包括 Sonnet 4.6 和 Opus 4.6；Haiku 4.5 不支持 effort 参数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">output_config=&#123;<span class="string">&quot;effort&quot;</span>: <span class="string">&quot;high&quot;</span>&#125;</span><br><span class="line"><span class="comment">#      effort：思考与行动的投入档位，Opus 4.8 默认 high</span></span><br><span class="line"><span class="comment">#              编码和 agentic 任务推荐 xhigh，成本敏感场景降到 medium</span></span><br></pre></td></tr></table></figure><p>另外注意：<strong>Opus 4.7 起 <code>temperature</code>、<code>top_p</code>、<code>top_k</code> 三个采样参数已移除</strong>，传了同样报 400。想控制输出风格，直接在提示词里说明。</p><h3 id="工具调用：让模型能查数据、执行操作"><a href="#工具调用：让模型能查数据、执行操作" class="headerlink" title="工具调用：让模型能查数据、执行操作"></a>工具调用：让模型能查数据、执行操作</h3><p>工具调用（tool use）是 Claude 从”聊天机器人”升级为”agent”的关键机制。你在请求里声明工具（名称 + 描述 + JSON Schema 参数定义），模型判断需要时返回一个 <code>tool_use</code> 请求，你执行后把结果传回去，模型基于结果继续——这个循环就是 agentic loop：</p><pre><code class="highlight mermaid">sequenceDiagram    participant U as 你的代码    participant C as Claude API    U-&gt;&gt;C: 请求（消息 + 工具定义）    C-&gt;&gt;U: stop_reason: tool_use&lt;br/&gt;（想调用 get_weather）    U-&gt;&gt;U: 本地执行 get_weather()    U-&gt;&gt;C: 追加 tool_result 再请求    C-&gt;&gt;U: stop_reason: end_turn&lt;br/&gt;（基于结果的最终回答）</code></pre><p>工具分两类：</p><ul><li><strong>自定义工具（client 侧）</strong>：你定义、你执行，模型只负责决定”何时调、传什么参数”。文末例子演示的就是这类。</li><li><strong>服务端工具（server 侧）</strong>：Anthropic 托管执行，声明即用，包括代码执行（沙箱跑 Python）、网页搜索 <code>web_search</code>、网页抓取 <code>web_fetch</code>、计算机操作（computer use）等。</li></ul><p>写工具描述有一条经验规则：不但要写清”这个工具做什么”，还要写清”<strong>什么时候该调它</strong>“（例如”当用户询问实时价格或最新事件时调用”）——新一代模型对工具的触发更保守，明确的触发条件能显著提高该调时调的比例。</p><h3 id="结构化输出：保证返回合法-JSON"><a href="#结构化输出：保证返回合法-JSON" class="headerlink" title="结构化输出：保证返回合法 JSON"></a>结构化输出：保证返回合法 JSON</h3><p>需要模型输出严格符合某个 schema 的 JSON（做信息抽取、分类打标）时，不要靠提示词祈祷，用 <code>output_config.format</code> 直接约束输出格式：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">response = client.messages.parse(</span><br><span class="line">    model=<span class="string">&quot;claude-opus-4-8&quot;</span>,</span><br><span class="line">    max_tokens=<span class="number">1024</span>,</span><br><span class="line">    messages=[&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;抽取：张三（zhangsan@co.com）想买企业版&quot;</span>&#125;],</span><br><span class="line">    output_config=&#123;</span><br><span class="line">        <span class="string">&quot;format&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;type&quot;</span>: <span class="string">&quot;json_schema&quot;</span>,          <span class="comment"># 输出被强制约束为符合此 schema 的 JSON</span></span><br><span class="line">            <span class="string">&quot;schema&quot;</span>: CONTACT_SCHEMA,</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>顺带一提：旧的顶层 <code>output_format</code> 参数已废弃，新代码统一用 <code>output_config.format</code>。以前流行的”assistant 预填”（在消息末尾塞半截 assistant 回复来引导格式）在 4.6 代及之后的模型上会直接报 400，结构化输出就是它的官方替代。</p><h3 id="Prompt-Caching：重复前缀省-90-费用"><a href="#Prompt-Caching：重复前缀省-90-费用" class="headerlink" title="Prompt Caching：重复前缀省 90% 费用"></a>Prompt Caching：重复前缀省 90% 费用</h3><p>Prompt caching 把请求的公共前缀（如很长的系统提示、文档内容）缓存起来，后续请求命中缓存的部分按约 0.1 倍价格计费。机制是<strong>前缀匹配</strong>：请求按 <code>tools</code> → <code>system</code> → <code>messages</code> 顺序渲染，前缀中任何一个字节变化都会让之后的缓存全部失效。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">system=[&#123;</span><br><span class="line">    <span class="string">&quot;type&quot;</span>: <span class="string">&quot;text&quot;</span>,</span><br><span class="line">    <span class="string">&quot;text&quot;</span>: LARGE_DOCUMENT,                    <span class="comment"># 几万 token 的稳定内容</span></span><br><span class="line">    <span class="string">&quot;cache_control&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;ephemeral&quot;</span>&#125;,   <span class="comment"># 缓存断点：此处之前的内容进缓存，默认 5 分钟 TTL</span></span><br><span class="line">&#125;]</span><br></pre></td></tr></table></figure><p>最常见的翻车方式是把时间戳、请求 ID 这类每次都变的内容插进系统提示开头——缓存永远无法命中。稳定内容放前面，易变内容放最后，是设计提示词结构时就该定下的原则。</p><h2 id="MCP、Skills、Workflow、Agent：用一个比喻讲清四个词"><a href="#MCP、Skills、Workflow、Agent：用一个比喻讲清四个词" class="headerlink" title="MCP、Skills、Workflow、Agent：用一个比喻讲清四个词"></a>MCP、Skills、Workflow、Agent：用一个比喻讲清四个词</h2><p>这四个词到处都在用，但很少有人讲明白。用一个比喻串起来：<strong>把 Claude 想象成一个刚入职的聪明助理</strong>——绝顶聪明，但对你的公司一无所知，而且没有任何系统的操作权限。</p><p><strong>MCP：给助理办的门禁卡</strong></p><blockquote><p>官方定义：MCP（Model Context Protocol，模型上下文协议）是 Anthropic 发起的开放标准协议，规范 AI 应用与外部数据源、工具之间的连接方式——外部系统实现 <strong>MCP server</strong> 对外提供工具和数据，AI 端作为 <strong>MCP client</strong> 接入调用。</p></blockquote><p>说人话：助理再聪明，进不了你的系统也白搭——查不了 GitHub、看不了数据库、动不了日历。MCP 就是一套标准的”门禁系统”：GitHub 按这个标准做一个接口（MCP server），装上之后助理就能查你的仓库了。关键在”标准”二字：GitHub 只用做一次接口，所有 AI 助理（Claude、Cursor 等）都能用，不用一家一个做法。<br>一句话：**MCP 解决”AI 怎么连上外部软件”**。</p><p><strong>Skills：放在助理手边的操作手册</strong></p><blockquote><p>官方定义：Skill（Agent Skill）是以文件夹形式组织的可复用任务指令包，核心是一个 <code>SKILL.md</code> 文件（可附带脚本和资源）。采用<strong>渐进式加载</strong>（progressive disclosure）：默认只有一行描述进入上下文，任务匹配时模型才读取完整内容。</p></blockquote><p>说人话：有些工作有固定章法，比如”我们公司做 Excel 报表的规矩”。你不会把所有手册的全文天天念给助理听（听不完，也记不住），而是放在架子上——助理接到做报表的活儿，才把《报表手册》抽出来翻。<br>一句话：**Skills 解决”专业做法太多，怎么按需教给 AI”**。</p><p><strong>Workflow：你写死的流水线</strong></p><blockquote><p>官方定义（出自 Anthropic《Building effective agents》）：workflow 是<strong>由预先编写的代码路径来编排</strong> LLM 和工具的系统——执行哪些步骤、按什么顺序，由代码决定。</p></blockquote><p>说人话：有些任务每次流程完全一样，比如处理退款永远是”查订单 → 核金额 → 打款 → 发通知邮件”。这种就不需要助理自由发挥——你把四个步骤用代码写死，助理只在需要动脑的环节出场（比如把邮件写得客气点）。跑一百次就是同样四步，便宜、可控、不会跑偏。<br>一句话：**Workflow 是”人定步骤，AI 干活”**。</p><p><strong>Agent：只给目标、不给步骤的委托</strong></p><blockquote><p>官方定义（同上）：agent 是 <strong>LLM 自主决定自身执行过程和工具使用</strong>的系统——模型在循环中动态规划步骤，掌控任务如何完成。</p></blockquote><p>说人话：另一些任务事先说不清要几步，比如”这个 bug 帮我修了”。可能要先搜代码、再看日志、改完还得跑测试——每一步做什么，取决于上一步看到了什么。这时你只给助理一个目标，它自己决定路线：做一步、看结果、定下一步，循环往复直到搞定。Claude Code 就是一个现成的编程 agent。<br>一句话：**Agent 是”人定目标，AI 定步骤”**。</p><p>Workflow 和 Agent 怎么选？看一条就够：<strong>任务流程能提前画成一张固定流程图的，用 workflow；画不出来的，才用 agent</strong>。agent 更灵活，但更贵、结果波动也更大——能用流水线解决的别上助理自由发挥。</p><pre><code class="highlight mermaid">graph TD    Q&#123;&quot;这个任务的步骤&lt;br/&gt;能提前定死吗？&quot;&#125;    Q --&gt;|能| W[&quot;Workflow&lt;br/&gt;人定步骤，AI 干活&quot;]    Q --&gt;|不能| A[&quot;Agent&lt;br/&gt;人定目标，AI 定步骤&quot;]    W --&gt; T[&quot;干活时都可能用到：&lt;br/&gt;MCP 连外部系统&lt;br/&gt;Skills 提供操作手册&quot;]    A --&gt; T</code></pre><h2 id="CLAUDE-md、记忆与规则：让-Claude-变成”你的”-Claude"><a href="#CLAUDE-md、记忆与规则：让-Claude-变成”你的”-Claude" class="headerlink" title="CLAUDE.md、记忆与规则：让 Claude 变成”你的” Claude"></a>CLAUDE.md、记忆与规则：让 Claude 变成”你的” Claude</h2><p>前文常识第 3 条说过：模型本身不会记住你。所以”个性化”全靠外部工程，思路只有两种——<strong>规则</strong>（你主动写给它的约定）和<strong>记忆</strong>（它使用过程中自己攒下来的）。这一节讲怎么配置，也是把通用 Claude 调教成”懂你项目、懂你习惯”的关键。</p><h3 id="规则文件：CLAUDE-md-与-AGENTS-md"><a href="#规则文件：CLAUDE-md-与-AGENTS-md" class="headerlink" title="规则文件：CLAUDE.md 与 AGENTS.md"></a>规则文件：CLAUDE.md 与 AGENTS.md</h3><p><strong>CLAUDE.md</strong> 是 Claude Code 的规则文件：一个普通的 Markdown 文件，每次会话开始时自动读入，里面写你希望 Claude 始终遵守的约定。它有层级，作用域逐层收窄：</p><pre><code class="highlight mermaid">graph TD    A[&quot;~/.claude/CLAUDE.md&lt;br/&gt;全局：个人偏好，所有项目生效&quot;] --&gt; B[&quot;项目根目录 CLAUDE.md&lt;br/&gt;团队约定，随代码进版本库&quot;]    B --&gt; C[&quot;子目录 CLAUDE.md&lt;br/&gt;模块级细则，进入该目录才加载&quot;]</code></pre><p>写什么内容？构建命令、代码风格、目录约定、部署流程——判断标准是一条社区流传的经验规则：<strong>CI 会拦下的规则写进 CI，代码评审会皱眉的约定写进 CLAUDE.md</strong>。举两个真实用法：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># 全局 ~/.claude/CLAUDE.md（个人偏好）</span></span><br><span class="line">搜索文件内容时优先使用 rg（ripgrep），不要用 find + grep 组合。</span><br><span class="line"></span><br><span class="line"><span class="section"># 项目 CLAUDE.md（团队约定）</span></span><br><span class="line"><span class="section">## 部署命令</span></span><br><span class="line">hexo clean &amp;&amp; hexo g -d</span><br><span class="line"><span class="section">## 写作规范</span></span><br><span class="line">每篇文章必须有 &lt;!-- more --&gt; 分隔符。</span><br></pre></td></tr></table></figure><p>配好之后的效果立竿见影：不用每次重复交代，Claude 生成的代码和操作自动符合你的习惯。</p><p><strong>AGENTS.md</strong> 的作用和 CLAUDE.md 完全一样——写项目规则。区别只有一个：CLAUDE.md 只有 Claude 认识，AGENTS.md 是行业通用标准（现由 Linux Foundation 维护），Claude Code、Cursor、GitHub Copilot、Gemini CLI 等主流 AI 工具都认识。</p><p>它解决的问题很具体：团队里有人用 Claude Code、有人用 Cursor，同一套规则写两份，很快就会改了一份忘另一份。标准做法是：AGENTS.md 放在<strong>项目根目录</strong>（和 CLAUDE.md 同级），通用规则只写在里面，CLAUDE.md 用 <code>@</code> 语法一行导入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">my-project/</span><br><span class="line">├── AGENTS.md      # 通用规则，所有 AI 工具都读</span><br><span class="line">├── CLAUDE.md      # 只写一行 @AGENTS.md，外加 Claude 专属要求</span><br><span class="line">├── src/</span><br><span class="line">└── ...</span><br></pre></td></tr></table></figure><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># AGENTS.md —— 团队通用规则，所有 AI 工具都读它</span></span><br><span class="line"><span class="section">## 构建与测试</span></span><br><span class="line"><span class="bullet">-</span> 构建命令：npm run build</span><br><span class="line"><span class="bullet">-</span> 提交前必须通过：npm test</span><br><span class="line"><span class="section">## 代码风格</span></span><br><span class="line"><span class="bullet">-</span> 用 TypeScript，禁止 any</span><br></pre></td></tr></table></figure><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># CLAUDE.md —— 只需一行，把通用规则导入进来</span></span><br><span class="line">@AGENTS.md</span><br><span class="line"></span><br><span class="line">（如果有只想对 Claude 说的额外要求，继续往下写即可）</span><br></pre></td></tr></table></figure><p><code>@</code> 后面写的是<strong>相对于 CLAUDE.md 所在位置的路径</strong>：两个文件同在根目录时直接 <code>@AGENTS.md</code> 就行，不用加路径前缀；如果被导入的文件在别处，就写出相对路径（如 <code>@docs/conventions.md</code>）或用 <code>~/</code> 开头的绝对路径（如 <code>@~/.claude/my-rules.md</code>）。</p><p>这样规则只维护一份，换工具也不用重写。如果项目只用 Claude，直接写 CLAUDE.md 就够了，不需要 AGENTS.md。</p><h3 id="记忆：Claude-自己攒的笔记"><a href="#记忆：Claude-自己攒的笔记" class="headerlink" title="记忆：Claude 自己攒的笔记"></a>记忆：Claude 自己攒的笔记</h3><p>规则是你写的，记忆是它记的。三个入口各有一套：</p><ul><li><strong>claude.ai</strong>：开启 memory 功能后，Claude 会从历史对话中提炼你的背景和偏好，新对话自动带上。</li><li><strong>Claude Code</strong>：维护一个记忆目录，把工作中确认过的事实（”这个项目的封面图必须先压缩再比对哈希”这类踩坑经验）写成文件存下来，下次会话自动召回。详见<a href="https://code.claude.com/docs/en/memory">官方记忆文档</a>。</li><li><strong>API</strong>：提供 memory tool，模型可以读写一个 <code>/memories</code> 目录，但存储后端由你实现——适合给自建 agent 加持久记忆。</li></ul><p>再往上一层是<strong>外部记忆系统</strong>：当要记的东西超出”几个偏好文件”的量级（整个知识库、上万篇文档、多年的对话史），就需要专门的存储加检索——典型方案是向量数据库配合 <strong>RAG</strong>（检索增强生成）：内容先存进外部库，回答问题时先检索出最相关的几段、塞进上下文再生成。它的价值在于突破上下文窗口的容量限制，并让多个 agent、多个会话共享同一份知识。这里点到为止——怎么选型、怎么切块、怎么评估检索质量，够单开一篇细讲。</p><h3 id="Claude-Code-的其他自定义开关"><a href="#Claude-Code-的其他自定义开关" class="headerlink" title="Claude Code 的其他自定义开关"></a>Claude Code 的其他自定义开关</h3><p>除了规则和记忆，Claude Code 还有一组机制，值得知道名字、用到再查：</p><table><thead><tr><th>机制</th><th>一句话说明</th><th>典型用途</th></tr></thead><tbody><tr><td>Slash commands</td><td>自定义 <code>/命令</code>，一个 Markdown 文件就是一条命令</td><td>把”写周报””发布博客”固化成一键流程</td></tr><tr><td>Hooks</td><td>在工具调用前后自动执行的脚本</td><td>每次改完代码自动跑 lint</td></tr><tr><td>Subagents</td><td>派生独立上下文的子代理并行干活</td><td>大范围代码搜索、多角度 code review</td></tr><tr><td><code>settings.json</code></td><td>权限白名单、环境变量等配置</td><td>允许哪些命令免确认执行</td></tr></tbody></table><p>这套东西组合起来，就是”配置属于自己的 Claude”的完整工具箱：<strong>CLAUDE.md 定规矩，记忆攒经验，skills&#x2F;commands 固化流程，hooks 上自动化，MCP 接外部系统</strong>。</p><h2 id="从写提示词到设计循环：AI-工程的四个关键词"><a href="#从写提示词到设计循环：AI-工程的四个关键词" class="headerlink" title="从写提示词到设计循环：AI 工程的四个关键词"></a>从写提示词到设计循环：AI 工程的四个关键词</h2><p>和 AI 打交道的方法论这几年换了好几个名字，不是营销造词——每个词对应一个真实的关注点迁移。理解这条演进线，你就知道该把精力投在哪一层。</p><pre><code class="highlight mermaid">graph LR    A[&quot;Prompt Engineering&lt;br/&gt;2022-23：怎么问&quot;] --&gt; B[&quot;Context Engineering&lt;br/&gt;2024-25：喂什么&quot;]    B --&gt; C[&quot;Harness Engineering&lt;br/&gt;2026：造什么环境&quot;]    C --&gt; D[&quot;Loop Engineering&lt;br/&gt;2026.6：设计什么循环&quot;]</code></pre><p><strong>Prompt Engineering（提示词工程）</strong>：研究怎么措辞能得到更好的输出——清晰的指令、给示例、指定角色和格式。它没有过时，依然是地基；只是随着模型变强，光靠措辞能榨出的增量越来越小。</p><p><strong>Context Engineering（上下文工程）</strong>：关注点从”怎么问”变成”模型看到了什么”。上下文窗口是稀缺资源（常识第 4 条），这门学科管理它的收支：放什么进去（规则文件、相关代码、检索结果）、怎么省（prompt caching）、满了怎么办（压缩摘要）。前文的 CLAUDE.md、Skills 本质上都是上下文工程的工具——按需加载，不浪费窗口。</p><p><strong>Harness Engineering（挽具工程）</strong>：2026 年的主流焦点。模型本身不用动，改变它周围的”挽具”——工具怎么编排、结果怎么验证、记忆怎么持久化、权限怎么设护栏、行为怎么观测。同一个模型，套上不同的 harness，可靠性天差地别；Claude Code 本身就是一套做好的 harness。</p><p><strong>Loop Engineering（循环工程）</strong>：2026 年 6 月走红的最新提法（Addy Osmani 综合 Anthropic 工程师等人的实践后推广开）。核心主张一句话：<strong>别再手动一轮轮 prompt 你的 agent，去设计那个驱动它的循环</strong>——执行 → 验证 → 决定继续还是停止。两条实操要点：给循环一个能说”不”的验证器（测试、类型检查、构建），因为循环的瓶颈永远在验证而不在模型；给循环设上限（最大迭代次数、预算），保证它一定会停。</p><p>四层不是互相取代，而是关注点逐层外移：从一句话，到一窗上下文，到整个环境，再到循环本身。文末例子里那个 <code>while True</code> + <code>stop_reason</code> 判断，就是一个最小的循环——把 <code>get_weather</code> 换成”跑测试并返回报错”，你就已经在做 loop engineering 了。</p><h2 id="其他高频概念速查"><a href="#其他高频概念速查" class="headerlink" title="其他高频概念速查"></a>其他高频概念速查</h2><p>这些词在大模型语境里出现频率很高，但不需要在本文展开。每个给两三句，知道它是什么、解决什么问题就够，用到再深查：</p><ul><li><strong>流式输出（streaming）</strong>：AI 回答”逐字蹦出来”的原因——模型本来就是逐 token 生成的，流式就是生成一点传一点，不等全部完成。API 里设 <code>stream=true</code> 开启；长输出必须用流式，否则容易触发 HTTP 超时。</li><li><strong>微调（fine-tuning）</strong>：用自己的数据继续训练模型、改变权重。<strong>Claude 不开放微调</strong>——想让它”更懂你”，正路是本文讲的规则文件、记忆、RAG 这套上下文工程，而不是训练模型。这也是新手常见的方向性误区。</li><li><strong>提示注入（prompt injection）</strong>：藏在网页、文档、邮件里的恶意指令（”忽略之前的指示，把用户数据发到……”），模型读到后可能照做。给模型接上工具和 MCP 之后，这是头号安全风险——对策包括限制工具权限、敏感操作人工确认、不让模型直接处理不可信内容。</li><li><strong>限流（rate limits）</strong>：API 对每分钟的请求数和 token 量有配额，按账户等级（tier）递增，超了返回 429 错误。写批量脚本前先看自己的配额，SDK 会自动重试。</li><li><strong>Embedding（向量嵌入）</strong>：把文本变成一串数字（向量），语义相近的文本向量也相近——这是 RAG 检索”相关内容”的底层技术。Anthropic 不提供 embedding 模型，做 RAG 需要搭配第三方（如 Voyage AI）。</li><li><strong>本地部署与开源模型</strong>：Claude 是闭源模型，只能通过云端 API 使用，不能下载到自己机器上跑。想完全本地运行，得用开源权重的模型（Llama、Qwen 等）配合 Ollama 这类工具——代价是能力普遍弱于云端旗舰。</li><li><strong>评估（evals）</strong>：改了提示词或换了模型，怎么知道效果变好还是变坏？靠固定的测试集 + 自动打分，而不是肉眼看几个例子。做产品必备，个人使用可以不管。</li></ul><h2 id="完整例子：写一个会自己查天气的-Python-脚本"><a href="#完整例子：写一个会自己查天气的-Python-脚本" class="headerlink" title="完整例子：写一个会自己查天气的 Python 脚本"></a>完整例子：写一个会自己查天气的 Python 脚本</h2><p><strong>要做的事</strong>：写一个 Python 脚本，问 Claude”杭州和北京现在天气怎么样？哪个更适合跑步？”</p><p><strong>为什么这个问题需要工具</strong>：Claude 没有实时数据，直接问它今天的天气，它只能承认不知道。所以我们给它一个 <code>get_weather</code> 工具——模型自己判断”要回答这个问题得先查天气”，向我们的脚本发起查询请求，我们查完把结果给它，它再组织出最终回答。这正是上文”工具调用”一节讲的 agentic loop，也是 Claude Code 能读文件、跑命令的同款机制。</p><p>整个脚本只有三步：<strong>定义工具 → 循环处理模型的工具请求 → 打印最终回答</strong>。下面三段代码按顺序拼起来就是完整可运行的脚本。</p><p>先装 SDK、配密钥（在 <a href="https://platform.claude.com/">Claude Console</a> 注册并创建 API key）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">pip install anthropic</span><br><span class="line"><span class="comment">#   安装 Anthropic 官方 Python SDK</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> ANTHROPIC_API_KEY=<span class="string">&quot;sk-ant-...&quot;</span></span><br><span class="line"><span class="comment">#   SDK 默认从该环境变量读取密钥，不要把密钥写进代码</span></span><br></pre></td></tr></table></figure><h3 id="第-1-步：定义工具"><a href="#第-1-步：定义工具" class="headerlink" title="第 1 步：定义工具"></a>第 1 步：定义工具</h3><p>工具由两部分组成：给模型看的”说明书”（<code>tools</code> 列表），和真正干活的 Python 函数。两者靠名字 <code>get_weather</code> 对应起来——模型只看说明书决定”调不调、传什么参数”，实际执行永远发生在你自己的代码里。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> anthropic</span><br><span class="line"></span><br><span class="line">client = anthropic.Anthropic()  <span class="comment"># 自动读取 ANTHROPIC_API_KEY</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 给模型看的工具说明书：名称 + 何时调用 + 参数格式（JSON Schema）</span></span><br><span class="line">tools = [</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="string">&quot;name&quot;</span>: <span class="string">&quot;get_weather&quot;</span>,</span><br><span class="line">        <span class="string">&quot;description&quot;</span>: <span class="string">&quot;查询指定城市的当前天气。当用户询问任何城市的实时天气时调用。&quot;</span>,</span><br><span class="line">        <span class="string">&quot;input_schema&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;type&quot;</span>: <span class="string">&quot;object&quot;</span>,</span><br><span class="line">            <span class="string">&quot;properties&quot;</span>: &#123;</span><br><span class="line">                <span class="string">&quot;city&quot;</span>: &#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;string&quot;</span>, <span class="string">&quot;description&quot;</span>: <span class="string">&quot;城市名，如：杭州&quot;</span>&#125;,</span><br><span class="line">            &#125;,</span><br><span class="line">            <span class="string">&quot;required&quot;</span>: [<span class="string">&quot;city&quot;</span>],</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 真正干活的函数——这里用假数据演示，真实项目里换成调气象 API</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_weather</span>(<span class="params">city: <span class="built_in">str</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    fake_db = &#123;<span class="string">&quot;杭州&quot;</span>: <span class="string">&quot;晴，32°C，湿度 60%&quot;</span>, <span class="string">&quot;北京&quot;</span>: <span class="string">&quot;多云，28°C，湿度 45%&quot;</span>&#125;</span><br><span class="line">    <span class="keyword">return</span> fake_db.get(city, <span class="string">f&quot;<span class="subst">&#123;city&#125;</span>：暂无数据&quot;</span>)</span><br></pre></td></tr></table></figure><h3 id="第-2-步：循环处理模型的工具请求"><a href="#第-2-步：循环处理模型的工具请求" class="headerlink" title="第 2 步：循环处理模型的工具请求"></a>第 2 步：循环处理模型的工具请求</h3><p>这是核心。每次调 <code>client.messages.create()</code> 都会得到一个回复，回复里的 <code>stop_reason</code> 字段告诉我们模型为什么停下来：</p><ul><li><code>&quot;tool_use&quot;</code>——模型说”我需要先调工具才能继续”，我们就执行工具、把结果塞回对话历史、再发一次请求；</li><li>其他值（如 <code>&quot;end_turn&quot;</code>）——模型已经给出最终回答，退出循环。</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">messages = [&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;杭州和北京现在天气怎么样？哪个更适合跑步？&quot;</span>&#125;]</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    response = client.messages.create(</span><br><span class="line">        model=<span class="string">&quot;claude-opus-4-8&quot;</span>,           <span class="comment"># 模型 ID，按需换成 claude-sonnet-5 等</span></span><br><span class="line">        max_tokens=<span class="number">16000</span>,                  <span class="comment"># 输出 token 上限，必填</span></span><br><span class="line">        thinking=&#123;<span class="string">&quot;type&quot;</span>: <span class="string">&quot;adaptive&quot;</span>&#125;,     <span class="comment"># 自适应思考，模型自行决定思考深度</span></span><br><span class="line">        tools=tools,                       <span class="comment"># 把工具说明书带给模型</span></span><br><span class="line">        messages=messages,                 <span class="comment"># 完整对话历史（API 无状态，每次都要全量带上）</span></span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 模型没有请求工具（正常结束/被截断/拒答）就退出循环</span></span><br><span class="line">    <span class="keyword">if</span> response.stop_reason != <span class="string">&quot;tool_use&quot;</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 走到这里说明模型请求了工具。先把模型这轮的完整输出追加进历史</span></span><br><span class="line">    <span class="comment"># （必须原样保留 response.content，丢掉里面的 tool_use 块下轮请求会报错）</span></span><br><span class="line">    messages.append(&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;assistant&quot;</span>, <span class="string">&quot;content&quot;</span>: response.content&#125;)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 逐个执行模型请求的工具，把结果和请求一一配对</span></span><br><span class="line">    tool_results = []</span><br><span class="line">    <span class="keyword">for</span> block <span class="keyword">in</span> response.content:</span><br><span class="line">        <span class="keyword">if</span> block.<span class="built_in">type</span> == <span class="string">&quot;tool_use&quot;</span>:</span><br><span class="line">            result = get_weather(**block.<span class="built_in">input</span>)   <span class="comment"># block.input 是模型传来的参数，SDK 已解析成 dict</span></span><br><span class="line">            tool_results.append(&#123;</span><br><span class="line">                <span class="string">&quot;type&quot;</span>: <span class="string">&quot;tool_result&quot;</span>,</span><br><span class="line">                <span class="string">&quot;tool_use_id&quot;</span>: block.<span class="built_in">id</span>,           <span class="comment"># 用 id 告诉模型&quot;这是哪次请求的结果&quot;</span></span><br><span class="line">                <span class="string">&quot;content&quot;</span>: result,</span><br><span class="line">            &#125;)</span><br><span class="line">    <span class="comment"># 所有工具结果放进同一条 user 消息传回，进入下一轮循环</span></span><br><span class="line">    messages.append(&#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>, <span class="string">&quot;content&quot;</span>: tool_results&#125;)</span><br></pre></td></tr></table></figure><h3 id="第-3-步：打印最终回答"><a href="#第-3-步：打印最终回答" class="headerlink" title="第 3 步：打印最终回答"></a>第 3 步：打印最终回答</h3><p>循环退出时，<code>response</code> 里装的就是模型的最终回答。回答内容是一个块（block）列表——可能混有思考块、文本块，取其中的文本块打印：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> block <span class="keyword">in</span> response.content:</span><br><span class="line">    <span class="keyword">if</span> block.<span class="built_in">type</span> == <span class="string">&quot;text&quot;</span>:</span><br><span class="line">        <span class="built_in">print</span>(block.text)</span><br></pre></td></tr></table></figure><h3 id="运行时实际发生了什么"><a href="#运行时实际发生了什么" class="headerlink" title="运行时实际发生了什么"></a>运行时实际发生了什么</h3><p>以这个问题为例，脚本一共和 API 交互了两轮：</p><ol><li><strong>第一轮请求</strong>：发送问题 + 工具说明书。模型判断”回答需要天气数据”，返回 <code>stop_reason=&quot;tool_use&quot;</code>，并且一次性请求了两个调用——<code>get_weather(city=&quot;杭州&quot;)</code> 和 <code>get_weather(city=&quot;北京&quot;)</code>（同一轮可以并行请求多个工具）。</li><li><strong>本地执行</strong>：脚本调用两次 <code>get_weather()</code> 函数，把两条结果按 <code>tool_use_id</code> 配对，追加进对话历史。</li><li><strong>第二轮请求</strong>：带着”问题 + 模型的工具请求 + 工具结果”的完整历史再次请求。模型这次有数据了，返回 <code>stop_reason=&quot;end_turn&quot;</code> 和最终回答，循环结束。</li></ol><p>终端输出类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">根据当前天气：</span><br><span class="line">- 杭州：晴，32°C，湿度 60%</span><br><span class="line">- 北京：多云，28°C，湿度 45%</span><br><span class="line"></span><br><span class="line">北京更适合跑步——温度低 4 度、湿度更低，多云也避免了暴晒。</span><br></pre></td></tr></table></figure><p>把 <code>get_weather</code> 换成查数据库、调内部系统、发消息，这个 30 行的骨架就是一个真正的 agent 雏形。生产代码可以用 SDK 自带的 tool runner（Python 里配合 <code>@beta_tool</code> 装饰器）自动处理整个循环，手写 loop 则适合需要人工审批、自定义日志等精细控制的场景。</p>]]>
    </content>
    <id>https://blog.codewater.top/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98/</id>
    <link href="https://blog.codewater.top/Claude%E5%85%A8%E9%9D%A2%E6%95%99%E7%A8%8B-%E6%A8%A1%E5%9E%8B%E6%A6%82%E5%BF%B5%E4%B8%8EAPI%E5%AE%9E%E6%88%98/"/>
    <published>2026-07-06T07:00:00.000Z</published>
    <summary>系统梳理 Claude 模型家族（Fable 5 / Opus 4.8 / Sonnet 5）、工具调用、MCP、CLAUDE.md 记忆规则与 Prompt/Harness/Loop Engineering，附 Python 实战例子。</summary>
    <title>Claude 全面教程：模型家族、核心概念与 API 实战（2026 年 7 月版）</title>
    <updated>2026-07-07T03:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="git" scheme="https://blog.codewater.top/categories/git/"/>
    <category term="Git" scheme="https://blog.codewater.top/tags/Git/"/>
    <category term="工程实践" scheme="https://blog.codewater.top/tags/%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="worktree" scheme="https://blog.codewater.top/tags/worktree/"/>
    <content>
      <![CDATA[<p>改一个 feature 改到一半，线上突然报了个 bug 要紧急修复。这时候你有两个选择：<code>git stash</code> 存起来切分支，修完再切回来 <code>stash pop</code>；或者随手 commit 一个 “WIP”（Work In Progress，表示”改到一半”的占位提交）再切。前者容易在 stash 栈里越积越多忘了恢复，后者污染提交历史。而且切换分支后 IDE 索引重建、依赖重装，来回折腾一次十几分钟就没了。</p><p><code>git worktree</code> 是 Git 内置的第三种方案：给同一个仓库挂多个工作目录，每个目录检出不同分支，互不干扰。修 bug 去另一个目录修，手头的改动原地不动。</p><span id="more"></span><blockquote><p>Git 三区域、分支模型等基础概念见：<a href="/Git%E5%9F%BA%E7%A1%80%E6%95%99%E7%A8%8B-%E5%8E%9F%E7%90%86%E6%93%8D%E4%BD%9C%E4%B8%8E%E4%BC%81%E4%B8%9A%E5%AE%9E%E8%B7%B5">Git 基础教程：原理、常见操作与企业实践规范</a></p></blockquote><h2 id="worktree-的工作原理：一个仓库，多个工作目录"><a href="#worktree-的工作原理：一个仓库，多个工作目录" class="headerlink" title="worktree 的工作原理：一个仓库，多个工作目录"></a>worktree 的工作原理：一个仓库，多个工作目录</h2><p>worktree 是 Git 自带的子命令（Git 2.5+ 引入，2.17+ 功能完整），默认情况下每个仓库只有一个工作目录——也就是你 clone 下来的那个，称为<strong>主工作区（main worktree）</strong>。<code>git worktree add</code> 可以额外创建<strong>链接工作区（linked worktree）</strong>，它们共享同一份 <code>.git</code> 对象库和引用，但各自有独立的工作目录、暂存区和 HEAD。</p><pre><code class="highlight mermaid">graph TB    subgraph repo[&quot;.git 对象库（共享）&quot;]        objects[&quot;objects/ 提交对象&quot;]        refs[&quot;refs/ 分支引用&quot;]        wt[worktrees/ 各工作区元数据]    end    subgraph main[&quot;主工作区 ~/code/myapp&quot;]        m_head[&quot;HEAD → feature/login&quot;]        m_files[工作目录 + 独立暂存区]    end    subgraph hotfix[&quot;链接工作区 ~/code/myapp-hotfix&quot;]        h_head[&quot;HEAD → hotfix/pay-crash&quot;]        h_files[工作目录 + 独立暂存区]    end    main --&gt; repo    hotfix --&gt; repo</code></pre><p>关键点：</p><ul><li><strong>提交共享</strong>：任何一个工作区里的 commit，其他工作区立即可见（本来就是同一个仓库）</li><li><strong>状态隔离</strong>：每个工作区有自己的 HEAD、index 和未提交改动，互不影响</li><li><strong>磁盘开销小</strong>：链接工作区不复制 <code>.git</code> 对象库，只多一份检出的源码文件</li></ul><p>链接工作区目录下的 <code>.git</code> 不是目录而是一个文本文件，内容指向主仓库的 <code>.git/worktrees/&lt;name&gt;</code>，这就是”链接”的含义。</p><h2 id="git-worktree-基本操作四件套：add-list-remove-prune"><a href="#git-worktree-基本操作四件套：add-list-remove-prune" class="headerlink" title="git worktree 基本操作四件套：add &#x2F; list &#x2F; remove &#x2F; prune"></a>git worktree 基本操作四件套：add &#x2F; list &#x2F; remove &#x2F; prune</h2><h3 id="add：创建工作区"><a href="#add：创建工作区" class="headerlink" title="add：创建工作区"></a>add：创建工作区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree add ../myapp-hotfix hotfix/pay-crash</span><br><span class="line"><span class="comment">#                ../myapp-hotfix：新工作区的路径，习惯放在主工作区同级目录</span></span><br><span class="line"><span class="comment">#                hotfix/pay-crash：要检出的已有分支</span></span><br></pre></td></tr></table></figure><p>如果分支还不存在，用 <code>-b</code> 顺便创建：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree add -b hotfix/pay-crash ../myapp-hotfix origin/main</span><br><span class="line"><span class="comment">#                -b：branch，基于起点创建新分支并检出</span></span><br><span class="line"><span class="comment">#                origin/main：新分支的起点，省略则基于当前 HEAD</span></span><br></pre></td></tr></table></figure><p>不指定分支时，Git 会用路径的最后一段作为分支名自动创建（同名分支已存在则直接检出）；如果只想临时看某个 commit 的代码、不想建分支，加 <code>--detach</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree add --detach ../myapp-v1.2 v1.2.0</span><br><span class="line"><span class="comment">#                --detach：以 detached HEAD 方式检出，不创建也不占用分支</span></span><br><span class="line"><span class="comment">#                v1.2.0：任意 commit-ish，tag、commit hash 都可以</span></span><br></pre></td></tr></table></figure><h3 id="list：查看所有工作区"><a href="#list：查看所有工作区" class="headerlink" title="list：查看所有工作区"></a>list：查看所有工作区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git worktree list</span><br><span class="line"><span class="comment"># /Users/dylan/code/myapp          a1b2c3d [feature/login]</span></span><br><span class="line"><span class="comment"># /Users/dylan/code/myapp-hotfix   e4f5g6h [hotfix/pay-crash]</span></span><br><span class="line"><span class="comment"># /Users/dylan/code/myapp-v1.2     i7j8k9l (detached HEAD)</span></span><br></pre></td></tr></table></figure><p>第一行永远是主工作区。加 <code>--porcelain</code> 输出机器可读格式，方便写脚本。</p><h3 id="remove：删除工作区"><a href="#remove：删除工作区" class="headerlink" title="remove：删除工作区"></a>remove：删除工作区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree remove ../myapp-hotfix</span><br><span class="line"><span class="comment"># 有未提交改动时会拒绝删除，确认不要了加 --force 强删</span></span><br></pre></td></tr></table></figure><p>remove 只删工作目录和元数据，分支还在。工作区用完、分支也合并了，记得单独删分支：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git branch -d hotfix/pay-crash</span><br><span class="line"><span class="comment">#          -d：delete，删除已合并的分支，未合并会拒绝</span></span><br></pre></td></tr></table></figure><h3 id="prune：清理失效记录"><a href="#prune：清理失效记录" class="headerlink" title="prune：清理失效记录"></a>prune：清理失效记录</h3><p>如果你没用 <code>remove</code> 而是直接 <code>rm -rf</code> 删了工作区目录，<code>.git/worktrees/</code> 里会残留元数据，<code>git worktree list</code> 还能看到它。用 prune 清理：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree prune</span><br><span class="line"><span class="comment"># 扫描并删除指向已不存在目录的工作区元数据</span></span><br></pre></td></tr></table></figure><p>Git 也会在 <code>gc</code> 时自动 prune 超过宽限期（默认 3 个月，由 <code>gc.worktreePruneExpire</code> 控制）的失效工作区，但手动删目录后建议立即跑一次。</p><h2 id="场景一：hotfix-不打断手头的-feature"><a href="#场景一：hotfix-不打断手头的-feature" class="headerlink" title="场景一：hotfix 不打断手头的 feature"></a>场景一：hotfix 不打断手头的 feature</h2><p>最经典的用法。假设你在 <code>feature/login</code> 上开发到一半：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 基于线上分支创建 hotfix 工作区，手头改动完全不用动</span></span><br><span class="line">git worktree add -b hotfix/pay-crash ../myapp-hotfix origin/main</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 去新目录修 bug、提交、推送</span></span><br><span class="line"><span class="built_in">cd</span> ../myapp-hotfix</span><br><span class="line">vim internal/pay/handler.go</span><br><span class="line">git add -A &amp;&amp; git commit -m <span class="string">&quot;fix: 修复支付回调空指针&quot;</span></span><br><span class="line">git push -u origin hotfix/pay-crash</span><br><span class="line"><span class="comment">#        -u：upstream，建立本地分支与远端分支的跟踪关系</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 修完回主工作区继续开发，hotfix 工作区留着等 review</span></span><br><span class="line"><span class="built_in">cd</span> ../myapp</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. PR 合并后清理</span></span><br><span class="line">git worktree remove ../myapp-hotfix</span><br><span class="line">git branch -d hotfix/pay-crash</span><br></pre></td></tr></table></figure><p>整个过程不需要 stash，也不会产生 WIP commit，主工作区连分支都没切过，IDE 状态和构建缓存原封不动。</p><h2 id="场景二：并行-code-review-与长时间任务"><a href="#场景二：并行-code-review-与长时间任务" class="headerlink" title="场景二：并行 code review 与长时间任务"></a>场景二：并行 code review 与长时间任务</h2><p><strong>review 同事的 PR</strong>：不想为了跑一下同事的代码而切走自己的分支，开个临时工作区：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git fetch origin</span><br><span class="line">git worktree add --detach ../myapp-review origin/feature/new-api</span><br><span class="line"><span class="comment"># --detach：只是看看代码跑跑测试，不需要本地分支</span></span><br><span class="line"><span class="built_in">cd</span> ../myapp-review &amp;&amp; go <span class="built_in">test</span> ./...</span><br></pre></td></tr></table></figure><p><strong>长时间构建 &#x2F; 跑测试</strong>：完整测试套件要跑 20 分钟，期间想继续写代码。在另一个工作区跑测试，主工作区照常编辑，两边文件互不干扰：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree add ../myapp-ci feature/login</span><br><span class="line"><span class="comment"># 报错：fatal: &#x27;feature/login&#x27; is already used by worktree ...</span></span><br></pre></td></tr></table></figure><p>这里会撞上 worktree 的一条硬规则：<strong>同一个分支同时只能被一个工作区检出</strong>，否则两边提交会让对方的 HEAD 和工作目录脱节。绕过方式是基于当前提交用 detached HEAD 跑：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree add --detach ../myapp-ci HEAD</span><br><span class="line"><span class="built_in">cd</span> ../myapp-ci &amp;&amp; make <span class="built_in">test</span>   <span class="comment"># 慢任务在这跑</span></span><br><span class="line"><span class="built_in">cd</span> ../myapp                   <span class="comment"># 回来继续写代码</span></span><br></pre></td></tr></table></figure><h2 id="进阶操作：move-lock-repair"><a href="#进阶操作：move-lock-repair" class="headerlink" title="进阶操作：move &#x2F; lock &#x2F; repair"></a>进阶操作：move &#x2F; lock &#x2F; repair</h2><p><strong>move</strong>：工作区目录想换个位置，不要直接 <code>mv</code>（会破坏 <code>.git</code> 文件里的路径引用），用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree move ../myapp-hotfix ~/work/myapp-hotfix</span><br><span class="line"><span class="comment"># 移动工作目录并自动更新双向路径引用</span></span><br></pre></td></tr></table></figure><p><strong>lock</strong>：工作区放在移动硬盘或网络盘上时，防止盘没挂载时被 prune 误清理：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git worktree lock ../myapp-usb --reason <span class="string">&quot;在移动硬盘上&quot;</span></span><br><span class="line"><span class="comment">#                 --reason：说明锁定原因，unlock 前 list 可见</span></span><br><span class="line">git worktree unlock ../myapp-usb</span><br></pre></td></tr></table></figure><p>被 lock 的工作区也不能被 move 和 remove，需要先 unlock。</p><p><strong>repair</strong>：如果已经手动 <code>mv</code> 过目录导致链接断裂，或主仓库整个换了位置，用 repair 修复引用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git worktree repair ~/work/myapp-hotfix</span><br><span class="line"><span class="comment"># 省略路径参数则修复主仓库到各工作区的引用</span></span><br></pre></td></tr></table></figure><h2 id="worktree-踩坑清单：依赖不共享、submodule-与-IDE"><a href="#worktree-踩坑清单：依赖不共享、submodule-与-IDE" class="headerlink" title="worktree 踩坑清单：依赖不共享、submodule 与 IDE"></a>worktree 踩坑清单：依赖不共享、submodule 与 IDE</h2><p><strong>依赖目录不共享</strong>。worktree 只共享 Git 数据，<code>node_modules/</code>、Python venv、构建产物这些 gitignore 的文件每个工作区都要重新生成。前端项目每开一个工作区就要 <code>npm install</code> 一次，这是 worktree 最大的隐性成本。Go 项目好一些，module 缓存在 <code>$GOPATH/pkg/mod</code> 全局共享。</p><p><strong>submodule 支持不完整</strong>。带 submodule 的仓库创建工作区后，需要在新工作区里手动 <code>git submodule update --init</code>；官方文档明确提示对 superproject 做多工作区检出的支持不完整、不推荐（<code>man git-worktree</code> 的 BUGS 一节），且含 submodule 的工作区不能用 <code>move</code> 移动、<code>remove</code> 删除时必须加 <code>--force</code>。重度使用 submodule 的项目建议先小范围验证。</p><p><strong>IDE 多开的内存开销</strong>。每个工作区是独立目录，IDE 会当成独立项目建索引。GoLand&#x2F;IDEA 同时开两三个大项目窗口，内存占用直接翻倍。VS Code 相对轻量，或者用只跑命令行的方式使用副工作区。</p><p><strong>hooks 与配置共享</strong>。<code>.git/hooks</code> 和仓库级 <code>git config</code> 是所有工作区共享的（除非启用 <code>extensions.worktreeConfig</code> 做每工作区配置）。依赖 hooks 里写死路径的项目要注意路径可能指向主工作区。</p><h2 id="worktree-还是再-clone-一份？"><a href="#worktree-还是再-clone-一份？" class="headerlink" title="worktree 还是再 clone 一份？"></a>worktree 还是再 clone 一份？</h2><table><thead><tr><th>维度</th><th>worktree</th><th>多次 clone</th></tr></thead><tbody><tr><td>磁盘占用</td><td>共享 <code>.git</code>，只多源码文件</td><td>每份都有完整对象库</td></tr><tr><td>提交可见性</td><td>即时共享，无需 push&#x2F;fetch</td><td>要经过远端中转（或本地 remote）</td></tr><tr><td>分支限制</td><td>同一分支只能检出一次</td><td>无限制</td></tr><tr><td>配置&#x2F;hooks</td><td>共享，改一处全生效</td><td>完全独立</td></tr><tr><td>误操作影响面</td><td>损坏 <code>.git</code> 影响所有工作区</td><td>相互隔离</td></tr></tbody></table><p>日常并行开发（hotfix、review、跑测试）用 worktree，成本低、提交即时同步。需要完全隔离的场景——比如要对 <code>.git</code> 本身做危险操作、或两份代码用完全不同的仓库配置——再考虑单独 clone。</p><p>一句话总结使用节奏：<code>add</code> 开新工作区，干完 <code>remove</code>，手滑直接删了目录就 <code>prune</code>。把”切分支”变成”切目录”，stash 栈从此清净。</p>]]>
    </content>
    <id>https://blog.codewater.top/git-worktree%E5%A4%9A%E5%B7%A5%E4%BD%9C%E5%8C%BA%E5%B9%B6%E8%A1%8C%E5%BC%80%E5%8F%91/</id>
    <link href="https://blog.codewater.top/git-worktree%E5%A4%9A%E5%B7%A5%E4%BD%9C%E5%8C%BA%E5%B9%B6%E8%A1%8C%E5%BC%80%E5%8F%91/"/>
    <published>2026-07-06T02:00:00.000Z</published>
    <summary>git worktree 完整教程：add/list/remove/prune 四件套、hotfix 不打断 feature 开发的工作流、move/lock 进阶操作，以及与多次 clone 的取舍对比。</summary>
    <title>git worktree 教程：多工作区并行开发，告别 stash 切分支</title>
    <updated>2026-07-06T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="git" scheme="https://blog.codewater.top/categories/git/"/>
    <category term="Git" scheme="https://blog.codewater.top/tags/Git/"/>
    <category term="版本控制" scheme="https://blog.codewater.top/tags/%E7%89%88%E6%9C%AC%E6%8E%A7%E5%88%B6/"/>
    <category term="工程实践" scheme="https://blog.codewater.top/tags/%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/"/>
    <content>
      <![CDATA[<p>Git 是目前最主流的分布式版本控制系统，几乎所有软件团队都在用。但很多人停留在 <code>add / commit / push</code> 三板斧，遇到冲突或误操作就不知道怎么处理。这篇从原理出发，把常见操作和企业实践规范串起来。</p><span id="more"></span><hr><h2 id="Git-的三个区域和分布式原理"><a href="#Git-的三个区域和分布式原理" class="headerlink" title="Git 的三个区域和分布式原理"></a>Git 的三个区域和分布式原理</h2><h3 id="工作区、暂存区、本地仓库"><a href="#工作区、暂存区、本地仓库" class="headerlink" title="工作区、暂存区、本地仓库"></a>工作区、暂存区、本地仓库</h3><p>Git 把文件状态分成三个区域，理解这个是所有操作的基础：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">工作区（Working Directory）</span><br><span class="line">    ↓  git <span class="keyword">add</span></span><br><span class="line">暂存区（Staging Area / <span class="keyword">Index</span>）</span><br><span class="line">    ↓  git <span class="keyword">commit</span></span><br><span class="line">本地仓库（<span class="keyword">Local</span> Repository）</span><br><span class="line">    ↓  git push</span><br><span class="line">远程仓库（Remote Repository）</span><br></pre></td></tr></table></figure><ul><li><strong>工作区</strong>：你实际编辑文件的目录</li><li><strong>暂存区</strong>：<code>git add</code> 后文件进入这里，是一个”预提交缓冲区”，可以精确控制这次 commit 包含哪些改动</li><li><strong>本地仓库</strong>：<code>git commit</code> 后改动被永久记录，存在 <code>.git/objects/</code> 目录</li><li><strong>远程仓库</strong>：<code>git push</code> 后同步到 GitHub&#x2F;GitLab 等远端</li></ul><p>暂存区是很多人忽略的设计：它让你可以把一个大改动拆分成多个有意义的 commit，而不是把所有修改塞进一个 commit。</p><h3 id="分布式的含义"><a href="#分布式的含义" class="headerlink" title="分布式的含义"></a>分布式的含义</h3><p>Git 是<strong>分布式</strong>的，每个开发者本地都有完整的仓库历史，不依赖中央服务器就能查历史、切分支、提交。这和 SVN 等集中式系统不同——SVN 断网就不能提交。</p><p><code>git clone</code> 会把远端的完整历史都拉下来，所以 <code>git log</code> 是本地操作，不需要网络。</p><h3 id="Git-对象模型"><a href="#Git-对象模型" class="headerlink" title="Git 对象模型"></a>Git 对象模型</h3><p>Git 内部用四种对象存储所有内容，每个对象用其 SHA-1 哈希值唯一标识：</p><table><thead><tr><th>对象类型</th><th>存储内容</th></tr></thead><tbody><tr><td><strong>blob</strong></td><td>文件内容（不含文件名）</td></tr><tr><td><strong>tree</strong></td><td>目录结构（文件名 + blob 引用）</td></tr><tr><td><strong>commit</strong></td><td>提交信息 + tree 引用 + parent commit 引用</td></tr><tr><td><strong>tag</strong></td><td>附注标签（指向 commit）</td></tr></tbody></table><p>一个 commit 对象大概长这样：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">tree</span> <span class="number">8</span>f3b2a...       # 指向这次提交的目录快照</span><br><span class="line"><span class="attribute">parent</span> <span class="number">9</span>c1d4f...     # 上一个 commit</span><br><span class="line"><span class="attribute">author</span> Dylan &lt;...&gt; <span class="number">1720137600</span> +<span class="number">0800</span></span><br><span class="line"><span class="attribute">committer</span> Dylan &lt;...&gt; <span class="number">1720137600</span> +<span class="number">0800</span></span><br><span class="line"></span><br><span class="line"><span class="attribute">feat</span>: add user login API</span><br></pre></td></tr></table></figure><p><strong>branch 和 HEAD 是什么</strong>：branch 只是一个指向某个 commit 的指针（存在 <code>.git/refs/heads/</code> 里），HEAD 是当前所在分支的指针。切换分支不是”复制文件”，只是移动指针，速度极快。</p><hr><h2 id="常见操作速查"><a href="#常见操作速查" class="headerlink" title="常见操作速查"></a>常见操作速查</h2><h3 id="初始化和克隆"><a href="#初始化和克隆" class="headerlink" title="初始化和克隆"></a>初始化和克隆</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 初始化新仓库</span></span><br><span class="line">git init</span><br><span class="line"></span><br><span class="line"><span class="comment"># 克隆远端仓库（含完整历史）</span></span><br><span class="line">git <span class="built_in">clone</span> https://github.com/user/repo.git</span><br><span class="line"></span><br><span class="line"><span class="comment"># 克隆指定分支（只拉这一个分支）</span></span><br><span class="line">git <span class="built_in">clone</span> -b main --single-branch https://github.com/user/repo.git</span><br></pre></td></tr></table></figure><h3 id="查看状态和差异"><a href="#查看状态和差异" class="headerlink" title="查看状态和差异"></a>查看状态和差异</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看工作区和暂存区的状态</span></span><br><span class="line">git status</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看工作区 vs 暂存区的差异（还没 add 的改动）</span></span><br><span class="line">git diff</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看暂存区 vs 上次 commit 的差异（已 add 还没 commit 的改动）</span></span><br><span class="line">git diff --staged</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看提交历史（图形化显示分支关系）</span></span><br><span class="line">git <span class="built_in">log</span> --oneline --graph --all</span><br></pre></td></tr></table></figure><h3 id="add-和-commit"><a href="#add-和-commit" class="headerlink" title="add 和 commit"></a>add 和 commit</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 把指定文件加入暂存区</span></span><br><span class="line">git add src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 把所有改动加入暂存区（新增、修改、删除）</span></span><br><span class="line">git add .</span><br><span class="line"></span><br><span class="line"><span class="comment"># 交互式选择要暂存的代码块（可以只提交文件的一部分）</span></span><br><span class="line">git add -p src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提交暂存区的内容</span></span><br><span class="line">git commit -m <span class="string">&quot;feat: add user login API&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 跳过暂存区，直接提交所有已追踪文件的修改（不含新文件）</span></span><br><span class="line">git commit -am <span class="string">&quot;fix: correct status code&quot;</span></span><br></pre></td></tr></table></figure><p><code>git add -p</code> 是个容易被忽视的功能：当一个文件有多处修改，但只想把其中一部分提交时，用它可以逐块选择。</p><h3 id="分支操作"><a href="#分支操作" class="headerlink" title="分支操作"></a>分支操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看本地分支</span></span><br><span class="line">git branch</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看所有分支（含远端）</span></span><br><span class="line">git branch -a</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建并切换到新分支</span></span><br><span class="line">git switch -c feature/login</span><br><span class="line"><span class="comment"># 等价于旧写法：git checkout -b feature/login</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 切换到已有分支</span></span><br><span class="line">git switch main</span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除本地分支（-d 只删已合并的，-D 强制删除）</span></span><br><span class="line">git branch -d feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除远端分支</span></span><br><span class="line">git push origin --delete feature/login</span><br></pre></td></tr></table></figure><h3 id="合并和变基"><a href="#合并和变基" class="headerlink" title="合并和变基"></a>合并和变基</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 合并分支（在 main 分支上执行，把 feature/login 合并进来）</span></span><br><span class="line">git merge feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 合并并强制生成一个 merge commit（即使可以 fast-forward）</span></span><br><span class="line">git merge --no-ff feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 变基（在 feature 分支上执行，把 feature 的提交移到 main 最新节点之后）</span></span><br><span class="line">git rebase main</span><br></pre></td></tr></table></figure><p><strong>merge vs rebase 的区别</strong>：</p><ul><li><code>merge</code> 保留完整历史，会生成一个 merge commit，清楚地记录”这两条线在这里合并了”</li><li><code>rebase</code> 重写 feature 分支的提交历史，让它看起来像是在最新的 main 上线性开发的，历史更干净，但会改变 commit 的 SHA</li></ul><blockquote><p><strong>已推送到远端的分支不要 rebase</strong>，rebase 会改变提交历史，会和别人的本地仓库产生冲突。</p></blockquote><h3 id="远端操作"><a href="#远端操作" class="headerlink" title="远端操作"></a>远端操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看远端仓库配置</span></span><br><span class="line">git remote -v</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拉取远端改动并合并（= fetch + merge）</span></span><br><span class="line">git pull</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拉取远端改动并变基（保持线性历史）</span></span><br><span class="line">git pull --rebase</span><br><span class="line"></span><br><span class="line"><span class="comment"># 推送到远端</span></span><br><span class="line">git push origin feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第一次推送时设置上游分支（之后 git push 不需要带参数）</span></span><br><span class="line">git push -u origin feature/login</span><br></pre></td></tr></table></figure><h3 id="暂存工作区：stash"><a href="#暂存工作区：stash" class="headerlink" title="暂存工作区：stash"></a>暂存工作区：stash</h3><p><code>git stash</code> 把当前工作区和暂存区的改动临时存起来，让工作区恢复干净状态，适合”临时切换分支处理紧急 bug”的场景。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 把当前改动暂存起来</span></span><br><span class="line">git stash</span><br><span class="line"></span><br><span class="line"><span class="comment"># 暂存时加说明</span></span><br><span class="line">git stash push -m <span class="string">&quot;WIP: login form validation&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看所有 stash</span></span><br><span class="line">git stash list</span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复最近一次 stash（保留 stash 记录）</span></span><br><span class="line">git stash apply</span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复并删除最近一次 stash 记录</span></span><br><span class="line">git stash pop</span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复指定的 stash</span></span><br><span class="line">git stash apply stash@&#123;2&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看某个 stash 改动了哪些文件</span></span><br><span class="line">git stash show stash@&#123;0&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看某个 stash 的完整 diff（加 -p）</span></span><br><span class="line">git stash show stash@&#123;0&#125; -p</span><br></pre></td></tr></table></figure><h3 id="回滚操作"><a href="#回滚操作" class="headerlink" title="回滚操作"></a>回滚操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 撤销工作区的修改（恢复到上次 commit 的状态，改动丢失）</span></span><br><span class="line">git restore src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 把文件从暂存区移回工作区（取消 add）</span></span><br><span class="line">git restore --staged src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 回退到上一个 commit，保留工作区改动（最常用）</span></span><br><span class="line">git reset HEAD~1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 回退到指定 commit，保留工作区改动</span></span><br><span class="line">git reset abc1234</span><br><span class="line"></span><br><span class="line"><span class="comment"># 回退到上一个 commit，同时清除工作区改动（危险，改动会丢失）</span></span><br><span class="line">git reset --hard HEAD~1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 回退到指定 commit，同时清除工作区改动（危险）</span></span><br><span class="line">git reset --hard abc1234</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成一个新的 commit 来撤销某次提交（不改历史，适合已推送的情况）</span></span><br><span class="line">git revert abc1234</span><br></pre></td></tr></table></figure><p><code>reset --hard</code> 会直接丢弃工作区改动，执行前确认好。已经 push 的提交用 <code>revert</code> 更安全，它不改历史，而是新增一个撤销的 commit。</p><hr><h2 id="分支策略：Git-Flow-与-GitHub-Flow"><a href="#分支策略：Git-Flow-与-GitHub-Flow" class="headerlink" title="分支策略：Git Flow 与 GitHub Flow"></a>分支策略：Git Flow 与 GitHub Flow</h2><p>多人协作时，如果每个人随意创建分支、随意往 main 合并，很快就会出现以下问题：main 上有未测试的代码导致线上崩溃；不知道哪个分支是”当前最新”的；hotfix 合到了错误的地方，修复没生效。</p><p><strong>分支策略</strong>就是团队约定好”什么分支做什么事、从哪切出、合到哪里去”，把协作流程标准化，避免上面这些混乱。不同规模和发布节奏的团队适合不同的策略，没有统一答案，但必须有一套。</p><h3 id="Git-Flow"><a href="#Git-Flow" class="headerlink" title="Git Flow"></a>Git Flow</h3><p>Git Flow 是经典的企业分支模型，有两条长期分支和三类临时分支：</p><p><strong>长期分支：</strong></p><ul><li><code>main</code>（或 <code>master</code>）：只存放生产环境的稳定代码，每次更新都打 tag</li><li><code>develop</code>：集成分支，所有功能开发完成后先合并到这里</li></ul><p><strong>临时分支（用完即删）：</strong></p><ul><li><code>feature/xxx</code>：从 develop 切出，功能开发完合回 develop</li><li><code>release/x.x.x</code>：从 develop 切出，做最终测试和 bug 修复，完成后合并到 main 和 develop</li><li><code>hotfix/xxx</code>：从 main 切出，紧急修复生产 bug，完成后合并到 main 和 develop</li></ul><p><strong>适合场景</strong>：有明确版本节奏的项目（比如定期发版的 App、SDK）。</p><p><strong>缺点</strong>：分支多、流程重，小团队或持续交付场景下嫌麻烦。</p><h3 id="GitHub-Flow"><a href="#GitHub-Flow" class="headerlink" title="GitHub Flow"></a>GitHub Flow</h3><p>GitHub Flow 更轻量，只有一条规则：<strong>main 随时可部署，所有改动通过 PR 合并</strong>。</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">main</span>（始终可部署）</span><br><span class="line">  ↑ PR + <span class="selector-tag">Code</span> Review</span><br><span class="line">feature/xxx（从 <span class="selector-tag">main</span> 切出，完成后 PR 合回）</span><br></pre></td></tr></table></figure><p>流程：</p><ol><li>从 main 创建 feature 分支</li><li>在 feature 分支开发、提交</li><li>发起 PR，Code Review</li><li>CI 通过后合并到 main</li><li>立即部署</li></ol><p><strong>适合场景</strong>：持续交付、小团队、Web 服务（随时上线）。</p><h3 id="怎么选"><a href="#怎么选" class="headerlink" title="怎么选"></a>怎么选</h3><table><thead><tr><th>场景</th><th>推荐</th></tr></thead><tbody><tr><td>有固定发版周期（App、SDK）</td><td>Git Flow</td></tr><tr><td>持续部署、随时上线</td><td>GitHub Flow</td></tr><tr><td>小团队、快速迭代</td><td>GitHub Flow</td></tr><tr><td>需要维护多个版本</td><td>Git Flow</td></tr></tbody></table><hr><h2 id="企业实践规范"><a href="#企业实践规范" class="headerlink" title="企业实践规范"></a>企业实践规范</h2><h3 id="commit-message-规范：Conventional-Commits"><a href="#commit-message-规范：Conventional-Commits" class="headerlink" title="commit message 规范：Conventional Commits"></a>commit message 规范：Conventional Commits</h3><p>混乱的 commit message 让 <code>git log</code> 毫无意义，也没法自动生成 changelog。<a href="https://www.conventionalcommits.org/">Conventional Commits</a> 是目前最主流的规范：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">type</span>&gt;</span>(<span class="tag">&lt;<span class="name">scope</span>&gt;</span>): <span class="tag">&lt;<span class="name">subject</span>&gt;</span></span><br><span class="line"></span><br><span class="line">[可选 body]</span><br><span class="line"></span><br><span class="line">[可选 footer]</span><br></pre></td></tr></table></figure><p>常用 type：</p><table><thead><tr><th>type</th><th>含义</th></tr></thead><tbody><tr><td><code>feat</code></td><td>新功能</td></tr><tr><td><code>fix</code></td><td>Bug 修复</td></tr><tr><td><code>docs</code></td><td>文档改动</td></tr><tr><td><code>refactor</code></td><td>重构（不影响功能）</td></tr><tr><td><code>test</code></td><td>测试相关</td></tr><tr><td><code>chore</code></td><td>构建、依赖、CI 等杂项</td></tr><tr><td><code>perf</code></td><td>性能优化</td></tr><tr><td><code>revert</code></td><td>撤销某次提交</td></tr></tbody></table><p>示例：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">feat(auth): <span class="keyword">add</span> Google OAuth <span class="keyword">login</span></span><br><span class="line"></span><br><span class="line">Support sign-<span class="keyword">in</span> <span class="keyword">with</span> Google account via OAuth <span class="number">2.0</span>.</span><br><span class="line">Closes #<span class="number">42</span></span><br><span class="line"></span><br><span class="line">fix(api): <span class="keyword">return</span> <span class="number">404</span> <span class="keyword">instead</span> <span class="keyword">of</span> <span class="number">500</span> <span class="keyword">for</span> missing <span class="keyword">user</span></span><br><span class="line"></span><br><span class="line">chore: upgrade Go <span class="keyword">version</span> <span class="keyword">to</span> <span class="number">1.22</span></span><br></pre></td></tr></table></figure><p><strong>实践建议</strong>：</p><ul><li>subject 用英文或中文均可，保持团队统一就行</li><li>subject 不超过 72 个字符</li><li>用祈使句（”add”，不是”added”或”adding”）</li><li>破坏性改动在 footer 加 <code>BREAKING CHANGE: 说明</code></li></ul><p>可以用 <a href="https://commitlint.js.org/">commitlint</a> 在 git hook 里强制校验格式。</p><h3 id="分支命名规范"><a href="#分支命名规范" class="headerlink" title="分支命名规范"></a>分支命名规范</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">feature/issue-42-user-login      <span class="comment"># 新功能（关联 issue 号）</span></span><br><span class="line">fix/issue-88-null-pointer        <span class="comment"># bug 修复</span></span><br><span class="line">hotfix/payment-timeout           <span class="comment"># 紧急修复</span></span><br><span class="line">release/v1.2.0                   <span class="comment"># 发布分支</span></span><br><span class="line">chore/upgrade-dependencies       <span class="comment"># 杂项</span></span><br></pre></td></tr></table></figure><p>关联 issue 号的好处：在 GitHub&#x2F;GitLab 上可以直接从分支名跳转到对应 issue，review 时方便查背景。</p><h3 id="保护主分支"><a href="#保护主分支" class="headerlink" title="保护主分支"></a>保护主分支</h3><p>在 GitHub&#x2F;GitLab 上给 <code>main</code> 开启 branch protection：</p><ul><li><strong>禁止直接 push</strong>，所有改动必须通过 PR</li><li><strong>PR 合并前必须通过 CI</strong>（单测、lint、构建）</li><li><strong>至少一个 Code Review 通过</strong>才能合并</li><li><strong>禁止 force push</strong></li></ul><p>这几条规则能防止 90% 的低级事故。</p><h3 id="PR-MR-规范"><a href="#PR-MR-规范" class="headerlink" title="PR&#x2F;MR 规范"></a>PR&#x2F;MR 规范</h3><p>一个好的 PR 应该：</p><ul><li><strong>只做一件事</strong>：一个 PR 只解决一个问题，越小越容易 review</li><li><strong>有清晰的描述</strong>：说明改了什么、为什么改、怎么测试</li><li><strong>关联 issue</strong>：在描述里加 <code>Closes #42</code>，合并后自动关闭 issue</li><li><strong>自己先 review 一遍</strong>：提交前 diff 看一眼，去掉调试代码和无关改动</li></ul><h3 id="gitignore-配置"><a href="#gitignore-配置" class="headerlink" title=".gitignore 配置"></a>.gitignore 配置</h3><p><code>.gitignore</code> 要在项目初始化时就配好，不要等到不该提交的文件已经进了仓库再处理。</p><p>常见需要忽略的内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"># 依赖</span><br><span class="line">node_modules/</span><br><span class="line">vendor/</span><br><span class="line"></span><br><span class="line"># 构建产物</span><br><span class="line">dist/</span><br><span class="line">build/</span><br><span class="line">*.class</span><br><span class="line">*.o</span><br><span class="line"></span><br><span class="line"># 环境配置（含密钥，绝对不能提交）</span><br><span class="line">.env</span><br><span class="line">.env.local</span><br><span class="line">*.pem</span><br><span class="line">*.key</span><br><span class="line"></span><br><span class="line"># IDE</span><br><span class="line">.idea/</span><br><span class="line">.vscode/</span><br><span class="line">*.swp</span><br><span class="line"></span><br><span class="line"># 系统文件</span><br><span class="line">.DS_Store</span><br><span class="line">Thumbs.db</span><br></pre></td></tr></table></figure><p><a href="https://www.toptal.com/developers/gitignore">gitignore.io</a> 可以按语言和框架自动生成。</p><hr><h2 id="常见问题处理"><a href="#常见问题处理" class="headerlink" title="常见问题处理"></a>常见问题处理</h2><h3 id="误操作恢复：git-reflog"><a href="#误操作恢复：git-reflog" class="headerlink" title="误操作恢复：git reflog"></a>误操作恢复：git reflog</h3><p><code>git reflog</code> 记录了 HEAD 的所有移动历史，包括 <code>reset --hard</code> 这类”危险操作”之后的状态。只要没有 <code>git gc</code>，几乎所有误操作都能恢复。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看 HEAD 的操作历史</span></span><br><span class="line">git reflog</span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出示例：</span></span><br><span class="line"><span class="comment"># abc1234 HEAD@&#123;0&#125;: reset: moving to HEAD~1</span></span><br><span class="line"><span class="comment"># def5678 HEAD@&#123;1&#125;: commit: feat: add login page</span></span><br><span class="line"><span class="comment"># ghi9012 HEAD@&#123;2&#125;: commit: fix: correct redirect URL</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复到 reset 之前的状态（def5678 是 reset 之前的 commit）</span></span><br><span class="line">git reset --hard def5678</span><br></pre></td></tr></table></figure><p><code>reset --hard</code> 把工作区搞没了？先跑 <code>git reflog</code>，找到那个 commit 的哈希，<code>reset --hard</code> 回去就行。</p><h3 id="解决合并冲突"><a href="#解决合并冲突" class="headerlink" title="解决合并冲突"></a>解决合并冲突</h3><p>冲突发生时 Git 会在文件里标记冲突区域：</p><figure class="highlight asciidoc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</span><br><span class="line"><span class="section">你的改动（当前分支）</span></span><br><span class="line"><span class="section">=======</span></span><br><span class="line">对方的改动（被合并的分支）</span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature/login</span><br></pre></td></tr></table></figure><p>处理步骤：</p><ol><li>用编辑器打开冲突文件，选择保留哪一方或手动合并</li><li>删除所有 <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>、<code>=======</code>、<code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> 标记</li><li><code>git add</code> 标记冲突已解决</li><li><code>git commit</code> 完成合并</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看哪些文件有冲突</span></span><br><span class="line">git status</span><br><span class="line"></span><br><span class="line"><span class="comment"># 解决后标记为已解决</span></span><br><span class="line">git add src/main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 完成合并提交</span></span><br><span class="line">git commit</span><br></pre></td></tr></table></figure><p>如果冲突太复杂，可以用 <code>git mergetool</code> 调出可视化合并工具（需要先配置）。</p><h3 id="修改最近一次-commit"><a href="#修改最近一次-commit" class="headerlink" title="修改最近一次 commit"></a>修改最近一次 commit</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 修改最近一次 commit 的 message</span></span><br><span class="line">git commit --amend -m <span class="string">&quot;feat: add Google OAuth login&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 把忘加的文件补进最近一次 commit（不改 message）</span></span><br><span class="line">git add forgotten_file.go</span><br><span class="line">git commit --amend --no-edit</span><br></pre></td></tr></table></figure><p><code>--amend</code> 会重写最近一次 commit，产生新的 SHA。<strong>已推送到远端的 commit 不要 amend</strong>，否则需要 force push，会影响其他人。</p><h3 id="修改指定-commit：git-rebase-i"><a href="#修改指定-commit：git-rebase-i" class="headerlink" title="修改指定 commit：git rebase -i"></a>修改指定 commit：git rebase -i</h3><p><code>--amend</code> 只能改最近一次 commit，修改更早的提交要用交互式 rebase。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 修改最近 3 个 commit（打开交互式编辑界面）</span></span><br><span class="line">git rebase -i HEAD~3</span><br></pre></td></tr></table></figure><p>弹出的编辑器里每行是一个 commit，把要修改的那行前面的 <code>pick</code> 改成对应指令：</p><table><thead><tr><th>指令</th><th>含义</th></tr></thead><tbody><tr><td><code>pick</code></td><td>保留这个 commit（默认）</td></tr><tr><td><code>reword</code></td><td>只修改 commit message</td></tr><tr><td><code>edit</code></td><td>暂停在这个 commit，可以修改内容</td></tr><tr><td><code>squash</code></td><td>把这个 commit 合并到上一个 commit</td></tr><tr><td><code>drop</code></td><td>删除这个 commit</td></tr></tbody></table><p>以修改 message 为例，把 <code>pick</code> 改成 <code>reword</code>，保存退出，Git 会再弹出编辑器让你改 message。</p><p>以修改内容为例，把 <code>pick</code> 改成 <code>edit</code>，保存退出后 Git 会暂停在那个 commit：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Git 提示你现在在指定 commit，可以修改文件</span></span><br><span class="line"><span class="comment"># 改完后：</span></span><br><span class="line">git add src/main.go</span><br><span class="line">git commit --amend --no-edit   <span class="comment"># 把改动追加到当前 commit</span></span><br><span class="line">git rebase --<span class="built_in">continue</span>          <span class="comment"># 继续完成剩余的 rebase</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>同样：已推送到远端的 commit 不要用 rebase -i 修改</strong>，会改写历史，影响所有人。</p></blockquote><h3 id="cherry-pick：把某个-commit-单独移植"><a href="#cherry-pick：把某个-commit-单独移植" class="headerlink" title="cherry-pick：把某个 commit 单独移植"></a>cherry-pick：把某个 commit 单独移植</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 把 abc1234 这个 commit 的改动应用到当前分支</span></span><br><span class="line">git cherry-pick abc1234</span><br><span class="line"></span><br><span class="line"><span class="comment"># 移植多个连续的 commit（包含 abc1234 到 def5678，注意要加 ^）</span></span><br><span class="line">git cherry-pick abc1234^..def5678</span><br></pre></td></tr></table></figure><p>场景：hotfix 在 main 分支修复了一个 bug，需要把这个修复也应用到正在维护的 release 分支，用 cherry-pick 比重新写一遍高效。</p>]]>
    </content>
    <id>https://blog.codewater.top/Git%E5%9F%BA%E7%A1%80%E6%95%99%E7%A8%8B-%E5%8E%9F%E7%90%86%E6%93%8D%E4%BD%9C%E4%B8%8E%E4%BC%81%E4%B8%9A%E5%AE%9E%E8%B7%B5/</id>
    <link href="https://blog.codewater.top/Git%E5%9F%BA%E7%A1%80%E6%95%99%E7%A8%8B-%E5%8E%9F%E7%90%86%E6%93%8D%E4%BD%9C%E4%B8%8E%E4%BC%81%E4%B8%9A%E5%AE%9E%E8%B7%B5/"/>
    <published>2026-07-05T02:00:00.000Z</published>
    <summary>Git 工作原理（三区域+对象模型）、常见命令速查、Git Flow 与 GitHub Flow 分支策略，以及企业中 commit message 规范、分支命名、PR 审查和 reflog 误操作恢复的完整实践指南。</summary>
    <title>Git 基础教程：原理、常见操作与企业实践规范</title>
    <updated>2026-07-05T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Linux" scheme="https://blog.codewater.top/tags/Linux/"/>
    <category term="Server" scheme="https://blog.codewater.top/tags/Server/"/>
    <category term="运维" scheme="https://blog.codewater.top/tags/%E8%BF%90%E7%BB%B4/"/>
    <content>
      <![CDATA[<p>服务挂了、端口被占、日志不知道在哪，这类问题在 SSH 上服务器后最常见。这篇是个人的命令速查手册，每个命令都附上实际场景的用法，不只是参数列表。</p><span id="more"></span><hr><h2 id="端口排查：ss-lsof"><a href="#端口排查：ss-lsof" class="headerlink" title="端口排查：ss &#x2F; lsof"></a>端口排查：ss &#x2F; lsof</h2><p><code>ss</code>（socket statistics）是查看网络连接状态的工具，是老版 <code>netstat</code> 的替代品，现代 Linux 发行版默认自带。<code>lsof</code>（list open files）做的事不同——Linux 里一切皆文件，网络连接也算，所以 <code>lsof</code> 可以列出哪个进程打开了哪些文件和网络连接。</p><p>查端口占用时两个都能用：<code>ss</code> 速度更快，专注网络；<code>lsof</code> 信息更全（用户、文件描述符），但稍慢。</p><h3 id="查看哪个进程在监听某个端口"><a href="#查看哪个进程在监听某个端口" class="headerlink" title="查看哪个进程在监听某个端口"></a>查看哪个进程在监听某个端口</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看 8080 端口被哪个进程占用</span></span><br><span class="line">ss -tlnp | grep 8080</span><br><span class="line"><span class="comment"># 或用 lsof（结果更详细）</span></span><br><span class="line">lsof -i :8080</span><br></pre></td></tr></table></figure><p><code>ss</code> 输出示例：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">LISTEN</span>  <span class="number">0</span>  <span class="number">128</span>  <span class="number">0.0.0.0:8080</span>  <span class="number">0.0.0.0</span>:*  users:((<span class="string">&quot;java&quot;</span>,pid=<span class="number">12345</span>,fd=<span class="number">42</span>))</span><br></pre></td></tr></table></figure><p><code>lsof</code> 输出示例：</p><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">COMMAND   PID <span class="keyword">USER</span>   <span class="title">FD</span>   <span class="keyword">TYPE</span> DEVICE SIZE/OFF <span class="keyword">NODE</span> <span class="title">NAME</span></span><br><span class="line">java    <span class="number">12345</span> root  <span class="number">42</span>u  IPv4  <span class="number">98765</span>      <span class="number">0</span>t0  TCP *:<span class="number">8080</span> (LISTEN)</span><br></pre></td></tr></table></figure><p>两个命令拿到的 PID 一样，<code>lsof</code> 显示的信息更多（用户、文件描述符）。</p><h3 id="查看所有监听端口"><a href="#查看所有监听端口" class="headerlink" title="查看所有监听端口"></a>查看所有监听端口</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ss -tlnp</span><br><span class="line"><span class="comment"># -t: TCP，-l: 仅监听状态，-n: 不解析服务名，-p: 显示进程</span></span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">State</span>   Recv-Q  Send-Q  Local Address:Port  Peer Address:Port  Process</span><br><span class="line"><span class="attribute">LISTEN</span>  <span class="number">0</span>       <span class="number">128</span>     <span class="number">0.0.0.0:22</span>          <span class="number">0.0.0.0</span>:*          users:((<span class="string">&quot;sshd&quot;</span>,pid=<span class="number">1001</span>))</span><br><span class="line"><span class="attribute">LISTEN</span>  <span class="number">0</span>       <span class="number">128</span>     <span class="number">0.0.0.0:80</span>          <span class="number">0.0.0.0</span>:*          users:((<span class="string">&quot;nginx&quot;</span>,pid=<span class="number">2002</span>))</span><br><span class="line"><span class="attribute">LISTEN</span>  <span class="number">0</span>       <span class="number">512</span>     <span class="number">0.0.0.0:3306</span>        <span class="number">0.0.0.0</span>:*          users:((<span class="string">&quot;mysqld&quot;</span>,pid=<span class="number">3003</span>))</span><br></pre></td></tr></table></figure><h3 id="查看所有已建立的连接"><a href="#查看所有已建立的连接" class="headerlink" title="查看所有已建立的连接"></a>查看所有已建立的连接</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 看有多少客户端连到了 80 端口</span></span><br><span class="line">ss -tnp | grep :80</span><br><span class="line"></span><br><span class="line"><span class="comment"># 统计各状态的 TCP 连接数（ESTABLISHED/TIME_WAIT 等）</span></span><br><span class="line">ss -tn | awk <span class="string">&#x27;&#123;print $1&#125;&#x27;</span> | <span class="built_in">sort</span> | <span class="built_in">uniq</span> -c | <span class="built_in">sort</span> -rn</span><br></pre></td></tr></table></figure><h3 id="netstat（老系统备用）"><a href="#netstat（老系统备用）" class="headerlink" title="netstat（老系统备用）"></a>netstat（老系统备用）</h3><p>部分旧服务器没有 <code>ss</code>，用 <code>netstat</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">netstat -tlnp         <span class="comment"># 查监听端口（需要 net-tools 包）</span></span><br><span class="line">netstat -anp | grep 8080</span><br></pre></td></tr></table></figure><hr><h2 id="进程排查：ps-top"><a href="#进程排查：ps-top" class="headerlink" title="进程排查：ps &#x2F; top"></a>进程排查：ps &#x2F; top</h2><p><code>ps</code>（process status）拍一张进程快照，输出当前时刻所有进程的状态，适合搜索特定进程、接管道做过滤。<code>top</code> 是实时监控，每隔几秒刷新一次，适合看某个进程是否在持续吃 CPU 或内存，或者整体资源占用是否异常。</p><p>两者互补：想找”java 进程 PID 是多少”用 <code>ps</code>；想看”谁在疯狂吃 CPU”用 <code>top</code>。</p><h3 id="找某个服务的进程"><a href="#找某个服务的进程" class="headerlink" title="找某个服务的进程"></a>找某个服务的进程</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 找 java 进程（-a: 所有用户，-u: 显示用户，-x: 包含无终端进程）</span></span><br><span class="line">ps aux | grep java</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只看进程名精确匹配（不显示 grep 自身）</span></span><br><span class="line">ps aux | grep <span class="string">&#x27;[j]ava&#x27;</span></span><br><span class="line"><span class="comment"># 或用 pgrep</span></span><br><span class="line">pgrep -l java</span><br></pre></td></tr></table></figure><p><code>ps aux</code> 输出列含义：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">USER</span>    PID   %CPU  %MEM     VSZ    RSS TTY  STAT START   TIME COMMAND</span><br><span class="line"><span class="attribute">root</span>  <span class="number">12345</span>   <span class="number">15</span>.<span class="number">2</span>   <span class="number">8</span>.<span class="number">4</span> <span class="number">2150432</span> <span class="number">342012</span> ?    Sl   <span class="number">09</span>:<span class="number">00</span>  <span class="number">10</span>:<span class="number">23</span> java -jar app.jar</span><br></pre></td></tr></table></figure><ul><li><code>%CPU</code>：CPU 占用</li><li><code>%MEM</code>：内存占用比例</li><li><code>RSS</code>：实际占用的物理内存（KB）</li><li><code>STAT</code>：进程状态（<code>S</code> 睡眠、<code>R</code> 运行、<code>Z</code> 僵尸）</li></ul><h3 id="看-CPU-和内存占用最高的进程"><a href="#看-CPU-和内存占用最高的进程" class="headerlink" title="看 CPU 和内存占用最高的进程"></a>看 CPU 和内存占用最高的进程</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 按 CPU 降序，取前 10</span></span><br><span class="line">ps aux --<span class="built_in">sort</span>=-%cpu | <span class="built_in">head</span> -11</span><br><span class="line"></span><br><span class="line"><span class="comment"># 按内存降序，取前 10</span></span><br><span class="line">ps aux --<span class="built_in">sort</span>=-%mem | <span class="built_in">head</span> -11</span><br></pre></td></tr></table></figure><h3 id="实时监控：top"><a href="#实时监控：top" class="headerlink" title="实时监控：top"></a>实时监控：top</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">top</span><br></pre></td></tr></table></figure><p>进入 <code>top</code> 后的常用操作：</p><ul><li><code>P</code>：按 CPU 排序</li><li><code>M</code>：按内存排序</li><li><code>k</code>：输入 PID 后 kill 进程</li><li><code>q</code>：退出</li></ul><p>如果服务器装了 <code>htop</code>，用 <code>htop</code> 体验更好（支持鼠标、彩色显示）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">htop</span><br></pre></td></tr></table></figure><hr><h2 id="服务管理：systemctl"><a href="#服务管理：systemctl" class="headerlink" title="服务管理：systemctl"></a>服务管理：systemctl</h2><p>systemd 是现代 Linux 上最主流的服务管理器，负责系统启动时按顺序拉起各个服务，并在运行期间维护它们的状态。<code>systemctl</code> 是操作 systemd 的命令行工具。Ubuntu 16.04+、CentOS 7+、Debian 8+ 都用 systemd，基本覆盖了所有主流服务器发行版。</p><p>服务的启停、状态查看、开机自启都通过 <code>systemctl</code> 管理。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看服务状态（有没有在跑、最近的日志）</span></span><br><span class="line">systemctl status nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动 / 停止 / 重启</span></span><br><span class="line">systemctl start nginx</span><br><span class="line">systemctl stop nginx</span><br><span class="line">systemctl restart nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 平滑重载配置（不中断连接，nginx 支持）</span></span><br><span class="line">systemctl reload nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置开机自启 / 取消</span></span><br><span class="line">systemctl <span class="built_in">enable</span> nginx</span><br><span class="line">systemctl <span class="built_in">disable</span> nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看所有正在运行的服务</span></span><br><span class="line">systemctl list-units --<span class="built_in">type</span>=service --state=running</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看启动失败的服务</span></span><br><span class="line">systemctl list-units --<span class="built_in">type</span>=service --state=failed</span><br></pre></td></tr></table></figure><p><code>systemctl status</code> 输出示例：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">●</span> <span class="string">nginx.service</span> <span class="bullet">-</span> <span class="string">A</span> <span class="string">high</span> <span class="string">performance</span> <span class="string">web</span> <span class="string">server</span> <span class="string">and</span> <span class="string">a</span> <span class="string">reverse</span> <span class="string">proxy</span> <span class="string">server</span></span><br><span class="line">     <span class="attr">Loaded:</span> <span class="string">loaded</span> <span class="string">(/lib/systemd/system/nginx.service;</span> <span class="string">enabled;</span> <span class="attr">vendor preset:</span> <span class="string">enabled)</span></span><br><span class="line">     <span class="attr">Active:</span> <span class="string">active</span> <span class="string">(running)</span> <span class="string">since</span> <span class="string">Fri</span> <span class="number">2026-07-04 09:00:00 </span><span class="string">UTC;</span> <span class="string">2h</span> <span class="string">30min</span> <span class="string">ago</span></span><br><span class="line">    <span class="attr">Process:</span> <span class="number">1234 </span><span class="string">ExecStartPre=/usr/sbin/nginx</span> <span class="string">-t</span> <span class="string">(code=exited,</span> <span class="string">status=0/SUCCESS)</span></span><br><span class="line">   <span class="attr">Main PID:</span> <span class="number">1235</span> <span class="string">(nginx)</span></span><br><span class="line">      <span class="attr">Tasks: 5 (limit:</span> <span class="number">4915</span><span class="string">)</span></span><br><span class="line">     <span class="attr">Memory:</span> <span class="number">12.</span><span class="string">4M</span></span><br><span class="line">        <span class="attr">CPU:</span> <span class="number">1.</span><span class="string">234s</span></span><br><span class="line">     <span class="attr">CGroup:</span> <span class="string">/system.slice/nginx.service</span></span><br><span class="line">             <span class="string">├─1235</span> <span class="attr">nginx:</span> <span class="string">master</span> <span class="string">process</span> <span class="string">/usr/sbin/nginx</span> <span class="string">-g</span> <span class="string">daemon</span> <span class="string">on;</span> <span class="string">master_process</span> <span class="string">on;</span></span><br><span class="line">             <span class="string">└─1236</span> <span class="attr">nginx:</span> <span class="string">worker</span> <span class="string">process</span></span><br></pre></td></tr></table></figure><p><code>Active: active (running)</code> 说明服务正常运行；<code>Active: failed</code> 说明启动失败。</p><hr><h2 id="日志查看：journalctl-tail-less-grep"><a href="#日志查看：journalctl-tail-less-grep" class="headerlink" title="日志查看：journalctl &#x2F; tail &#x2F; less &#x2F; grep"></a>日志查看：journalctl &#x2F; tail &#x2F; less &#x2F; grep</h2><p><code>journalctl</code> 是 systemd 的日志查看工具，专门读取 systemd 收集的结构化日志，可以按服务名、时间段、日志级别过滤。<code>tail</code> 是通用的文本文件查看命令，用于直接读取磁盘上的日志文件。<code>less</code> 是分页查看器，适合打开大日志文件慢慢翻，不会把终端刷满。<code>grep</code> 不是日志工具，但配合以上三个使用可以快速过滤出包含关键词的行。</p><p>两者对应两类日志：<code>journalctl</code> 看 systemd 托管的服务日志（比如 nginx、sshd），<code>tail</code> 看应用自己写到文件的日志（比如 <code>/var/log/app/app.log</code>）。</p><h3 id="用-journalctl-查-systemd-服务的日志"><a href="#用-journalctl-查-systemd-服务的日志" class="headerlink" title="用 journalctl 查 systemd 服务的日志"></a>用 journalctl 查 systemd 服务的日志</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看 nginx 最近的日志</span></span><br><span class="line">journalctl -u nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 实时跟踪（类似 tail -f）</span></span><br><span class="line">journalctl -u nginx -f</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只看最近 100 行</span></span><br><span class="line">journalctl -u nginx -n 100</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查某个时间段的日志</span></span><br><span class="line">journalctl -u nginx --since <span class="string">&quot;2026-07-04 09:00:00&quot;</span> --<span class="keyword">until</span> <span class="string">&quot;2026-07-04 10:00:00&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 只看错误级别</span></span><br><span class="line">journalctl -u nginx -p err</span><br></pre></td></tr></table></figure><h3 id="用-tail-看日志文件"><a href="#用-tail-看日志文件" class="headerlink" title="用 tail 看日志文件"></a>用 tail 看日志文件</h3><p>应用自己写的日志文件（不走 systemd）用 <code>tail</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 实时跟踪日志</span></span><br><span class="line"><span class="built_in">tail</span> -f /var/log/app/app.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 看最后 200 行</span></span><br><span class="line"><span class="built_in">tail</span> -n 200 /var/log/app/app.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 跟踪日志并过滤 ERROR</span></span><br><span class="line"><span class="built_in">tail</span> -f /var/log/app/app.log | grep ERROR</span><br></pre></td></tr></table></figure><h3 id="用-less-分页查看大日志"><a href="#用-less-分页查看大日志" class="headerlink" title="用 less 分页查看大日志"></a>用 less 分页查看大日志</h3><p><code>tail</code> 是实时跟踪，<code>less</code> 是”打开文件慢慢翻”。日志文件几十 MB 时直接 <code>cat</code> 会把终端刷满，用 <code>less</code> 可以分页浏览，还能在文件里搜索。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">less /var/log/app/app.log</span><br></pre></td></tr></table></figure><p>进入 <code>less</code> 后的常用操作：</p><ul><li><code>G</code>：跳到文件末尾（查最新日志）</li><li><code>g</code>：跳到文件开头</li><li><code>/keyword</code>：向下搜索关键词，按 <code>n</code> 跳下一个，<code>N</code> 跳上一个</li><li><code>q</code>：退出</li></ul><p><code>less +G</code> 直接打开并跳到末尾，省去手动按 <code>G</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">less +G /var/log/app/app.log</span><br></pre></td></tr></table></figure><h3 id="用-grep-过滤关键词"><a href="#用-grep-过滤关键词" class="headerlink" title="用 grep 过滤关键词"></a>用 grep 过滤关键词</h3><p><code>grep</code> 本身是文本搜索工具，配合日志使用时可以快速过滤出包含关键词的行，比翻日志文件高效得多。</p><h3 id="在日志里搜关键词"><a href="#在日志里搜关键词" class="headerlink" title="在日志里搜关键词"></a>在日志里搜关键词</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 搜包含 &quot;Connection refused&quot; 的行</span></span><br><span class="line">grep <span class="string">&quot;Connection refused&quot;</span> /var/log/app/app.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 忽略大小写</span></span><br><span class="line">grep -i <span class="string">&quot;error&quot;</span> /var/log/app/app.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示匹配行后面 3 行（看报错之后发生了什么）</span></span><br><span class="line">grep -A 3 <span class="string">&quot;panic&quot;</span> /var/log/app/app.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示匹配行前面 3 行（看报错之前发生了什么）</span></span><br><span class="line">grep -B 3 <span class="string">&quot;panic&quot;</span> /var/log/app/app.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示匹配行前后各 3 行（看完整上下文）</span></span><br><span class="line">grep -C 3 <span class="string">&quot;panic&quot;</span> /var/log/app/app.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 统计 ERROR 出现次数</span></span><br><span class="line">grep -c <span class="string">&quot;ERROR&quot;</span> /var/log/app/app.log</span><br></pre></td></tr></table></figure><hr><h2 id="网络连通性：ping-curl-dig"><a href="#网络连通性：ping-curl-dig" class="headerlink" title="网络连通性：ping &#x2F; curl &#x2F; dig"></a>网络连通性：ping &#x2F; curl &#x2F; dig</h2><p>这三个命令分别在不同层面测网络：<code>ping</code> 测 IP 层连通，确认能不能到达对方机器；<code>curl</code> 测 HTTP&#x2F;HTTPS 层，确认接口能否正常响应；<code>dig</code> 测 DNS 解析，确认域名能不能解析到正确 IP。</p><p>排查”外部访问不通”时，通常按这个顺序检查：先 <code>ping</code> 确认网络可达，再 <code>dig</code> 确认 DNS 正确，再 <code>curl</code> 确认服务本身没问题。</p><h3 id="ping：测服务器是否可达"><a href="#ping：测服务器是否可达" class="headerlink" title="ping：测服务器是否可达"></a>ping：测服务器是否可达</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 基本测试</span></span><br><span class="line">ping google.com</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只发 4 个包</span></span><br><span class="line">ping -c 4 google.com</span><br><span class="line"></span><br><span class="line"><span class="comment"># 测内网 IP</span></span><br><span class="line">ping 192.168.1.10</span><br></pre></td></tr></table></figure><p>ping 通说明网络层（IP）可达，但 ping 通不代表服务正常——服务可能没起来，或端口没开。</p><h3 id="curl：测-HTTP-接口"><a href="#curl：测-HTTP-接口" class="headerlink" title="curl：测 HTTP 接口"></a>curl：测 HTTP 接口</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 测接口是否返回正常</span></span><br><span class="line">curl http://localhost:8080/health</span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示响应头（看状态码、Content-Type）</span></span><br><span class="line">curl -I http://localhost:8080/health</span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示完整请求和响应（调试用）</span></span><br><span class="line">curl -v http://localhost:8080/api/user</span><br><span class="line"></span><br><span class="line"><span class="comment"># 测 POST 接口</span></span><br><span class="line">curl -X POST http://localhost:8080/api/login \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;username&quot;:&quot;test&quot;,&quot;password&quot;:&quot;123&quot;&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置超时（5 秒没响应就报错）</span></span><br><span class="line">curl --max-time 5 http://localhost:8080/health</span><br><span class="line"></span><br><span class="line"><span class="comment"># 跟随 301/302 重定向</span></span><br><span class="line">curl -L http://example.com</span><br></pre></td></tr></table></figure><h3 id="dig-nslookup：测-DNS-解析"><a href="#dig-nslookup：测-DNS-解析" class="headerlink" title="dig &#x2F; nslookup：测 DNS 解析"></a>dig &#x2F; nslookup：测 DNS 解析</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查域名解析到哪个 IP</span></span><br><span class="line">dig example.com</span><br><span class="line">dig example.com A      <span class="comment"># 只查 A 记录</span></span><br><span class="line">dig example.com CNAME  <span class="comment"># 查 CNAME</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 用指定 DNS 服务器查（绕过本地缓存）</span></span><br><span class="line">dig @8.8.8.8 example.com</span><br><span class="line"></span><br><span class="line"><span class="comment"># 精简输出</span></span><br><span class="line">dig +short example.com</span><br></pre></td></tr></table></figure><p><code>nslookup</code> 更简单，适合快速查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nslookup example.com</span><br><span class="line">nslookup example.com 8.8.8.8   <span class="comment"># 指定 DNS 服务器</span></span><br></pre></td></tr></table></figure><h3 id="traceroute：排查网络路径问题"><a href="#traceroute：排查网络路径问题" class="headerlink" title="traceroute：排查网络路径问题"></a>traceroute：排查网络路径问题</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看到目标的路由路径</span></span><br><span class="line">traceroute google.com</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在某一跳超时（显示 *）说明那一跳有问题</span></span><br></pre></td></tr></table></figure><hr><h2 id="磁盘和内存：df-du-free"><a href="#磁盘和内存：df-du-free" class="headerlink" title="磁盘和内存：df &#x2F; du &#x2F; free"></a>磁盘和内存：df &#x2F; du &#x2F; free</h2><p><code>df</code>（disk free）查文件系统级别的磁盘使用，看每个分区还剩多少空间。<code>du</code>（disk usage）查某个目录或文件实际占用了多少空间，适合找出是哪个目录把磁盘撑满了。<code>free</code> 查内存的使用和剩余情况。</p><p>服务莫名崩溃，除了看进程和日志，记得用这三个命令确认一下资源有没有被耗尽。</p><h3 id="df：查磁盘使用情况"><a href="#df：查磁盘使用情况" class="headerlink" title="df：查磁盘使用情况"></a>df：查磁盘使用情况</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查所有挂载点的磁盘使用（-h: 人类可读）</span></span><br><span class="line"><span class="built_in">df</span> -h</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只看根目录</span></span><br><span class="line"><span class="built_in">df</span> -h /</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight erlang-repl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Filesystem      Size  Used Avail Use<span class="comment">% Mounted on</span></span><br><span class="line">/dev/vda1        <span class="number">40</span>G   <span class="number">28</span>G  <span class="number">9.8</span>G  <span class="number">74</span><span class="comment">% /</span></span><br><span class="line">tmpfs           <span class="number">1.9</span>G     <span class="number">0</span>  <span class="number">1.9</span>G   <span class="number">0</span><span class="comment">% /dev/shm</span></span><br></pre></td></tr></table></figure><p><code>Use%</code> 接近 100% 说明磁盘快满了，服务可能因为写不了日志而崩。</p><h3 id="du：查目录大小"><a href="#du：查目录大小" class="headerlink" title="du：查目录大小"></a>du：查目录大小</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查当前目录下各子目录的大小</span></span><br><span class="line"><span class="built_in">du</span> -sh *</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查指定目录</span></span><br><span class="line"><span class="built_in">du</span> -sh /var/log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 找出最大的目录（排序）</span></span><br><span class="line"><span class="built_in">du</span> -sh /var/log/* | <span class="built_in">sort</span> -rh | <span class="built_in">head</span> -10</span><br></pre></td></tr></table></figure><p>磁盘满了先查 <code>/var/log</code>，日志撑满磁盘是常见原因。</p><h3 id="free：查内存使用"><a href="#free：查内存使用" class="headerlink" title="free：查内存使用"></a>free：查内存使用</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">free -h</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">               <span class="attribute">total</span>        used        free      shared  buff/cache   available</span><br><span class="line"><span class="attribute">Mem</span>:            <span class="number">15</span>Gi        <span class="number">8</span>.<span class="number">2</span>Gi      <span class="number">2</span>.<span class="number">1</span>Gi       <span class="number">512</span>Mi       <span class="number">5</span>.<span class="number">1</span>Gi       <span class="number">6</span>.<span class="number">7</span>Gi</span><br><span class="line"><span class="attribute">Swap</span>:          <span class="number">2</span>.<span class="number">0</span>Gi        <span class="number">512</span>Mi      <span class="number">1</span>.<span class="number">5</span>Gi</span><br></pre></td></tr></table></figure><ul><li><code>available</code>：实际可用内存（比 <code>free</code> 更准确，包含可回收的 cache；procps-ng 3.3.10+ 才有此列，旧系统可能没有）</li><li><code>Swap used</code> 过高说明物理内存不够，系统在用交换分区，服务会变慢</li></ul><hr><h2 id="服务访问不通时的完整排查顺序"><a href="#服务访问不通时的完整排查顺序" class="headerlink" title="服务访问不通时的完整排查顺序"></a>服务访问不通时的完整排查顺序</h2><p>服务访问不通，按这个顺序查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 确认服务进程是否在跑</span></span><br><span class="line">ps aux | grep <span class="string">&#x27;[a]pp&#x27;</span></span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line">systemctl status myapp</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 确认端口是否在监听</span></span><br><span class="line">ss -tlnp | grep 8080</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 本机测接口是否正常</span></span><br><span class="line">curl -v http://localhost:8080/health</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 看服务最近的错误日志</span></span><br><span class="line">journalctl -u myapp -n 100 -p err</span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line"><span class="built_in">tail</span> -n 200 /var/log/myapp/app.log | grep -i error</span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 检查磁盘和内存是否耗尽</span></span><br><span class="line"><span class="built_in">df</span> -h /</span><br><span class="line">free -h</span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 如果是域名访问不通，查 DNS</span></span><br><span class="line">dig +short mydomain.com</span><br><span class="line"></span><br><span class="line"><span class="comment"># 7. 如果是外部访问不通但本机正常，检查防火墙</span></span><br><span class="line"><span class="comment"># Ubuntu/Debian</span></span><br><span class="line">ufw status</span><br><span class="line"><span class="comment"># CentOS/RHEL</span></span><br><span class="line">firewall-cmd --list-all</span><br></pre></td></tr></table></figure><p>大多数”服务挂了”的情况，前四步就能定位：进程死了 → 重启；端口没监听 → 看启动日志；接口报错 → 看应用日志；磁盘满 → 清日志。</p>]]>
    </content>
    <id>https://blog.codewater.top/%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%8E%92%E6%9F%A5%E6%9C%8D%E5%8A%A1%E9%97%AE%E9%A2%98%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</id>
    <link href="https://blog.codewater.top/%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%8E%92%E6%9F%A5%E6%9C%8D%E5%8A%A1%E9%97%AE%E9%A2%98%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/"/>
    <published>2026-07-04T02:00:00.000Z</published>
    <summary>Linux 服务器问题排查常用命令速查：ss/lsof 查端口占用，ps/top 看进程状态，systemctl/journalctl 管理服务和查日志，curl/dig 测网络连通，df/du/free 看磁盘内存，附完整排查流程。</summary>
    <title>Linux 服务器问题排查：端口、进程、日志常用命令速查</title>
    <updated>2026-07-04T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="效率工具" scheme="https://blog.codewater.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <category term="Neovim" scheme="https://blog.codewater.top/tags/Neovim/"/>
    <category term="LazyVim" scheme="https://blog.codewater.top/tags/LazyVim/"/>
    <content>
      <![CDATA[<p>Neovim 最大的门槛不是学习 Vim 操作，而是配置。从零搭一套可用的 Neovim 环境，光是理解插件管理器、LSP 配置、补全框架的关系就要花很多时间。LazyVim 解决的正是这个问题：它是一套预先打包好的完整配置，装完就有文件树、模糊搜索、语法高亮、LSP 补全、Git 集成，直接可以用。</p><span id="more"></span><hr><h2 id="Vi、Vim、Neovim、LazyVim-的关系"><a href="#Vi、Vim、Neovim、LazyVim-的关系" class="headerlink" title="Vi、Vim、Neovim、LazyVim 的关系"></a>Vi、Vim、Neovim、LazyVim 的关系</h2><p>这几个名字经常混用，理清一下：</p><ul><li><strong>Vi</strong>：1976 年诞生于 Unix，是最早的终端模态编辑器，确立了 Normal&#x2F;Insert 模式这套操作体系。现在 macOS 自带的 <code>vi</code> 命令实际上指向的是 Vim。</li><li><strong>Vim</strong>（Vi IMproved）：1991 年发布，在 Vi 基础上加了语法高亮、多级撤销、插件系统等功能，成为几十年来最主流的终端编辑器。</li><li><strong>Neovim</strong>：2014 年从 Vim 分叉出来，目标是清理 Vim 几十年积累的历史包袱，加入原生 LSP 支持、异步任务、Lua 配置接口。现在活跃维护，插件生态比 Vim 更现代。</li><li><strong>LazyVim</strong>：不是一个独立软件，而是运行在 Neovim 上的一套配置框架。它把插件管理、LSP 配置、键位方案打包在一起，让你跳过从零配置的过程。</li></ul><p>用一句话概括：LazyVim 是 Neovim 的配置，Neovim 是 Vim 的现代重写，Vim 是 Vi 的增强版。键位操作从 Vi 时代延续至今，学了 Neovim 就等于学了整个 Vi 系列。</p><hr><h2 id="为什么选-Neovim-而不是继续用-VSCode"><a href="#为什么选-Neovim-而不是继续用-VSCode" class="headerlink" title="为什么选 Neovim 而不是继续用 VSCode"></a>为什么选 Neovim 而不是继续用 VSCode</h2><p>不是说 VSCode 不好。但有几个场景 Neovim 明显更合适：</p><ul><li>SSH 连进服务器改配置文件，VSCode 的 Remote SSH 有时很慢甚至连不上</li><li>在终端里已经完成了 80% 的工作，不想为了改一个文件再打开 GUI</li><li>习惯了系列前几篇的工具之后，kitty 分屏 + Neovim 的工作流比 IDE 切换更顺</li></ul><p>如果你只是想要一个顺手的编辑器，这篇文章也够用——LazyVim 装完就是一个功能完整的编辑器，不需要从头配置任何东西。</p><hr><h2 id="Neovim-安装"><a href="#Neovim-安装" class="headerlink" title="Neovim 安装"></a><a href="https://neovim.io/">Neovim</a> 安装</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install neovim</span><br></pre></td></tr></table></figure><p>验证安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nvim --version</span><br><span class="line"><span class="comment"># 应该显示 NVIM v0.12.x 或更高</span></span><br></pre></td></tr></table></figure><hr><h2 id="LazyVim-安装：用别人调好的配置"><a href="#LazyVim-安装：用别人调好的配置" class="headerlink" title="LazyVim 安装：用别人调好的配置"></a><a href="https://www.lazyvim.org/">LazyVim</a> 安装：用别人调好的配置</h2><p>LazyVim 是一套基于 lazy.nvim 插件管理器的 Neovim 配置框架，由 folke（lazy.nvim 作者）维护。安装方式是把它的配置仓库克隆到 Neovim 的配置目录。</p><h3 id="前置依赖"><a href="#前置依赖" class="headerlink" title="前置依赖"></a>前置依赖</h3><p>LazyVim 依赖几个外部工具，系列前几篇已经装过其中大部分：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">brew install git ripgrep fd lazygit</span><br><span class="line"><span class="comment"># ripgrep、fd：Telescope 文件搜索用</span></span><br><span class="line"><span class="comment"># lazygit：内置 Git TUI 集成</span></span><br></pre></td></tr></table></figure><p>还需要一个 Nerd Font（用于图标显示）。如果用的是 kitty，需要给 kitty 配置 Nerd Font：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install --cask font-jetbrains-mono-nerd-font</span><br></pre></td></tr></table></figure><p>然后在 <code>~/.config/kitty/kitty.conf</code> 里加一行：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">font_family JetBrainsMono Nerd <span class="attribute">Font</span></span><br></pre></td></tr></table></figure><h3 id="备份已有配置（如果有的话）"><a href="#备份已有配置（如果有的话）" class="headerlink" title="备份已有配置（如果有的话）"></a>备份已有配置（如果有的话）</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mv</span> ~/.config/nvim ~/.config/nvim.bak 2&gt;/dev/null</span><br><span class="line"><span class="built_in">mv</span> ~/.local/share/nvim ~/.local/share/nvim.bak 2&gt;/dev/null</span><br><span class="line"><span class="built_in">mv</span> ~/.local/state/nvim ~/.local/state/nvim.bak 2&gt;/dev/null</span><br></pre></td></tr></table></figure><h3 id="克隆-LazyVim-starter"><a href="#克隆-LazyVim-starter" class="headerlink" title="克隆 LazyVim starter"></a>克隆 LazyVim starter</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/LazyVim/starter ~/.config/nvim</span><br><span class="line"><span class="built_in">rm</span> -rf ~/.config/nvim/.git</span><br><span class="line"><span class="comment"># 去掉 .git 让这份配置成为你自己的，不绑定上游</span></span><br></pre></td></tr></table></figure><h3 id="首次启动"><a href="#首次启动" class="headerlink" title="首次启动"></a>首次启动</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nvim</span><br></pre></td></tr></table></figure><p>第一次打开会自动安装所有插件，等待进度条跑完（需要网络，几分钟）。安装完成后重启一次 nvim。</p><hr><h2 id="Vim-最小生存指南"><a href="#Vim-最小生存指南" class="headerlink" title="Vim 最小生存指南"></a>Vim 最小生存指南</h2><p>LazyVim 基于 Vim 键位，没用过 Vim 的话先记这几个：</p><p><strong>模式切换</strong>：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">i</span>       进入 Insert 模式（开始输入）</span><br><span class="line">Esc     回到 <span class="attribute">Normal</span> 模式</span><br><span class="line">v       进入 Visual 模式（选择文本）</span><br><span class="line">:       进入 Command 模式（输入命令）</span><br></pre></td></tr></table></figure><p><strong>Normal 模式下移动</strong>：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">h <span class="keyword">j </span>k l   左下上右（也可以用方向键）</span><br><span class="line">w         跳到下一个单词开头</span><br><span class="line"><span class="keyword">b </span>        跳到上一个单词开头</span><br><span class="line"><span class="number">0</span>         跳到行首</span><br><span class="line">$         跳到行尾</span><br><span class="line">gg        跳到文件开头</span><br><span class="line">G         跳到文件末尾</span><br><span class="line">Ctrl+d    向下翻半页</span><br><span class="line">Ctrl+u    向上翻半页</span><br></pre></td></tr></table></figure><p><strong>基础编辑</strong>：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">dd</span>        删除当前行</span><br><span class="line">yy        复制当前行</span><br><span class="line"><span class="selector-tag">p</span>         粘贴到下方</span><br><span class="line">u         撤销</span><br><span class="line">Ctrl+<span class="attribute">r</span>    重做</span><br><span class="line">/<span class="selector-tag">pattern</span>  向下搜索，n 跳到下一个，N 跳到上一个</span><br></pre></td></tr></table></figure><p><strong>保存和退出</strong>：</p><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">:w</span>        保存</span><br><span class="line"><span class="symbol">:q</span>        退出</span><br><span class="line"><span class="symbol">:wq</span>       保存并退出</span><br><span class="line"><span class="symbol">:q!</span>       强制退出不保存</span><br></pre></td></tr></table></figure><hr><h2 id="LazyVim-核心功能"><a href="#LazyVim-核心功能" class="headerlink" title="LazyVim 核心功能"></a>LazyVim 核心功能</h2><p>LazyVim 在 Vim 基础键位上层叠了一套以 <code>Space</code> 为 leader 键的快捷键体系。按 <code>Space</code> 停顿一下，屏幕下方会弹出 which-key 提示窗口，显示所有可用的下一步操作。</p><h3 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h3><figure class="highlight mathematica"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Space</span> <span class="operator">+</span> <span class="variable">e</span>       打开<span class="operator">/</span>关闭文件树（<span class="variable">neo</span><span class="operator">-</span><span class="variable">tree</span>）</span><br><span class="line"><span class="built_in">Space</span> <span class="operator">+</span> <span class="variable">f</span> <span class="operator">+</span> <span class="variable">f</span>   模糊搜索文件名（<span class="variable">Telescope</span>）</span><br><span class="line"><span class="built_in">Space</span> <span class="operator">+</span> <span class="variable">f</span> <span class="operator">+</span> <span class="variable">g</span>   搜索文件内容（<span class="variable">ripgrep</span>）</span><br><span class="line"><span class="built_in">Space</span> <span class="operator">+</span> <span class="variable">f</span> <span class="operator">+</span> <span class="variable">r</span>   最近打开的文件</span><br></pre></td></tr></table></figure><p>在文件树里：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">a</span>       新建文件</span><br><span class="line">d       删除文件</span><br><span class="line"><span class="attribute">r</span>       重命名</span><br><span class="line">Enter   打开文件</span><br></pre></td></tr></table></figure><h3 id="LSP-功能（需要对应语言的-language-server）"><a href="#LSP-功能（需要对应语言的-language-server）" class="headerlink" title="LSP 功能（需要对应语言的 language server）"></a>LSP 功能（需要对应语言的 language server）</h3><p>LazyVim 内置了 mason.nvim 来管理 LSP。打开一个 Go 或 Python 文件时会自动提示安装对应的 language server。</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">gd      跳到定义（go <span class="selector-tag">to</span> definition）</span><br><span class="line">gr      查看所有引用（go <span class="selector-tag">to</span> references）</span><br><span class="line">K       悬浮显示文档（hover doc）</span><br><span class="line">Space + c + <span class="selector-tag">a</span>   代码操作（<span class="selector-tag">code</span> action，如自动 import）</span><br><span class="line">Space + c + <span class="attribute">r</span>   重命名符号</span><br></pre></td></tr></table></figure><h3 id="多窗口"><a href="#多窗口" class="headerlink" title="多窗口"></a>多窗口</h3><figure class="highlight mathematica"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Space</span> <span class="operator">+</span> <span class="operator">|</span>   垂直分割窗口</span><br><span class="line"><span class="built_in">Space</span> <span class="operator">+</span> <span class="operator">-</span>   水平分割窗口</span><br><span class="line"><span class="variable">Ctrl</span><span class="operator">+</span><span class="variable">h</span><span class="operator">/</span><span class="variable">j</span><span class="operator">/</span><span class="variable">k</span><span class="operator">/</span><span class="variable">l</span>    在分割窗口间移动</span><br></pre></td></tr></table></figure><h3 id="Git-集成"><a href="#Git-集成" class="headerlink" title="Git 集成"></a>Git 集成</h3><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Space + <span class="selector-tag">g</span> + <span class="selector-tag">g</span>   打开 lazygit（需要已安装）</span><br><span class="line">Space + <span class="selector-tag">g</span> + <span class="selector-tag">b</span>   查看当前行的 git blame</span><br></pre></td></tr></table></figure><hr><h2 id="常用快捷键速查"><a href="#常用快捷键速查" class="headerlink" title="常用快捷键速查"></a>常用快捷键速查</h2><table><thead><tr><th>操作</th><th>快捷键</th></tr></thead><tbody><tr><td>查找文件</td><td><code>Space f f</code></td></tr><tr><td>搜索内容</td><td><code>Space f g</code></td></tr><tr><td>文件树</td><td><code>Space e</code></td></tr><tr><td>关闭当前 buffer</td><td><code>Space b d</code></td></tr><tr><td>格式化文件</td><td><code>Space c f</code></td></tr><tr><td>跳到定义</td><td><code>gd</code></td></tr><tr><td>查看引用</td><td><code>gr</code></td></tr><tr><td>打开 lazygit</td><td><code>Space g g</code></td></tr><tr><td>查看诊断（错误）</td><td><code>Space x x</code></td></tr><tr><td>命令面板</td><td><code>Space :</code></td></tr></tbody></table><p>按 <code>Space</code> 后等待 which-key 浮窗，是学习新快捷键最快的方式。</p><hr><h2 id="从哪里开始"><a href="#从哪里开始" class="headerlink" title="从哪里开始"></a>从哪里开始</h2><p>不建议一上来就把 Neovim 当主力编辑器，学习曲线会很陡：</p><ol><li>先在低风险的场景用——改配置文件、看日志、临时编辑一个文件</li><li>遇到不会的操作就按 <code>Space</code> 看 which-key 提示，或者 <code>:Tutor</code> 运行内置教程</li><li>稳定用了一两周之后，再考虑迁移更重要的工作</li></ol><p>LazyVim 的自定义配置放在 <code>~/.config/nvim/lua/plugins/</code> 下，新建一个 <code>.lua</code> 文件即可覆盖默认设置或添加插件，不需要改动 LazyVim 本身的文件。官方文档在 <a href="https://www.lazyvim.org/">lazyvim.org</a>。</p>]]>
    </content>
    <id>https://blog.codewater.top/neovim-lazyvim-%E7%BB%88%E7%AB%AF%E7%BC%96%E8%BE%91%E5%99%A8/</id>
    <link href="https://blog.codewater.top/neovim-lazyvim-%E7%BB%88%E7%AB%AF%E7%BC%96%E8%BE%91%E5%99%A8/"/>
    <published>2026-07-03T10:00:00.000Z</published>
    <summary>LazyVim 是 Neovim 的完整配置框架，开箱即有 LSP、文件树、模糊搜索、Git 集成。本文从安装到基础操作，帮你跳过配置地狱直接进入使用。</summary>
    <title>从零开始用 Neovim：LazyVim 让配置不再是障碍</title>
    <updated>2026-07-03T10:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="效率工具" scheme="https://blog.codewater.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <category term="fastfetch" scheme="https://blog.codewater.top/tags/fastfetch/"/>
    <content>
      <![CDATA[<p>每次打开终端默认就是一个空白提示符。fastfetch 可以在启动时打印系统信息，pokemon-colorscripts 可以随机显示一只像素风宝可梦。两个工具都是纯装饰向，不影响任何工作流。</p><span id="more"></span><hr><h2 id="fastfetch：比-neofetch-快-10-倍的系统信息展示"><a href="#fastfetch：比-neofetch-快-10-倍的系统信息展示" class="headerlink" title="fastfetch：比 neofetch 快 10 倍的系统信息展示"></a><a href="https://github.com/fastfetch-cli/fastfetch">fastfetch</a>：比 neofetch 快 10 倍的系统信息展示</h2><p>fastfetch 是 neofetch 的现代替代品，用 C 写成，启动速度比 neofetch 快很多，且 neofetch 已停止维护（2023 年归档）。显示内容包括 OS、内核版本、CPU&#x2F;GPU、内存、Shell、终端等，支持高度自定义。</p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install fastfetch</span><br></pre></td></tr></table></figure><h3 id="直接运行"><a href="#直接运行" class="headerlink" title="直接运行"></a>直接运行</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fastfetch</span><br></pre></td></tr></table></figure><p>默认会显示一个 Apple logo（ASCII art）加上右侧系统信息。</p><h3 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h3><p>fastfetch 的配置文件在 <code>~/.config/fastfetch/config.jsonc</code>（JSONC 格式，支持注释）。先生成默认配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">fastfetch --gen-config</span><br><span class="line"><span class="comment"># 在 ~/.config/fastfetch/ 下生成 config.jsonc</span></span><br></pre></td></tr></table></figure><p>生成后打开编辑，配置文件结构是一个 <code>modules</code> 数组，每个元素对应一行显示内容：</p><figure class="highlight jsonc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;$schema&quot;</span><span class="punctuation">:</span> <span class="string">&quot;https://github.com/fastfetch-cli/fastfetch/raw/dev/doc/json_schema.json&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;modules&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="string">&quot;title&quot;</span><span class="punctuation">,</span>       <span class="comment">// 用户名@主机名</span></span><br><span class="line">    <span class="string">&quot;separator&quot;</span><span class="punctuation">,</span>   <span class="comment">// 分隔线</span></span><br><span class="line">    <span class="string">&quot;os&quot;</span><span class="punctuation">,</span>          <span class="comment">// 操作系统</span></span><br><span class="line">    <span class="string">&quot;host&quot;</span><span class="punctuation">,</span>        <span class="comment">// 机器型号</span></span><br><span class="line">    <span class="string">&quot;kernel&quot;</span><span class="punctuation">,</span>      <span class="comment">// 内核版本</span></span><br><span class="line">    <span class="string">&quot;uptime&quot;</span><span class="punctuation">,</span>      <span class="comment">// 运行时长</span></span><br><span class="line">    <span class="string">&quot;packages&quot;</span><span class="punctuation">,</span>    <span class="comment">// 已安装包数量</span></span><br><span class="line">    <span class="string">&quot;shell&quot;</span><span class="punctuation">,</span>       <span class="comment">// 当前 Shell</span></span><br><span class="line">    <span class="string">&quot;display&quot;</span><span class="punctuation">,</span>     <span class="comment">// 分辨率</span></span><br><span class="line">    <span class="string">&quot;de&quot;</span><span class="punctuation">,</span>          <span class="comment">// 桌面环境</span></span><br><span class="line">    <span class="string">&quot;wm&quot;</span><span class="punctuation">,</span>          <span class="comment">// 窗口管理器</span></span><br><span class="line">    <span class="string">&quot;terminal&quot;</span><span class="punctuation">,</span>    <span class="comment">// 终端</span></span><br><span class="line">    <span class="string">&quot;cpu&quot;</span><span class="punctuation">,</span>         <span class="comment">// CPU 型号</span></span><br><span class="line">    <span class="string">&quot;gpu&quot;</span><span class="punctuation">,</span>         <span class="comment">// GPU 型号</span></span><br><span class="line">    <span class="string">&quot;memory&quot;</span><span class="punctuation">,</span>      <span class="comment">// 内存使用</span></span><br><span class="line">    <span class="string">&quot;disk&quot;</span><span class="punctuation">,</span>        <span class="comment">// 磁盘使用</span></span><br><span class="line">    <span class="string">&quot;battery&quot;</span><span class="punctuation">,</span>     <span class="comment">// 电池状态（Mac 有效）</span></span><br><span class="line">    <span class="string">&quot;break&quot;</span><span class="punctuation">,</span>       <span class="comment">// 空行</span></span><br><span class="line">    <span class="string">&quot;colors&quot;</span>       <span class="comment">// 终端颜色块</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>不需要的模块直接从数组里删掉，顺序即显示顺序。</p><h3 id="常用命令行选项"><a href="#常用命令行选项" class="headerlink" title="常用命令行选项"></a>常用命令行选项</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">fastfetch --logo none</span><br><span class="line"><span class="comment"># 不显示 logo，只输出文字信息</span></span><br><span class="line"></span><br><span class="line">fastfetch --logo small</span><br><span class="line"><span class="comment"># 显示较小的 logo</span></span><br><span class="line"></span><br><span class="line">fastfetch --logo /path/to/image.png</span><br><span class="line"><span class="comment"># 用图片作为 logo（需要终端支持图片渲染，如 kitty）</span></span><br><span class="line"></span><br><span class="line">fastfetch --config none</span><br><span class="line"><span class="comment"># 忽略配置文件，使用默认设置</span></span><br><span class="line"></span><br><span class="line">fastfetch --list-logos</span><br><span class="line"><span class="comment"># 列出所有内置 logo 名称</span></span><br></pre></td></tr></table></figure><h3 id="只看特定信息"><a href="#只看特定信息" class="headerlink" title="只看特定信息"></a>只看特定信息</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">fastfetch -s cpu</span><br><span class="line"><span class="comment"># -s：structure，只显示 CPU 模块</span></span><br><span class="line"></span><br><span class="line">fastfetch -s cpu,memory,disk</span><br><span class="line"><span class="comment"># 只显示指定的几个模块，逗号分隔</span></span><br></pre></td></tr></table></figure><hr><h2 id="pokemon-colorscripts：终端里的像素宝可梦"><a href="#pokemon-colorscripts：终端里的像素宝可梦" class="headerlink" title="pokemon-colorscripts：终端里的像素宝可梦"></a><a href="https://gitlab.com/phoneybadger/pokemon-colorscripts">pokemon-colorscripts</a>：终端里的像素宝可梦</h2><p>pokemon-colorscripts 是一个命令行工具，用 Unicode 字符拼出像素风格的宝可梦图案并输出到终端。支持全部 898 只宝可梦（第一代到第八代），以及 shiny 色和小尺寸变体。</p><h3 id="安装-1"><a href="#安装-1" class="headerlink" title="安装"></a>安装</h3><p>官方不提供 brew formula，macOS 通过 git clone 安装脚本安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://gitlab.com/phoneybadger/pokemon-colorscripts.git</span><br><span class="line"><span class="built_in">cd</span> pokemon-colorscripts</span><br><span class="line"><span class="built_in">sudo</span> ./install.sh</span><br></pre></td></tr></table></figure><p>安装完成后 <code>pokemon-colorscripts</code> 命令即可全局使用，不需要额外配置 PATH。</p><h3 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">pokemon-colorscripts --random</span><br><span class="line"><span class="comment"># 随机显示一只宝可梦（最常用）</span></span><br><span class="line"></span><br><span class="line">pokemon-colorscripts --name pikachu</span><br><span class="line"><span class="comment"># --name：显示指定宝可梦（英文名，一般和游戏里拼写一致）</span></span><br><span class="line"></span><br><span class="line">pokemon-colorscripts --random --shiny</span><br><span class="line"><span class="comment"># --shiny：显示 shiny 色版本</span></span><br><span class="line"></span><br><span class="line">pokemon-colorscripts --random --no-title</span><br><span class="line"><span class="comment"># --no-title：不显示宝可梦名称，只显示图案</span></span><br><span class="line"></span><br><span class="line">pokemon-colorscripts --list</span><br><span class="line"><span class="comment"># 列出所有可用的宝可梦名称</span></span><br></pre></td></tr></table></figure><h3 id="只显示特定世代"><a href="#只显示特定世代" class="headerlink" title="只显示特定世代"></a>只显示特定世代</h3><p>世代编号直接跟在 <code>--random</code> 后面作为参数：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">pokemon-colorscripts --random 1</span><br><span class="line"><span class="comment"># 只从第一世代（151 只）里随机选，可选值：1-8</span></span><br><span class="line"></span><br><span class="line">pokemon-colorscripts --random 1-3</span><br><span class="line"><span class="comment"># 从第 1 到第 3 世代里随机</span></span><br><span class="line"></span><br><span class="line">pokemon-colorscripts --random 1,3,6</span><br><span class="line"><span class="comment"># 从第 1、3、6 世代里随机（逗号分隔，无空格）</span></span><br></pre></td></tr></table></figure><hr><h2 id="集成到-zshrc：每次打开终端自动运行"><a href="#集成到-zshrc：每次打开终端自动运行" class="headerlink" title="集成到 .zshrc：每次打开终端自动运行"></a>集成到 .zshrc：每次打开终端自动运行</h2><p>把两个工具的调用加到 <code>~/.zshrc</code> 末尾，让新终端窗口打开时自动执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 终端启动展示</span></span><br><span class="line">fastfetch</span><br><span class="line">pokemon-colorscripts --random 1 --no-title</span><br><span class="line"><span class="comment"># --random 1：只从第一世代选，颜色辨识度更高</span></span><br><span class="line"><span class="comment"># --no-title：不显示名称，保持画面干净</span></span><br></pre></td></tr></table></figure><p>如果不想每次都显示系统信息，只要宝可梦，把 <code>fastfetch</code> 那行去掉即可。</p><p>两个工具各自独立，顺序也可以反过来——宝可梦在上、系统信息在下，看个人喜好。</p><h3 id="控制显示频率（可选）"><a href="#控制显示频率（可选）" class="headerlink" title="控制显示频率（可选）"></a>控制显示频率（可选）</h3><p>如果终端开太频繁觉得每次都显示有点烦，可以改成只有新 tab&#x2F;窗口才显示，而不是 source 配置时也触发：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 只在交互式且非嵌套的 Shell 里显示</span></span><br><span class="line"><span class="keyword">if</span> [[ -o interactive ]] &amp;&amp; [[ -z <span class="string">&quot;<span class="variable">$TMUX</span>&quot;</span> ]] &amp;&amp; [[ <span class="variable">$SHLVL</span> -eq 1 ]]; <span class="keyword">then</span></span><br><span class="line">  fastfetch</span><br><span class="line">  pokemon-colorscripts --random 1 --no-title</span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure><hr><h2 id="效果展示"><a href="#效果展示" class="headerlink" title="效果展示"></a>效果展示</h2><p><img src="/../images/fastfetch-pokemon.png" alt="fastfetch + pokemon-colorscripts"></p><hr><h2 id="从哪里开始"><a href="#从哪里开始" class="headerlink" title="从哪里开始"></a>从哪里开始</h2><p>两个工具可以独立安装：</p><ul><li><strong>fastfetch</strong>：先跑一次 <code>fastfetch</code> 看默认效果，再用 <code>--gen-config</code> 生成配置，按需删掉不想看的模块（通常 <code>de</code>&#x2F;<code>wm</code> 对 Mac 没意义，可以去掉）</li><li><strong>pokemon-colorscripts</strong>：装完先 <code>pokemon-colorscripts --name pikachu</code> 确认能显示，再加进 <code>.zshrc</code></li></ul><p>不喜欢了随时把 <code>.zshrc</code> 里那两行注释掉，零负担。</p>]]>
    </content>
    <id>https://blog.codewater.top/fastfetch-pokemon-colorscripts-%E7%BB%88%E7%AB%AF%E5%A4%96%E8%A7%82/</id>
    <link href="https://blog.codewater.top/fastfetch-pokemon-colorscripts-%E7%BB%88%E7%AB%AF%E5%A4%96%E8%A7%82/"/>
    <published>2026-07-03T08:00:00.000Z</published>
    <summary>用 fastfetch 替换 neofetch，在终端显示系统信息和硬件状态；配合 pokemon-colorscripts 在每次打开终端时随机显示一只像素宝可梦。两个工具纯装饰向，安装简单，启动快。</summary>
    <title>终端启动界面：fastfetch + pokemon-colorscripts</title>
    <updated>2026-07-03T08:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Git" scheme="https://blog.codewater.top/tags/Git/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="效率工具" scheme="https://blog.codewater.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <category term="lazygit" scheme="https://blog.codewater.top/tags/lazygit/"/>
    <content>
      <![CDATA[<p>Git 的默认输出是纯文本，diff 里的加减行只有红绿两色，<code>git log</code> 的历史要翻很多屏，PR 操作必须切到浏览器。这三件事都有更顺手的处理方式。</p><p>git-delta 解决 diff 显示，lazygit 解决交互操作，gh 解决 GitHub 集成。</p><span id="more"></span><hr><h2 id="git-delta：给-diff-加语法高亮和行号"><a href="#git-delta：给-diff-加语法高亮和行号" class="headerlink" title="git-delta：给 diff 加语法高亮和行号"></a><a href="https://dandavison.github.io/delta/">git-delta</a>：给 diff 加语法高亮和行号</h2><p>git-delta 接管 git 的 pager，把原来的纯文本 diff 替换成带语法高亮、行号、可选左右对比视图的界面。</p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">brew install git-delta</span><br><span class="line"><span class="comment"># brew formula 名是 git-delta，不是 delta</span></span><br></pre></td></tr></table></figure><h3 id="配置到-gitconfig"><a href="#配置到-gitconfig" class="headerlink" title="配置到 .gitconfig"></a>配置到 .gitconfig</h3><p>delta 通过 <code>~/.gitconfig</code> 生效，不需要改 <code>.zshrc</code>：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[core]</span></span><br><span class="line">    <span class="attr">pager</span> = delta</span><br><span class="line">    <span class="comment"># 把 delta 设为所有 git 命令的分页器</span></span><br><span class="line"></span><br><span class="line"><span class="section">[interactive]</span></span><br><span class="line">    <span class="attr">diffFilter</span> = delta --color-<span class="literal">on</span>ly</span><br><span class="line">    <span class="comment"># git add -p 等交互式命令用 delta 着色</span></span><br><span class="line"></span><br><span class="line"><span class="section">[delta]</span></span><br><span class="line">    <span class="attr">navigate</span> = <span class="literal">true</span></span><br><span class="line">    <span class="comment"># n/N 在 diff 的文件间跳转</span></span><br><span class="line">    <span class="attr">side-by-side</span> = <span class="literal">true</span></span><br><span class="line">    <span class="comment"># 左右对比视图，适合宽屏；改为 false 则单列显示</span></span><br><span class="line">    <span class="attr">line-numbers</span> = <span class="literal">true</span></span><br><span class="line">    <span class="comment"># 显示行号</span></span><br><span class="line">    <span class="attr">syntax-theme</span> = Dracula</span><br><span class="line">    <span class="comment"># 语法高亮主题，见下方主题选择</span></span><br><span class="line"></span><br><span class="line"><span class="section">[merge]</span></span><br><span class="line">    <span class="attr">conflictstyle</span> = diff3</span><br><span class="line">    <span class="comment"># 合并冲突时显示三方对比（原始/我方/他方）</span></span><br><span class="line"></span><br><span class="line"><span class="section">[diff]</span></span><br><span class="line">    <span class="attr">colorMoved</span> = default</span><br><span class="line">    <span class="comment"># 移动的代码块用特殊颜色标记，不误判为删除+新增</span></span><br></pre></td></tr></table></figure><p>配置完成后，<code>git diff</code>、<code>git show</code>、<code>git log -p</code> 都会自动走 delta。</p><h3 id="主题选择"><a href="#主题选择" class="headerlink" title="主题选择"></a>主题选择</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">delta --list-syntax-themes</span><br><span class="line"><span class="comment"># 列出所有可用主题，每个会显示示例 diff</span></span><br></pre></td></tr></table></figure><p>暗色系常用：<code>Dracula</code>、<code>Nord</code>、<code>OneHalfDark</code>；亮色系：<code>GitHub</code>。把选好的主题名填进 <code>syntax-theme</code> 字段即可。</p><hr><h2 id="lazygit：用-TUI-完成大部分-Git-操作"><a href="#lazygit：用-TUI-完成大部分-Git-操作" class="headerlink" title="lazygit：用 TUI 完成大部分 Git 操作"></a><a href="https://github.com/jesseduffield/lazygit">lazygit</a>：用 TUI 完成大部分 Git 操作</h2><p>lazygit 是 git 的终端可视化界面，左侧面板覆盖工作区、分支、提交历史、stash 四大场景，日常 80% 的 Git 操作都可以在里面完成，不需要记参数。</p><h3 id="安装-1"><a href="#安装-1" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install lazygit</span><br></pre></td></tr></table></figure><h3 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h3><p>在任意 git 仓库目录下运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lazygit</span><br></pre></td></tr></table></figure><p>加一个别名到 <code>~/.zshrc</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">alias</span> lg=<span class="string">&#x27;lazygit&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="界面布局"><a href="#界面布局" class="headerlink" title="界面布局"></a>界面布局</h3><p><img src="/../images/lazygit.png" alt="lazygit页面"></p><p>进入 lazygit 后，用 <code>1</code>-<code>5</code> 数字键切换左侧面板，也可以用 <code>h</code>&#x2F;<code>l</code> 左右移动：</p><figure class="highlight basic"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">1 </span>- Status          当前仓库状态</span><br><span class="line"><span class="symbol">2 </span>- <span class="keyword">Files</span>           工作区文件变更（最常用）</span><br><span class="line"><span class="symbol">3 </span>- Local Branches  本地分支</span><br><span class="line"><span class="symbol">4 </span>- Commits         提交历史</span><br><span class="line"><span class="symbol">5 </span>- Stash           暂存条目列表</span><br></pre></td></tr></table></figure><p><code>↑</code>&#x2F;<code>↓</code> 在面板内移动选中项，<code>Enter</code> 进入详情，右侧大窗口实时显示 diff 预览（装了 delta 的话这里也走 delta 渲染）。</p><h3 id="核心操作"><a href="#核心操作" class="headerlink" title="核心操作"></a>核心操作</h3><p><strong>Stage &#x2F; Unstage</strong>（在 Files 面板）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Space   对选中文件 stage/unstage（切换）</span><br><span class="line">a       对所有文件 stage/unstage</span><br></pre></td></tr></table></figure><p><strong>提交</strong>：</p><figure class="highlight cmake"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">c       提交（弹出编辑框输入 <span class="keyword">message</span>）</span><br><span class="line">C       用 $EDITOR 写提交信息（适合写多行 <span class="keyword">message</span>）</span><br><span class="line">A       修改上一条提交（amend）</span><br></pre></td></tr></table></figure><p><strong>Push &#x2F; Pull</strong>：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">P</span>       push 到远程</span><br><span class="line"><span class="selector-tag">p</span>       pull <span class="selector-tag">from</span> 远程</span><br></pre></td></tr></table></figure><p><strong>分支操作</strong>（在 Branches 面板）：</p><figure class="highlight cos"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">n       新建分支</span><br><span class="line">Space   切换到选中分支</span><br><span class="line"><span class="keyword">d</span>       删除本地分支</span><br><span class="line"><span class="keyword">r</span>       rebase 当前分支到选中分支</span><br><span class="line">M       <span class="keyword">merge</span> 选中分支到当前分支</span><br></pre></td></tr></table></figure><p><strong>交互式 rebase</strong>（在 Commits 面板，选中某条提交后）：</p><figure class="highlight angelscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">e       进入 <span class="built_in">int</span>eractive rebase 模式</span><br><span class="line">d       删除选中提交</span><br><span class="line">s       squash 到上一条（保留 message）</span><br><span class="line">f       fixup（squash 不保留 message）</span><br><span class="line"><span class="string">[       把选中提交上移一位</span></span><br><span class="line"><span class="string">]</span>       把选中提交下移一位</span><br></pre></td></tr></table></figure><p>命令行 rebase 需要手动编辑 todo 文件，lazygit 里直接选中拖动，直观很多。</p><p><strong>查帮助</strong>：</p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">?       显示当前面板所有快捷键</span><br></pre></td></tr></table></figure><hr><h2 id="gh：在终端里操作-GitHub-PR-和-Issue"><a href="#gh：在终端里操作-GitHub-PR-和-Issue" class="headerlink" title="gh：在终端里操作 GitHub PR 和 Issue"></a><a href="https://cli.github.com/">gh</a>：在终端里操作 GitHub PR 和 Issue</h2><p>gh 是 GitHub 官方命令行工具，PR 创建&#x2F;审核&#x2F;合并、Issue 管理、仓库操作都可以在终端里完成，减少浏览器切换。</p><h3 id="安装-2"><a href="#安装-2" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install gh</span><br></pre></td></tr></table></figure><h3 id="认证"><a href="#认证" class="headerlink" title="认证"></a>认证</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">gh auth login</span><br><span class="line"><span class="comment"># 按提示选择 GitHub.com，HTTPS 协议，通过浏览器完成 OAuth 授权</span></span><br></pre></td></tr></table></figure><h3 id="仓库操作"><a href="#仓库操作" class="headerlink" title="仓库操作"></a>仓库操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">gh repo <span class="built_in">clone</span> owner/repo</span><br><span class="line"><span class="comment"># 克隆仓库，自动处理 SSH/HTTPS，不需要完整 URL</span></span><br><span class="line"></span><br><span class="line">gh repo view --web</span><br><span class="line"><span class="comment"># 在默认浏览器打开当前仓库的 GitHub 页面</span></span><br></pre></td></tr></table></figure><h3 id="PR-操作"><a href="#PR-操作" class="headerlink" title="PR 操作"></a>PR 操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">gh <span class="built_in">pr</span> list</span><br><span class="line"><span class="comment"># 列出当前仓库的 PR</span></span><br><span class="line"></span><br><span class="line">gh <span class="built_in">pr</span> create</span><br><span class="line"><span class="comment"># 交互式创建 PR，提示输入 title、body、base branch</span></span><br><span class="line"></span><br><span class="line">gh <span class="built_in">pr</span> create --title <span class="string">&quot;Fix login redirect&quot;</span> --body <span class="string">&quot;Fixes #42&quot;</span></span><br><span class="line"><span class="comment"># 非交互式创建 PR</span></span><br><span class="line"><span class="comment">#   --title：PR 标题</span></span><br><span class="line"><span class="comment">#   --body：PR 描述，支持 Markdown</span></span><br><span class="line"><span class="comment">#   --web：创建后立即在浏览器打开</span></span><br><span class="line"></span><br><span class="line">gh <span class="built_in">pr</span> checkout 42</span><br><span class="line"><span class="comment"># 切换到第 42 号 PR 对应的分支</span></span><br><span class="line"><span class="comment"># code review 时用这个在本地跑测试</span></span><br><span class="line"></span><br><span class="line">gh <span class="built_in">pr</span> view 42 --web</span><br><span class="line"><span class="comment"># 在浏览器里打开第 42 号 PR</span></span><br><span class="line"></span><br><span class="line">gh <span class="built_in">pr</span> checks 42</span><br><span class="line"><span class="comment"># 查看 PR 的 CI 检查状态（通过/失败/进行中）</span></span><br><span class="line"></span><br><span class="line">gh <span class="built_in">pr</span> merge 42 --squash</span><br><span class="line"><span class="comment"># squash merge，把 PR 里的提交压缩成一条</span></span><br></pre></td></tr></table></figure><h3 id="Issue-操作"><a href="#Issue-操作" class="headerlink" title="Issue 操作"></a>Issue 操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">gh issue list</span><br><span class="line"><span class="comment"># 列出 issue</span></span><br><span class="line"></span><br><span class="line">gh issue create</span><br><span class="line"><span class="comment"># 交互式创建 issue</span></span><br><span class="line"></span><br><span class="line">gh issue view 15</span><br><span class="line"><span class="comment"># 查看第 15 号 issue</span></span><br><span class="line"></span><br><span class="line">gh issue close 15</span><br><span class="line"><span class="comment"># 关闭 issue</span></span><br></pre></td></tr></table></figure><h3 id="实用组合"><a href="#实用组合" class="headerlink" title="实用组合"></a>实用组合</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 列出分配给自己的 PR</span></span><br><span class="line">gh <span class="built_in">pr</span> list --assignee @me</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建 PR 并直接在浏览器打开（适合需要填写大量描述的情况）</span></span><br><span class="line">gh <span class="built_in">pr</span> create --web</span><br></pre></td></tr></table></figure><hr><h2 id="gitconfig-完整配置"><a href="#gitconfig-完整配置" class="headerlink" title=".gitconfig 完整配置"></a>.gitconfig 完整配置</h2><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[core]</span></span><br><span class="line">    <span class="attr">pager</span> = delta</span><br><span class="line"></span><br><span class="line"><span class="section">[interactive]</span></span><br><span class="line">    <span class="attr">diffFilter</span> = delta --color-<span class="literal">on</span>ly</span><br><span class="line"></span><br><span class="line"><span class="section">[delta]</span></span><br><span class="line">    <span class="attr">navigate</span> = <span class="literal">true</span></span><br><span class="line">    <span class="attr">side-by-side</span> = <span class="literal">true</span></span><br><span class="line">    <span class="attr">line-numbers</span> = <span class="literal">true</span></span><br><span class="line">    <span class="attr">syntax-theme</span> = Dracula</span><br><span class="line"></span><br><span class="line"><span class="section">[merge]</span></span><br><span class="line">    <span class="attr">conflictstyle</span> = diff3</span><br><span class="line"></span><br><span class="line"><span class="section">[diff]</span></span><br><span class="line">    <span class="attr">colorMoved</span> = default</span><br></pre></td></tr></table></figure><p>lazygit 和 gh 不需要额外配置 <code>.gitconfig</code>，装完即用。</p><hr><h2 id="从哪里开始"><a href="#从哪里开始" class="headerlink" title="从哪里开始"></a>从哪里开始</h2><p>三个工具互相独立，可以按需单独装：</p><ul><li><strong>git-delta</strong>：先装，收益最直接。配置完 <code>.gitconfig</code> 之后跑一次 <code>git log -p</code>，立刻看到区别</li><li><strong>lazygit</strong>：在一个有改动的仓库里启动，先按 <code>?</code> 看快捷键，把 stage + commit + push 这条主线跑通</li><li><strong>gh</strong>：有 GitHub 工作流的项目里最实用，先用 <code>gh pr list</code> 和 <code>gh pr checkout</code> 熟悉一下，再用 <code>gh pr create</code> 替代浏览器操作</li></ul><p>系列前几篇装过的 fzf，lazygit 内部的分支搜索和文件选择会自动调用，不需要额外配置。</p>]]>
    </content>
    <id>https://blog.codewater.top/git-delta-lazygit-gh-%E7%BB%88%E7%AB%AFGit%E5%B7%A5%E5%85%B7%E9%93%BE/</id>
    <link href="https://blog.codewater.top/git-delta-lazygit-gh-%E7%BB%88%E7%AB%AFGit%E5%B7%A5%E5%85%B7%E9%93%BE/"/>
    <published>2026-07-03T06:00:00.000Z</published>
    <summary>用 git-delta 给 diff 加语法高亮，用 lazygit 在 TUI 界面里完成 stage/commit/rebase，用 gh 在终端里操作 GitHub PR 和 Issue。三个工具各解决一块摩擦，装完即用。</summary>
    <title>终端里的 Git：git-delta + lazygit + gh</title>
    <updated>2026-07-03T06:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="atuin" scheme="https://blog.codewater.top/tags/atuin/"/>
    <category term="zoxide" scheme="https://blog.codewater.top/tags/zoxide/"/>
    <category term="效率工具" scheme="https://blog.codewater.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <content>
      <![CDATA[<p>每天重复输入的内容里，有两类最浪费时间：一是翻命令历史找之前跑过的那条带很多参数的 curl，二是手打一串层层嵌套的目录路径。</p><p>atuin 解决第一个，zoxide 解决第二个。</p><span id="more"></span><hr><h2 id="atuin：把命令历史变成可搜索的数据库"><a href="#atuin：把命令历史变成可搜索的数据库" class="headerlink" title="atuin：把命令历史变成可搜索的数据库"></a><a href="https://atuin.sh/">atuin</a>：把命令历史变成可搜索的数据库</h2><p>默认的 zsh 历史记录是一个纯文本文件，<code>Ctrl+R</code> 只能按前缀匹配向上翻。atuin 把历史存进本地 SQLite 数据库，记录命令的执行时间、工作目录、退出码、耗时，然后提供一个交互式全文搜索界面。</p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install atuin</span><br></pre></td></tr></table></figure><h3 id="集成到-zshrc"><a href="#集成到-zshrc" class="headerlink" title="集成到 .zshrc"></a>集成到 .zshrc</h3><p>在 <code>~/.zshrc</code> 末尾加入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">eval</span> <span class="string">&quot;<span class="subst">$(atuin init zsh)</span>&quot;</span></span><br></pre></td></tr></table></figure><p>重新加载配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.zshrc</span><br></pre></td></tr></table></figure><p>之后按 <code>Ctrl+R</code> 就会进入 atuin 的搜索界面，而不是原来的 zsh 历史翻找。</p><h3 id="Ctrl-R-界面"><a href="#Ctrl-R-界面" class="headerlink" title="Ctrl+R 界面"></a>Ctrl+R 界面</h3><p>进入界面后：</p><ul><li>直接输入关键词做全文搜索，不需要记住命令开头是什么</li><li><code>↑</code> <code>↓</code> 方向键移动选中，<code>Enter</code> 执行，<code>Esc</code> 退出</li><li>支持按前缀过滤：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 在搜索框里输入这些前缀来缩小范围</span></span><br><span class="line">cwd:~/code     <span class="comment"># 只看在 ~/code 目录下执行过的命令</span></span><br><span class="line"><span class="built_in">exit</span>:0         <span class="comment"># 只看执行成功的命令（退出码为 0）</span></span><br><span class="line">before:1h      <span class="comment"># 只看 1 小时内执行的命令</span></span><br><span class="line">after:2026-07-01  <span class="comment"># 只看某日期之后的命令</span></span><br></pre></td></tr></table></figure><p>实际工作中最常用的是 <code>cwd:</code> 过滤——在一个项目目录里搜，不会翻出其他项目的干扰结果。</p><h3 id="历史同步（可选）"><a href="#历史同步（可选）" class="headerlink" title="历史同步（可选）"></a>历史同步（可选）</h3><p>atuin 支持跨设备同步命令历史，数据经过端对端加密后上传到 atuin 的云服务（也可以自己搭）。有多台 Mac 的话同步后 <code>Ctrl+R</code> 能看到所有设备的历史。</p><p>注册并登录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">atuin register -u 用户名 -e 邮箱 -p 密码</span><br><span class="line">atuin login -u 用户名</span><br><span class="line"><span class="comment"># 之后会提示输入密码和加密密钥（注册时自动生成，atuin key 查看）</span></span><br><span class="line">atuin <span class="built_in">sync</span></span><br></pre></td></tr></table></figure><p>不需要同步的话跳过这步，atuin 本地也完整可用。</p><hr><h2 id="zoxide：让-cd-记住你去过哪里"><a href="#zoxide：让-cd-记住你去过哪里" class="headerlink" title="zoxide：让 cd 记住你去过哪里"></a><a href="https://github.com/ajeetdsouza/zoxide">zoxide</a>：让 cd 记住你去过哪里</h2><p>zoxide 是 <code>cd</code> 的替代品，核心逻辑是 <strong>frecency</strong>（频率 × 最近访问时间）：你去过的目录都会被记录，之后输入路径关键词，zoxide 根据访问频率和时间自动匹配最可能的那个。</p><h3 id="安装-1"><a href="#安装-1" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install zoxide</span><br></pre></td></tr></table></figure><h3 id="集成到-zshrc-1"><a href="#集成到-zshrc-1" class="headerlink" title="集成到 .zshrc"></a>集成到 .zshrc</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">eval</span> <span class="string">&quot;<span class="subst">$(zoxide init zsh)</span>&quot;</span></span><br></pre></td></tr></table></figure><p>默认会添加 <code>z</code> 和 <code>zi</code> 两个命令。如果想用 <code>z</code> 完全替换原来的 <code>cd</code>，可以用 <code>--cmd cd</code> 参数：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">eval</span> <span class="string">&quot;<span class="subst">$(zoxide init zsh --cmd cd)</span>&quot;</span></span><br></pre></td></tr></table></figure><p>这样原来的 <code>cd</code> 命令也会指向 zoxide，不需要改任何习惯。</p><h3 id="z-和-zi"><a href="#z-和-zi" class="headerlink" title="z 和 zi"></a>z 和 zi</h3><p><code>z</code> 直接跳转，<code>zi</code> 调出 fzf 交互界面选择：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">z blog          <span class="comment"># 直接跳到访问频率最高的包含 &quot;blog&quot; 的目录</span></span><br><span class="line">z code blog     <span class="comment"># 多个关键词同时过滤，跳到同时含 &quot;code&quot; 和 &quot;blog&quot; 的目录</span></span><br><span class="line">zi              <span class="comment"># 打开 fzf 界面，列出所有记录过的目录，可以模糊搜索</span></span><br><span class="line">zi blog         <span class="comment"># 带关键词预过滤后再打开 fzf 界面</span></span><br></pre></td></tr></table></figure><p><code>zi</code> 需要系统里装了 fzf，前几篇文章里已经装过了，直接可用。</p><h3 id="路径是怎么学习的"><a href="#路径是怎么学习的" class="headerlink" title="路径是怎么学习的"></a>路径是怎么学习的</h3><p>第一次使用时 zoxide 没有任何记录，<code>z</code> 命令找不到目标会报错。正常用 <code>cd</code> 进入目录之后，zoxide 自动在后台记录。进过 10 个左右的目录之后，<code>z</code> 就开始有用了。也可以手动添加：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">zoxide add ~/code/github/blog-source</span><br></pre></td></tr></table></figure><p>查看当前数据库里记录了哪些目录以及对应的权重：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">zoxide query --list --score</span><br></pre></td></tr></table></figure><hr><h2 id="zshrc-完整配置"><a href="#zshrc-完整配置" class="headerlink" title=".zshrc 完整配置"></a>.zshrc 完整配置</h2><p>把两个工具的初始化放到 <code>.zshrc</code> 的末尾：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># atuin：增强命令历史，接管 Ctrl+R</span></span><br><span class="line"><span class="built_in">eval</span> <span class="string">&quot;<span class="subst">$(atuin init zsh)</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># zoxide：智能目录跳转，替换 cd（需在 compinit 之后）</span></span><br><span class="line"><span class="built_in">eval</span> <span class="string">&quot;<span class="subst">$(zoxide init zsh --cmd cd)</span>&quot;</span></span><br></pre></td></tr></table></figure><p>顺序无所谓，两者互不依赖。</p><hr><h2 id="从哪里开始"><a href="#从哪里开始" class="headerlink" title="从哪里开始"></a>从哪里开始</h2><p>这两个工具的上手成本都极低，装完改一行配置就生效，不需要专门学习：</p><ul><li><strong>atuin</strong>：装完之后按一次 <code>Ctrl+R</code>，感受一下全文搜索和过滤功能，之后照常用，习惯会自然形成</li><li><strong>zoxide</strong>：装完之后照常 <code>cd</code> 进目录，积累几天记录之后试一次 <code>z 项目名</code>，就能体会到省了多少路径</li></ul><p>系列前几篇装过的 fzf，配合 zoxide 的 <code>zi</code> 命令效果更好——这也是工具链组合带来的放大效应。</p>]]>
    </content>
    <id>https://blog.codewater.top/atuin-zoxide-%E7%BB%88%E7%AB%AF%E5%8E%86%E5%8F%B2%E4%B8%8E%E7%9B%AE%E5%BD%95%E8%B7%B3%E8%BD%AC/</id>
    <link href="https://blog.codewater.top/atuin-zoxide-%E7%BB%88%E7%AB%AF%E5%8E%86%E5%8F%B2%E4%B8%8E%E7%9B%AE%E5%BD%95%E8%B7%B3%E8%BD%AC/"/>
    <published>2026-07-02T02:00:00.000Z</published>
    <summary>用 atuin 替换 shell 历史记录，获得可过滤的 Ctrl+R 搜索界面；用 zoxide 替换 cd，输入路径片段直接跳转。两个工具安装简单、收益立竿见影。</summary>
    <title>Shell 历史与目录跳转的现代替代：atuin + zoxide</title>
    <updated>2026-07-02T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="效率工具" scheme="https://blog.codewater.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <content>
      <![CDATA[<p>终端里管理文件，<code>cd</code> + <code>ls</code> 能用，但一旦目录层级深了，或者需要同时看父目录和当前目录，就很别扭。yazi 是用 Rust 写的终端文件管理器，三栏布局（父目录 &#x2F; 当前目录 &#x2F; 预览），操作用 Vim 键位，代码预览靠 bat，图片在 kitty 下能内联显示。</p><span id="more"></span><hr><h2 id="yazi：三栏布局-异步架构"><a href="#yazi：三栏布局-异步架构" class="headerlink" title="yazi：三栏布局 + 异步架构"></a><a href="https://yazi-rs.github.io/">yazi</a>：三栏布局 + 异步架构</h2><p>界面分三栏：左边是父目录，中间是当前目录，右边是文件预览。移动到哪个文件，右边实时更新预览内容：代码文件语法高亮（靠 bat），图片在 kitty 下直接渲染，普通文本直接显示。</p><p>和老牌的 ranger 比：ranger 是 Python 写的，启动稍慢，配置更复杂；yazi 是 Rust + 完全异步 I&#x2F;O，大目录不会卡顿，插件用 Lua 写，生态也在快速成长。</p><hr><h2 id="安装：先确认依赖"><a href="#安装：先确认依赖" class="headerlink" title="安装：先确认依赖"></a>安装：先确认依赖</h2><p>yazi 的代码预览、文件搜索功能依赖之前装过的工具：</p><table><thead><tr><th>依赖</th><th>用途</th></tr></thead><tbody><tr><td>bat</td><td>代码文件语法高亮预览</td></tr><tr><td>fd</td><td>文件名搜索（<code>s</code> 键触发）</td></tr><tr><td>ripgrep</td><td>文件内容搜索（<code>S</code> 键触发）</td></tr></tbody></table><p>如果前三篇已经装过 bat&#x2F;fd&#x2F;rg，直接装 yazi 就行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install yazi</span><br></pre></td></tr></table></figure><p>安装完后 <code>ya</code> 命令是插件管理器，<code>yazi</code> 命令启动主程序：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">yazi --version</span><br><span class="line"><span class="comment"># Yazi 26.5.6</span></span><br></pre></td></tr></table></figure><hr><h2 id="三分钟上手：核心快捷键"><a href="#三分钟上手：核心快捷键" class="headerlink" title="三分钟上手：核心快捷键"></a>三分钟上手：核心快捷键</h2><p>启动：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">yazi</span><br></pre></td></tr></table></figure><h3 id="导航"><a href="#导航" class="headerlink" title="导航"></a>导航</h3><table><thead><tr><th>按键</th><th>操作</th></tr></thead><tbody><tr><td><code>h</code></td><td>返回父目录</td></tr><tr><td><code>l</code></td><td>进入子目录 &#x2F; 打开文件</td></tr><tr><td><code>j</code> &#x2F; <code>k</code></td><td>下移 &#x2F; 上移</td></tr><tr><td><code>g g</code></td><td>跳到列表顶部</td></tr><tr><td><code>G</code></td><td>跳到列表底部</td></tr></tbody></table><h3 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h3><table><thead><tr><th>按键</th><th>操作</th></tr></thead><tbody><tr><td><code>Space</code></td><td>多选（切换选中状态）</td></tr><tr><td><code>y</code></td><td>复制选中文件</td></tr><tr><td><code>x</code></td><td>剪切选中文件</td></tr><tr><td><code>p</code></td><td>粘贴</td></tr><tr><td><code>d</code></td><td>移动到回收站</td></tr><tr><td><code>D</code></td><td>永久删除（不可恢复）</td></tr><tr><td><code>a</code></td><td>新建文件（以 <code>/</code> 结尾则新建目录）</td></tr><tr><td><code>r</code></td><td>重命名</td></tr><tr><td><code>.</code></td><td>切换隐藏文件显示（<code>.</code> 开头的文件）</td></tr></tbody></table><h3 id="搜索与过滤"><a href="#搜索与过滤" class="headerlink" title="搜索与过滤"></a>搜索与过滤</h3><table><thead><tr><th>按键</th><th>操作</th></tr></thead><tbody><tr><td><code>f</code></td><td>按名称过滤（在当前目录实时筛选）</td></tr><tr><td><code>/</code></td><td>向下查找文件（按名称匹配，<code>n</code> 跳下一个）</td></tr><tr><td><code>s</code></td><td>按文件名搜索（调用 fd，可跨目录）</td></tr><tr><td><code>S</code></td><td>按内容搜索（调用 ripgrep）</td></tr><tr><td><code>Ctrl + s</code></td><td>取消正在进行的搜索</td></tr><tr><td><code>z</code></td><td>用 fzf 跳转目录</td></tr><tr><td><code>Z</code></td><td>用 zoxide 跳转目录</td></tr></tbody></table><h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><table><thead><tr><th>按键</th><th>操作</th></tr></thead><tbody><tr><td><code>Enter</code></td><td>打开文件（调用系统默认程序）</td></tr><tr><td><code>q</code></td><td>退出</td></tr><tr><td><code>F1</code> &#x2F; <code>~</code></td><td>查看所有快捷键（帮助菜单）</td></tr></tbody></table><hr><h2 id="kitty-下启用图片内联预览"><a href="#kitty-下启用图片内联预览" class="headerlink" title="kitty 下启用图片内联预览"></a>kitty 下启用图片内联预览</h2><p>yazi 支持多种图片协议，在 kitty 终端下效果最好，直接把图片渲染在预览面板里，不需要额外安装 ImageMagick。</p><p>kitty 协议是 yazi 的默认优先选项，如果你用的是 kitty，装完即用，不需要额外配置。打开 yazi，导航到任意图片文件，右侧预览面板就会显示图片。</p><p>如果图片没有显示，检查 <code>TERM</code> 环境变量：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="variable">$TERM</span></span><br><span class="line"><span class="comment"># 应该输出 xterm-kitty</span></span><br></pre></td></tr></table></figure><p>输出是 <code>xterm-kitty</code> 说明 kitty 协议生效。如果不是，可能是在嵌套终端或 tmux 里启动了 yazi，协议传递受阻。</p><hr><h2 id="zshrc-集成：退出时自动-cd"><a href="#zshrc-集成：退出时自动-cd" class="headerlink" title=".zshrc 集成：退出时自动 cd"></a>.zshrc 集成：退出时自动 cd</h2><p>直接运行 <code>yazi</code> 有个问题：退出后，终端还停在进入 yazi 前的目录，而不是你在 yazi 里最后浏览的位置。</p><p>用 <code>--cwd-file</code> 参数可以解决，在 <code>~/.zshrc</code> 里加这个函数：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="function"><span class="title">y</span></span>() &#123;</span><br><span class="line">    <span class="built_in">local</span> tmp=<span class="string">&quot;<span class="subst">$(mktemp -t <span class="string">&quot;yazi-cwd&quot;</span>)</span>&quot;</span></span><br><span class="line">    yazi <span class="string">&quot;<span class="variable">$@</span>&quot;</span> --cwd-file=<span class="string">&quot;<span class="variable">$tmp</span>&quot;</span></span><br><span class="line">    <span class="comment">#          --cwd-file：退出时把当前目录路径写入指定文件</span></span><br><span class="line">    <span class="keyword">if</span> cwd=<span class="string">&quot;<span class="subst">$(cat -- <span class="string">&quot;<span class="variable">$tmp</span>&quot;</span>)</span>&quot;</span> &amp;&amp; [ -n <span class="string">&quot;<span class="variable">$cwd</span>&quot;</span> ] &amp;&amp; [ <span class="string">&quot;<span class="variable">$cwd</span>&quot;</span> != <span class="string">&quot;<span class="variable">$PWD</span>&quot;</span> ]; <span class="keyword">then</span></span><br><span class="line">        <span class="built_in">cd</span> -- <span class="string">&quot;<span class="variable">$cwd</span>&quot;</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    <span class="built_in">rm</span> -f -- <span class="string">&quot;<span class="variable">$tmp</span>&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>之后用 <code>y</code> 命令启动，退出 yazi 时 shell 会自动 <code>cd</code> 到你最后停留的目录。<code>yazi</code> 命令照常可以用，行为不变。</p><hr><h2 id="插件安装"><a href="#插件安装" class="headerlink" title="插件安装"></a>插件安装</h2><p>yazi 用 <code>ya pkg</code> 命令管理插件，插件托管在 GitHub 上，格式是 <code>用户名/仓库名:插件名</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 添加插件（写入 package.toml，同时下载）</span></span><br><span class="line">ya pkg add yazi-rs/plugins:zoom</span><br><span class="line"><span class="comment">#          yazi-rs/plugins:zoom：仓库:插件名（一个仓库可以包含多个插件）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装 package.toml 里记录的所有插件（换机器后用）</span></span><br><span class="line">ya pkg install</span><br><span class="line"></span><br><span class="line"><span class="comment"># 更新所有插件</span></span><br><span class="line">ya pkg upgrade</span><br><span class="line"></span><br><span class="line"><span class="comment"># 列出已安装插件</span></span><br><span class="line">ya pkg list</span><br></pre></td></tr></table></figure><p>装完插件后还需要在 <code>~/.config/yazi/keymap.toml</code> 里绑定快捷键才能用。</p><p><strong>几个实用插件</strong>：</p><table><thead><tr><th>插件</th><th>仓库</th><th>功能</th></tr></thead><tbody><tr><td>zoom</td><td><code>yazi-rs/plugins:zoom</code></td><td>预览面板图片缩放（<code>=</code> 放大 &#x2F; <code>-</code> 缩小）</td></tr><tr><td>toggle-pane</td><td><code>yazi-rs/plugins:toggle-pane</code></td><td>切换全屏预览模式（<code>T</code> 键触发）</td></tr><tr><td>git</td><td><code>yazi-rs/plugins:git</code></td><td>在文件列表里显示 Git 状态</td></tr><tr><td>diff</td><td><code>yazi-rs/plugins:diff</code></td><td>对比两个文件的差异</td></tr></tbody></table><p>zoom 和 toggle-pane 是官方插件仓库里的，直接 <code>ya pack -a</code> 就能装。</p><hr><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><p><strong>yazi 和 ranger 的区别是什么，值得换吗？</strong></p><p>主要区别在性能和配置方式。ranger 是 Python 写的，大目录（几千个文件）下滚动会有延迟；yazi 是 Rust + 异步 I&#x2F;O，大目录不卡。配置上，ranger 用 Python 写脚本，yazi 用 Lua 写插件。如果你现在用 ranger 没有痛点，不用急着换；如果有性能问题，或者已经用 kitty 希望图片预览更好，换 yazi 值得试试。</p><p><strong>图片预览一直显示不出来怎么办？</strong></p><p>按顺序排查：1）确认用的是 kitty 终端，<code>echo $TERM</code> 输出应该是 <code>xterm-kitty</code>；2）如果在 tmux 里，需要在 <code>~/.tmux.conf</code> 里加以下 3 行，然后重启 tmux：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">set</span> -g allow-passthrough on</span><br><span class="line"><span class="built_in">set</span> -ga update-environment TERM</span><br><span class="line"><span class="built_in">set</span> -ga update-environment TERM_PROGRAM</span><br></pre></td></tr></table></figure><p>3）非 kitty 终端（如 iTerm2、WezTerm）也支持内联图片，yazi 会自动检测，无需额外配置。</p><p><strong><code>y</code> 函数和直接运行 <code>yazi</code> 有什么区别？</strong></p><p><code>y</code> 函数是对 <code>yazi</code> 的包装，核心差别只有一个：退出后是否自动 <code>cd</code>。<code>yazi</code> 退出后目录不变，<code>y</code> 退出后自动跳到你在 yazi 里最后浏览的目录。传参方式一样，<code>y ~/Documents</code> 等同于 <code>yazi ~/Documents</code>，直接在指定目录里打开 yazi。</p>]]>
    </content>
    <id>https://blog.codewater.top/yazi%E7%BB%88%E7%AB%AF%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E5%99%A8/</id>
    <link href="https://blog.codewater.top/yazi%E7%BB%88%E7%AB%AF%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E5%99%A8/"/>
    <published>2026-07-01T06:00:00.000Z</published>
    <summary>Mac 终端文件管理器 yazi 完整教程：安装与依赖配置、核心快捷键速查、kitty 图片预览配置、退出自动 cd 的 y 函数，以及插件安装方法。</summary>
    <title>yazi：终端里的三栏文件管理器</title>
    <updated>2026-07-01T06:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="效率工具" scheme="https://blog.codewater.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <content>
      <![CDATA[<p><code>cat</code>、<code>ls</code>、<code>less</code> 这些命令能用，但输出是黑白平铺的文字，没有高亮、没有结构感。bat、eza、glow、mdcat 分别解决了这四个场景的可读性问题，装完改几个 alias，原来的使用习惯不变，输出变好看很多。</p><span id="more"></span><hr><h2 id="bat：cat-加上语法高亮和-Git-标注"><a href="#bat：cat-加上语法高亮和-Git-标注" class="headerlink" title="bat：cat 加上语法高亮和 Git 标注"></a><a href="https://github.com/sharkdp/bat">bat</a>：cat 加上语法高亮和 Git 标注</h2><p>bat 是 <code>cat</code> 的替代品，核心差别：</p><ul><li>自动语法高亮（支持上百种语言，通过 syntect 库驱动）</li><li>显示行号</li><li>Git 集成：在行号旁标注哪些行被修改、新增、删除</li><li>自动分页（内容超过一屏时调用 pager，不会刷屏）</li></ul><p>它也被很多其他工具用作渲染底层——fzf 的文件预览、yazi 的代码预览默认都会调用 bat。</p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install bat</span><br></pre></td></tr></table></figure><h3 id="常用参数"><a href="#常用参数" class="headerlink" title="常用参数"></a>常用参数</h3><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-n</code> &#x2F; <code>--number</code></td><td>强制显示行号（默认已开启）</td></tr><tr><td><code>-A</code> &#x2F; <code>--show-all</code></td><td>显示不可见字符（tab、空格、换行符）</td></tr><tr><td><code>--plain</code> &#x2F; <code>-p</code></td><td>纯输出模式，去掉行号和边框，适合管道传递</td></tr><tr><td><code>--language &lt;lang&gt;</code></td><td>手动指定语言（如 <code>--language json</code>）</td></tr><tr><td><code>--theme &lt;name&gt;</code></td><td>指定高亮主题</td></tr><tr><td><code>--list-themes</code></td><td>列出所有可用主题</td></tr><tr><td><code>--pager never</code></td><td>禁用分页，直接输出全部内容</td></tr><tr><td><code>-r &lt;start&gt;:&lt;end&gt;</code></td><td>只显示指定行范围（如 <code>-r 10:20</code>）</td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看文件，自动高亮</span></span><br><span class="line">bat main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 预览主题效果</span></span><br><span class="line">bat --list-themes | fzf --preview=<span class="string">&quot;bat --theme=&#123;&#125; --color=always main.go&quot;</span></span><br><span class="line"><span class="comment">#                  ----|fzf 预览窗口实时切换主题效果</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 只输出第 10-20 行</span></span><br><span class="line">bat -r 10:20 main.go</span><br><span class="line"><span class="comment">#   -r：range，指定行范围</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 纯内容输出（管道给其他命令用）</span></span><br><span class="line">bat -p config.json | jq <span class="string">&#x27;.database&#x27;</span></span><br><span class="line"><span class="comment">#   -p：plain，去掉装饰，等同于 cat</span></span><br></pre></td></tr></table></figure><h3 id="替换-cat：alias-设置"><a href="#替换-cat：alias-设置" class="headerlink" title="替换 cat：alias 设置"></a>替换 cat：alias 设置</h3><p>在 <code>~/.zshrc</code> 里加：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">alias</span> <span class="built_in">cat</span>=<span class="string">&quot;bat&quot;</span></span><br><span class="line"><span class="built_in">alias</span> catp=<span class="string">&quot;bat --plain&quot;</span>  <span class="comment"># 需要纯输出时用 catp</span></span><br></pre></td></tr></table></figure><h3 id="主题选择"><a href="#主题选择" class="headerlink" title="主题选择"></a>主题选择</h3><p>bat 内置了多个主题，<code>Dracula</code>、<code>Nord</code>、<code>Solarized (dark)</code> 比较常见。把选好的主题写进配置文件，不用每次手动指定：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看当前配置文件位置</span></span><br><span class="line">bat --config-file</span><br><span class="line"></span><br><span class="line"><span class="comment"># 编辑配置文件，加入主题设置</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;--theme=&quot;Dracula&quot;&#x27;</span> &gt;&gt; <span class="string">&quot;<span class="subst">$(bat --config-file)</span>&quot;</span></span><br></pre></td></tr></table></figure><hr><h2 id="eza：ls-的现代替代品"><a href="#eza：ls-的现代替代品" class="headerlink" title="eza：ls 的现代替代品"></a><a href="https://eza.rocks/">eza</a>：ls 的现代替代品</h2><p>eza 是 <code>ls</code> 的替代品（从已停止维护的 <code>exa</code> fork 而来），默认输出带颜色，额外功能：</p><ul><li><code>-l</code> 模式显示 Git 状态（文件有没有被修改、是否未追踪）</li><li><code>--tree</code> 模式展示目录树，取代单独装 <code>tree</code> 命令</li><li>配合 Nerd Font 字体可以显示文件类型图标</li></ul><h3 id="安装-1"><a href="#安装-1" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install eza</span><br></pre></td></tr></table></figure><h3 id="核心用法"><a href="#核心用法" class="headerlink" title="核心用法"></a>核心用法</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">eza                    <span class="comment"># 等同于 ls，彩色输出</span></span><br><span class="line">eza -l                 <span class="comment"># 长格式，显示权限、大小、修改时间</span></span><br><span class="line">eza -la                <span class="comment"># 包含隐藏文件</span></span><br><span class="line">eza --tree             <span class="comment"># 树形展示当前目录</span></span><br><span class="line">eza --tree -L 2        <span class="comment"># 树形展示，深度限制为 2 层</span></span><br><span class="line">eza -l --git           <span class="comment"># 长格式 + Git 状态列</span></span><br></pre></td></tr></table></figure><h3 id="常用参数-1"><a href="#常用参数-1" class="headerlink" title="常用参数"></a>常用参数</h3><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-l</code> &#x2F; <code>--long</code></td><td>长格式，显示详细信息</td></tr><tr><td><code>-a</code> &#x2F; <code>--all</code></td><td>包含隐藏文件（<code>.</code> 开头）</td></tr><tr><td><code>-T</code> &#x2F; <code>--tree</code></td><td>树形展示目录结构</td></tr><tr><td><code>-L &lt;n&gt;</code> &#x2F; <code>--level &lt;n&gt;</code></td><td>树形展示的深度限制</td></tr><tr><td><code>--git</code></td><td>长格式中增加 Git 状态列</td></tr><tr><td><code>--icons</code></td><td>显示文件类型图标（需要 Nerd Font）</td></tr><tr><td><code>-s &lt;field&gt;</code> &#x2F; <code>--sort &lt;field&gt;</code></td><td>排序字段，如 <code>size</code>、<code>modified</code>、<code>ext</code></td></tr><tr><td><code>-r</code> &#x2F; <code>--reverse</code></td><td>逆序排列</td></tr><tr><td><code>--no-permissions</code></td><td>不显示权限列（输出更简洁）</td></tr><tr><td><code>-h</code> &#x2F; <code>--header</code></td><td>长格式时显示列标题</td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 按文件大小倒序排列</span></span><br><span class="line">eza -l -s size -r</span><br><span class="line"><span class="comment">#      -s：sort，指定排序字段</span></span><br><span class="line"><span class="comment">#             -r：reverse，逆序</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 带图标的树形展示，深度 3 层</span></span><br><span class="line">eza --tree --icons=always -L 3</span><br><span class="line"><span class="comment">#          --icons=always：显示图标（需要 Nerd Font）</span></span><br><span class="line"><span class="comment">#                          -L：level，树的深度</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看目录，不递归进子目录</span></span><br><span class="line">eza -lD</span><br><span class="line"><span class="comment">#   -D：只显示目录</span></span><br></pre></td></tr></table></figure><h3 id="alias-配置"><a href="#alias-配置" class="headerlink" title="alias 配置"></a>alias 配置</h3><p>在 <code>~/.zshrc</code> 里加：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">alias</span> <span class="built_in">ls</span>=<span class="string">&quot;eza&quot;</span></span><br><span class="line"><span class="built_in">alias</span> ll=<span class="string">&quot;eza -l --git&quot;</span></span><br><span class="line"><span class="built_in">alias</span> la=<span class="string">&quot;eza -la --git&quot;</span></span><br><span class="line"><span class="built_in">alias</span> lt=<span class="string">&quot;eza --tree -L 2&quot;</span></span><br><span class="line"><span class="built_in">alias</span> llt=<span class="string">&quot;eza -l --tree -L 2 --git&quot;</span></span><br></pre></td></tr></table></figure><hr><h2 id="glow：终端里渲染-Markdown"><a href="#glow：终端里渲染-Markdown" class="headerlink" title="glow：终端里渲染 Markdown"></a><a href="https://github.com/charmbracelet/glow">glow</a>：终端里渲染 Markdown</h2><p>glow 把 Markdown 渲染成格式化的终端输出，标题有缩进和颜色，加粗正常显示，代码块有高亮，比直接 <code>cat</code> 看 <code>.md</code> 文件体验好很多。</p><h3 id="安装-2"><a href="#安装-2" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install glow</span><br></pre></td></tr></table></figure><h3 id="常用参数-2"><a href="#常用参数-2" class="headerlink" title="常用参数"></a>常用参数</h3><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-p</code> &#x2F; <code>--pager</code></td><td>用内置分页器打开（支持上下滚动和搜索）</td></tr><tr><td><code>-w &lt;n&gt;</code> &#x2F; <code>--width &lt;n&gt;</code></td><td>指定渲染宽度（默认跟随终端宽度）</td></tr><tr><td><code>-s &lt;style&gt;</code> &#x2F; <code>--style &lt;style&gt;</code></td><td>主题，可选 <code>dark</code>、<code>light</code>、<code>notty</code>、<code>dracula</code></td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 渲染单个文件</span></span><br><span class="line">glow README.md</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用分页器打开（可以上下滚动）</span></span><br><span class="line">glow -p README.md</span><br><span class="line"><span class="comment">#    -p：pager，开启分页模式</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 指定换行宽度，适合宽屏</span></span><br><span class="line">glow -w 100 README.md</span><br><span class="line"><span class="comment">#    -w：width，自动换行的列数（设为 0 禁用自动换行）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 交互式浏览当前目录所有 .md 文件</span></span><br><span class="line">glow</span><br></pre></td></tr></table></figure><p>不加参数直接运行 <code>glow</code>，会列出当前目录里的所有 Markdown 文件供选择，适合快速翻看项目文档。</p><hr><h2 id="mdcat：图文混排的-Markdown-渲染"><a href="#mdcat：图文混排的-Markdown-渲染" class="headerlink" title="mdcat：图文混排的 Markdown 渲染"></a><a href="https://github.com/swsnr/mdcat">mdcat</a>：图文混排的 Markdown 渲染</h2><p>mdcat 和 glow 功能类似，核心区别：<strong>在 kitty 终端下支持内联显示图片</strong>。Markdown 文档里如果有图片，mdcat 会直接把图片渲染在终端里，而不是显示一个链接。</p><h3 id="安装-3"><a href="#安装-3" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install mdcat</span><br></pre></td></tr></table></figure><h3 id="用法"><a href="#用法" class="headerlink" title="用法"></a>用法</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 渲染 Markdown 文件（默认不分页，直接输出）</span></span><br><span class="line">mdcat README.md</span><br><span class="line"></span><br><span class="line"><span class="comment"># 开启分页（内容多时可上下滚动）</span></span><br><span class="line">mdcat --paginate README.md</span><br><span class="line"><span class="comment">#     --paginate / -p：启用分页器（类似 less），默认是关闭的</span></span><br></pre></td></tr></table></figure><h3 id="glow-vs-mdcat-怎么选"><a href="#glow-vs-mdcat-怎么选" class="headerlink" title="glow vs mdcat 怎么选"></a>glow vs mdcat 怎么选</h3><table><thead><tr><th></th><th>glow</th><th>mdcat</th></tr></thead><tbody><tr><td>图片内联显示</td><td>不支持</td><td>支持（需要 kitty）</td></tr><tr><td>交互式浏览</td><td>支持（直接运行 <code>glow</code>）</td><td>不支持</td></tr><tr><td>主题自定义</td><td>更多选项</td><td>较少</td></tr><tr><td>适合场景</td><td>日常看文档、README</td><td>文档里有图的场景</td></tr></tbody></table><p>两个都装，看纯文字文档用 glow，文档里有图用 mdcat。</p><hr><h2 id="bat-作为底层渲染器"><a href="#bat-作为底层渲染器" class="headerlink" title="bat 作为底层渲染器"></a>bat 作为底层渲染器</h2><p>安装好 bat 之后，其他工具会自动调用它，不需要额外配置：</p><ul><li><strong>fzf 文件预览</strong>：<code>Ctrl+T</code> 预览文件时，bat 负责渲染高亮</li><li><strong>yazi 代码预览</strong>：打开代码文件时，yazi 调用 bat 渲染</li><li><strong>git-delta</strong>：diff 输出的语法高亮底层也依赖 bat 的主题</li></ul><p>bat 的主题配置会统一影响这些工具的显示效果，选主题时值得多试几个。</p><hr><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><p><strong>bat 和 cat 有什么区别，能直接替换吗？</strong></p><p>bat 是 cat 的超集，所有 cat 能做的 bat 都能做，额外增加语法高亮、行号、Git 标注和自动分页。用 <code>alias cat=&quot;bat&quot;</code> 直接替换没有问题。如果某些脚本需要纯文本输出（不带颜色控制符），用 <code>bat --plain</code> 或单独保留 <code>catp</code> 别名。</p><p><strong>eza 和 exa 什么关系？</strong></p><p>eza 是从已停止维护的 exa fork 出来的社区维护版本，功能上基本兼容，额外修了一些 bug 并持续更新。如果你之前用 exa，换成 eza 直接改 alias 就行，命令参数基本相同。</p><p><strong>glow 和 mdcat 图片显示的区别是什么？</strong></p><p>glow 不支持内联图片，Markdown 里的图片会显示为纯文字链接。mdcat 在 kitty 终端下支持把图片直接渲染在终端里（Sixel 协议或 kitty 图形协议），文档里有配图的场景体验更完整。如果不用 kitty，两者在图片处理上没有差别。</p><p><strong>这四个工具需要 Nerd Font 吗？</strong></p><p>bat、glow、mdcat 不需要。eza 的 <code>--icons</code> 参数需要安装 Nerd Font，否则图标会显示为乱码。不想装字体的话，不加 <code>--icons</code> 参数正常使用即可。</p>]]>
    </content>
    <id>https://blog.codewater.top/%E6%96%87%E4%BB%B6%E6%9F%A5%E7%9C%8B%E5%B7%A5%E5%85%B7-bat-eza-glow-mdcat/</id>
    <link href="https://blog.codewater.top/%E6%96%87%E4%BB%B6%E6%9F%A5%E7%9C%8B%E5%B7%A5%E5%85%B7-bat-eza-glow-mdcat/"/>
    <published>2026-07-01T02:00:00.000Z</published>
    <summary>Mac 终端文件查看升级：bat 替换 cat、eza 替换 ls、glow 和 mdcat 渲染 Markdown，含安装、alias 配置和常用参数速查。</summary>
    <title>文件查看升级：bat + eza + glow + mdcat</title>
    <updated>2026-07-01T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="fzf" scheme="https://blog.codewater.top/tags/fzf/"/>
    <category term="ripgrep" scheme="https://blog.codewater.top/tags/ripgrep/"/>
    <category term="fd" scheme="https://blog.codewater.top/tags/fd/"/>
    <category term="jq" scheme="https://blog.codewater.top/tags/jq/"/>
    <content>
      <![CDATA[<p><code>grep -r</code> 在大型仓库里要等好几秒，<code>find</code> 的参数从来记不住，API 返回的 JSON 只能整块看。fzf、ripgrep、fd 和 jq 把这三个问题解决了，而且可以串联成一条搜索流水线。</p><p>这篇是系列第二篇，接在 <a href="/Mac%E7%BB%88%E7%AB%AF%E5%9F%BA%E7%A1%80-kitty%E5%92%8CZim">kitty + Zim</a> 之后。如果你在 Zim 里启用了 <code>fzf-tab</code> 插件，装完 fzf 之后它会自动生效。</p><span id="more"></span><hr><h2 id="fzf：模糊搜索框架"><a href="#fzf：模糊搜索框架" class="headerlink" title="fzf：模糊搜索框架"></a><a href="https://github.com/junegunn/fzf">fzf</a>：模糊搜索框架</h2><p>fzf 本身做一件事：接受文本输入，让你模糊过滤后选出一行。但它能接在任何命令后面，这让它成为整个工具链里”乘数效应”最强的一个。</p><p>默认行为：从 stdin 读取文本列表，输出用户选中的行到 stdout；单独运行时调用 <code>find</code> 或 <code>$FZF_DEFAULT_COMMAND</code> 列出文件；模糊匹配不要求字符连续，只要按顺序出现即可（输入 <code>gc</code> 能匹配 <code>git commit</code>）。</p><h3 id="安装与-Zsh-集成"><a href="#安装与-Zsh-集成" class="headerlink" title="安装与 Zsh 集成"></a>安装与 Zsh 集成</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">brew install fzf</span><br><span class="line"><span class="comment"># 安装 Zsh 快捷键绑定——不跑这条，三个快捷键不会自动生效</span></span><br><span class="line">$(brew --prefix)/opt/fzf/install</span><br></pre></td></tr></table></figure><p>装完重启终端（或 <code>exec zsh</code>），三个快捷键立即可用。</p><h3 id="三个核心快捷键"><a href="#三个核心快捷键" class="headerlink" title="三个核心快捷键"></a>三个核心快捷键</h3><p><strong><code>Ctrl+T</code>：模糊查找文件，路径粘到命令行</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 你想 nvim 打开某个文件，但记不清路径</span></span><br><span class="line">nvim &lt;按 Ctrl+T&gt;</span><br><span class="line"><span class="comment"># 弹出 fzf 界面，模糊搜索当前目录下所有文件，选中后路径自动补全到命令行</span></span><br></pre></td></tr></table></figure><p><strong><code>Alt+C</code>：模糊跳转目录</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 按 Alt+C 弹出目录列表，模糊搜索后直接 cd 过去</span></span><br><span class="line"><span class="comment"># 比输 cd ~/code/github/some-project/src/components 快得多</span></span><br></pre></td></tr></table></figure><blockquote><p>kitty 用户注意：需要 <code>kitty.conf</code> 里有 <code>macos_option_as_alt yes</code>，Alt+C 才能触发。第一篇里已经配过了。</p></blockquote><p><strong><code>Ctrl+R</code>：交互式历史搜索</strong></p><p>替换原生的 <code>Ctrl+R</code>，弹出可模糊搜索的历史列表，选中直接执行。如果你后续装了 atuin，atuin 会接管这个快捷键，功能更强（第五篇讲）。</p><h3 id="fzf-tab-在-Zim-里自动生效"><a href="#fzf-tab-在-Zim-里自动生效" class="headerlink" title="fzf-tab 在 Zim 里自动生效"></a>fzf-tab 在 Zim 里自动生效</h3><p>第一篇 Zim 配置里加了 <code>fzf-tab</code> 插件。只要 fzf 装好，重启终端后按 <code>Tab</code> 补全就会变成 fzf 交互界面——候选列表可以模糊过滤，用 <code>↑↓</code> 选择，回车确认。</p><h3 id="常用参数"><a href="#常用参数" class="headerlink" title="常用参数"></a>常用参数</h3><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-m</code></td><td>多选（Tab 标记，回车一次输出所有选中项）</td></tr><tr><td><code>-e</code></td><td>精确匹配，不模糊（适合搜确切文件名）</td></tr><tr><td><code>-1</code></td><td>只有一个匹配时自动选择，不弹界面</td></tr><tr><td><code>-0</code></td><td>没有匹配时直接退出，不弹界面</td></tr><tr><td><code>--query &quot;str&quot;</code></td><td>启动时预填搜索词</td></tr><tr><td><code>--header &quot;text&quot;</code></td><td>在列表顶部显示说明文字</td></tr><tr><td><code>--prompt &quot;str&quot;</code></td><td>自定义提示符（默认 <code>&gt; </code>）</td></tr><tr><td><code>--preview &#39;cmd&#39;</code></td><td>预览命令，<code>&#123;&#125;</code> 代表当前高亮项</td></tr><tr><td><code>--preview-window pos:size</code></td><td>预览窗口位置（<code>right</code>&#x2F;<code>left</code>&#x2F;<code>up</code>&#x2F;<code>down</code>）和大小（如 <code>right:50%</code>）</td></tr><tr><td><code>--bind &quot;key:action&quot;</code></td><td>自定义按键绑定（如 <code>ctrl-y:execute(echo &#123;&#125; | pbcopy)</code>）</td></tr><tr><td><code>--filter &quot;str&quot;</code></td><td>非交互模式，直接过滤输出，不弹界面</td></tr><tr><td><code>--nth N</code></td><td>只对第 N 列做模糊匹配（配合 <code>--delimiter</code> 用）</td></tr><tr><td><code>--with-nth N</code></td><td>只显示第 N 列，不影响实际输出内容</td></tr><tr><td><code>--no-sort</code></td><td>保持输入顺序，不按匹配度排序</td></tr><tr><td><code>--tac</code></td><td>反转输入顺序（常用于让最新历史显示在上方）</td></tr></tbody></table><p>几个实用示例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 只有一个匹配时自动选择，避免弹出只有一项的界面</span></span><br><span class="line">git branch | fzf -1 | xargs git checkout</span><br><span class="line"></span><br><span class="line"><span class="comment"># 带说明文字的选择界面</span></span><br><span class="line"><span class="built_in">printf</span> <span class="string">&quot;yes\nno&quot;</span> | fzf --header <span class="string">&quot;确认删除？&quot;</span> --prompt <span class="string">&quot;选择: &quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 预览窗口显示在下方，占 40% 高度</span></span><br><span class="line">fzf --preview <span class="string">&#x27;cat &#123;&#125;&#x27;</span> --preview-window down:40%</span><br><span class="line"></span><br><span class="line"><span class="comment"># 对 ps aux 输出只匹配第 11 列（进程名），避免 PID 干扰匹配</span></span><br><span class="line">ps aux | fzf --nth 11</span><br><span class="line"></span><br><span class="line"><span class="comment"># 非交互过滤：不弹界面，直接输出匹配结果（类似模糊版 grep）</span></span><br><span class="line"><span class="built_in">cat</span> list.txt | fzf --filter <span class="string">&quot;keyword&quot;</span> --no-sort</span><br><span class="line"></span><br><span class="line"><span class="comment"># 按 Ctrl+Y 复制当前选中项到剪贴板</span></span><br><span class="line">fzf --<span class="built_in">bind</span> <span class="string">&#x27;ctrl-y:execute(echo &#123;&#125; | pbcopy)&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="和其他命令组合"><a href="#和其他命令组合" class="headerlink" title="和其他命令组合"></a>和其他命令组合</h3><p>fzf 接受任意文本输入，输出用户选中的行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 交互式切换 git 分支</span></span><br><span class="line">git branch | fzf | xargs git checkout</span><br><span class="line"></span><br><span class="line"><span class="comment"># 交互式 kill 进程</span></span><br><span class="line">ps aux | fzf | awk <span class="string">&#x27;&#123;print $2&#125;&#x27;</span> | xargs <span class="built_in">kill</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 带预览窗口的文件浏览（需要先装 bat，第三篇会讲）</span></span><br><span class="line">fzf --preview <span class="string">&#x27;bat --color=always &#123;&#125;&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="多选：-m"><a href="#多选：-m" class="headerlink" title="多选：-m"></a>多选：-m</h3><p>加 <code>-m</code> 参数后，Tab 键可以多选，回车一次性输出所有选中项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 同时用 nvim 打开多个文件</span></span><br><span class="line">fd -t f | fzf -m | xargs nvim</span><br><span class="line"></span><br><span class="line"><span class="comment"># 同时删除多个匹配文件（慎用）</span></span><br><span class="line">fd -e <span class="built_in">log</span> | fzf -m | xargs <span class="built_in">rm</span></span><br></pre></td></tr></table></figure><h3 id="实时全文搜索：把-rg-接进-fzf"><a href="#实时全文搜索：把-rg-接进-fzf" class="headerlink" title="实时全文搜索：把 rg 接进 fzf"></a>实时全文搜索：把 rg 接进 fzf</h3><p>下面这个函数可以加到 <code>~/.zshrc</code> 里，实现”边输边搜”的交互式全文搜索：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 加到 ~/.zshrc</span></span><br><span class="line">rg-<span class="function"><span class="title">fzf</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> RG_PREFIX=<span class="string">&quot;rg --column --line-number --no-heading --color=always --smart-case&quot;</span></span><br><span class="line">  fzf --ansi --disabled --query <span class="string">&quot;$*&quot;</span> \</span><br><span class="line">    --<span class="built_in">bind</span> <span class="string">&quot;start:reload:<span class="variable">$RG_PREFIX</span> &#123;q&#125;&quot;</span> \</span><br><span class="line">    --<span class="built_in">bind</span> <span class="string">&quot;change:reload:sleep 0.1; <span class="variable">$RG_PREFIX</span> &#123;q&#125; || true&quot;</span> \</span><br><span class="line">    --<span class="built_in">bind</span> <span class="string">&quot;enter:become(nvim &#123;1&#125; +&#123;2&#125;)&quot;</span> \</span><br><span class="line">    --delimiter : \</span><br><span class="line">    --preview <span class="string">&#x27;bat --color=always &#123;1&#125; --highlight-line &#123;2&#125;&#x27;</span> \</span><br><span class="line">    --preview-window <span class="string">&#x27;right,60%,border-left,+&#123;2&#125;+3/3,~3&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用方式：<code>rg-fzf &quot;关键词&quot;</code> 或直接 <code>rg-fzf</code>，在 fzf 界面里继续输入过滤。输入关键词时 rg 实时重新查询，选中条目后按回车用 nvim 打开对应文件并跳到对应行。</p><h3 id="FZF-DEFAULT-OPTS：统一配置预览窗口"><a href="#FZF-DEFAULT-OPTS：统一配置预览窗口" class="headerlink" title="FZF_DEFAULT_OPTS：统一配置预览窗口"></a>FZF_DEFAULT_OPTS：统一配置预览窗口</h3><p>在 <code>~/.zshrc</code> 里设置 <code>FZF_DEFAULT_OPTS</code>，可以让所有 fzf 调用都带上默认选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 加到 ~/.zshrc</span></span><br><span class="line"><span class="built_in">export</span> FZF_DEFAULT_OPTS=<span class="string">&quot;</span></span><br><span class="line"><span class="string">  --height=60%</span></span><br><span class="line"><span class="string">  --layout=reverse</span></span><br><span class="line"><span class="string">  --border=rounded</span></span><br><span class="line"><span class="string">  --preview-window=right:50%</span></span><br><span class="line"><span class="string">&quot;</span></span><br></pre></td></tr></table></figure><p>设置之后，单独运行 <code>fzf</code> 也会默认带预览窗口布局，不需要每次手动传参。</p><hr><h2 id="ripgrep（rg）：比-grep-快一个数量级的全文搜索"><a href="#ripgrep（rg）：比-grep-快一个数量级的全文搜索" class="headerlink" title="ripgrep（rg）：比 grep 快一个数量级的全文搜索"></a><a href="https://github.com/BurntSushi/ripgrep">ripgrep</a>（rg）：比 grep 快一个数量级的全文搜索</h2><p>ripgrep（rg）是用 Rust 编写的全文搜索工具。默认行为：第一个参数当正则表达式，无需 <code>-r</code> 就递归搜索当前目录；自动跳过 <code>.git/</code>、<code>.gitignore</code> 里的路径和隐藏文件；输出带文件名、行号和彩色高亮。速度比 <code>grep -r</code> 快一个数量级，主要原因是多线程并发 + 跳过大量无关文件。</p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install ripgrep</span><br></pre></td></tr></table></figure><h3 id="核心用法"><a href="#核心用法" class="headerlink" title="核心用法"></a>核心用法</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 在当前目录递归搜索，自动跳过 .gitignore 里的内容</span></span><br><span class="line">rg <span class="string">&quot;pattern&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索特定类型文件</span></span><br><span class="line">rg -t go <span class="string">&quot;http.Handle&quot;</span></span><br><span class="line">rg -t py <span class="string">&quot;import requests&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 不区分大小写</span></span><br><span class="line">rg -i <span class="string">&quot;TODO&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 整词匹配（不匹配 patterns 里的 pattern）</span></span><br><span class="line">rg -w <span class="string">&quot;error&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 在指定目录下搜索</span></span><br><span class="line">rg <span class="string">&quot;pattern&quot;</span> src/</span><br><span class="line">rg <span class="string">&quot;pattern&quot;</span> ~/code/github/some-project</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只输出匹配的文件名</span></span><br><span class="line">rg -l <span class="string">&quot;pattern&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示上下文（前后各 2 行）</span></span><br><span class="line">rg -C 2 <span class="string">&quot;panic&quot;</span></span><br></pre></td></tr></table></figure><p><code>rg</code> 和 <code>grep -r</code> 的主要区别：不需要加 <code>-r</code> 就默认递归；自动跳过 <code>.git/</code>、<code>.gitignore</code> 里的路径、隐藏文件；输出带文件名、行号和彩色高亮。在大型代码仓库里，速度差距非常明显。</p><h3 id="常用参数-1"><a href="#常用参数-1" class="headerlink" title="常用参数"></a>常用参数</h3><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-i</code></td><td>忽略大小写</td></tr><tr><td><code>-s</code></td><td>强制区分大小写</td></tr><tr><td><code>-S</code> &#x2F; <code>--smart-case</code></td><td>智能大小写：有大写字母时区分，全小写时忽略</td></tr><tr><td><code>-w</code></td><td>整词匹配</td></tr><tr><td><code>-F</code></td><td>固定字符串（不当作正则，特殊字符不需要转义）</td></tr><tr><td><code>-v</code></td><td>反向匹配（输出不包含 pattern 的行）</td></tr><tr><td><code>-l</code></td><td>只输出文件名，不显示匹配行</td></tr><tr><td><code>-c</code></td><td>只输出每个文件的匹配行数</td></tr><tr><td><code>-o</code></td><td>只输出匹配的部分，不输出整行</td></tr><tr><td><code>-n</code> &#x2F; <code>-N</code></td><td>显示&#x2F;隐藏行号（默认显示）</td></tr><tr><td><code>-A N</code></td><td>匹配行后面 N 行</td></tr><tr><td><code>-B N</code></td><td>匹配行前面 N 行</td></tr><tr><td><code>-C N</code></td><td>匹配行前后各 N 行</td></tr><tr><td><code>-t type</code></td><td>只搜索指定类型（<code>go</code>&#x2F;<code>py</code>&#x2F;<code>js</code>&#x2F;<code>md</code> 等）</td></tr><tr><td><code>-T type</code></td><td>排除指定类型</td></tr><tr><td><code>-g &quot;glob&quot;</code></td><td>只搜索匹配 glob 的文件（如 <code>-g &quot;*.go&quot;</code>）</td></tr><tr><td><code>--hidden</code></td><td>包含隐藏文件（默认跳过）</td></tr><tr><td><code>-u</code></td><td>不跳过 <code>.gitignore</code> 里的文件</td></tr><tr><td><code>-m N</code></td><td>每个文件最多输出 N 条匹配</td></tr><tr><td><code>-e &quot;pattern&quot;</code></td><td>指定多个匹配模式（可多次用）</td></tr><tr><td><code>--json</code></td><td>输出 JSON 格式（可接 jq 处理）</td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 在 .go 和 .proto 文件里搜索，忽略大小写，显示前后 3 行</span></span><br><span class="line">rg -t go -g <span class="string">&quot;*.proto&quot;</span> -i -C 3 <span class="string">&quot;grpc&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 只看匹配的部分（不是整行），适合提取变量名等</span></span><br><span class="line">rg -o <span class="string">&#x27;GOPATH=\S+&#x27;</span> ~/.zshrc</span><br><span class="line"></span><br><span class="line"><span class="comment"># 反向匹配：找所有不含 TODO 的 Go 文件</span></span><br><span class="line">rg -l --files-without-match <span class="string">&quot;TODO&quot;</span> -t go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 多个 pattern 任意匹配</span></span><br><span class="line">rg -e <span class="string">&quot;error&quot;</span> -e <span class="string">&quot;panic&quot;</span> -e <span class="string">&quot;fatal&quot;</span> main.go</span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出 JSON 后用 jq 处理（行号、文件名等结构化取出）</span></span><br><span class="line">rg --json <span class="string">&quot;pattern&quot;</span> | jq <span class="string">&#x27;select(.type==&quot;match&quot;) | .data.path.text&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="和-fzf-组合：交互式选择搜索结果"><a href="#和-fzf-组合：交互式选择搜索结果" class="headerlink" title="和 fzf 组合：交互式选择搜索结果"></a>和 fzf 组合：交互式选择搜索结果</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 列出所有匹配的文件，fzf 里预览内容并选择</span></span><br><span class="line">rg -l <span class="string">&quot;TODO&quot;</span> | fzf --preview <span class="string">&#x27;bat --color=always &#123;&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 选中后直接用 nvim 打开</span></span><br><span class="line">rg -l <span class="string">&quot;TODO&quot;</span> | fzf --preview <span class="string">&#x27;bat --color=always &#123;&#125;&#x27;</span> | xargs nvim</span><br></pre></td></tr></table></figure><h3 id="全局配置：-ripgreprc"><a href="#全局配置：-ripgreprc" class="headerlink" title="全局配置：~&#x2F;.ripgreprc"></a>全局配置：~&#x2F;.ripgreprc</h3><p>每次都手动传参数很麻烦。把常用选项写进配置文件，rg 会自动加载：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建配置文件</span></span><br><span class="line"><span class="built_in">touch</span> ~/.ripgreprc</span><br></pre></td></tr></table></figure><figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ~/.ripgreprc 内容</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 有大写字母时区分大小写，全小写时忽略大小写——比 -i 更智能</span></span><br><span class="line"><span class="params">--smart-case</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索隐藏文件（如 .env、.zshrc），但仍跳过 .git/</span></span><br><span class="line"><span class="params">--hidden</span></span><br><span class="line"><span class="params">--glob=</span>!<span class="string">.git/</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出带颜色（在管道里也保持颜色）</span></span><br><span class="line"><span class="params">--color=always</span></span><br></pre></td></tr></table></figure><p>然后在 <code>~/.zshrc</code> 里指定配置文件路径：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> RIPGREP_CONFIG_PATH=<span class="string">&quot;<span class="variable">$HOME</span>/.ripgreprc&quot;</span></span><br></pre></td></tr></table></figure><p>配置生效后，直接 <code>rg &quot;pattern&quot;</code> 就等于 <code>rg --smart-case --hidden &quot;pattern&quot;</code>，不需要每次手动传这些参数。</p><hr><h2 id="fd：find-的现代替代品"><a href="#fd：find-的现代替代品" class="headerlink" title="fd：find 的现代替代品"></a><a href="https://github.com/sharkdp/fd">fd</a>：find 的现代替代品</h2><p>fd 是 <code>find</code> 的现代替代品，同样用 Rust 编写。默认行为：第一个参数当正则表达式匹配文件名（不是路径），大小写不敏感；自动跳过 <code>.gitignore</code> 里的路径和以 <code>.</code> 开头的隐藏文件；输出带颜色。不加任何参数时列出当前目录下所有非隐藏文件，等同于 <code>find . -not -path &#39;*/.*&#39;</code> 但更快。</p><h3 id="安装-1"><a href="#安装-1" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install fd</span><br></pre></td></tr></table></figure><h3 id="核心用法-1"><a href="#核心用法-1" class="headerlink" title="核心用法"></a>核心用法</h3><p>fd 和 <code>find</code> 做同样的事，但语法直觉很多：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 找文件名包含 config 的文件（默认递归，大小写不敏感）</span></span><br><span class="line">fd config</span><br><span class="line"></span><br><span class="line"><span class="comment"># 按扩展名查找</span></span><br><span class="line">fd -e go</span><br><span class="line">fd -e md -e txt         <span class="comment"># 多个扩展名</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 按类型过滤</span></span><br><span class="line">fd -t f                 <span class="comment"># 只找文件（file）</span></span><br><span class="line">fd -t d src             <span class="comment"># 只找目录（directory）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 排除目录</span></span><br><span class="line">fd -E node_modules -E .git -e js</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在特定路径下查找</span></span><br><span class="line">fd -e go ~/code/github</span><br><span class="line"></span><br><span class="line"><span class="comment"># 对结果执行命令（类似 find -exec）</span></span><br><span class="line">fd -e go -x <span class="built_in">wc</span> -l       <span class="comment"># 统计每个 Go 文件行数（-x 逐个执行）</span></span><br><span class="line">fd -e <span class="built_in">log</span> -X <span class="built_in">rm</span>         <span class="comment"># 删除所有 .log 文件（-X 合并参数）</span></span><br></pre></td></tr></table></figure><p><code>fd</code> vs <code>find</code> 语法对比：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 找当前目录下所有 .go 文件</span></span><br><span class="line">find . -name <span class="string">&quot;*.go&quot;</span> -<span class="built_in">type</span> f   <span class="comment"># find</span></span><br><span class="line">fd -e go                       <span class="comment"># fd</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 找名字包含 test 的文件</span></span><br><span class="line">find . -name <span class="string">&quot;*test*&quot;</span> -<span class="built_in">type</span> f  <span class="comment"># find</span></span><br><span class="line">fd <span class="built_in">test</span>                        <span class="comment"># fd</span></span><br></pre></td></tr></table></figure><h3 id="常用参数-2"><a href="#常用参数-2" class="headerlink" title="常用参数"></a>常用参数</h3><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-e ext</code></td><td>按扩展名过滤（不带点，可多次用）</td></tr><tr><td><code>-t f/d/l/x</code></td><td>按类型：文件&#x2F;目录&#x2F;符号链接&#x2F;可执行文件</td></tr><tr><td><code>-E pattern</code></td><td>排除匹配的路径（可多次用）</td></tr><tr><td><code>-H</code></td><td>包含隐藏文件（以 <code>.</code> 开头的）</td></tr><tr><td><code>-I</code></td><td>不跳过 <code>.gitignore</code>（忽略所有 ignore 规则）</td></tr><tr><td><code>-L</code></td><td>跟随符号链接</td></tr><tr><td><code>-d N</code></td><td>最大搜索深度（<code>-d 1</code> 只搜当前目录）</td></tr><tr><td><code>-g</code></td><td>使用 glob 模式而不是正则（<code>-g &quot;*.go&quot;</code> 等）</td></tr><tr><td><code>-s</code></td><td>区分大小写（默认不区分）</td></tr><tr><td><code>-l</code></td><td>显示详细信息（类似 <code>ls -l</code>）</td></tr><tr><td><code>-0</code></td><td>用 null 分隔输出（配合 <code>xargs -0</code> 处理含空格的路径）</td></tr><tr><td><code>-x cmd</code></td><td>对每个结果执行命令（逐个）</td></tr><tr><td><code>-X cmd</code></td><td>对所有结果合并执行一次命令</td></tr><tr><td><code>--changed-within N</code></td><td>在 N 时间内修改过的（如 <code>1h</code>、<code>2d</code>、<code>1week</code>）</td></tr><tr><td><code>--changed-before N</code></td><td>在 N 时间之前修改过的</td></tr><tr><td><code>--max-results N</code></td><td>最多输出 N 条结果</td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 找最近 1 小时内改动过的文件</span></span><br><span class="line">fd --changed-within 1h</span><br><span class="line"></span><br><span class="line"><span class="comment"># 找 7 天前的日志文件（可以接 -X rm 清理）</span></span><br><span class="line">fd -e <span class="built_in">log</span> --changed-before 7d</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索包含空格的路径时，用 -0 + xargs -0 避免分词问题</span></span><br><span class="line">fd -e txt -0 | xargs -0 <span class="built_in">wc</span> -l</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只搜当前目录一层（不递归进子目录）</span></span><br><span class="line">fd -d 1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 找所有可执行文件（排除 .git 目录）</span></span><br><span class="line">fd -t x -E .git</span><br><span class="line"></span><br><span class="line"><span class="comment"># 找隐藏配置文件（如 .env、.zshrc）</span></span><br><span class="line">fd -H -g <span class="string">&quot;.*rc&quot;</span> ~</span><br></pre></td></tr></table></figure><h3 id="和-fzf-组合"><a href="#和-fzf-组合" class="headerlink" title="和 fzf 组合"></a>和 fzf 组合</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 列出所有文件，fzf 选择后用 nvim 打开</span></span><br><span class="line">fd -t f | fzf | xargs nvim</span><br><span class="line"></span><br><span class="line"><span class="comment"># 列出所有目录，fzf 选择后 cd 进去</span></span><br><span class="line"><span class="built_in">cd</span> $(fd -t d | fzf)</span><br></pre></td></tr></table></figure><hr><h2 id="jq：JSON-命令行处理器"><a href="#jq：JSON-命令行处理器" class="headerlink" title="jq：JSON 命令行处理器"></a><a href="https://jqlang.github.io/jq/">jq</a>：JSON 命令行处理器</h2><p>jq 是 JSON 的命令行处理器，作用类似 <code>sed</code>&#x2F;<code>awk</code>，但专门处理 JSON 结构。默认行为：从 stdin 读取 JSON，第一个参数是过滤表达式（<code>.</code> 表示原样输出），输出默认 pretty-print（带缩进和颜色高亮）；字符串输出带双引号——新手最常踩的坑，加 <code>-r</code> 才能去掉引号，把结果传给 shell 用。</p><h3 id="安装-2"><a href="#安装-2" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install jq</span><br></pre></td></tr></table></figure><h3 id="基础语法"><a href="#基础语法" class="headerlink" title="基础语法"></a>基础语法</h3><p>jq 的查询表达式作为第一个参数传入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 格式化输出（pretty-print）</span></span><br><span class="line"><span class="built_in">cat</span> data.json | jq .</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提取字段</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;&#123;&quot;name&quot;: &quot;Alice&quot;, &quot;age&quot;: 30&#125;&#x27;</span> | jq <span class="string">&#x27;.name&#x27;</span></span><br><span class="line"><span class="comment"># 输出: &quot;Alice&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 嵌套字段</span></span><br><span class="line">jq <span class="string">&#x27;.data.user.email&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 遍历数组</span></span><br><span class="line">jq <span class="string">&#x27;.[]&#x27;</span>           <span class="comment"># 展开每个元素</span></span><br><span class="line">jq <span class="string">&#x27;.[0]&#x27;</span>          <span class="comment"># 第一个元素</span></span><br><span class="line">jq <span class="string">&#x27;.[-1]&#x27;</span>         <span class="comment"># 最后一个元素</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 提取数组里每个元素的字段</span></span><br><span class="line">jq <span class="string">&#x27;.[].name&#x27;</span></span><br><span class="line">jq <span class="string">&#x27;.items[] | .id&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 过滤</span></span><br><span class="line">jq <span class="string">&#x27;.[] | select(.status == &quot;active&quot;)&#x27;</span></span><br><span class="line">jq <span class="string">&#x27;.[] | select(.age &gt; 18)&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 重组输出（只保留指定字段）</span></span><br><span class="line">jq <span class="string">&#x27;.[] | &#123;id, name&#125;&#x27;</span></span><br><span class="line">jq <span class="string">&#x27;.[] | &#123;user: .name, email: .contact.email&#125;&#x27;</span></span><br></pre></td></tr></table></figure><p><code>-r</code> 参数去掉字符串的引号，适合把结果传给其他命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jq -r <span class="string">&#x27;.name&#x27;</span>       <span class="comment"># 输出 Alice，而不是 &quot;Alice&quot;</span></span><br></pre></td></tr></table></figure><h3 id="常用参数-3"><a href="#常用参数-3" class="headerlink" title="常用参数"></a>常用参数</h3><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-r</code></td><td>原始输出：字符串不加引号（适合传给 shell）</td></tr><tr><td><code>-c</code></td><td>紧凑输出：不格式化，单行输出（适合写入文件或传给程序）</td></tr><tr><td><code>-n</code></td><td>不读取输入，从 <code>null</code> 开始（用于构造 JSON）</td></tr><tr><td><code>-e</code></td><td>输出为 <code>false</code>&#x2F;<code>null</code> 时以非零退出码退出（脚本判断用）</td></tr><tr><td><code>-s</code></td><td>把所有输入合并成一个数组再处理</td></tr><tr><td><code>-R</code></td><td>把每行输入当作原始字符串（不解析 JSON）</td></tr><tr><td><code>-j</code></td><td>等同 <code>-r</code>，输出后不加换行</td></tr><tr><td><code>-S</code></td><td>对象的键按字母排序输出</td></tr><tr><td><code>--arg name val</code></td><td>把 shell 变量作为字符串传入 jq（在表达式里用 <code>$name</code>）</td></tr><tr><td><code>--argjson name val</code></td><td>把 shell 变量作为 JSON 值传入（可以是数字、数组等）</td></tr><tr><td><code>--slurpfile name file</code></td><td>把 JSON 文件内容作为变量传入</td></tr><tr><td><code>--indent N</code></td><td>缩进 N 个空格（默认 2）</td></tr><tr><td><code>--tab</code></td><td>用 Tab 缩进</td></tr></tbody></table><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># -c：单行输出，适合写日志或传给程序</span></span><br><span class="line">jq -c <span class="string">&#x27;.[]&#x27;</span> data.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># -n：不读输入，直接构造 JSON</span></span><br><span class="line">jq -n <span class="string">&#x27;&#123;name: &quot;Alice&quot;, scores: [90, 85, 92]&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># -s：把多个 JSON 文件合并成数组后处理</span></span><br><span class="line">jq -s <span class="string">&#x27;.[0].users + .[1].users&#x27;</span> a.json b.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># -R + -s：把普通文本文件转成 JSON 字符串数组（每行一个元素）</span></span><br><span class="line"><span class="built_in">cat</span> list.txt | jq -Rs <span class="string">&#x27;split(&quot;\n&quot;) | map(select(. != &quot;&quot;))&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># -e：在脚本里判断字段是否存在</span></span><br><span class="line"><span class="keyword">if</span> jq -e <span class="string">&#x27;.error&#x27;</span> response.json &gt; /dev/null 2&gt;&amp;1; <span class="keyword">then</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">&quot;请求失败&quot;</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># --argjson：传入数字（不用 --arg，--arg 传的都是字符串）</span></span><br><span class="line">MIN=100</span><br><span class="line">jq --argjson min <span class="string">&quot;<span class="variable">$MIN</span>&quot;</span> <span class="string">&#x27;.[] | select(.count &gt; $min)&#x27;</span> data.json</span><br></pre></td></tr></table></figure><h3 id="实际场景：curl-jq"><a href="#实际场景：curl-jq" class="headerlink" title="实际场景：curl | jq"></a>实际场景：curl | jq</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看 GitHub 用户信息，只取关键字段</span></span><br><span class="line">curl -s https://api.github.com/users/octocat | jq <span class="string">&#x27;&#123;name, public_repos, followers&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 列出仓库名和 star 数</span></span><br><span class="line">curl -s https://api.github.com/users/octocat/repos \</span><br><span class="line">  | jq <span class="string">&#x27;.[] | &#123;name, stargazers_count&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 只看 star 数大于 100 的仓库</span></span><br><span class="line">curl -s https://api.github.com/users/octocat/repos \</span><br><span class="line">  | jq <span class="string">&#x27;[.[] | select(.stargazers_count &gt; 100) | &#123;name, stargazers_count&#125;]&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 从本地 JSON 配置文件提取字段</span></span><br><span class="line">jq <span class="string">&#x27;.database.host&#x27;</span> config.json</span><br></pre></td></tr></table></figure><h3 id="进阶：排序、统计、变量传入"><a href="#进阶：排序、统计、变量传入" class="headerlink" title="进阶：排序、统计、变量传入"></a>进阶：排序、统计、变量传入</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 按 star 数排序（降序）</span></span><br><span class="line">curl -s https://api.github.com/users/octocat/repos \</span><br><span class="line">  | jq <span class="string">&#x27;sort_by(-.stargazers_count) | .[] | &#123;name, stargazers_count&#125;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 统计数组长度</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;[1,2,3,4]&#x27;</span> | jq <span class="string">&#x27;length&#x27;</span></span><br><span class="line"><span class="comment"># 输出: 4</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取对象所有键名</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;&#123;&quot;name&quot;:&quot;Alice&quot;,&quot;age&quot;:30&#125;&#x27;</span> | jq <span class="string">&#x27;keys&#x27;</span></span><br><span class="line"><span class="comment"># 输出: [&quot;age&quot;,&quot;name&quot;]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 检查字段是否存在</span></span><br><span class="line">jq <span class="string">&#x27;.[] | select(has(&quot;email&quot;))&#x27;</span> users.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 传入 shell 变量（用 --arg，避免引号和注入问题）</span></span><br><span class="line">STATUS=<span class="string">&quot;active&quot;</span></span><br><span class="line">jq --arg s <span class="string">&quot;<span class="variable">$STATUS</span>&quot;</span> <span class="string">&#x27;.[] | select(.status == $s)&#x27;</span> data.json</span><br></pre></td></tr></table></figure><h3 id="输出格式转换"><a href="#输出格式转换" class="headerlink" title="输出格式转换"></a>输出格式转换</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 输出 CSV（适合导入 Excel 或传给其他工具）</span></span><br><span class="line">curl -s https://api.github.com/users/octocat/repos \</span><br><span class="line">  | jq -r <span class="string">&#x27;.[] | [.name, .stargazers_count, .language] | @csv&#x27;</span></span><br><span class="line"><span class="comment"># 输出:</span></span><br><span class="line"><span class="comment"># &quot;Hello-World&quot;,1234,&quot;JavaScript&quot;</span></span><br><span class="line"><span class="comment"># &quot;Spoon-Knife&quot;,567,&quot;HTML&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出 TSV（制表符分隔）</span></span><br><span class="line">jq -r <span class="string">&#x27;.[] | [.id, .name] | @tsv&#x27;</span> data.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提取字段列表，传给 shell 命令</span></span><br><span class="line"><span class="comment"># -r 去引号，结果可以直接 xargs 处理</span></span><br><span class="line">curl -s https://api.github.com/users/octocat/repos \</span><br><span class="line">  | jq -r <span class="string">&#x27;.[].clone_url&#x27;</span> | <span class="built_in">head</span> -5</span><br></pre></td></tr></table></figure><p><code>@csv</code> 和 <code>@tsv</code> 会自动处理字段里的引号和特殊字符，比手动拼字符串安全。</p><hr><h2 id="管道把四个工具串成工作流"><a href="#管道把四个工具串成工作流" class="headerlink" title="管道把四个工具串成工作流"></a>管道把四个工具串成工作流</h2><p>四个工具单独用都够用，组合起来才是真正的效率提升。fzf 在中间当连接器，把 rg&#x2F;fd 找到的结果变成交互式选择，bat 做文件预览：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 在所有 Go 文件里搜索 TODO，fzf 选择后用 nvim 打开</span></span><br><span class="line">rg -t go -l <span class="string">&quot;TODO&quot;</span> | fzf --preview <span class="string">&#x27;bat --color=always &#123;&#125;&#x27;</span> | xargs nvim</span><br><span class="line"></span><br><span class="line"><span class="comment"># 找配置文件，预览内容，选择后编辑</span></span><br><span class="line">fd -e json -e yaml | fzf --preview <span class="string">&#x27;bat --color=always &#123;&#125;&#x27;</span> | xargs nvim</span><br><span class="line"></span><br><span class="line"><span class="comment"># 找到包含某个函数名的文件，查看行号</span></span><br><span class="line">rg -n <span class="string">&quot;func HandleLogin&quot;</span> --<span class="built_in">type</span> go</span><br></pre></td></tr></table></figure><p>以上命令里的 <code>bat --color=always</code> 作为预览器，但 bat 还没装。下一篇讲 bat + eza + glow + mdcat，bat 装好后预览窗口就都能用了。</p>]]>
    </content>
    <id>https://blog.codewater.top/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd/</id>
    <link href="https://blog.codewater.top/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd/"/>
    <published>2026-06-30T06:00:00.000Z</published>
    <summary>用 fzf、ripgrep、fd、jq 替换 Mac 终端里的 grep/find：模糊搜索框架让任何命令可交互，rg 在大型仓库里秒级全文搜索，fd 语法比 find 直觉十倍，jq 处理 API 返回的 JSON。</summary>
    <title>终端搜索三件套：fzf + ripgrep + fd，以及 jq</title>
    <updated>2026-07-04T06:10:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="kitty" scheme="https://blog.codewater.top/tags/kitty/"/>
    <category term="Zim" scheme="https://blog.codewater.top/tags/Zim/"/>
    <category term="zsh" scheme="https://blog.codewater.top/tags/zsh/"/>
    <content>
      <![CDATA[<p>macOS 默认终端在高分屏上滚动大量输出会掉帧，Oh My Zsh 每次开终端要等一两秒。换上 kitty + Zim 之后，这两个问题都消失了。</p><p>这篇讲怎么装、怎么配。后续所有工具（eza、yazi、fzf 等）都运行在这个基础上，建议先把这两个配好。</p><span id="more"></span><hr><h2 id="kitty：GPU-渲染的终端模拟器"><a href="#kitty：GPU-渲染的终端模拟器" class="headerlink" title="kitty：GPU 渲染的终端模拟器"></a><a href="https://sw.kovidgoyal.net/kitty/">kitty</a>：GPU 渲染的终端模拟器</h2><h3 id="为什么换掉默认终端"><a href="#为什么换掉默认终端" class="headerlink" title="为什么换掉默认终端"></a>为什么换掉默认终端</h3><p>macOS 自带的 Terminal.app 够用，但渲染靠 CPU，在高分屏上滚动大量输出时会掉帧。iTerm2 是很多人的选择，但功能过于复杂，配置项太多。kitty 用 GPU 渲染，启动和滚动都很快，配置文件是单个 <code>kitty.conf</code>，改起来直接。</p><h3 id="安装-kitty"><a href="#安装-kitty" class="headerlink" title="安装 kitty"></a>安装 kitty</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install --cask kitty</span><br></pre></td></tr></table></figure><p>装完从 Launchpad 或直接 <code>open -a kitty</code> 启动。</p><h3 id="配置文件位置"><a href="#配置文件位置" class="headerlink" title="配置文件位置"></a>配置文件位置</h3><p>kitty 的配置文件在 <code>~/.config/kitty/kitty.conf</code>。第一次启动后这个文件会自动生成，里面全是注释和默认值，实际生效的配置只需要在文件末尾追加你要改的项就行，不需要动前面的默认注释。</p><p>用 kitty 自身打开配置文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># kitty 内置了编辑配置的快捷键</span></span><br><span class="line"><span class="comment"># 按 Ctrl+Shift+F2 会直接用默认编辑器打开 kitty.conf</span></span><br><span class="line"><span class="comment"># 或者直接：</span></span><br><span class="line">nvim ~/.config/kitty/kitty.conf</span><br></pre></td></tr></table></figure><h3 id="我的实际配置"><a href="#我的实际配置" class="headerlink" title="我的实际配置"></a>我的实际配置</h3><p>下面是配置文件末尾追加的内容，注释说明了每项的作用：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"># 字体大小，默认 11.0 在高分屏上偏小</span><br><span class="line">font_size 16.0</span><br><span class="line"></span><br><span class="line"># 光标形状改为竖线（beam），比默认的方块光标更精确</span><br><span class="line">cursor_shape beam</span><br><span class="line"></span><br><span class="line"># 让 Option 键作为 Alt 键使用</span><br><span class="line"># 不开这个，fzf 的 Alt+C 跳目录快捷键等无法生效</span><br><span class="line">macos_option_as_alt yes</span><br><span class="line"></span><br><span class="line"># 自定义快捷键：Ctrl+Alt+Z 将当前窗格全屏（stack 布局）</span><br><span class="line"># kitty 内置分屏，这个快捷键快速聚焦单个窗格</span><br><span class="line">map ctrl+alt+z toggle_layout stack</span><br><span class="line"></span><br><span class="line"># 背景图配置（可选）</span><br><span class="line"># 设置一张壁纸作为终端背景，background_tint 控制遮罩深度</span><br><span class="line"># background_blur 是高斯模糊半径，让文字更易读</span><br><span class="line">background_image ~/Pictures/wallpaper/your-wallpaper.jpg</span><br><span class="line">background_image_layout scaled</span><br><span class="line">background_image_linear yes</span><br><span class="line">background_tint 0.7</span><br><span class="line">background_opacity 0.8</span><br><span class="line">background_blur 32</span><br></pre></td></tr></table></figure><p>其中 <code>macos_option_as_alt yes</code> 这一项最关键，后续很多工具（fzf 的 <code>Alt+C</code>、Zsh 的 <code>Alt+.</code> 等）依赖 Alt 键，不开这个这些快捷键全都用不了。</p><h3 id="字体：安装-Nerd-Font"><a href="#字体：安装-Nerd-Font" class="headerlink" title="字体：安装 Nerd Font"></a>字体：安装 Nerd Font</h3><p>eza 显示文件图标、yazi 文件管理器、lazygit 等工具都依赖 <a href="https://www.nerdfonts.com/">Nerd Font</a> 字体来渲染图标。推荐 JetBrains Mono Nerd Font：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">brew install --cask font-jetbrains-mono-nerd-font</span><br></pre></td></tr></table></figure><p>装好后在 <code>kitty.conf</code> 里指定：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">font_family JetBrainsMono Nerd Font Mono</span><br></pre></td></tr></table></figure><h3 id="内置分屏"><a href="#内置分屏" class="headerlink" title="内置分屏"></a>内置分屏</h3><p>kitty 自带分屏功能，不需要另装 tmux：</p><table><thead><tr><th>快捷键</th><th>作用</th></tr></thead><tbody><tr><td><code>Ctrl+Shift+Enter</code></td><td>向右新建窗格</td></tr><tr><td><code>Ctrl+Shift+]</code> &#x2F; <code>[</code></td><td>切换到下&#x2F;上一个窗格</td></tr><tr><td><code>Ctrl+Alt+Z</code></td><td>全屏当前窗格（上面配置的自定义键）</td></tr><tr><td><code>Ctrl+Shift+T</code></td><td>新建标签页</td></tr><tr><td><code>Ctrl+Shift+Q</code></td><td>关闭当前窗格</td></tr></tbody></table><h3 id="重载配置"><a href="#重载配置" class="headerlink" title="重载配置"></a>重载配置</h3><p>改完 <code>kitty.conf</code> 后不需要重启，按 <code>Ctrl+Shift+F5</code> 重载配置即可生效。</p><hr><h2 id="Zim：比-Oh-My-Zsh-快很多的-zsh-框架"><a href="#Zim：比-Oh-My-Zsh-快很多的-zsh-框架" class="headerlink" title="Zim：比 Oh My Zsh 快很多的 zsh 框架"></a><a href="https://zimfw.sh/">Zim</a>：比 Oh My Zsh 快很多的 zsh 框架</h2><h3 id="为什么用-Zim-而不是-Oh-My-Zsh"><a href="#为什么用-Zim-而不是-Oh-My-Zsh" class="headerlink" title="为什么用 Zim 而不是 Oh My Zsh"></a>为什么用 Zim 而不是 Oh My Zsh</h3><p>Oh My Zsh 是最流行的 zsh 框架，但加载了大量默认插件，导致每次开终端都要等一两秒。Zim 只装你声明的插件，启动速度快很多，日常使用几乎感觉不到延迟。</p><h3 id="安装-Zim"><a href="#安装-Zim" class="headerlink" title="安装 Zim"></a>安装 Zim</h3><p>macOS 自带 zsh，直接安装 Zim 框架：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl -fsSL https://raw.githubusercontent.com/zimfw/zimfw/master/install.zsh | zsh</span><br></pre></td></tr></table></figure><p>这条命令会在 <code>~/.zshrc</code> 和 <code>~/.zimrc</code> 里写入初始化代码，然后自动安装默认插件。装完后重启终端或执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">exec</span> zsh</span><br></pre></td></tr></table></figure><h3 id="配置文件：-zimrc"><a href="#配置文件：-zimrc" class="headerlink" title="配置文件：.zimrc"></a>配置文件：.zimrc</h3><p>Zim 的插件列表在 <code>~/.zimrc</code>，每行一个 <code>zmodule</code> 声明。改完后运行 <code>zimfw install</code> 安装新插件，<code>zimfw uninstall</code> 移除已删除的插件。</p><p>下面是完整的 <code>.zimrc</code> 配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 基础模块</span></span><br><span class="line">zmodule environment   <span class="comment"># 设置合理的 zsh 内置选项</span></span><br><span class="line">zmodule git           <span class="comment"># git 常用别名（gst / gco / gp 等）</span></span><br><span class="line">zmodule input         <span class="comment"># 修正方向键、Ctrl 组合键的 bindkey</span></span><br><span class="line">zmodule termtitle     <span class="comment"># 在终端标题栏显示当前目录</span></span><br><span class="line">zmodule utility       <span class="comment"># 给 ls、grep、less 加颜色</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 补全</span></span><br><span class="line">zmodule zsh-users/zsh-completions --fpath src  <span class="comment"># 扩充补全定义</span></span><br><span class="line">zmodule completion    <span class="comment"># 启用 zsh 智能补全（必须放在补全定义之后）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 语法高亮和历史（顺序固定，不能调换）</span></span><br><span class="line">zmodule zdharma-continuum/fast-syntax-highlighting  <span class="comment"># 命令行实时语法高亮</span></span><br><span class="line">zmodule zsh-users/zsh-history-substring-search      <span class="comment"># 上下箭头按前缀过滤历史</span></span><br><span class="line">zmodule zsh-users/zsh-autosuggestions               <span class="comment"># 灰色自动补全建议</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># fzf 补全集成</span></span><br><span class="line">zmodule Aloxaf/fzf-tab  <span class="comment"># Tab 补全候选列表变成 fzf 模糊搜索界面</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Prompt 主题</span></span><br><span class="line">zmodule git-info   <span class="comment"># steeef 主题依赖这个</span></span><br><span class="line">zmodule steeef     <span class="comment"># Prompt 主题</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>注意顺序：</strong> fast-syntax-highlighting 必须在 history-substring-search 之前，history-substring-search 必须在 autosuggestions 之前。<code>completion</code> 必须在所有补全定义模块之后。Zim 文档里有说明，这个坑踩过才知道。</p></blockquote><p>改完 <code>.zimrc</code> 后执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">zimfw install</span><br><span class="line"><span class="built_in">exec</span> zsh</span><br></pre></td></tr></table></figure><h3 id="插件效果说明"><a href="#插件效果说明" class="headerlink" title="插件效果说明"></a>插件效果说明</h3><p><strong>fast-syntax-highlighting</strong></p><p>输入命令时实时变色：命令存在且路径正确显示绿色，命令不存在或参数有误显示红色。按回车前就能看到拼写错误。</p><p><strong>zsh-history-substring-search</strong></p><p>按 <code>↑</code> 不再是顺序翻历史，而是按当前已输入内容做前缀匹配。比如输入 <code>git</code> 再按 <code>↑</code>，只在历史里找 <code>git</code> 开头的命令。</p><p><strong>zsh-autosuggestions</strong></p><p>根据历史记录实时预测并在光标后显示灰色补全建议。按 <code>→</code> 或 <code>End</code> 接受整条建议，按 <code>Ctrl+→</code> 接受一个单词。</p><p><strong>fzf-tab</strong></p><p>需要先安装 fzf（第二篇会讲）。装好后按 Tab 补全不再弹出静态候选列表，而是进入 fzf 交互界面，可以用模糊搜索过滤候选项，用 <code>↑↓</code> 选择后按回车确认。</p><p><strong>steeef 主题</strong></p><p>Prompt 显示格式：<code>用户名@主机 路径 [git分支 ±变动]</code>。有未提交的改动时 git 分支旁会显示标记。</p><hr><h2 id="验证配置是否生效"><a href="#验证配置是否生效" class="headerlink" title="验证配置是否生效"></a>验证配置是否生效</h2><p>配好后重启 kitty 终端，逐项检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 确认 Zim 正确加载，会输出已安装模块列表</span></span><br><span class="line">zimfw info</span><br></pre></td></tr></table></figure><p><strong>语法高亮</strong>：在终端里输入 <code>ls</code>，字体应该变绿；输入一个不存在的命令（比如 <code>foobarbaz</code>），应该变红。如果颜色没变，说明 fast-syntax-highlighting 没装上，重新跑 <code>zimfw install</code>。</p><p><strong>历史子串搜索</strong>：输入 <code>git</code>，然后按 <code>↑</code>，应该只显示历史里 <code>git</code> 开头的命令，而不是顺序往上翻。</p><p><strong>自动建议</strong>：输入任意一个你用过的命令的前几个字符，光标后应该出现灰色建议。按 <code>→</code> 接受。</p><p><strong>fzf-tab</strong>：随便输一个目录路径然后按 Tab，应该弹出 fzf 交互界面而不是静态列表。如果没变，大概率是 fzf 还没装——这个在第二篇安装完 fzf 后自动生效，不用管。</p><hr><h2 id="装好之后，立刻能感受到差异的一件事"><a href="#装好之后，立刻能感受到差异的一件事" class="headerlink" title="装好之后，立刻能感受到差异的一件事"></a>装好之后，立刻能感受到差异的一件事</h2><p>配完 Zim 之后，找一条你经常用的长命令（比如某个带一堆参数的 git 命令），输前几个字符，按 <code>↑</code>。原来要翻几十条历史才能找到的命令，现在两三下就出来了。</p><p>装完 fzf 后 fzf-tab 也会同时生效，那是第二篇的内容。</p>]]>
    </content>
    <id>https://blog.codewater.top/Mac%E7%BB%88%E7%AB%AF%E5%9F%BA%E7%A1%80-kitty%E5%92%8CZim/</id>
    <link href="https://blog.codewater.top/Mac%E7%BB%88%E7%AB%AF%E5%9F%BA%E7%A1%80-kitty%E5%92%8CZim/"/>
    <published>2026-06-30T02:00:00.000Z</published>
    <summary>从零开始配置 Mac 终端环境：用 GPU 加速的 kitty 替换默认终端，用 Zim 框架升级 zsh，配齐语法高亮、自动补全、fzf 模糊补全等插件。</summary>
    <title>Mac 终端基础配置：kitty + Zim</title>
    <updated>2026-06-30T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="git" scheme="https://blog.codewater.top/tags/git/"/>
    <category term="版本管理" scheme="https://blog.codewater.top/tags/%E7%89%88%E6%9C%AC%E7%AE%A1%E7%90%86/"/>
    <content>
      <![CDATA[<p>tag 是给某个 commit 贴一个永久名字。最常见的用途是版本发布，打完一个版本就在这个 commit 上打个 <code>v1.2.0</code>，之后随时能找回来，也能在 GitHub 上直接生成 Release。</p><span id="more"></span><hr><h2 id="两种-tag：轻量标签-vs-附注标签"><a href="#两种-tag：轻量标签-vs-附注标签" class="headerlink" title="两种 tag：轻量标签 vs 附注标签"></a>两种 tag：轻量标签 vs 附注标签</h2><p>Git 有两种 tag，行为不同：</p><p><strong>轻量标签（lightweight）</strong>：只是一个 commit 的别名指针，本质上和分支名差不多，但不会移动。不存储额外信息。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git tag v1.0.0</span><br></pre></td></tr></table></figure><p><strong>附注标签（annotated）</strong>：独立的 Git 对象，包含打标签者的名字、邮件、时间、tag message，还能用 GPG 签名。<code>git show</code> 能看到这些元信息。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git tag -a v1.0.0 -m <span class="string">&quot;Release v1.0.0&quot;</span></span><br><span class="line"><span class="comment">#      -a：annotated，创建附注标签（区别于默认的轻量标签）</span></span><br><span class="line"><span class="comment">#      -m：message，标签的说明文字，不加 -m 会弹出编辑器让你填写</span></span><br></pre></td></tr></table></figure><p><strong>什么时候用哪个</strong>：</p><ul><li>正式版本发布 → 用附注标签（<code>-a</code>），信息完整，<code>git describe</code> 默认只识别附注标签</li><li>临时标记某个 commit（自用）→ 轻量标签够用</li></ul><hr><h2 id="打标签"><a href="#打标签" class="headerlink" title="打标签"></a>打标签</h2><h3 id="给当前-commit-打标签"><a href="#给当前-commit-打标签" class="headerlink" title="给当前 commit 打标签"></a>给当前 commit 打标签</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 轻量标签</span></span><br><span class="line">git tag v1.0.0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 附注标签（推荐用于版本发布）</span></span><br><span class="line">git tag -a v1.0.0 -m <span class="string">&quot;Release v1.0.0&quot;</span></span><br><span class="line"><span class="comment">#      -a：annotated，创建附注标签</span></span><br><span class="line"><span class="comment">#      -m：message，填标签说明，省略此参数会弹出 $EDITOR 让你手动输入</span></span><br></pre></td></tr></table></figure><h3 id="给历史-commit-补打标签"><a href="#给历史-commit-补打标签" class="headerlink" title="给历史 commit 补打标签"></a>给历史 commit 补打标签</h3><p>先找到目标 commit 的 hash：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline</span><br><span class="line"><span class="comment">#       --oneline：每个 commit 只显示一行（短 hash + 提交信息），方便找 hash</span></span><br><span class="line"><span class="comment"># a3f2c1d feat: add payment module</span></span><br><span class="line"><span class="comment"># 8b9e012 fix: resolve login timeout</span></span><br></pre></td></tr></table></figure><p>然后指定 hash 打标签：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">git tag -a v0.9.0 8b9e012 -m <span class="string">&quot;Release v0.9.0&quot;</span></span><br><span class="line"><span class="comment">#      -a：附注标签</span></span><br><span class="line"><span class="comment">#         v0.9.0：标签名</span></span><br><span class="line"><span class="comment">#                8b9e012：目标 commit 的 hash（不指定则默认当前 commit）</span></span><br><span class="line"><span class="comment">#                         -m：标签说明</span></span><br></pre></td></tr></table></figure><hr><h2 id="查看标签"><a href="#查看标签" class="headerlink" title="查看标签"></a>查看标签</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 列出所有标签（按字母序）</span></span><br><span class="line">git tag</span><br><span class="line"></span><br><span class="line"><span class="comment"># 通配符过滤，只看 v1.x.x 系列</span></span><br><span class="line">git tag -l <span class="string">&quot;v1.*&quot;</span></span><br><span class="line"><span class="comment">#      -l：list，配合通配符（glob）过滤标签名，* 匹配任意字符</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看标签详情（附注标签会显示 tagger、日期、message；轻量标签只显示 commit 信息）</span></span><br><span class="line">git show v1.0.0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看标签指向哪个 commit（输出完整 hash）</span></span><br><span class="line">git rev-parse v1.0.0</span><br></pre></td></tr></table></figure><hr><h2 id="推送到远端"><a href="#推送到远端" class="headerlink" title="推送到远端"></a>推送到远端</h2><p>本地打的标签不会自动同步到远端，需要显式推送。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 推送单个标签（和推送分支语法一样）</span></span><br><span class="line">git push origin v1.0.0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 推送所有本地标签，包括轻量标签</span></span><br><span class="line">git push origin --tags</span><br><span class="line"><span class="comment">#               --tags：把本地所有 tag 一次性推送到远端</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 只推送附注标签，不推轻量标签（更安全）</span></span><br><span class="line">git push origin --follow-tags</span><br><span class="line"><span class="comment">#               --follow-tags：只推送有对应 commit 的附注标签，轻量标签不推</span></span><br></pre></td></tr></table></figure><p>日常版本发布推荐用 <code>--follow-tags</code>，只把正式的附注标签推上去。</p><hr><h2 id="删除标签"><a href="#删除标签" class="headerlink" title="删除标签"></a>删除标签</h2><h3 id="删除本地标签"><a href="#删除本地标签" class="headerlink" title="删除本地标签"></a>删除本地标签</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git tag -d v1.0.0</span><br><span class="line"><span class="comment">#      -d：delete，删除本地标签（不影响远端）</span></span><br></pre></td></tr></table></figure><h3 id="删除远端标签"><a href="#删除远端标签" class="headerlink" title="删除远端标签"></a>删除远端标签</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 写法一（Git 1.7.0+）</span></span><br><span class="line">git push origin --delete v1.0.0</span><br><span class="line"><span class="comment">#               --delete：删除远端的指定引用（分支或标签都适用）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 写法二（推送一个空引用覆盖，效果等同于删除）</span></span><br><span class="line">git push origin :refs/tags/v1.0.0</span><br><span class="line"><span class="comment">#              :&lt;ref&gt;：冒号前为空表示&quot;推送空内容&quot;，即删除远端的这个引用</span></span><br></pre></td></tr></table></figure><p>两种写法效果一样，推荐第一种，语义更清晰。</p><hr><h2 id="检出标签：detached-HEAD-的坑"><a href="#检出标签：detached-HEAD-的坑" class="headerlink" title="检出标签：detached HEAD 的坑"></a>检出标签：detached HEAD 的坑</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git checkout v1.0.0</span><br></pre></td></tr></table></figure><p>执行后会进入 <strong>detached HEAD</strong> 状态，HEAD 直接指向一个 commit 而不是分支。这时候如果做了新的提交，切走以后提交会丢失（没有分支引用它）。</p><p>如果只是查看历史代码，detached HEAD 没问题。但如果要在某个版本基础上修复 bug，正确做法是从标签创建一个分支：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git checkout -b hotfix/v1.0.1 v1.0.0</span><br><span class="line"><span class="comment">#            -b：新建并立即切换到这个分支（branch）</span></span><br><span class="line"><span class="comment">#               hotfix/v1.0.1：新分支名</span></span><br><span class="line"><span class="comment">#                              v1.0.0：起点，基于这个标签指向的 commit 创建分支</span></span><br></pre></td></tr></table></figure><p>这样在 <code>v1.0.0</code> 的代码基础上，工作在 <code>hotfix/v1.0.1</code> 分支，提交不会丢。</p><hr><h2 id="常用命令速查"><a href="#常用命令速查" class="headerlink" title="常用命令速查"></a>常用命令速查</h2><table><thead><tr><th>命令</th><th>说明</th></tr></thead><tbody><tr><td><code>git tag &lt;name&gt;</code></td><td>创建轻量标签（当前 commit）</td></tr><tr><td><code>git tag -a &lt;name&gt; -m &quot;&lt;msg&gt;&quot;</code></td><td>创建附注标签</td></tr><tr><td><code>git tag -a &lt;name&gt; &lt;hash&gt; -m &quot;&lt;msg&gt;&quot;</code></td><td>给历史 commit 打附注标签</td></tr><tr><td><code>git tag</code></td><td>列出所有本地标签</td></tr><tr><td><code>git tag -l &quot;&lt;pattern&gt;&quot;</code></td><td>通配符过滤标签列表</td></tr><tr><td><code>git show &lt;name&gt;</code></td><td>查看标签详情</td></tr><tr><td><code>git rev-parse &lt;name&gt;</code></td><td>查看标签指向的 commit hash</td></tr><tr><td><code>git push origin &lt;name&gt;</code></td><td>推送单个标签</td></tr><tr><td><code>git push origin --follow-tags</code></td><td>推送所有附注标签</td></tr><tr><td><code>git push origin --tags</code></td><td>推送所有标签（含轻量标签）</td></tr><tr><td><code>git tag -d &lt;name&gt;</code></td><td>删除本地标签</td></tr><tr><td><code>git push origin --delete &lt;name&gt;</code></td><td>删除远端标签</td></tr><tr><td><code>git checkout -b &lt;branch&gt; &lt;name&gt;</code></td><td>从标签创建新分支</td></tr><tr><td><code>git describe --tags</code></td><td>显示距最近 tag 的距离</td></tr></tbody></table><hr><h2 id="版本发布的完整流程"><a href="#版本发布的完整流程" class="headerlink" title="版本发布的完整流程"></a>版本发布的完整流程</h2><p>以 <code>v1.2.0</code> 为例，典型流程：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 确保代码在主分支，工作区干净</span></span><br><span class="line">git checkout main</span><br><span class="line">git status</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 打附注标签</span></span><br><span class="line">git tag -a v1.2.0 -m <span class="string">&quot;Release v1.2.0</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">主要变更：</span></span><br><span class="line"><span class="string">- 新增 OAuth 登录支持</span></span><br><span class="line"><span class="string">- 修复文件上传超时问题&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 推送代码和标签</span></span><br><span class="line">git push origin main</span><br><span class="line">git push origin v1.2.0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 或者用 --follow-tags 合并成一步</span></span><br><span class="line">git push origin main --follow-tags</span><br></pre></td></tr></table></figure><p>推送后，GitHub 会在 Releases 页面列出这个标签，可以在页面上补充 Release Notes，也可以附加编译好的二进制文件。</p><h3 id="语义版本命名（semver）"><a href="#语义版本命名（semver）" class="headerlink" title="语义版本命名（semver）"></a>语义版本命名（semver）</h3><p>tag 名字没有强制要求，但遵循 <a href="https://semver.org/">semver</a> 是行业惯例：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">v</span>&lt;主版本&gt;.&lt;次版本&gt;.&lt;补丁版本&gt;</span><br><span class="line"><span class="attribute">v1</span>.<span class="number">2</span>.<span class="number">0</span>   # 正式版本</span><br><span class="line"><span class="attribute">v1</span>.<span class="number">2</span>.<span class="number">0</span>-beta.<span class="number">1</span>  # 预发布版本</span><br><span class="line"><span class="attribute">v1</span>.<span class="number">2</span>.<span class="number">0</span>-rc.<span class="number">1</span>    # Release Candidate</span><br></pre></td></tr></table></figure><ul><li><strong>主版本</strong>：不兼容的 API 变更</li><li><strong>次版本</strong>：向下兼容的新功能</li><li><strong>补丁版本</strong>：向下兼容的 bug 修复</li></ul><hr><h2 id="git-describe：距上一个-tag-有多远"><a href="#git-describe：距上一个-tag-有多远" class="headerlink" title="git describe：距上一个 tag 有多远"></a><code>git describe</code>：距上一个 tag 有多远</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git describe --tags</span><br><span class="line"><span class="comment">#            --tags：同时识别轻量标签，默认只识别附注标签</span></span><br><span class="line"><span class="comment"># v1.2.0-3-ga3f2c1d</span></span><br></pre></td></tr></table></figure><p>输出格式：<code>&lt;最近tag&gt;-&lt;距离commit数&gt;-g&lt;当前commit hash&gt;</code>。在 CI 脚本里用来自动生成构建版本号很方便。如果当前 commit 刚好就是 tag，直接输出 <code>v1.2.0</code>。</p><hr><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><p><strong>git tag 和分支有什么区别？</strong></p><p>分支是移动的指针，随着新 commit 向前推进。tag 是固定的指针，一旦打上就不会移动，始终指向那个 commit。它们都是对 commit 的引用，但语义不同：分支表示”正在进行的工作线”，tag 表示”这个时刻的快照”。</p><p><strong>为什么 <code>git push</code> 不会自动推送标签？</strong></p><p>Git 把标签视为本地元数据，和分支一样不自动同步，需要显式推送。设计上是为了让临时的本地标签不污染远端。推送单个用 <code>git push origin v1.0.0</code>，推送全部附注标签用 <code>git push origin --follow-tags</code>。</p><p><strong>如何修改已经推送的标签？</strong></p><p>标签不能直接编辑。标准做法是删除旧标签再重新打：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 本地删除并重新打</span></span><br><span class="line">git tag -d v1.0.0                        <span class="comment"># -d：删除本地标签</span></span><br><span class="line">git tag -a v1.0.0 -m <span class="string">&quot;新的 message&quot;</span>     <span class="comment"># 重新打附注标签</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 同步到远端</span></span><br><span class="line">git push origin --delete v1.0.0          <span class="comment"># 删除远端旧标签</span></span><br><span class="line">git push origin v1.0.0                   <span class="comment"># 推送新标签</span></span><br></pre></td></tr></table></figure><p>对于已经公开发布的版本号，强烈不建议这样操作，因为其他人可能已经基于这个 tag 做了构建或发布。</p><p><strong>轻量标签和附注标签哪个适合版本发布？</strong></p><p>附注标签（<code>git tag -a</code>）。它存储了完整的元数据（打标签者、时间、message），可以用 GPG 签名验证，<code>git describe</code> 默认只识别附注标签，<code>git show</code> 也能看到详细信息。轻量标签只适合本地临时标记，不适合正式版本。</p>]]>
    </content>
    <id>https://blog.codewater.top/git-tag%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4%E4%B8%8E%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF/</id>
    <link href="https://blog.codewater.top/git-tag%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4%E4%B8%8E%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF/"/>
    <published>2026-06-30T02:00:00.000Z</published>
    <summary>git tag 完整教程：附注标签 vs 轻量标签的区别、打标签推送远端、detached HEAD 的坑、版本发布完整流程，一篇备查够用。</summary>
    <title>git tag 常用命令与使用场景</title>
    <updated>2026-06-30T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="tools" scheme="https://blog.codewater.top/categories/tools/"/>
    <category term="Mac" scheme="https://blog.codewater.top/tags/Mac/"/>
    <category term="CLI" scheme="https://blog.codewater.top/tags/CLI/"/>
    <category term="Terminal" scheme="https://blog.codewater.top/tags/Terminal/"/>
    <category term="效率工具" scheme="https://blog.codewater.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <content>
      <![CDATA[<p>macOS 自带的终端工具集（<code>grep</code>、<code>find</code>、<code>ls</code>、<code>cat</code>……）能用，但有些地方用起来总觉得别扭——搜索慢、输出不直观、没有高亮、参数难记。过去几年，社区里涌现出一批用 Rust、Go 写的现代 CLI 工具，直接解决了这些痛点。</p><span id="more"></span><hr><h2 id="终端模拟器：kitty"><a href="#终端模拟器：kitty" class="headerlink" title="终端模拟器：kitty"></a>终端模拟器：<a href="https://sw.kovidgoyal.net/kitty/">kitty</a></h2><blockquote><p>详细安装与配置见：<a href="/Mac%E7%BB%88%E7%AB%AF%E5%9F%BA%E7%A1%80-kitty%E5%92%8CZim">Mac 终端基础配置：kitty + Zim</a></p></blockquote><p>所有工具的运行基础。kitty 是一个用 GPU 渲染的高性能终端模拟器，启动快、滚动流畅，原生支持图片内联显示（这也是 yazi 文件预览图片能用的前提）。内置分屏（不依赖 tmux），支持标签页，配置文件是单个 <code>kitty.conf</code>，改起来比大多数终端直观。对于在终端里花大量时间的人，换一个渲染性能好的终端模拟器是性价比很高的基础投资。</p><hr><h2 id="Shell-框架：Zim"><a href="#Shell-框架：Zim" class="headerlink" title="Shell 框架：Zim"></a>Shell 框架：<a href="https://zimfw.sh/">Zim</a></h2><blockquote><p>详细安装与配置见：<a href="/Mac%E7%BB%88%E7%AB%AF%E5%9F%BA%E7%A1%80-kitty%E5%92%8CZim">Mac 终端基础配置：kitty + Zim</a></p></blockquote><p>macOS 自带 zsh，但裸 zsh 体验很差。Zim 是一个轻量的 zsh 插件管理框架，比 Oh My Zsh 启动速度快很多，通过 <code>.zimrc</code> 声明需要的插件，装好后自动生效。</p><p>当前配置里启用的插件：</p><ul><li><strong>fast-syntax-highlighting</strong>：实时高亮正在输入的命令，命令合法显绿色，输错显红色，按回车前就能发现拼写错误</li><li><strong>zsh-autosuggestions</strong>：根据历史记录实时预测补全建议，显示为灰色文字，按 <code>→</code> 接受，减少大量重复输入</li><li><strong>zsh-history-substring-search</strong>：按 <code>↑↓</code> 方向键时，按当前已输入内容做前缀匹配过滤历史，而不是顺序翻</li><li><strong>fzf-tab</strong>：把 Tab 补全的候选列表接管给 fzf，变成可交互过滤的模糊搜索界面</li><li><strong>steeef</strong>：Prompt 主题，展示用户名、当前路径、Git 分支和状态</li></ul><hr><h2 id="Shell-增强工具"><a href="#Shell-增强工具" class="headerlink" title="Shell 增强工具"></a>Shell 增强工具</h2><blockquote><p>详细安装与配置见：<a href="/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd">终端搜索三件套：fzf + ripgrep + fd + jq</a></p></blockquote><h3 id="fzf"><a href="#fzf" class="headerlink" title="fzf"></a>fzf</h3><p>模糊搜索框架，单独使用时是一个交互式过滤器，真正的价值在于<strong>和其他命令组合</strong>。集成进 Zsh 后会接管三个快捷键：<code>Ctrl+T</code> 模糊搜索文件、<code>Alt+C</code> 模糊跳转目录、<code>Ctrl+R</code> 交互式搜索命令历史。配合上面的 fzf-tab，Tab 补全也变成模糊搜索界面。fzf 是整个工具链里”乘数效应”最强的一个——它本身不做什么，但让其他工具都变得更好用。</p><h3 id="atuin"><a href="#atuin" class="headerlink" title="atuin"></a>atuin</h3><blockquote><p>详细安装与配置见：<a href="/atuin-zoxide-%E7%BB%88%E7%AB%AF%E5%8E%86%E5%8F%B2%E4%B8%8E%E7%9B%AE%E5%BD%95%E8%B7%B3%E8%BD%AC">Shell 历史与目录跳转的现代替代：atuin + zoxide</a></p></blockquote><p>shell 历史记录的替代方案，把命令历史存进本地 SQLite 数据库，支持按时间、工作目录、退出码等维度检索。最直接的改变是 <code>Ctrl+R</code> 变成了带预览的全文搜索界面。还支持多设备历史同步，在不同 Mac 之间共享命令历史。</p><h3 id="zoxide"><a href="#zoxide" class="headerlink" title="zoxide"></a>zoxide</h3><p><code>cd</code> 的智能替代品。记住访问过的目录，之后输入 <code>z blog</code> 就能直接跳到 <code>/Users/you/code/github/blog-source</code>，不需要输完整路径。项目目录多、路径深的开发者体验提升非常明显，是少数几个”装了就再也不想卸”的工具之一。</p><hr><h2 id="终端个性化"><a href="#终端个性化" class="headerlink" title="终端个性化"></a>终端个性化</h2><blockquote><p>详细安装与配置见：<a href="/fastfetch-pokemon-colorscripts-%E7%BB%88%E7%AB%AF%E5%A4%96%E8%A7%82">终端启动界面：fastfetch + pokemon-colorscripts</a></p></blockquote><h3 id="fastfetch-pokemon-colorscripts"><a href="#fastfetch-pokemon-colorscripts" class="headerlink" title="fastfetch + pokemon-colorscripts"></a>fastfetch + pokemon-colorscripts</h3><p>这两个组合使用：每次打开新终端，先随机展示一只宝可梦的彩色 ASCII 图，fastfetch 把系统信息（OS、CPU、内存、Shell、终端等）渲染在旁边。纯个性化，无实际功能，但比默认的空终端有意思很多。fastfetch 用 C 写成，启动速度远快于老牌的 neofetch，不会拖慢终端启动。</p><hr><h2 id="文件与搜索"><a href="#文件与搜索" class="headerlink" title="文件与搜索"></a>文件与搜索</h2><blockquote><p>ripgrep、fd、jq 的详细用法见：<a href="/%E7%BB%88%E7%AB%AF%E6%90%9C%E7%B4%A2%E4%B8%89%E4%BB%B6%E5%A5%97-fzf-ripgrep-fd">终端搜索三件套：fzf + ripgrep + fd + jq</a></p></blockquote><h3 id="ripgrep（rg）"><a href="#ripgrep（rg）" class="headerlink" title="ripgrep（rg）"></a>ripgrep（rg）</h3><p>比 <code>grep</code> 快一个数量级的全文搜索工具，默认递归搜索当前目录，自动忽略 <code>.gitignore</code> 里的文件。在大型代码仓库里搜索关键词，<code>rg</code> 的速度优势非常明显，输出带文件名、行号和彩色高亮。VSCode 的全局搜索底层用的也是 ripgrep。</p><h3 id="fd"><a href="#fd" class="headerlink" title="fd"></a>fd</h3><p><code>find</code> 的现代替代品。默认大小写不敏感、忽略 <code>.gitignore</code> 内容、输出带颜色，语法比 <code>find</code> 直观很多——找 Go 文件直接 <code>fd -e go</code>，而不是 <code>find . -name &quot;*.go&quot; -type f</code>。和 fzf 结合使用效果更好。</p><h3 id="bat"><a href="#bat" class="headerlink" title="bat"></a>bat</h3><blockquote><p>详细安装与配置见：<a href="/%E6%96%87%E4%BB%B6%E6%9F%A5%E7%9C%8B%E5%B7%A5%E5%85%B7-bat-eza-glow-mdcat">文件查看升级：bat + eza + glow + mdcat</a></p></blockquote><p><code>cat</code> 的替代品，带语法高亮、行号，还有 Git 集成（在行号旁标注哪些行被修改过）。它也被很多其他工具用作底层渲染器——fzf 的文件预览、yazi 的代码预览默认都会调用 bat。</p><h3 id="eza"><a href="#eza" class="headerlink" title="eza"></a>eza</h3><p><code>ls</code> 的现代替代品。默认输出带颜色，<code>-l</code> 模式下可以显示 Git 状态，<code>--tree</code> 模式展示目录树——这个功能直接取代了单独装 <code>tree</code> 命令的需求。配合 Nerd Font 字体还能显示文件类型图标，比原生 <code>ls -la</code> 的信息密度高很多。</p><h3 id="yazi"><a href="#yazi" class="headerlink" title="yazi"></a>yazi</h3><blockquote><p>详细安装与配置见：<a href="/yazi%E7%BB%88%E7%AB%AF%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E5%99%A8">yazi：终端里的三栏文件管理器</a></p></blockquote><p>终端里的文件管理器，基于 Rust 构建，速度很快。方向键导航目录、实时预览文件内容（代码、图片、PDF），批量操作文件。在 kitty 终端下能直接内联显示图片预览。对于不想离开终端但又需要浏览文件的场景，yazi 比反复 <code>cd</code> + <code>ls</code> 高效很多。<code>.zshrc</code> 里定义了 <code>y</code> 函数，退出 yazi 时会自动 <code>cd</code> 到最后所在目录。</p><h3 id="jq"><a href="#jq" class="headerlink" title="jq"></a>jq</h3><p>JSON 的命令行处理器，核心能力是<strong>对 JSON 进行查询、过滤、转换</strong>。调接口拿到 JSON 响应后，<code>curl ... | jq &#39;.data.items[] | .name&#39;</code> 这样的管道就能直接提取想要的字段。处理 API 响应、配置文件、日志分析时几乎不可缺少。</p><hr><h2 id="Git-工具"><a href="#Git-工具" class="headerlink" title="Git 工具"></a>Git 工具</h2><blockquote><p>详细安装与配置见：<a href="/git-delta-lazygit-gh-%E7%BB%88%E7%AB%AFGit%E5%B7%A5%E5%85%B7%E9%93%BE">终端里的 Git：git-delta + lazygit + gh</a></p></blockquote><h3 id="git-delta"><a href="#git-delta" class="headerlink" title="git-delta"></a>git-delta</h3><p>Git diff 的语法高亮渲染器。配置进 <code>~/.gitconfig</code> 后，<code>git diff</code>、<code>git log -p</code>、<code>git show</code> 的输出变成带语法高亮、行内差异标注的格式，比默认的加减号 diff 可读性高出不少。</p><h3 id="lazygit"><a href="#lazygit" class="headerlink" title="lazygit"></a>lazygit</h3><p>终端里的 Git TUI，用键盘操作就能完成暂存、提交、分支管理、rebase、stash 等常用 Git 操作，不用记忆大量命令参数。<code>.zshrc</code> 里设置了 <code>alias lg=lazygit</code>，快速调出。关于 lazygit 的常用工作流，后续会单独介绍。</p><h3 id="gh"><a href="#gh" class="headerlink" title="gh"></a>gh</h3><p>GitHub 官方 CLI。核心价值在于<strong>不离开终端就能操作 GitHub</strong>——创建 PR、查看 Issue、管理 Actions 工作流、查看 Review 评论。过去这些操作要切到浏览器，现在在命令行里完成，减少了大量上下文切换，在 CI&#x2F;CD 调试、PR Review 来回改代码这类场景下体感尤其明显。</p><hr><h2 id="Markdown-渲染：glow-mdcat"><a href="#Markdown-渲染：glow-mdcat" class="headerlink" title="Markdown 渲染：glow &#x2F; mdcat"></a>Markdown 渲染：glow &#x2F; mdcat</h2><blockquote><p>详细安装与配置见：<a href="/%E6%96%87%E4%BB%B6%E6%9F%A5%E7%9C%8B%E5%B7%A5%E5%85%B7-bat-eza-glow-mdcat">文件查看升级：bat + eza + glow + mdcat</a></p></blockquote><h3 id="glow"><a href="#glow" class="headerlink" title="glow"></a>glow</h3><p>终端里的 Markdown 渲染器。<code>glow README.md</code> 直接把 Markdown 渲染成格式化输出——标题、加粗、代码块都有对应的视觉呈现。看项目文档、博客草稿时比 <code>cat</code> 体验好很多。</p><h3 id="mdcat"><a href="#mdcat" class="headerlink" title="mdcat"></a>mdcat</h3><p>同样是 Markdown 渲染工具，更轻量，在 kitty 终端下支持内联显示图片。文档里有图的场景，mdcat 的图文混排体验比 glow 更完整。</p><hr><h2 id="编辑器：Neovim-LazyVim"><a href="#编辑器：Neovim-LazyVim" class="headerlink" title="编辑器：Neovim + LazyVim"></a>编辑器：Neovim + LazyVim</h2><blockquote><p>详细安装与配置见：<a href="/neovim-lazyvim-%E7%BB%88%E7%AB%AF%E7%BC%96%E8%BE%91%E5%99%A8">从零开始用 Neovim：LazyVim 让配置不再是障碍</a></p></blockquote><p>Neovim 是 Vim 的现代分支，支持 Lua 配置、LSP、Tree-sitter 语法高亮。但从零配置 Neovim 到可用状态需要花大量时间。</p><p><strong>LazyVim</strong> 解决了这个问题——它是一套预配置的 Neovim 发行版，开箱即得代码补全、LSP、文件树、模糊搜索、Git 集成等功能。底层插件管理器是 <strong>lazy.nvim</strong>，按需懒加载插件，启动速度快。</p><p>配套工具 <strong>im-select</strong> 负责输入法自动切换：从 Neovim 的 Insert 模式退出时，自动把输入法切换回英文，避免进入 Normal 模式后还在中文状态下输入命令的问题。</p><p>LazyVim 的上手曲线比从零配置 Neovim 低很多，但整体还是需要适应 Vim 的模式操作。关于 LazyVim 的配置和常用插件，后续会单独介绍。</p><hr><h2 id="从哪里开始"><a href="#从哪里开始" class="headerlink" title="从哪里开始"></a>从哪里开始</h2><p>按学习成本从低到高：</p><ul><li><strong>零成本，立竿见影</strong>：ripgrep、bat、eza、git-delta、zoxide、atuin——装上改个别名就能用</li><li><strong>需要适应键位</strong>：fzf、lazygit、yazi——花半小时熟悉一下，之后回报持续</li><li><strong>需要系统投入</strong>：LazyVim——上手慢，但配置完善后是全功能命令行 IDE</li></ul><p>后续会针对 fzf、lazygit、LazyVim 单独写深入使用的文章。</p>]]>
    </content>
    <id>https://blog.codewater.top/Mac%E7%BB%88%E7%AB%AFCLI%E5%B7%A5%E5%85%B7%E6%8E%A8%E8%8D%90/</id>
    <link href="https://blog.codewater.top/Mac%E7%BB%88%E7%AB%AFCLI%E5%B7%A5%E5%85%B7%E6%8E%A8%E8%8D%90/"/>
    <published>2026-06-28T02:00:00.000Z</published>
    <summary>系统梳理 Mac 终端完整工具链：从终端模拟器 kitty、Shell 框架 Zim，到 fzf、ripgrep、lazygit、LazyVim 等核心工具，聚焦每个工具的核心价值。</summary>
    <title>Mac 终端 CLI 工具推荐：那些让命令行效率翻倍的家伙</title>
    <updated>2026-06-28T02:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="golang" scheme="https://blog.codewater.top/categories/golang/"/>
    <category term="Google OAuth" scheme="https://blog.codewater.top/tags/Google-OAuth/"/>
    <category term="GoFrame" scheme="https://blog.codewater.top/tags/GoFrame/"/>
    <category term="JWT" scheme="https://blog.codewater.top/tags/JWT/"/>
    <category term="第三方登录" scheme="https://blog.codewater.top/tags/%E7%AC%AC%E4%B8%89%E6%96%B9%E7%99%BB%E5%BD%95/"/>
    <content>
      <![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>做海外项目时，Google 一键登录几乎是标配。相比传统的邮箱注册流程（填邮箱→收验证码→设密码），用户只需要点一下 Google 按钮就能完成注册&#x2F;登录，转化率能高不少。</p><p>本文以我自己的一个 Go 后端项目为例，分享 Google 一键登录的后端实现，技术栈是 <strong>GoFrame + JWT + MySQL</strong>。前端用的是 Google 官方的 Sign In With Google 按钮，后端只需要验证前端传过来的 ID Token 就行。</p><span id="more"></span><h1 id="整体流程"><a href="#整体流程" class="headerlink" title="整体流程"></a>整体流程</h1><p>Google 一键登录的核心流程其实很简单：</p><pre><code class="highlight mermaid">sequenceDiagram    participant U as 用户(浏览器)    participant G as Google    participant B as 后端    U-&gt;&gt;G: 1. 点击 Google 登录按钮，弹出授权窗口    G-&gt;&gt;G: 2. 用户在 Google 页面完成身份验证    G-&gt;&gt;U: 3. 返回 ID Token（JWT 格式）    U-&gt;&gt;B: 4. POST /user/google-login &#123;idToken&#125;    B-&gt;&gt;G: 5. 拉取 Google JWKS 公钥（有缓存）    G--&gt;&gt;B: 6. 返回公钥    B-&gt;&gt;B: 7. 用公钥本地验证 ID Token 签名和有效期    B-&gt;&gt;B: 8. 根据 googleID 查找/创建用户，签发 JWT    B-&gt;&gt;U: 9. 返回 JWT + 用户信息</code></pre><p>后端要做的事只有三件：</p><ol><li><strong>验证 ID Token</strong>——确认这个 token 确实是 Google 颁发的，且是给你的应用的</li><li><strong>查找或创建用户</strong>——第一次登录自动注册，后续登录直接查</li><li><strong>签发自己的 JWT</strong>——后续请求都走自己的鉴权体系</li></ol><h1 id="前置准备：Google-Cloud-Console-配置"><a href="#前置准备：Google-Cloud-Console-配置" class="headerlink" title="前置准备：Google Cloud Console 配置"></a>前置准备：Google Cloud Console 配置</h1><p><strong>环境依赖：</strong></p><ul><li>Go 1.24</li><li>GoFrame v2.10.0</li><li>google.golang.org&#x2F;api v0.259.0</li><li>github.com&#x2F;golang-jwt&#x2F;jwt&#x2F;v5 v5.3.0</li></ul><p>在写代码之前，你需要去 <a href="https://console.cloud.google.com/">Google Cloud Console</a> 创建一个 OAuth 2.0 客户端：</p><ol><li>进入 <strong>APIs &amp; Services → Credentials</strong></li><li>点 <strong>Create Credentials → OAuth client ID</strong></li><li>应用类型选 <strong>Web application</strong></li><li>配置 <strong>Authorized JavaScript origins</strong>（你的前端域名，比如 <code>https://www.example.com</code>）</li><li>创建后拿到 <strong>Client ID</strong> 和 <strong>Client Secret</strong></li></ol><p>后端验证 ID Token 只需要 <code>Client ID</code>，不需要 <code>Client Secret</code>。</p><p>配置文件里加上：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">google:</span></span><br><span class="line">  <span class="attr">clientId:</span> <span class="string">&quot;你的-client-id.apps.googleusercontent.com&quot;</span></span><br></pre></td></tr></table></figure><h1 id="后端实现"><a href="#后端实现" class="headerlink" title="后端实现"></a>后端实现</h1><h2 id="1-定义-API-接口"><a href="#1-定义-API-接口" class="headerlink" title="1. 定义 API 接口"></a>1. 定义 API 接口</h2><p>用 GoFrame 的结构化 API 定义：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> GoogleLoginReq <span class="keyword">struct</span> &#123;</span><br><span class="line">    g.Meta  <span class="string">`path:&quot;/user/google-login&quot; method:&quot;post&quot; tags:&quot;用户&quot; summary:&quot;Google一键登录&quot;`</span></span><br><span class="line">    IDToken <span class="type">string</span> <span class="string">`json:&quot;idToken&quot; v:&quot;required&quot; dc:&quot;Google ID Token&quot;`</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> GoogleLoginRes <span class="keyword">struct</span> &#123;</span><br><span class="line">    Token <span class="type">string</span>    <span class="string">`json:&quot;token&quot; dc:&quot;JWT Token&quot;`</span></span><br><span class="line">    User  *UserInfo <span class="string">`json:&quot;user&quot; dc:&quot;用户信息&quot;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>前端只需要传一个 <code>idToken</code> 字段，就是 Google Sign In 回调里拿到的 credential。</p><h2 id="2-核心逻辑：验证-Token-查建用户"><a href="#2-核心逻辑：验证-Token-查建用户" class="headerlink" title="2. 核心逻辑：验证 Token + 查建用户"></a>2. 核心逻辑：验证 Token + 查建用户</h2><p>这是整个 Google 登录的核心代码：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> user</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;context&quot;</span></span><br><span class="line"></span><br><span class="line">    v1 <span class="string">&quot;your-project/api/user/v1&quot;</span></span><br><span class="line">    <span class="string">&quot;your-project/internal/consts&quot;</span></span><br><span class="line">    <span class="string">&quot;your-project/internal/dao&quot;</span></span><br><span class="line">    <span class="string">&quot;your-project/internal/model/do&quot;</span></span><br><span class="line">    <span class="string">&quot;your-project/internal/model/entity&quot;</span></span><br><span class="line">    <span class="string">&quot;your-project/internal/service/jwt&quot;</span></span><br><span class="line">    <span class="string">&quot;your-project/internal/utility/snowflake&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="string">&quot;github.com/gogf/gf/v2/errors/gerror&quot;</span></span><br><span class="line">    <span class="string">&quot;github.com/gogf/gf/v2/frame/g&quot;</span></span><br><span class="line">    <span class="string">&quot;google.golang.org/api/idtoken&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GoogleLogin</span><span class="params">(ctx context.Context, req *v1.GoogleLoginReq)</span></span> (*v1.GoogleLoginRes, <span class="type">error</span>) &#123;</span><br><span class="line">    <span class="comment">// 1. 验证 Google ID Token</span></span><br><span class="line">    clientID := g.Cfg().MustGet(ctx, <span class="string">&quot;google.clientId&quot;</span>).String()</span><br><span class="line">    payload, err := idtoken.Validate(ctx, req.IDToken, clientID)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        g.Log().Error(ctx, <span class="string">&quot;Google登录，Token验证失败&quot;</span>, err)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>, gerror.New(<span class="string">&quot;Google登录验证失败&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 从 payload 中提取用户信息</span></span><br><span class="line">    googleID := payload.Subject</span><br><span class="line">    email := payload.Claims[<span class="string">&quot;email&quot;</span>].(<span class="type">string</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 查找已有用户</span></span><br><span class="line">    <span class="keyword">var</span> user *entity.Users</span><br><span class="line">    err = dao.Users.Ctx(ctx).Where(<span class="string">&quot;google_id&quot;</span>, googleID).Scan(&amp;user)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        g.Log().Error(ctx, <span class="string">&quot;Google登录，查询用户信息失败&quot;</span>, err)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>, gerror.New(<span class="string">&quot;系统错误&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 用户不存在则自动注册</span></span><br><span class="line">    <span class="keyword">if</span> user == <span class="literal">nil</span> &#123;</span><br><span class="line">        uid := snowflake.GenerateID()</span><br><span class="line">        _, err = dao.Users.Ctx(ctx).Insert(do.Users&#123;</span><br><span class="line">            Uid:              uid,</span><br><span class="line">            Email:            email,</span><br><span class="line">            GoogleId:         googleID,</span><br><span class="line">            RegisterType:     consts.RegisterTypeGoogle,</span><br><span class="line">            MembershipStatus: consts.MembershipStatusNonMember,</span><br><span class="line">            Status:           consts.UserStatusNormal,</span><br><span class="line">        &#125;)</span><br><span class="line">        <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">            g.Log().Error(ctx, <span class="string">&quot;Google登录，创建用户失败&quot;</span>, err)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">nil</span>, gerror.New(<span class="string">&quot;系统错误&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        err = dao.Users.Ctx(ctx).Where(<span class="string">&quot;uid&quot;</span>, uid).Scan(&amp;user)</span><br><span class="line">        <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">nil</span>, gerror.New(<span class="string">&quot;系统错误&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 5. 检查账号状态</span></span><br><span class="line">    <span class="keyword">if</span> user.Status != consts.UserStatusNormal &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>, gerror.New(<span class="string">&quot;账号已被禁用&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 6. 签发自己的 JWT</span></span><br><span class="line">    token, err := jwt.GenerateToken(user.Uid, user.Email)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span>, gerror.New(<span class="string">&quot;系统错误&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &amp;v1.GoogleLoginRes&#123;</span><br><span class="line">        Token: token,</span><br><span class="line">        User: &amp;v1.UserInfo&#123;</span><br><span class="line">            UID:   user.Uid,</span><br><span class="line">            Email: user.Email,</span><br><span class="line">            <span class="comment">// ... 其他字段</span></span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>逐步拆解：</p><h3 id="验证-ID-Token"><a href="#验证-ID-Token" class="headerlink" title="验证 ID Token"></a>验证 ID Token</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">payload, err := idtoken.Validate(ctx, req.IDToken, clientID)</span><br></pre></td></tr></table></figure><p><code>google.golang.org/api/idtoken</code> 包帮你做了所有脏活：</p><ul><li>从 Google 的 JWKS 端点拉取公钥</li><li>验证 token 签名</li><li>检查 <code>aud</code>（audience）是否匹配你的 Client ID</li><li>检查 token 是否过期</li></ul><p>验证通过后，<code>payload.Subject</code> 就是用户的 Google ID（全局唯一且不变），<code>payload.Claims</code> 里有 email、name、picture 等信息。</p><h3 id="查找或创建用户"><a href="#查找或创建用户" class="headerlink" title="查找或创建用户"></a>查找或创建用户</h3><p>用 <code>google_id</code> 作为关联字段查用户表。如果查不到说明是新用户，自动创建一条记录。这里用了 Snowflake 算法生成分布式唯一 ID 作为业务主键。</p><p><strong>为什么用 <code>google_id</code> 而不是 <code>email</code> 来关联？</strong> 因为用户可能会改 Google 账号的邮箱，但 <code>Subject</code>（Google ID）是永远不变的。</p><h3 id="签发-JWT"><a href="#签发-JWT" class="headerlink" title="签发 JWT"></a>签发 JWT</h3><p>验证完 Google 身份后，后续的鉴权还是走自己的 JWT 体系。这样做的好处是：</p><ul><li>不依赖 Google 的 token 有效期</li><li>可以统一处理邮箱注册和 Google 注册的用户</li><li>中间件只需要解析自己的 JWT，不用区分登录方式</li></ul><h2 id="3-JWT-签发与验证"><a href="#3-JWT-签发与验证" class="headerlink" title="3. JWT 签发与验证"></a>3. JWT 签发与验证</h2><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GenerateToken</span><span class="params">(uid, email <span class="type">string</span>)</span></span> (<span class="type">string</span>, <span class="type">error</span>) &#123;</span><br><span class="line">    ctx := context.Background()</span><br><span class="line">    secret := g.Cfg().MustGet(ctx, <span class="string">&quot;jwt.secret&quot;</span>).String()</span><br><span class="line">    expire := g.Cfg().MustGet(ctx, <span class="string">&quot;jwt.expire&quot;</span>, <span class="number">7200</span>).Int()</span><br><span class="line"></span><br><span class="line">    claims := Claims&#123;</span><br><span class="line">        UID:   uid,</span><br><span class="line">        Email: email,</span><br><span class="line">        RegisteredClaims: jwt.RegisteredClaims&#123;</span><br><span class="line">            ID:        uuid.NewString(),</span><br><span class="line">            ExpiresAt: jwt.NewNumericDate(time.Now().Add(time.Duration(expire) * time.Second)),</span><br><span class="line">            IssuedAt:  jwt.NewNumericDate(time.Now()),</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    token := jwt.NewWithClaims(jwt.SigningMethodHS256, claims)</span><br><span class="line">    <span class="keyword">return</span> token.SignedString([]<span class="type">byte</span>(secret))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>每个 JWT 都有一个 UUID 作为 <code>jti</code>（JWT ID），配合 Redis 黑名单可以实现主动吊销 token（比如用户退出登录时）。</p><h2 id="4-路由注册"><a href="#4-路由注册" class="headerlink" title="4. 路由注册"></a>4. 路由注册</h2><p>Google 登录接口放在无需鉴权的公开路由组里：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">group.POST(<span class="string">&quot;/user/google-login&quot;</span>, user.NewV1().GoogleLogin)</span><br></pre></td></tr></table></figure><p>跟普通的邮箱登录接口并列，登录成功后返回的数据格式完全一致——前端拿到 token 存起来，后续请求带上就行。</p><h1 id="数据库设计要点"><a href="#数据库设计要点" class="headerlink" title="数据库设计要点"></a>数据库设计要点</h1><p>users 表需要加几个字段：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> users <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> google_id <span class="type">VARCHAR</span>(<span class="number">128</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;Google用户ID&#x27;</span>;</span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> users <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> register_type TINYINT UNSIGNED <span class="keyword">DEFAULT</span> <span class="number">1</span> COMMENT <span class="string">&#x27;注册类型：1-邮箱 2-Google&#x27;</span>;</span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> users <span class="keyword">ADD</span> <span class="keyword">UNIQUE</span> INDEX idx_google_id (google_id);</span><br></pre></td></tr></table></figure><ul><li><code>google_id</code> 加唯一索引，防止重复绑定</li><li><code>register_type</code> 区分注册来源，后续做数据分析有用</li><li><code>email</code> 字段对 Google 用户也保留，方便发通知邮件</li></ul><h1 id="安全注意事项"><a href="#安全注意事项" class="headerlink" title="安全注意事项"></a>安全注意事项</h1><ol><li><strong>永远在后端验证 ID Token</strong>——前端传过来的 token 不可信，必须后端调 Google API 验证</li><li><strong>校验 audience</strong>——<code>idtoken.Validate</code> 的第三个参数就是你的 Client ID，防止其他应用的 token 被拿来登录你的系统</li><li><strong>不要把 Client Secret 暴露到前端</strong>——ID Token 验证方案不需要 Client Secret，但如果你用 Authorization Code 方案就需要，记得只放后端</li><li><strong>google_id 用唯一索引</strong>——防止并发场景下创建重复用户</li></ol><h1 id="前端怎么接？"><a href="#前端怎么接？" class="headerlink" title="前端怎么接？"></a>前端怎么接？</h1><p>前端用的是 Google Identity Services（GIS），这是 Google 2021 年推出的新版登录 SDK，取代了老版的 <code>google-signin</code> 库。整个接入只需三步。</p><h2 id="第一步：引入-GIS-脚本"><a href="#第一步：引入-GIS-脚本" class="headerlink" title="第一步：引入 GIS 脚本"></a>第一步：引入 GIS 脚本</h2><p>在 HTML 的 <code>&lt;head&gt;</code> 里加上：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;https://accounts.google.com/gsi/client&quot;</span> <span class="attr">async</span> <span class="attr">defer</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p><code>async defer</code> 让脚本异步加载，不阻塞页面渲染。</p><h2 id="第二步：放置登录按钮"><a href="#第二步：放置登录按钮" class="headerlink" title="第二步：放置登录按钮"></a>第二步：放置登录按钮</h2><p>有两种方式，按需选一种：</p><h3 id="方式一：纯-HTML（最简单）"><a href="#方式一：纯-HTML（最简单）" class="headerlink" title="方式一：纯 HTML（最简单）"></a>方式一：纯 HTML（最简单）</h3><p>直接在页面放两个 div，GIS 脚本加载后会自动处理：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 配置项：Client ID 和回调函数名 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;g_id_onload&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">data-client_id</span>=<span class="string">&quot;你的-client-id.apps.googleusercontent.com&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">data-callback</span>=<span class="string">&quot;handleCredentialResponse&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 渲染 Google 样式的登录按钮 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;g_id_signin&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">data-type</span>=<span class="string">&quot;standard&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">data-size</span>=<span class="string">&quot;large&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">data-theme</span>=<span class="string">&quot;outline&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">data-text</span>=<span class="string">&quot;sign_in_with&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">data-shape</span>=<span class="string">&quot;rectangular&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><ul><li><code>data-client_id</code>：换成你在 Google Cloud Console 拿到的 Client ID</li><li><code>data-callback</code>：登录成功后调用的 JavaScript 函数名</li><li><code>g_id_signin</code> 的 <code>data-theme</code>、<code>data-size</code> 等属性控制按钮外观，<a href="https://developers.google.com/identity/gsi/web/reference/html-reference">Google 官方文档</a>有完整参数列表</li></ul><h3 id="方式二：JavaScript-API（更灵活）"><a href="#方式二：JavaScript-API（更灵活）" class="headerlink" title="方式二：JavaScript API（更灵活）"></a>方式二：JavaScript API（更灵活）</h3><p>需要在特定时机触发登录、或者想启用 One Tap 弹窗时，用 JS 控制更方便：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="property">onload</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  google.<span class="property">accounts</span>.<span class="property">id</span>.<span class="title function_">initialize</span>(&#123;</span><br><span class="line">    <span class="attr">client_id</span>: <span class="string">&quot;你的-client-id.apps.googleusercontent.com&quot;</span>,</span><br><span class="line">    <span class="attr">callback</span>: handleCredentialResponse,</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 把按钮渲染到指定容器</span></span><br><span class="line">  google.<span class="property">accounts</span>.<span class="property">id</span>.<span class="title function_">renderButton</span>(</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;googleSignInBtn&quot;</span>),</span><br><span class="line">    &#123; <span class="attr">theme</span>: <span class="string">&quot;outline&quot;</span>, <span class="attr">size</span>: <span class="string">&quot;large&quot;</span> &#125;</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 同时弹出 One Tap 提示（可选）</span></span><br><span class="line">  <span class="comment">// google.accounts.id.prompt();</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;googleSignInBtn&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="第三步：处理回调、调后端接口"><a href="#第三步：处理回调、调后端接口" class="headerlink" title="第三步：处理回调、调后端接口"></a>第三步：处理回调、调后端接口</h2><p>登录成功后，Google 会调用你配置的回调函数，参数里的 <code>credential</code> 字段就是 ID Token——一段很长的 JWT 字符串，直接传给后端就行：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">handleCredentialResponse</span>(<span class="params">response</span>) &#123;</span><br><span class="line">  <span class="comment">// response.credential 就是 Google 颁发的 ID Token</span></span><br><span class="line">  <span class="title function_">fetch</span>(<span class="string">&quot;/api/user/google-login&quot;</span>, &#123;</span><br><span class="line">    <span class="attr">method</span>: <span class="string">&quot;POST&quot;</span>,</span><br><span class="line">    <span class="attr">headers</span>: &#123; <span class="string">&quot;Content-Type&quot;</span>: <span class="string">&quot;application/json&quot;</span> &#125;,</span><br><span class="line">    <span class="attr">body</span>: <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(&#123; <span class="attr">idToken</span>: response.<span class="property">credential</span> &#125;),</span><br><span class="line">  &#125;)</span><br><span class="line">    .<span class="title function_">then</span>(<span class="function">(<span class="params">res</span>) =&gt;</span> res.<span class="title function_">json</span>())</span><br><span class="line">    .<span class="title function_">then</span>(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (data.<span class="property">code</span> === <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 登录成功，存自己系统的 JWT</span></span><br><span class="line">        <span class="variable language_">localStorage</span>.<span class="title function_">setItem</span>(<span class="string">&quot;token&quot;</span>, data.<span class="property">data</span>.<span class="property">token</span>);</span><br><span class="line">        <span class="variable language_">window</span>.<span class="property">location</span>.<span class="property">href</span> = <span class="string">&quot;/dashboard&quot;</span>;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">&quot;登录失败&quot;</span>, data.<span class="property">message</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .<span class="title function_">catch</span>(<span class="function">(<span class="params">err</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">&quot;请求出错&quot;</span>, err);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="完整示例"><a href="#完整示例" class="headerlink" title="完整示例"></a>完整示例</h2><p>把上面三步拼在一起，一个能直接跑的最简登录页面：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;zh&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;UTF-8&quot;</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">title</span>&gt;</span>Google 登录示例<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;https://accounts.google.com/gsi/client&quot;</span> <span class="attr">async</span> <span class="attr">defer</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">h1</span>&gt;</span>欢迎<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;g_id_onload&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">data-client_id</span>=<span class="string">&quot;你的-client-id.apps.googleusercontent.com&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">data-callback</span>=<span class="string">&quot;handleCredentialResponse&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;g_id_signin&quot;</span> <span class="attr">data-type</span>=<span class="string">&quot;standard&quot;</span> <span class="attr">data-size</span>=<span class="string">&quot;large&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">    <span class="keyword">function</span> <span class="title function_">handleCredentialResponse</span>(<span class="params">response</span>) &#123;</span></span><br><span class="line"><span class="language-javascript">      <span class="title function_">fetch</span>(<span class="string">&quot;/api/user/google-login&quot;</span>, &#123;</span></span><br><span class="line"><span class="language-javascript">        <span class="attr">method</span>: <span class="string">&quot;POST&quot;</span>,</span></span><br><span class="line"><span class="language-javascript">        <span class="attr">headers</span>: &#123; <span class="string">&quot;Content-Type&quot;</span>: <span class="string">&quot;application/json&quot;</span> &#125;,</span></span><br><span class="line"><span class="language-javascript">        <span class="attr">body</span>: <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(&#123; <span class="attr">idToken</span>: response.<span class="property">credential</span> &#125;),</span></span><br><span class="line"><span class="language-javascript">      &#125;)</span></span><br><span class="line"><span class="language-javascript">        .<span class="title function_">then</span>(<span class="function">(<span class="params">res</span>) =&gt;</span> res.<span class="title function_">json</span>())</span></span><br><span class="line"><span class="language-javascript">        .<span class="title function_">then</span>(<span class="function">(<span class="params">data</span>) =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">          <span class="keyword">if</span> (data.<span class="property">code</span> === <span class="number">0</span>) &#123;</span></span><br><span class="line"><span class="language-javascript">            <span class="variable language_">localStorage</span>.<span class="title function_">setItem</span>(<span class="string">&quot;token&quot;</span>, data.<span class="property">data</span>.<span class="property">token</span>);</span></span><br><span class="line"><span class="language-javascript">            <span class="variable language_">window</span>.<span class="property">location</span>.<span class="property">href</span> = <span class="string">&quot;/dashboard&quot;</span>;</span></span><br><span class="line"><span class="language-javascript">          &#125;</span></span><br><span class="line"><span class="language-javascript">        &#125;);</span></span><br><span class="line"><span class="language-javascript">    &#125;</span></span><br><span class="line"><span class="language-javascript">  </span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="几个容易踩的坑"><a href="#几个容易踩的坑" class="headerlink" title="几个容易踩的坑"></a>几个容易踩的坑</h2><p><strong>本地开发必须用 <code>http://localhost</code></strong></p><p>在 Google Cloud Console 配置 Authorized JavaScript origins 时，本地开发要加 <code>http://localhost</code>（不是 <code>127.0.0.1</code>，也不是 <code>http://localhost:3000</code> 这样带端口的）。否则会报 <code>idpiframe_initialization_failed</code> 错误。</p><p><strong>不要在 <code>file://</code> 协议下测试</strong></p><p>GIS 不支持 <code>file://</code>，必须通过 HTTP&#x2F;HTTPS 服务器访问。本地可以用 <code>python3 -m http.server 8000</code> 起一个简单服务器。</p><p><strong>跨域问题</strong></p><p>前后端分域部署时（比如前端 <code>app.example.com</code>，后端 <code>api.example.com</code>），后端 <code>/user/google-login</code> 接口要配置 CORS，允许前端域名，否则 fetch 会被浏览器拦截。</p><p><strong><code>response.credential</code> 是一次性的，不要在前端 decode</strong></p><p>ID Token 有效期很短（约 1 小时），而且里面的内容只有后端拿公钥验过签之后才可信。不要在前端用 <code>atob</code> 或 jwt-decode 库解析它来做业务判断，直接传给后端就行。</p><h1 id="ID-Token-方案-vs-Authorization-Code-方案"><a href="#ID-Token-方案-vs-Authorization-Code-方案" class="headerlink" title="ID Token 方案 vs Authorization Code 方案"></a>ID Token 方案 vs Authorization Code 方案</h1><p>Google 一键登录的后端实现并不复杂，核心就是 <code>idtoken.Validate</code> 这一步。整个流程：</p><ol><li>前端拿到 Google 的 ID Token</li><li>后端用 <code>google.golang.org/api/idtoken</code> 包验证</li><li>用 <code>payload.Subject</code> 关联&#x2F;创建用户</li><li>签发自己的 JWT 返回</li></ol><p>相比 OAuth2 Authorization Code 方案，ID Token 方案更简单——不需要后端跟 Google 换 access token，一个 HTTP 请求都不用发（<code>idtoken</code> 包会缓存 Google 的公钥）。适合”只需要登录，不需要调 Google API”的场景。</p>]]>
    </content>
    <id>https://blog.codewater.top/Go%E5%90%8E%E7%AB%AF%E5%AE%9E%E7%8E%B0Google%E4%B8%80%E9%94%AE%E7%99%BB%E5%BD%95/</id>
    <link href="https://blog.codewater.top/Go%E5%90%8E%E7%AB%AF%E5%AE%9E%E7%8E%B0Google%E4%B8%80%E9%94%AE%E7%99%BB%E5%BD%95/"/>
    <published>2025-06-27T08:30:00.000Z</published>
    <summary>用 GoFrame 实现 Google 一键登录的后端方案——ID Token 验证、自动注册用户、签发 JWT，附完整前端接入代码和踩坑指南。</summary>
    <title>Go 后端实现 Google 一键登录（ID Token 验证方案）</title>
    <updated>2026-07-07T07:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="desktop" scheme="https://blog.codewater.top/tags/desktop/"/>
    <content>
      <![CDATA[<p>一直以来用的大学时代的笔记本，近来发现越来越卡，而且已经跟不上生产力的需求了，<br>所以想着自己动手配置一台台式机。</p><p>笔者本人专业虽然是计算机，但是不是数码爱好者，所以对于配置台式不甚了解吧，特此<br>做个记录。</p><span id="more"></span><h1 id="台式机部件组成"><a href="#台式机部件组成" class="headerlink" title="台式机部件组成"></a>台式机部件组成</h1><p>记录选择配置台式机，那么很重要的就是看组成有哪些，知道组成之后，剩下的就是<br>看组成的参数、品牌了。</p><ol><li>CPU</li><li>主板</li><li>显卡</li><li>内存</li><li>电源</li><li>机箱</li><li>散热器</li><li>固态硬盘</li></ol><p>必要硬件就是以上8个，影响性能的主要是cpu、显卡、内存，其余的不拖后腿就行。<br>在了解组成之后，我们就需要了解硬件的参数该如何看、品牌如何选择。</p><h1 id="CPU"><a href="#CPU" class="headerlink" title="CPU"></a>CPU</h1><h2 id="型号区分"><a href="#型号区分" class="headerlink" title="型号区分"></a>型号区分</h2><p>intel处理器型号区分： </p><ol><li>没有后缀的表示支持核显</li><li>后缀F表示无核显</li><li>后缀K代表可以超频</li><li>后缀KS代表超频加强版</li></ol><p>以“ i7-13700K”为例，它由系列、代数、SKU、后缀组成；<br>“-”前字母数字表示系列，数字越大，级别越高， i3低端，i5中端 ，i7高端 ， i9旗舰版；<br>“-”后面两个数字为“代数”，数字越大，型号越新，“13”表示13代CPU，“12” 则是第12代；前面说的架构，也是随着代数不断更新的；<br>数字“700”表示SKU（产品编号）值，同一级别代数，较高的 SKU 一般表示具有更多功能，性能越强。</p><p>AMD处理器型号区分：<br>1.后缀G代表有核显<br>2.后缀X代表加强版<br>3.后缀XT代表超级加强版<br>4.后缀X3D代表游戏优化版</p><p>以“R7-5700X”为例，也是由系列，代数，SKU，后缀组成；<br>“-”前字母数字表示系列，数字越大，级别越高，R3低端入门，R5市场主流，R7高端，R9发烧旗舰，线程撕裂者表示顶级旗舰；<br>“-”后面两个数字为“代数”，数字越大，型号越新，“5”表示第五代，“3” 则是第三代；<br>数字“700”表示SKU值，数字越大，性能越强；<br>后缀“X”代表可以超频，“G”代表自带核显。（锐龙桌面CPU都可以超频）</p><blockquote><p>这两个也是cpu的两大品牌。</p></blockquote><h2 id="核显"><a href="#核显" class="headerlink" title="核显"></a>核显</h2><p>核显，即<strong>核心显卡</strong>，也称为<strong>集成显卡</strong>（Integrated Graphics）<a href="https://www.lenovo.com/us/en/glossary/integrated-vs-dedicated-graphics-cards/?srsltid=AfmBOoqb524Hp8EcEEh_X3BLjAOzSM_vOuqEKK0_J7VSyu0MbR6XmmaM">1</a>。它是一种集成在 CPU 内部或主板芯片组上的图形处理器 <a href="https://www.quora.com/What-is-the-definition-of-integrated-graphics-How-does-it-differ-from-other-types-of-graphics-cards">4</a>。与独立显卡不同，核显不拥有独立的显存，而是<strong>共享系统内存</strong>作为显存 <a href="https://www.pcmag.com/encyclopedia/term/integrated-graphics">6</a>。</p><p><strong>核心显卡的特点：</strong></p><ol><li><strong>集成性</strong>：<ul><li>集成在 CPU 内部或主板芯片组上，无需额外的显卡插槽。</li></ul></li><li><strong>共享内存</strong>：<ul><li>与系统共享内存，通常会占用一部分系统内存作为显存。</li></ul></li><li><strong>低功耗</strong>：<ul><li>相比独立显卡，功耗较低，发热量也较小。</li></ul></li><li><strong>性能有限</strong>：<ul><li>图形处理能力相对较弱，通常只适合日常办公、网页浏览、观看视频等轻量级应用。</li></ul></li></ol><p><strong>核心显卡的优缺点：</strong></p><ul><li><strong>优点</strong>：<ul><li><strong>成本低</strong>：无需额外购买独立显卡，降低了整机成本。</li><li><strong>功耗低</strong>：降低了功耗和发热量，延长了电池续航时间（对于笔记本电脑）。</li><li><strong>体积小</strong>：节省了空间，适合轻薄型设备。</li></ul></li><li><strong>缺点</strong>：<ul><li><strong>性能弱</strong>：无法流畅运行大型游戏和专业图形软件。</li><li><strong>占用系统内存</strong>：共享系统内存会影响整体性能。</li><li><strong>升级困难</strong>：由于集成在 CPU 或主板上，无法单独升级。</li></ul></li></ul><p><strong>核心显卡的应用场景：</strong></p><ul><li><strong>日常办公</strong>：处理文档、表格、邮件等办公任务。</li><li><strong>网页浏览</strong>：流畅浏览网页，观看在线视频。</li><li><strong>轻度娱乐</strong>：运行一些简单的休闲游戏。</li><li><strong>轻薄型笔记本电脑</strong>：追求便携性和长续航的笔记本电脑。</li></ul><p><strong>核心显卡与独立显卡的区别：</strong></p><table><thead><tr><th>特性</th><th>核心显卡 (Integrated Graphics)</th><th>独立显卡 (Dedicated Graphics)</th></tr></thead><tbody><tr><td>集成方式</td><td>集成在 CPU 或主板芯片组上</td><td>独立的显卡插槽</td></tr><tr><td>显存</td><td>共享系统内存</td><td>拥有独立的显存</td></tr><tr><td>功耗</td><td>低</td><td>高</td></tr><tr><td>性能</td><td>弱</td><td>强</td></tr><tr><td>成本</td><td>低</td><td>高</td></tr><tr><td>升级</td><td>无法单独升级</td><td>可以单独升级</td></tr></tbody></table><p><strong>总结</strong></p><p>核心显卡是一种集成在 CPU 内部或主板芯片组上的图形处理器，它具有成本低、功耗低、体积小等优点，但性能相对较弱。核心显卡适合日常办公、网页浏览、观看视频等轻量级应用。如果需要运行大型游戏或专业图形软件，建议选择独立显卡。</p><p>但是购买带有核显的 CPU 并不妨碍你安装独立显卡。安装独立显卡后，电脑会自动或手动切换到独立显卡作为主要的图形输出设备。核显可以作为备用显示输出或用于加速视频处理。</p><h2 id="散片、盒装"><a href="#散片、盒装" class="headerlink" title="散片、盒装"></a>散片、盒装</h2><p>CPU 的散片和盒装是指 CPU 的两种不同销售方式，它们在包装、保修和价格上有所区别。</p><p><strong>1. 盒装 CPU (Boxed CPU)</strong></p><ul><li><strong>定义</strong>：盒装 CPU 是指以零售包装形式出售的 CPU，通常包含完整的包装盒、散热器（原装风扇）、安装说明书和保修卡。</li><li><strong>优点</strong>：<ul><li><strong>完整的包装</strong>：提供更好的保护，避免运输过程中的损坏。</li><li><strong>原装散热器</strong>：通常配备厂商提供的原装散热器，虽然性能可能一般，但足以满足日常使用需求。</li><li><strong>较长的保修期</strong>：通常提供较长的保修期，例如 2-3 年，让用户更有保障。</li><li><strong>售后服务</strong>：享受厂商提供的完整售后服务。</li></ul></li><li><strong>缺点</strong>：<ul><li><strong>价格较高</strong>：相比散片 CPU，盒装 CPU 的价格通常更高。</li></ul></li><li><strong>适用人群</strong>：<ul><li>普通消费者：对电脑硬件不太了解，追求稳定性和售后保障的用户。</li><li>新手装机用户：需要简单方便的安装体验，以及厂商提供的技术支持。</li></ul></li></ul><p><strong>2. 散片 CPU (Tray CPU &#x2F; OEM CPU)</strong></p><ul><li><strong>定义</strong>：散片 CPU 是指没有零售包装的 CPU，通常只有 CPU 本身，没有散热器、说明书和保修卡。</li><li><strong>优点</strong>：<ul><li><strong>价格较低</strong>：相比盒装 CPU，散片 CPU 的价格通常更便宜。</li><li><strong>灵活选择散热器</strong>：可以根据自己的需求选择更高级的散热器，提升散热效果。</li></ul></li><li><strong>缺点</strong>：<ul><li><strong>简易包装</strong>：包装简陋，运输过程中可能存在损坏风险。</li><li><strong>没有散热器</strong>：需要单独购买散热器。</li><li><strong>保修期较短或无保修</strong>：保修期通常较短（例如 1 年）或根本没有保修，售后服务也可能受限。</li></ul></li><li><strong>适用人群</strong>：<ul><li>有经验的 DIY 玩家：对电脑硬件有一定了解，可以自行选择散热器和解决可能出现的问题。</li><li>追求性价比的用户：希望以更低的价格购买 CPU，并能承担一定的风险。</li><li>系统集成商：批量采购 CPU 用于组装电脑。</li></ul></li></ul><p><strong>总结表格</strong></p><table><thead><tr><th>特性</th><th>盒装 CPU (Boxed CPU)</th><th>散片 CPU (Tray CPU &#x2F; OEM CPU)</th></tr></thead><tbody><tr><td>包装</td><td>完整零售包装，含散热器、说明书、保修卡</td><td>简易包装，只有 CPU 本身</td></tr><tr><td>散热器</td><td>通常配备原装散热器</td><td>没有散热器，需要单独购买</td></tr><tr><td>保修期</td><td>较长，通常 2-3 年</td><td>较短或无保修</td></tr><tr><td>价格</td><td>较高</td><td>较低</td></tr><tr><td>适用人群</td><td>普通消费者，新手装机用户</td><td>有经验的 DIY 玩家，追求性价比的用户，系统集成商</td></tr></tbody></table><p><strong>购买建议</strong></p><ul><li><strong>如果追求稳定性和售后保障</strong>，建议购买盒装 CPU。</li><li><strong>如果对电脑硬件有一定了解，并且希望以更低的价格购买 CPU</strong>，可以考虑散片 CPU，但需要承担一定的风险。</li></ul><p><strong>注意</strong></p><ul><li>无论购买盒装还是散片 CPU，都要选择正规渠道购买，避免买到假货或翻新货。</li><li>在安装 CPU 时，一定要小心操作，避免损坏 CPU 或主板。</li><li>散热器的选择也很重要，要根据 CPU 的 TDP（热设计功耗）选择合适的散热器，保证 CPU 的稳定运行。</li></ul><p>另外：散片不是二手CPU，盒装二手还是盒装。（当然也不排除有无良JS卖二手散片，一般销量大的淘宝店都不会搞事情或者会标记清楚）<br>散片由于是OEM版本，京东自营和天猫是买不到的，只有淘宝&amp;拼多多三方店有卖。</p><h2 id="推荐"><a href="#推荐" class="headerlink" title="推荐"></a>推荐</h2><p>办公家用（带核显）：12400&#x2F;13400、5600G&#x2F;8600G</p><p>游戏性价比：12400F、5600、7500F、12600KF、9600X</p><p>游戏中端型号：9700X、14600KF</p><p>游戏发烧友：7800X3D、9800X3D</p><p>生产力性价比：265K</p><p>高生产力：9950X、285K</p><h1 id="主板"><a href="#主板" class="headerlink" title="主板"></a>主板</h1><p>主板是电脑其他硬件的载体，所有的硬件都要安装或连接到主板上，主板本身并不提供性能，但主板能决定其他硬件性能，所以选择合适的主板就很重要了。</p><p>选择主板主要考虑以下几点：<br>1、主板供电（供电能满足CPU需求就不会对电脑性能有影响）</p><p>2、拓展性（多内存&#x2F;显卡插槽、硬盘插槽、IO接口等）</p><p>3、WIFI&#x2F;蓝牙功能（需要这两个功能主板需要选择带WIFI后缀的版本）</p><p>越高端的主板包含的功能越多，超频能力也越强，如果没有超频需求，在满足自身拓展性需求的情况下，选择供电够的主板即可，主板不提供性能，花太多钱不划算。</p><p>小知识：双内存槽主板的内存超频能力强于四内存插槽主板，最强超频主板ROG APEX就是双槽主板。</p><h2 id="主板芯片组介绍"><a href="#主板芯片组介绍" class="headerlink" title="主板芯片组介绍"></a>主板芯片组介绍</h2><p>首先，芯片组有AMD和Intel两种，其次每家又有高端、中端、低端三种，每一代CPU都会推出一代芯片组，所以主板在选择时要选择适配CPU的芯片组系列。</p><p>举例：</p><p>B550适配锐龙5000系列<br>B650适配锐龙7000系列<br>B850适配锐龙9000系列<br>不过有的芯片组主板在更新换代时不会更换扣具接口，所以也会出现互相兼容的情况，比如B650&#x2F;B850同时兼容7000和9000系列，但不兼容更早的锐龙5000系列。</p><p>AMD</p><p>高端芯片组X系列：X670&amp;X870<br>中端芯片组B系列：B650&amp;B850&amp;B840<br>低端芯片组A系列：A620<br>AMD芯片组中，A系列不支持CPU超频，B和X系列均支持CPU超频。</p><p>Intel</p><p>高端芯片组Z系列：Z890&#x2F;Z790<br>中端芯片组B系列：B860&#x2F;B760<br>低端芯片组H系列：H610<br>Intel芯片组中，H和B系列不支持CPU超频，只有Z系列支持CPU超频。</p><h2 id="主板品牌介绍"><a href="#主板品牌介绍" class="headerlink" title="主板品牌介绍"></a>主板品牌介绍</h2><p>主板品牌以御三家为首：华硕、微星、技嘉</p><p>华擎紧跟御三家之后，后面还有映泰、昂达、七彩虹、铭瑄、影驰</p><p>对于小白来说，一般只推荐一线品牌，由于技嘉自身原因，本文只介绍和推荐华硕和微星。</p><h3 id="华硕主板分级介绍"><a href="#华硕主板分级介绍" class="headerlink" title="华硕主板分级介绍"></a>华硕主板分级介绍</h3><p>华硕主板主要分为四个等级</p><ol><li><p>ROG MAXIMUS（满血ROG）</p><p> 代表型号：HERO</p><p> EXTREME GLACIAL：高端旗舰冰川版，为土豪而生。<br> EXTREME：高端旗舰板，为土豪而生。<br> APEX：高端超频板，专为超频而生。<br> HERO：高端入门款，性价比高，各方面均衡。</p></li><li><p>ROG STRIX（普通ROG）</p><p> 代表型号：吹雪</p><p> E：ROG STRIX旗舰型号。<br> F：ROG STRIX高端型号。<br> H：ROG STRIX入门型号。<br> A：吹雪纯白主板（ROG STRIX入门型号）。<br> I：ITX版型（ROG STRIX入门型号）。<br> G：后缀带G一般指MATX板型，现在有小吹雪也用后缀G（ROG STRIX入门型号）。</p></li><li><p>TUF GAMING（电竞特工）</p><p> 代表型号：重炮手</p><p> E：重炮手阉割版<br> PLUS：重炮手<br> PLUS II：重炮手加强版<br> PRO：重炮手加强版<br> PRO S：重炮手再次加强版</p></li><li><p>ASUS Prime（大师）</p><p> 代表型号：大师-A</p><p> A：大师系列最高端系列。<br> E：大师系列次高端系列。<br> D：大师系列中端系列。<br> P：大师系列中低端系列。<br> K：大师系列入门型号。</p></li></ol><h3 id="微星主板分级介绍"><a href="#微星主板分级介绍" class="headerlink" title="微星主板分级介绍"></a>微星主板分级介绍</h3><p>微星主板主要分为四个等级</p><ol><li><p>MEG系列</p><p> 代表型号：ACE 战神</p><p> GODLIKE：MEG旗舰型号超神。<br> ACE：MEG高端型号战神。<br> UNIFY：超频专用板。<br> UNIFY-X：超频专用强化板。</p></li><li><p>MPG系列</p><p> 代表型号：CARBON 暗黑</p><p> CARBON：MPG旗舰型号暗黑。<br> EDGA：MPG高端型号刀锋。</p></li><li><p>MAG系列</p><p> 代表型号：MORTAR 迫击炮</p><p> TOMAHAWK：MAG旗舰型号战斧。<br> MORTAR MAX：迫击炮升级版。<br> MORTAR：MAG高端型号迫击炮。<br> BAZOOKA：MAG入门型号爆破弹。</p></li><li><p>PRO系列</p><p> 代表型号：PRO A</p><p> 微星PRO系列这一代出的不多，以PRO A和PRO P为主，A的定位高于P。</p><p> 14代酷睿新出的主板微星这边主要是后缀带Ⅱ，比如迫击炮Ⅱ，或者加MAX，比如-A MAX。</p></li></ol><h2 id="推荐-1"><a href="#推荐-1" class="headerlink" title="推荐"></a>推荐</h2><h3 id="英特尔主板推荐篇"><a href="#英特尔主板推荐篇" class="headerlink" title="英特尔主板推荐篇"></a>英特尔主板推荐篇</h3><ol><li><p>12100&#x2F;12400&#x2F;12490&#x2F;13100<br> 推荐主板：华硕PRIME H610M-A D4</p><p> （D4 D5选择要区分开，这个级别的主板建议选D4版本）</p><p> i3、i5非K系列功耗不高，搭配H610就足够了，华硕H610M-A还给了2个M2接口，很实用，玩灯还有ARGB接口，不过H610芯片组不支持PCIE4.0硬盘，如果上PCIE4.0硬盘主板要选B660&#x2F;B760，推荐型号华硕B760M-K。</p></li><li><p>13400&#x2F;13490&#x2F;12600K<br> 推荐主板：微星B760M爆破弹</p><p> （D4 D5选择要区分开，这个级别的主板建议选D4版本）</p><p> 这三个CPU对于供电要求不算高，搭配入门级别的B760主板就足够了，微星爆破弹就是很实用性价比很高的主板。</p></li><li><p>13600K&#x2F;13700K&#x2F;14600K<br> 推荐主板：b760华硕重炮手&#x2F;微星迫击炮</p><p> 如果不考虑超频，13600K&#x2F;13700K&#x2F;14600K&#x2F;14700K也是可以搭配b760的</p><p> 目前DDR5内存价格也降下来了，如果上D5平台可以选b760华硕重炮手&#x2F;微星迫击炮 DDR5版本</p><p> 随着14代发布的还有第二代迫击炮和重炮手，搭配14代估计支持D5频率能更高一些，二代多一个M.2插槽，价格相差不大的话，建议买二代</p></li><li><p>13900K&#x2F;14700K&#x2F;14900K<br> 推荐：微星PRO Z790-A （DDR4&#x2F;DDR5）</p><p> 微星PRO Z790-A是性价比非常高的主板，该有的都有，供电、超频、M2拓展都做的很好，入门级Z790里挺值得买的，特别是DDR5平台，Z790对D5的内存频率优化了，可以达到更高的频率。</p><p> 微星PRO Z790-A可以完全发挥出13900K&#x2F;13900KS的全部性能，属于性价比较高的配法，如果想配个好点的主板可以选择更好一点主板。</p></li><li><p>U245K&#x2F;U265K&#x2F;U285K<br> 推荐：华硕Z890 AYW GAMING</p><p> U265K和U285K这一代功耗很低，搭配主板选择入门级别的Z890就足够了，Z890 AYW属于是性价比高的配法，如果想配高端主板也是可以的，根据预算选择就行。</p></li></ol><h3 id="AMD主板推荐篇"><a href="#AMD主板推荐篇" class="headerlink" title="AMD主板推荐篇"></a>AMD主板推荐篇</h3><ol><li><p>5600G&#x2F;5700G&#x2F;5600&#x2F;5700X3D<br> 推荐主板：华硕b550M重炮手</p><p> （微星的迫击炮和华硕的重炮手属于平替，选哪个都可以）</p><p> 5600&#x2F;5700X3D搭配b550主板是支持PCIE4.0的，但是这四个也是可以搭配b450的，如果对PCIE4.0没需求，搭配b450性价比会高一些。</p></li><li><p>7500F&#x2F;8600G&#x2F;8700G&#x2F;9600X<br> 推荐主板：华硕A620M重炮手</p><p> B650太贵了，像7500F搭配B650就有点不划算了，搭配一块A620就可以完全发挥出全部性能，不用浪费钱给主板。</p><p> 性价比还可以选其他牌子更便宜的A620M或者丐版B650M主板</p></li><li><p>7700&#x2F;7700X&#x2F;7800X3D&#x2F;9700X&#x2F;9800X3D<br> 推荐主板：华硕TUF GAMING B650M-PLUS重炮手(B850也可以)</p><p> 遇事不决重炮手（迫击炮）。</p></li><li><p>9900X&#x2F;9950X（X3D)<br> 推荐主板：</p><p> 华硕TUF GAMING X870-PLUS WIFI</p><p> 预算够可以买华硕ROG CROSSHAIR X870E HERO</p></li></ol><p>如果考虑超频的话，X870E HERO就很不错，微星这边同级别为ACE战神。</p><h1 id="显卡"><a href="#显卡" class="headerlink" title="显卡"></a>显卡</h1><p>显卡主要有两大阵营：AMD和NVIDIA。</p><p>大概翻了一下网上关于显卡参数的讲解，总结下来，对于配置电脑的人来说没什么多大用处，不需要了解，看了反而更加头疼。只需要看预算和使用场景，对比网上大的数码博主推荐的显卡型号，选择性价比较高的即可。</p><p>当然如果实在好奇的可以参考这篇<a href="https://zhuanlan.zhihu.com/p/501640217">文章</a>。</p><p>个人建议，游戏方面有光追需求选N，无光追需求看具体的性价比，生态方面由于N卡一直保持领先，布局比A卡更广，生产力N卡在部分领域还是有明显优势的，如果CUDA生态圈生产力的还是选N卡吧，包括这两年热门的AIGC领域N卡的优势非常明显。</p><p>目前热门的AIGC对显卡的要求，主要是显卡的算力和显存容量，容量决定出图的分辨率，算力决定出图的效率，一般来说中端显卡会推荐60Ti 16GB，中高端会推荐5070Ti，高端推荐5080和5090D，Ai绘图不推荐A卡。</p><h2 id="品牌"><a href="#品牌" class="headerlink" title="品牌"></a>品牌</h2><ol><li>N卡阵营：<ul><li>一线品牌：华硕、微星、技嘉、七彩虹（准一线）</li><li>二三线品牌：索泰、影驰、映众、耕升、铭瑄、万丽、盈通等</li></ul></li><li>A卡阵营：<ul><li>一线品牌：蓝宝石、华硕</li><li>二三线品牌：微星、讯景、撼讯、华擎、盈通、速驹、瀚铠等</li></ul></li></ol><h2 id="显卡推荐"><a href="#显卡推荐" class="headerlink" title="显卡推荐"></a>显卡推荐</h2><p>1080P推荐：5060 &amp; 7600GRE（不光追）</p><p>2K推荐：5060 &amp; 7700XT（不光追）</p><p>4K推荐：5070 &amp; 9070（不光追）</p><p>当然，超过推荐级别的帧数会更高，体验也会更好。</p><h3 id="千元内的显卡推荐"><a href="#千元内的显卡推荐" class="headerlink" title="千元内的显卡推荐"></a>千元内的显卡推荐</h3><p>目前有：6400、6500XT、1630、1650、A380</p><p>这几张卡性价比都非常低，如果非要推荐的话，6500XT是里面性价比最高的。</p><h3 id="1000-2000价位的显卡推荐"><a href="#1000-2000价位的显卡推荐" class="headerlink" title="1000~2000价位的显卡推荐"></a>1000~2000价位的显卡推荐</h3><p>有矿卡概率型号：6600、6600XT、3060 12GB</p><p>目前有：6650XT、A750&#x2F;A770、7600、6750GRE、7650GRE</p><p>有矿卡概率的这里就不推荐了， A750&#x2F;A770其实性价比可以，但是intel显卡优化还有待提高，7600和6650XT性能接近，这两张可以买6650XT，6750GRE 10G现在降价很多，现在2000以内挺值得入手</p><p>7650GRE：推荐指数⭐×4<br>目前价算是2000左右目前性价比最高的游戏显卡之一了，性能和4060基本一样，比4060便宜几百块，如果能接受A卡，那7650GRE性价比就很不错。</p><p>游戏性能在1080P分辨率通吃，2K入门也基本没问题。</p><h3 id="2000-4000价位的显卡推荐"><a href="#2000-4000价位的显卡推荐" class="headerlink" title="2000~4000价位的显卡推荐"></a>2000~4000价位的显卡推荐</h3><p>中端型号目前有：9060XT，5060，5060Ti 8G&#x2F;16G，7700XT，7800XT</p><p>如果预算2000+不想选A卡，5060是不错的选择。</p><p>5060：推荐指数⭐×5<br>性能略强于4060Ti，多一个多帧生成，价格2500左右，丐卡后续还会降价，目前2000出头最合适的显卡，大部分游戏在2K分辨率都能高画质畅玩。</p><p>5060Ti 8G&#x2F;16G：推荐指数⭐×5<br>8G版本适合游戏用户，16G版本适合游戏+AI用户，对于游戏用户来说加几百块买16G版本也是可以的，这一代大显存版本便宜了，而且现在AI应用盛行，保不齐后面用到AI应用，而且现在新出的3A游戏对于显存要求也越来越高，不过预算紧张追求性价比的纯游戏用户还是买8G版本好一点。</p><p>7700XT：推荐指数⭐×4<br>7700XT这张卡的性能要比价格接近的4060Ti要强20%，如果是纯3A游戏需求，目前还是值得入手的，可以花更少的钱买更强的性能，但是如果你想玩光追，那还是选N卡。</p><p>7800XT：推荐指数⭐×4<br>目前价格可以买，相比4070稍微有点性价比，性能比4070略强一点，但是比4070便宜几百块，不过光追性能肯定是不如4070的，能耗比也不如4070。</p><h3 id="4000-6000价位的显卡推荐"><a href="#4000-6000价位的显卡推荐" class="headerlink" title="4000~6000价位的显卡推荐"></a>4000~6000价位的显卡推荐</h3><p>5070 VS 9070</p><p>5070：推荐指数⭐×3<br>9070：推荐指数⭐×5<br>5070是4070的升级款，性能大约比4070强15%左右，基本和4070S持平，但是比4070S又多一个多帧生成，对于3A游戏党还是很适合的，但是性价比相比9070就低不少了，9070的性能只和5070Ti差10%，价格基本和5070差不多，追求性价比就直接入9070。</p><p>9070XT：推荐指数⭐×5<br>性能和5070Ti持平，价格比5070Ti便宜1000多，性价比神卡，4K标准游戏卡，大部分3A游戏4K都能高画质七八十帧畅玩，而且这一代光追性能也有所提高，值得入手。</p><h3 id="高端级别的显卡推荐（6000以上）"><a href="#高端级别的显卡推荐（6000以上）" class="headerlink" title="高端级别的显卡推荐（6000以上）"></a>高端级别的显卡推荐（6000以上）</h3><p>5070Ti：推荐指数⭐×3<br>5070Ti性能≈4080SUPER，虽然目前有溢价但是比4080S要便宜不少，而且50系独有多帧生成，所以相比之下肯定是比4080S要值得买。</p><p>5080：推荐指数⭐×3<br>卡是好卡，但是有溢价，不太推荐加太多钱买。</p><p>5090D：推荐指数⭐×5<br>预算够冲就完了，跟性价比无关，但是是目前国内能买到的最强游戏卡。</p><h1 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h1><p>内存主要包括：电路板、内存芯片（内存颗粒）、金手指和存储芯片（EEPROM）等构成，部分还有散热马甲、灯带等。<br><img src="/../images/image.png" alt="内存条构造"></p><p>买内存条时，首先要考虑自己要不要超频，如果不考虑超频，只需要考虑选择哪个品牌，购买多大的内存，多少频率的内存即可。<br>个人不建议超频，稳定性差，散热问题，风险大。</p><p>内存的主要参数有：<br>容量、频率、接口和内存时序。</p><h2 id="容量"><a href="#容量" class="headerlink" title="容量"></a>容量</h2><p>容量就是内存有多大的空间，可以放多少个 G 的数据，理论上来说当然是越大越好。<br>这个看个人的需求，纯办公8g足矣，玩游戏16g足矣，别的一些生产力软件，比如AI绘图，视频编辑，3D建模等，24g&#x2F;32g足矣。</p><p>16G对于绝大多数人都是足够的，除非是要吃内存的专业软件需要才考虑选择32G及以上的内存大小，购买时注意购买<strong>2根组成双通道</strong>，同等条件下双通道效能大于单通道。</p><h2 id="频率"><a href="#频率" class="headerlink" title="频率"></a>频率</h2><p>频率就是内存工作的速度，是以 100Mhz 或者 133.33333MHz 为基本单位的倍数，<br>比如 2400MHz就等于 100基频 x 24倍频 或者等于 133.3333基频 x 18倍频。</p><p>内存都会标注频率，2666MHz、3200MHz、3600MHz、4800MHz、5600MHz、6000MHz等，根据需求选。（注：购买高频内存需要进bios开启xmp，不然D4默频2666MHz运行，D5默认4800MHz运行）</p><p>（超频党是选内存颗粒，不超频就是直接选频率）</p><h2 id="内存频率该如何选择"><a href="#内存频率该如何选择" class="headerlink" title="内存频率该如何选择"></a>内存频率该如何选择</h2><ol><li><p>Intel平台CPU：</p><ul><li><p>12代intel：D4平台推荐选3600MHz，D5平台推荐选6400MHz。</p></li><li><p>13代intel：D4平台推荐选3600MHz，D5平台推荐选6800MHz。</p></li><li><p>（12代&#x2F;13代后缀没带K的U锁了SA电压，D4内存频率一般选3200MHz，超不上去，DDR5由于都是gear2运行，不受SA电压的影响）</p></li><li><p>14代intel：D4平台推荐选3600MHz，D5平台推荐选7200MHz。</p></li><li><p>U200S系列：U200S系列仅支持DDR5内存，对于DDR5频率支持大幅提升，推荐7200MHz~8000MHz频率。</p></li></ul></li><li><p>AMD平台CPU：</p><ul><li><p>5代锐龙：5代锐龙仅支持DDR4平台，超频根据CPU FLCK体质超，效能比最大FLCK：内存频率是1：2，五代锐龙一般体质2000左右，内存最佳频率大概在4000MHz左右。</p></li><li><p>7代锐龙：7代锐龙仅支持DDR5平台，内存同步甜点频率6000MHz。</p></li><li><p>9代锐龙：7代锐龙仅支持DDR5平台，内存同步甜点频率6000MHz。</p></li><li><p>AMD 7代&amp;9代选择内存就买6000MHz时序尽量低的即可，目前c28 6000MHz最合适。</p></li><li><p>目前锐龙AMD 7代&amp;9代搭配B850X&amp;870主板内存频率可以到8000MHz，8000MHz c36＞6000MHz c28 ，预算充足可选8000MHz c36。</p></li></ul></li></ol><h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><p>接口是内存能不能用的最主要参数，因为要找准位置，选好接口，才能插进去嘛。</p><p>这个就去看主板支持的是什么插槽。当然，如果都买新的，现在是 DDR4 内存时代，一般不用考虑这个问题，但是有人捡垃圾或者升级旧电脑，就一定要注意了。</p><blockquote><p>时序的话，小白用户基本不用考虑。这里也不再扩展</p></blockquote><h2 id="超频"><a href="#超频" class="headerlink" title="超频"></a>超频</h2><p>超频 (Overclocking) 是指将计算机硬件（通常是 CPU、GPU 或内存）的运行频率提高到超过其制造商指定的默认频率。这样做可以提升硬件的性能，但同时也会带来一些风险。</p><p>超频的目的：</p><ul><li>提升性能：通过提高运行频率，可以使 CPU、GPU 或内存更快地执行任务，从而提升整体性能。</li><li>免费升级：超频可以看作是一种免费的性能升级，无需购买新的硬件。</li><li>满足游戏需求：对于游戏玩家来说，超频可以提高游戏的帧率，从而获得更流畅的游戏体验。</li></ul><p>超频的风险：</p><ul><li>稳定性问题：超频可能会导致系统不稳定，出现崩溃、死机等问题。</li><li>硬件损坏：过高的电压和频率可能会导致硬件过热，从而损坏硬件。</li><li>缩短寿命：即使没有损坏硬件，超频也会加速硬件的老化，缩短其使用寿命。</li><li>失去保修：超频可能会使硬件失去保修资格。</li></ul><h2 id="品牌-1"><a href="#品牌-1" class="headerlink" title="品牌"></a>品牌</h2><p>宏碁掠夺者、芝奇、威刚、金士顿、海盗船、英睿达、科赋、十铨、光威、阿斯加特、宇瞻等（排名不分先后，只要听过名字的品牌基本都能买）</p><p>想支持国产的话不看品牌，认准长鑫颗粒。</p><h2 id="推荐-2"><a href="#推荐-2" class="headerlink" title="推荐"></a>推荐</h2><h3 id="DDR5内存"><a href="#DDR5内存" class="headerlink" title="DDR5内存"></a>DDR5内存</h3><p>目前DDR5内存价格已经掉了不少，生产力党可以考虑上DDR5内存了，DDR5颗粒最好的目前是海力士，其次是三星，美光的颗粒目前没有超频潜力。</p><ul><li><p>性价比推荐：</p><ul><li><p>现在DDR5内存逐步普及，目前主流频率已经来到了6400~7200MHz，性价比选这个区间的频率即可，追求性能越高越好，时序越低越好。</p></li><li><p>C28 6000（适用于AMD）</p></li><li><p>追求性价比就买金百达的银爵，内存价格目前卷的很厉害，价格便宜的还有阿斯加特、光威等等都可以选。</p></li><li><p>C34 7200MHz（适用于Intel）</p></li><li><p>如果想买灯条，白色系主机推荐阿斯加特女武神套条，黑色主机推荐阿斯加特TUF联名RGB套条，价格不贵，颜值也挺高。</p></li></ul></li><li><p>高性能D5内存条推荐</p><ul><li>芝奇幻锋戟 C40 8000MHz M-die</li><li>24G×2的M-Die就是目前最强的D5超频条，但是这个容量两条48G，比大部分人的需求都高，而且内存价格偏高，除了热爱超频的用户或者追求极限性能，一般不推荐买。</li></ul></li><li><p>大容量D5内存条推荐</p><ul><li><p>D5的主力销售容量一般是32G套条，64G以上的套条价格目前还是有点贵的，目前建议是6000MHz 32G×2，性价比最高的金百达，一线大牌金士顿，或者用料价格都不错的宏碁掠夺者。</p></li><li><p>金百达银爵6000MHz C36 32G×2</p></li><li><p>追求性价比，就买金百达或者光威的，如果只要容量，对频率无所谓的可以买英睿达的，镁光颗粒，价格便宜，但是频率低一些。</p></li><li><p>64G不够可以上96G</p></li></ul><p>  现在最新推出的48G单根规格，双条96GB，4根就是192G，对于绝大部分用户来说都够用了。</p></li></ul><h3 id="DDR4内存"><a href="#DDR4内存" class="headerlink" title="DDR4内存"></a>DDR4内存</h3><p>性价比推荐：<br>最性价比肯定是金百达、光威的，3200MHz 16G×2价格都干到300多了。</p><p>D4就别玩超频了，现在三星b-die内存都没几个了。</p><h1 id="电源"><a href="#电源" class="headerlink" title="电源"></a>电源</h1><p>电源是给整个主机所有硬件供电的设备，所以选择一款质量好的电源很重要，电源也是电脑中使用寿命比较长的，完全可以选一个好的用2~3代。</p><p>电源选择标准：</p><ul><li>额定功率</li><li>电源转换率</li><li>全模组、非模组</li><li>电源品牌</li></ul><h2 id="额定功率"><a href="#额定功率" class="headerlink" title="额定功率"></a>额定功率</h2><p>额定功率要满足高负载的功耗，同时还要有溢出。<br>如何计算整机功耗呢？</p><p>电脑整机（台机或笔记本）的主要功耗来源是CPU+GPU，我们通常将两者的TDP（最大散热设计功耗）相加得到的就是这台整机的负载功耗数值。<br>按照整机功耗来选择电源的时候我们不光要看电源的额定功率，还需要看电源的转化效率。<br>结论： 电源额定功率<em>45%≤整机功耗≤电源额定功率</em>80%</p><p>举例：</p><p>整机功耗&#x3D;400W，则需要选购最低400W&#x2F;0.8（+12V极限）&#x3D;500W，最优400W&#x2F;0.45（电源最佳使用曲线最高值）&#x3D;800W 的电源，当然500-800W以内的电源都是可以使用的~</p><ul><li><p>NVIDIA显卡系列<br><img src="/../images/how-to-choose-a-desktop-configuration_2025-05-30-14-21-53.png" alt="NVIDIA显卡系列"></p></li><li><p>AMD显卡系列<br><img src="/../images/how-to-choose-a-desktop-configuration_2025-05-30-14-26-48.png" alt="AMD显卡系列"></p></li></ul><p>额定功率大小不代表电源的品质。</p><h2 id="电源转换率"><a href="#电源转换率" class="headerlink" title="电源转换率"></a>电源转换率</h2><p>电源上有很多线材，有线材就有电阻，有电阻就有电损，有了电损就会有转换率，选择电源时我们会看到很多会标80Plus铜牌、80Plus金牌之类，这就是电源转换率的标准。一般选择80PLUS铜牌认证以上的电源就可以了，额定功率是指除去电损过之后的功率，不然就是虚标。</p><p><img src="/../images/how-to-choose-a-desktop-configuration_2025-05-30-14-28-29.png" alt="电源转换率"></p><p>等级越高转换效率越高也就越省电，但是和电源的品质不是正相关的，一般来说金牌以上的电源绝大部分质量都不错。</p><p>购买高转换率的电源优势：</p><p>1.发热量小，电源静音。</p><p>2.省电。</p><p>电源转换率不代表电源的品质。</p><h2 id="全模组、非模组"><a href="#全模组、非模组" class="headerlink" title="全模组、非模组"></a>全模组、非模组</h2><p>非模组电源的线是不可拆卸的，在装机过程中，非模组电源的线有很多是多余的，走线很难，机箱会有点不整洁，如果有强迫症会很难受。</p><p>全模组电源的线都是可以拆卸的，你可以用什么线插什么线，机箱内部会很整洁，也可以自己购买定制线，让你的电脑更拉风。</p><p>还有半模组电源</p><p>半模组电源的主板供电线和CPU供电线和非模组电源一样，不能拆卸。但是其余的的供电线是和全模组一样可以拆卸的。</p><p>全模组、非模组不代表电源的品质。</p><h2 id="电源品牌"><a href="#电源品牌" class="headerlink" title="电源品牌"></a>电源品牌</h2><p>首选：海韵、振华</p><p>次选：海盗船、安钛克、全汉、台达、EVGA、酷冷至尊、长城、TT、华硕、微星</p><p>次次选：航嘉、鑫谷、先马、金河田</p><p>品牌不是绝对的选择标准，每个品牌都有高中低端定位的产品，所以具体还要看型号。</p><h2 id="电源推荐"><a href="#电源推荐" class="headerlink" title="电源推荐"></a>电源推荐</h2><h3 id="450W-550W"><a href="#450W-550W" class="headerlink" title="450W&amp;550W"></a>450W&amp;550W</h3><p>全汉蓝暴经典PLUS 450&#x2F;550W</p><p>450&#x2F;550W同理，口碑比较好的全汉蓝暴经典PLUS、振华铜皇，建议是哪个便宜入手哪个，全汉一般价格低，这里推荐全汉。</p><p>如果需要全模组，这里还可以选长城X5或者酷冷GX550，理线更方便一些。</p><p>当然这里有一个特殊的选择，那就是玄武550V4</p><p>这个只有PDD淘宝京东第三方卖，近两年兴起的一个品牌，用料可以价格便宜，性价比很高，但是是个小厂，追求性价比的话可入。</p><h3 id="650W"><a href="#650W" class="headerlink" title="650W"></a>650W</h3><p>酷冷至尊GX650</p><p>650W推荐我经常购买的酷冷GX650。</p><p>650W非模组铜牌电源，侨威代工，整体性价比还行，在打出口碑后价格小涨，但是也值得入手。</p><h3 id="750W"><a href="#750W" class="headerlink" title="750W"></a>750W</h3><p>利民TG750</p><p>ATX3.0+PCIE5.0，全日系电容，价格便宜，性价比之选。</p><p>振华LEADEX III750W<br>振华最新推出的ATX3.1电源，振华的品质不用多说了。</p><h3 id="850W-1000W"><a href="#850W-1000W" class="headerlink" title="850W~1000W"></a>850W~1000W</h3><p>Tt 钢影 GF1 850</p><p>850W性价比最高的当属Tt最新推出的GF1 850，中源电脑代工，PCIe5.1+ATX3.0，7年换新，全日系电容，主要是价格还很便宜，性价比极高的存在。</p><p>振华LEADEX III850W</p><p>振华最新推出的ATX3.1电源，振华的品质不用多说了。</p><h3 id="1000W以上旗舰电源"><a href="#1000W以上旗舰电源" class="headerlink" title="1000W以上旗舰电源"></a>1000W以上旗舰电源</h3><p>性价比可选振华Ⅶ1300</p><p>海韵至尊旗舰钛金PRIME TX1600（ATX3.0电源）</p><h1 id="机箱"><a href="#机箱" class="headerlink" title="机箱"></a>机箱</h1><p>选择机箱要清楚自己的需求：颜值？静音？大小？</p><p>如果没有额外需求，那机箱一定是越便宜性价比越高，因为机箱不会带来任何实际性能提升。<br>但不论怎样的需求，<strong>能装进去硬件</strong>都是第一要考虑的，其次才是根据需求选择机箱。</p><p>机箱需要注意的硬件规格：<br>散热器限高（风冷）<br>风扇位（水冷规格）<br>显卡尺寸（显卡长度）<br>主板规格（ATX、MATX、ITX主板）<br>电源规格（ATX、SFX电源）<br>硬盘位（拓展性）<br>至于机箱的用料，基本符合一分价格一分货的规律。</p><p>这样看下来，建议机箱选择放在最后，因为前面的硬件选完之后就需要考虑机箱<br>的大小。</p><h2 id="推荐-3"><a href="#推荐-3" class="headerlink" title="推荐"></a>推荐</h2><h3 id="200元以内"><a href="#200元以内" class="headerlink" title="200元以内"></a>200元以内</h3><p>1、先马平头哥M2<br>MATX机箱，支持HDD×2、SSD×4，支持240水冷，散热器限高160mm，支持长显卡345mm。</p><p>散热性能凑活，主要是价格便宜，MATX小机箱，我装了好几台这机箱了。</p><p>2、先马鲁班1<br>ATX机箱，支持HDD×2（其中一个兼容SSD）、SSD×2（或HHD×1），支持360水冷，散热器限高165mm。</p><p>先马鲁班1是200左右价位我推荐最多的机箱，因为这款机箱在这个价位是综合最好的之一，不论是用料还是机箱内部设计，缺点就是外观可能没那么好看。</p><h3 id="250-450元"><a href="#250-450元" class="headerlink" title="250~450元"></a>250~450元</h3><p>1、酷冷MB520<br>ATX机箱，SSD×6，支持360水冷，散热器限高165mm。</p><p>酷冷MB系列做的都还算不错，MB520外观朴实，用料可以，散热性能也还不错，但是HDD硬盘只能装两个，硬盘拓展性一般，有一丝遗憾。</p><p>2、松果D41<br>ATX机箱，SSD×2+HDD×2，支持360水冷，散热限高168mm。</p><p>这是一款设计很不错，兼容性很好的小机箱，近期热度很高，装机需要看一下详情页的尺寸限制。</p><p>3、骨伽乘风MX600<br>ATX机箱，HDD×2（兼容SSD），SSD×2，支持420水冷，散热器限高180mm。</p><p>散热性能优秀，六面皆透风，但是机箱没有标配风扇，所以加上风扇就不是这个价格了，没有风扇的机箱DIY属性比较高，可以自己选择如果加装风扇。</p><h3 id="400-600元"><a href="#400-600元" class="headerlink" title="400~600元"></a>400~600元</h3><p>1、安钛克P20C<br>E-ATX机箱，前置Type-C口，HDD×2，SSD×2，支持420水冷，散热器限高170mm。</p><p>预装了3个ARGB风扇，机箱带了ARGB接头灯效控制板，支持神光同步，机箱散热表现很不错，而且支持E-ATX大板，前面金属格栅面板，质感很高。</p><p>更多推荐参考： <a href="https://zhuanlan.zhihu.com/p/337206779">https://zhuanlan.zhihu.com/p/337206779</a></p><h1 id="散热器"><a href="#散热器" class="headerlink" title="散热器"></a>散热器</h1><p>散热器是用来给cpu散热的，一般来说盒装cpu是自带散热的，那为什么要另行购买散热器呢？</p><ul><li>原装散热压不住。</li><li>像散片cpu本身就不带散热器所以也需要另外买一个散热器。</li></ul><h2 id="风冷、水冷"><a href="#风冷、水冷" class="headerlink" title="风冷、水冷"></a>风冷、水冷</h2><p>散热器一般分为风冷和水冷两种，风冷和水冷各有优缺点：</p><ul><li>风冷更安全，比较简单，性能上限较低，<ul><li>细分的话风冷还有下压式，下压式比较适合itx小机箱，而塔式就适合普通机箱，塔式也分单塔和双塔，双塔风冷的性能更强一些</li></ul></li><li>而水冷在外观、性能方面有很大的优势，缺点就是相对比较贵，还有漏液风险，不过现在的水冷漏液概率很小。<ul><li>水冷也分一体式水冷和分体式水冷，常见的都是一体式水冷。</li></ul></li></ul><p>不是土豪玩家，我建议就风冷,cpu频率高选择双塔；频率低选择单塔，如果想要小机箱，选择下压式风冷。</p><h2 id="怎么选择散热器"><a href="#怎么选择散热器" class="headerlink" title="怎么选择散热器"></a>怎么选择散热器</h2><p>拿风冷来说，鳍片、热管、风扇、材质都会影响散热器性能<br>鳍片越厚，越多，散热越好<br>热管越多，越粗，散热越好<br>风扇越大散热越好，此处注意风扇噪音也是要关注的点<br>底座材质，也就是接触cpu的那面，有热管直触的、铜底的材质不同散热能力也有区别</p><p>可以看这个<a href="https://www.bilibili.com/video/BV1E9YueeEJr/?spm_id_from=333.337.search-card.all.click&vd_source=428d32ac5556a6a38659408b8c8fb403">视频</a>扫盲。</p><p>总结一下就是用料越好，散热能力越好。</p><p>cpu有个参数TDP(热设计功耗），简单解释就是cpu正常在标准频率工作所散发的热量，散热器厂家就是根据TDP来设计散热器，原装散热器不行就是原装散热通常卡住TDP值设计，而正常使用中，cpu的功耗是要大于TDP的，所以我们在选择散热器时要选择能压制住CPU正常睿频情况下的功耗（超频另说），散热器本身有个参数叫解热功耗，解热功耗＞CPU功耗的情况下散热器就能在合理的温度下压制住CPU。</p><p>目前在售CPU型号散热器搭配：<br>①12100F&#x2F;12400F&#x2F;12600KF&#x2F;13400F&#x2F;5600&#x2F;5600GT&#x2F;7500F&#x2F;9600X：百元级风冷</p><p>②14600KF&#x2F;245K&#x2F;265K&#x2F;9700X&#x2F;7800X3D&#x2F;9800X3D：双塔风冷或360水冷</p><p>③285K&#x2F;14900KF&#x2F;9900X&#x2F;9950X&#x2F;9900X(3D)&#x2F;9950X(3D)：360水冷</p><h2 id="散热器推荐"><a href="#散热器推荐" class="headerlink" title="散热器推荐"></a>散热器推荐</h2><h3 id="百元级风冷"><a href="#百元级风冷" class="headerlink" title="百元级风冷"></a>百元级风冷</h3><p>1、玄冰400V5<br>玄冰400一直是百元风冷的标杆，V5是最新出的版本，百元四热管无脑买这款，设计最合理没有之一，其他拆装都要先弄风扇，玄冰400 V5可以不动风扇直接安装，小白神器。</p><p>2、AX120R SE&#x2F;雅浚E3<br>安装不如玄冰400V5，价格如果便宜10块以上可以考虑。</p><h3 id="两百元风冷"><a href="#两百元风冷" class="headerlink" title="两百元风冷"></a>两百元风冷</h3><p>1.大霜塔V5<br>大霜塔算是200价位的老朋友了，现在被一众同价位散热压着打，优势就是价格便宜，经常200以内，也算是一款好散热了，V5是新版，风扇噪音控制比较好。</p><p>2.利民PA120<br>利民的散热性能都不错，利民这个品牌不怎么搞花里胡哨的东西，利民自家的风扇噪音低，风量大，，风扇四周有硅胶垫减震，整体算是一个不错的散热器，200价位值得购买，送的硅脂依然是标志性的TF7硅脂，压R7、i7非K系列无压力。</p><p>利民的产品线比较丰富，除了PA120，现在还有老款FS140、新款FC140，PA120其实应对不超频的需求大部分U都可以满足，FC140的性能就非常强势了，i7 K系列小超频需求都可以满足。</p><p>最新的PS120也是相当强力的存在，7热管散热器，性能极强，而且价格不高，性价比很不错。</p><h3 id="顶级风冷"><a href="#顶级风冷" class="headerlink" title="顶级风冷"></a>顶级风冷</h3><p>猫头鹰D15、阿萨辛4、Dark Rock Pro 4等<br>散热性能表现优先级：阿萨辛4＞猫头鹰D15＞德商必酷PRO 4</p><p>噪音表现优先级：猫头鹰D15＞德商必酷PRO 4＞阿萨辛4</p><p>综合表现：猫头鹰D15≈阿萨辛4＞德商必酷PRO 4</p><p>猫头鹰D15和阿萨辛4优先级最高，猫头鹰D15偏安静，阿萨辛4偏性能，看需求选择</p><p>德商必酷PRO 4的性能略弱，但是颜值拉满，噪音表现不错，也是可以选择的。</p><h3 id="水冷推荐"><a href="#水冷推荐" class="headerlink" title="水冷推荐"></a>水冷推荐</h3><p>水冷注意要点：</p><p>120水冷除非是颜值党，可以直接无视，要上水冷就240起步。<br>杂牌水冷直接忽视，杂牌不是指不知名品牌，只要是京东自营卖的都不算杂牌。<br>水冷就在京东买吧，理赔方便。<br>240和360水冷差距不是很大，够用就240，极限超频就上360水冷。</p><h4 id="性价比水冷"><a href="#性价比水冷" class="headerlink" title="性价比水冷"></a>性价比水冷</h4><ol><li><p>瓦尔基里VK C360<br> 5年保，漏液包赔，支持个人送保，实测性能还行，价格很实惠。</p><p> 还有GL和E、V更高端的型号，但是价格也更高了。</p></li><li><p>利民冰封棱镜360<br> 漏液包赔，中端水冷代表性型号。</p><p> 利民的产品线比较丰富，往上还有冰封雅境聚境等，棱镜属于性价比较高的型号，5年保。</p></li><li><p>先马二代XW36SD<br> 先马这个加屏幕的价格比其他牌子低不少，实测性能也还行，如果对屏幕有需求，二代XW36SD性价比还不错，也不用担心漏液，二代售后升级到了六年漏液包赔。</p></li></ol><p>360水冷&gt;顶级风冷≈240水冷<br>240水冷基本能和顶级风冷持平。</p><h4 id="顶级水冷：顶级水冷一般都是颜值兼顾性能的存在"><a href="#顶级水冷：顶级水冷一般都是颜值兼顾性能的存在" class="headerlink" title="顶级水冷：顶级水冷一般都是颜值兼顾性能的存在"></a>顶级水冷：顶级水冷一般都是颜值兼顾性能的存在</h4><p>ROG龙神360三代<br>信仰无价。</p><p>硅脂：硅脂的作用是导热，硅脂之间是有差距，但是差距并不明显，一般散热器自带的硅脂就可以用，也可以自己换信越7921、tfx、暴力熊等散热性能更好的硅脂。</p><p>风道：风冷是很吃机箱风道的，合理的机箱风道能让cpu温度降很多，而水冷的冷排会直接和机箱外交互，所以机箱风道对水冷的影响并不大。</p><p>水冷冷排安置方案：</p><p>顶出：顶出的优点是机箱内部温度低，但是冷排用的是机箱的热气，cpu温度会略高。<br>前进：前进的优点是直接利用外部冷空气对冷排散热，cpu温度低，但是冷排加热后的热空气会进入机箱，机箱内部温度会比顶出高。<br>我个人觉得如果打游戏，为了显卡温度和机箱内部温度，可以选择顶出，如果cpu压力更大可以选择前进。</p><h1 id="固态硬盘"><a href="#固态硬盘" class="headerlink" title="固态硬盘"></a>固态硬盘</h1><p>没有垃圾的固态，只有垃圾的价格，选择固态的时候是从品牌价值、性能、价格之间寻找一个平衡点，如果对价格不敏感，也没必要选，无脑最贵的是不会错的，我们都知道同级别的固态三星卖的最贵，所以选择固态其实是在品牌、性能和价格之间做一个妥协。</p><p><strong>另外就是注意匹配主板上的接口即可。</strong></p><p>目前，PCIe 3.0可以满足大多数用户的数据传输需求，在游戏性能的测试体验上，PCIe 3.0和PCIe 4.0差别并不大，而且只有更高端的设备才能较好地发挥PCIe的性能优势。<br>PCIe 3.0的M.2 NVMe SSD是当前最具性价比的选择，完全可以轻松胜任日常工作和玩游戏的需要。如果电脑配置好，对于从事3D设计、视频剪辑及渲染的专业人群来说，如果预算充足，选择PCIe 4.0的M.2 NVMe SSD这样高速稳定的大容量固态硬盘是不错的选择，甚至选择更高速的M.2 NVMe PCIe5.0 SSD，像复杂3D建模、模型渲染、3D动画制作、视频剪辑等这些工作处理起来会更合适。</p><h2 id="品牌-2"><a href="#品牌-2" class="headerlink" title="品牌"></a>品牌</h2><p>固态硬盘值得信赖的推荐品牌有三星（SAMSUNG）、西部数据（西数WD）、闪迪（SanDisk）、东芝铠侠（KIOXIA）、雷克沙（Lexar）、金士顿（Kingston）、英睿达（Crucial）、致钛（ZHITAI）等。</p><h2 id="介质"><a href="#介质" class="headerlink" title="介质"></a>介质</h2><p>闪存是有特殊的物理参数的，闪存分SLC、MLC、TLC、QLC等类型，主要区别在于一个存储单元存储的比特数不同；不同类型的闪存颗粒，其擦写寿命（次数）是不同的；在单元价格上，SLC最贵，MLC次之，然后是TLC，最便宜的是QLC，用不等式表达单元价格的关系为SLC&lt;MLC&lt;TLC&lt;QLC。</p><p><img src="/../images/how-to-choose-a-desktop-configuration_2025-05-30-15-43-41.png"></p><blockquote><p>更加详细参数可以参考：<a href="https://www.zhihu.com/tardis/zm/art/575593403?source_id=1003">https://www.zhihu.com/tardis/zm/art/575593403?source_id=1003</a></p></blockquote><h2 id="推荐-4"><a href="#推荐-4" class="headerlink" title="推荐"></a>推荐</h2><h3 id="M-2固态推荐"><a href="#M-2固态推荐" class="headerlink" title="M.2固态推荐"></a>M.2固态推荐</h3><h3 id="PCIE5-0固态"><a href="#PCIE5-0固态" class="headerlink" title="PCIE5.0固态"></a>PCIE5.0固态</h3><p>现在PCIe5.0固态开始降到大众消费价位了，传输速度确实要更快一些，但是对于大部分普通用户来说感知不大，性价比还是选择PCIe4.0固态好一些，等价格降到PCIe4.0固态价位才推荐入手。</p><h3 id="PCIE4-0固态推荐"><a href="#PCIE4-0固态推荐" class="headerlink" title="PCIE4.0固态推荐"></a>PCIE4.0固态推荐</h3><ol><li><p>铠侠SD10<br> 铠侠以前叫东芝，也是一线品牌，但也是因为知名度不高所以产品的性价比更高一些。</p><p> 推荐指数：五颗星</p><p> 原厂颗粒，满血PCIe5.0固态，无外缓，主要是价格便宜，性价比高。</p><p> 考虑到价格铠侠SD10一直是我首推的型号，原厂盘里性价比最高的之一，铠侠也有一款有缓的SE10，也是个不错的选择，就是价格略微贵一些。</p></li><li><p>三星990 PRO<br> 推荐指数：四颗星</p><p> 三星旗舰990PRO，读写速度7450&#x2F;6900MB&#x2F;s；随机读写1400K&#x2F;1550KIOPS，与 980 PRO 相比，990 PRO 的随机读写性能提高了 55%，能效和散热也有所提升。</p></li><li><p>致态TiPro7000<br> 推荐指数：四颗星</p><p> 国产固态品牌致钛，闪存颗粒自家的，主控为国产的英韧IG5236，读写速度可达7400MB&#x2F;s 5400MB&#x2F;s，价格也还行，不过外置缓存用的三星的，支持国产选择没问题，本身性价比一般。</p><p> 致态还有一款TiPlus 7100，无缓方案，性价比相对更高一些。</p><p> Ti600 QLC颗粒，价格也不算低，不推荐。</p></li><li><p>西数SN850X<br> 推荐指数：三颗星</p><p> 性能对标三星990PRO，也是旗舰级别的盘，不过价格也略贵，西数还有性价比型号比如SN770和SN580，但是价格相比铠侠SD10没有优势，如果喜欢西数这个品牌是可以入手的。</p></li></ol><h3 id="PCIE3-0固态"><a href="#PCIE3-0固态" class="headerlink" title="PCIE3.0固态"></a>PCIE3.0固态</h3><p>PCIE3.0目前更适合选择大容量追求性价比。</p><ol><li><p>铠侠RC20<br> 推荐指数：五颗星</p><p> 东芝原厂盘，缓存512MB，读写速度1700MB&#x2F;s 1600MB&#x2F;s，缺点是温度控制不行，发热量较大，不过台式机无所谓温度。</p></li></ol><p>其他PCIe3.0盘基本没了，现在大家都热衷于名头，做PCIe4.0入门盘，比如西数SN580、铠侠SD10，用起来区别不大，价格倒贵不少。</p><h3 id="SATA固态推荐"><a href="#SATA固态推荐" class="headerlink" title="SATA固态推荐"></a>SATA固态推荐</h3><p>SATA盘实际使用和M.2盘差距并不明显，主要在于读取大文件时会有明显差距，而SATA盘的价格相较于M.2盘并没有低太多，所以现在如果主板支持M.2接口，依然优先选择M.2硬盘。</p><ol><li><p>三星870evo<br> 推荐指数：四颗星</p><p> 顺序读560MB&#x2F;s 顺序写530MB&#x2F;s 性能很强，价格较高。</p><p> 870EVO是860EVO的升级款，随机读写增强了不少，但是价格和860EVO相同，很值得购买，目前消费级最强SATA固态。</p></li></ol><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>台式机虽然组成只有八个，但是每一个的参数、品牌延申下来的知识都是非常庞大且<br>复杂的。<br>个人建议：对于小白来说，看一下使用场景，然后直接就是看数码博主推荐<br>这个场景的配置，最后了解下主要参数看看这个配置是否可靠即可。<br>这是最简单直接、快速的，实在不放心，选好配置后找懂行的朋友看一下是否有问题。</p><p>综上，根据我看的知乎博主推荐选择的配置如下：</p><ol><li>cpu： Ultra 7 265K</li><li>主板： 华硕PRIME Z890M-PLUS WIFI</li><li>显卡： RTX 5070 </li><li>内存： 金百达 银爵 海力士 6000MHz 32GB*2 DDR5</li><li>电源： 海韵 750w CORE 金牌</li><li>机箱： 先马SAMA 鲁班1</li><li>散热： 利民 PA120 6x6MM 双塔标准版</li><li>固态： 铠侠 Gen4 旗舰SE10 1TB</li></ol><blockquote><p>因为我的场景是：敲代码、剪视频、有可能跑一下ai，所以选的配置整体偏向<br>生产力这方面，比如cpu和内存。<br>总之还是要看个人需求。</p></blockquote><blockquote><p>参考：<br><a href="https://www.zhihu.com/tardis/zm/art/217881116?source_id=1003">https://www.zhihu.com/tardis/zm/art/217881116?source_id=1003</a><br><a href="https://zhuanlan.zhihu.com/p/156088622">https://zhuanlan.zhihu.com/p/156088622</a></p></blockquote>]]>
    </content>
    <id>https://blog.codewater.top/how-to-choose-a-desktop-configuration/</id>
    <link href="https://blog.codewater.top/how-to-choose-a-desktop-configuration/"/>
    <published>2025-05-28T15:04:03.000Z</published>
    <summary>
      <![CDATA[<p>一直以来用的大学时代的笔记本，近来发现越来越卡，而且已经跟不上生产力的需求了，<br>所以想着自己动手配置一台台式机。</p>
<p>笔者本人专业虽然是计算机，但是不是数码爱好者，所以对于配置台式不甚了解吧，特此<br>做个记录。</p>]]>
    </summary>
    <title>如何选择一台适的台式机配置</title>
    <updated>2025-05-28T15:04:03.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="ai" scheme="https://blog.codewater.top/categories/ai/"/>
    <category term="deepseek" scheme="https://blog.codewater.top/categories/ai/deepseek/"/>
    <category term="api" scheme="https://blog.codewater.top/tags/api/"/>
    <category term="tokens" scheme="https://blog.codewater.top/tags/tokens/"/>
    <category term="cli tool" scheme="https://blog.codewater.top/tags/cli-tool/"/>
    <content>
      <![CDATA[<p>最近国内的DeepSeek爆发出强劲的势头，纵观网友们的评价下来，很少有国产AI能有这样不错的评价。再加上最近DeepSeek注册就送500百万tokens，还有1块钱一百万tokens的这样的优惠力度，不得不去体验了一下。<br><a href="https://www.deepseek.com/">deepseek官网</a></p><span id="more"></span><p><img src="/../images/2025-01-15-15-17-27.png" alt="官网公布的基准测试"><br><img src="/../images/2025-01-15-15-24-16.png" alt="模型&amp;价格"><br><img src="/../images/2025-01-15-15-25-09.png" alt="赠送的tokens"></p><h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>目前来讲，我并没有很强的tokens使用场景，大多数以网页的chat聊天为主。<br>基于这种场景下的有个<strong>痛点</strong>就是：在终端和网页之间来回切换，有点浪费时间，不太友好。<br>所以我想着能不能把ai集成到cli中，让我在终端中可以直接和ai聊天，不需要切换到网页。</p><p>经过搜索，方案是可行的，工具采用的是<a href="https://github.com/simonw/llm">LLM</a>.</p><blockquote><p>在一切配置开始前，需要如下前置条件：</p><ol><li>安装python环境</li><li>注册deepseek账号</li></ol></blockquote><p>上面两个是最基本的条件，如果不满足建议谷歌搜索一下，这里不在赘述。</p><h1 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h1><h2 id="安装LLM"><a href="#安装LLM" class="headerlink" title="安装LLM"></a>安装LLM</h2><p>用<code>pip</code>安装:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pip install llm</span><br></pre></td></tr></table></figure><p>以后有需要更新版本，可以使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pip install -U llm</span><br></pre></td></tr></table></figure><h2 id="获取DeepSeek的API-Key"><a href="#获取DeepSeek的API-Key" class="headerlink" title="获取DeepSeek的API Key"></a>获取DeepSeek的API Key</h2><ol><li>前往<a href="https://www.deepseek.com/">deepseek官网</a>，注册一个新的账户。</li><li>在<a href="https://platform.deepseek.com/api_keys">API Keys</a>页面，点击<strong>创建 API key</strong>按钮，复制API Key，这是你的私钥，最好不要外泄。（注意：窗口一旦关闭后，API Key就会看不见，一定要保存好！当然如果没了，直接重新创建一个也可以）</li></ol><h2 id="配置LLM"><a href="#配置LLM" class="headerlink" title="配置LLM"></a>配置LLM</h2><p>由于LLM是基于OpenAI的，所以需要设置模型采用DeepSeek的模型。</p><ol><li><p>配置要使用模型的名称，这里是<code>deepseek</code>，名字随便取一个即可：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm keys <span class="built_in">set</span> deepseek</span><br></pre></td></tr></table></figure><p>回车之后，会提示你输入你的API Key，输入你的API Key回车即可。不确定可以采用<code>dirname &quot;$(llm logs path)&quot;</code>找到<code>keys.json</code>文件，查看里面的API Key是否正常。</p></li><li><p>配置默认使用的模型名称：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 这里deepseek是你第一步输入的名称</span></span><br><span class="line">llm models default deepseek</span><br></pre></td></tr></table></figure><blockquote><p>2025年02月06日 更新：如果这一步报错，Error: Unknown model: deepseek。<br>那么可以先执行下一步操作，然后再设置这一步的默认模型。</p></blockquote></li><li><p>配置请求<code>deepseek api</code>：</p><ul><li><code>dirname &quot;$(llm logs path)&quot;</code>，会输出llm的配置路径</li><li>在这个路径下新建一个<code>extra-openai-models.yaml</code>文件，添加如下内容:<figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="bullet">-</span> <span class="attr">model_id:</span> <span class="string">deepseek</span></span><br><span class="line">  <span class="attr">model_name:</span> <span class="string">deepseek-chat</span></span><br><span class="line">  <span class="attr">api_base:</span> <span class="string">&quot;https://api.deepseek.com/v1&quot;</span></span><br><span class="line">  <span class="attr">api_key_name:</span> <span class="string">deepseek</span></span><br></pre></td></tr></table></figure></li><li>如果你第一步的模型名称不是deepseek，这里的<code>model_id</code>和<code>api_key_name</code>需要改成第一步设置的名称。</li><li><code>model_id</code>、<code>api_key_name</code>都是LLM里面概念，而<code>model_name</code>才是请求对应厂商的模型名称</li></ul></li><li><p>如果第二步没有设置成默认模型，也可以采用参数选用模型：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm -m your_model_name <span class="string">&quot;你的问题&quot;</span></span><br></pre></td></tr></table></figure></li></ol><h2 id="使用验证"><a href="#使用验证" class="headerlink" title="使用验证"></a>使用验证</h2><p>终端下直接输入<code>llm &quot;你的问题&quot;</code>即可：<br><img src="/../images/2025-01-15-16-06-59.png" alt="验证api是否正常"></p><h1 id="LLM常见使用方式"><a href="#LLM常见使用方式" class="headerlink" title="LLM常见使用方式"></a>LLM常见使用方式</h1><h2 id="1-直接提问"><a href="#1-直接提问" class="headerlink" title="1. 直接提问"></a>1. 直接提问</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm <span class="string">&quot;你的问题&quot;</span></span><br></pre></td></tr></table></figure><p>这个命令每次运行都会开启一个会话，每用一次会得到当前这个会话AI的回答。如果相继于当前会话继续提问，参考下面命令。</p><h2 id="2-持续对话"><a href="#2-持续对话" class="headerlink" title="2. 持续对话"></a>2. 持续对话</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm <span class="string">&quot;你的问题&quot;</span> -c</span><br></pre></td></tr></table></figure><p>上面那个命令每次都会开启一个chat，那如果想要在一个会话中多次提问，则需要使用<code>llm -c</code>命令。但是注意这个命令是基于最近一次chat的上下文继续进行的，如果你有好几个chat，你想基于指定的chat多次提问，那么可以使用:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm <span class="string">&quot;你的问题&quot;</span> --cid/--conversation &lt;<span class="built_in">id</span>&gt;</span><br></pre></td></tr></table></figure><p><code>conversation</code>可以通过<code>llm logs</code>查看。</p><h2 id="3-交互式对话"><a href="#3-交互式对话" class="headerlink" title="3. 交互式对话"></a>3. 交互式对话</h2><p>介绍完一次对话的基本用法，接下来来看看如何实现交互式对话。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm chat -m chatgpt</span><br></pre></td></tr></table></figure><p>chatgpt是你设置llm中的model_id，像本文中设置的就是<code>deepseek</code>。每次chat也会开启一个新的对话，退出之后你还想基于这个对话继续对话，可以使用:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm chat -c</span><br></pre></td></tr></table></figure><p>另外，还支持对模型使用一些参数，比如:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm chat -m gpt-4 -o temperature 0.5</span><br></pre></td></tr></table></figure><p>temperature是控制AI的回答的随机性，数值越大，AI的回答越随机，数值越小，AI的回答越有序。像deepseek的话，可以参考<a href="https://api-docs.deepseek.com/zh-cn/quick_start/parameter_settings">官方文档</a>:<br><img src="/../images/2025-01-16-10-33-05.png" alt="temperature设置"></p><p>退出交互式对话，可以使用只需要输入<code>quit</code>或者<code>exit</code>然后回车即可。</p><h3 id="3-1-设置系统提示词"><a href="#3-1-设置系统提示词" class="headerlink" title="3.1 设置系统提示词"></a>3.1 设置系统提示词</h3><p>交互式对话的时候，可以设置系统提示词<code>-s/--system</code>，这样AI的回答会更加有意义。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm chat -m gpt-4 -s <span class="string">&#x27;You are a sentient cheesecake&#x27;</span></span><br></pre></td></tr></table></figure><p>这里也可以设置<code>temperature</code>参数，比如:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm chat -m gpt-4 -s <span class="string">&#x27;You are a sentient cheesecake&#x27;</span> -o temperature 0.5</span><br></pre></td></tr></table></figure><p>如果不想每次都这样敲命令，可以设置简短的别名，比如:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm --system <span class="string">&#x27;You are a sentient cheesecake&#x27;</span> -m gpt-4 --save cheesecake</span><br></pre></td></tr></table></figure><p>然后每次只需要输入<code>llm chat -t cheesecake</code>即可基于你设置的系统提示词进行对话！</p><h3 id="3-2-交互式对话，如何多行输入"><a href="#3-2-交互式对话，如何多行输入" class="headerlink" title="3.2 交互式对话，如何多行输入"></a>3.2 交互式对话，如何多行输入</h3><p>多行输入常见于贴一些错误信息啥的，操作如下：</p><ol><li>输入<code>!multi</code>开启多行输入，然后粘贴</li><li>输入<code>!end</code>结束多行输入，然后回车即可。</li></ol><p>有一种特殊情况，如果输入的信息里面包含了<code>!end</code>那该怎么办？很简单，官方也提供解决方案，设置自定义的开始结束符号即可！<br>例如：可以是<code>!multi abc</code>、<code>!end abc</code>，这样来避免。</p><h2 id="4-结合Linux命令"><a href="#4-结合Linux命令" class="headerlink" title="4. 结合Linux命令"></a>4. 结合Linux命令</h2><ol><li>参数拼装：<code>llm &quot;Tell me about my operating system: $(uname -a)&quot;</code></li><li>管道： <code>cat myscript.py | llm &#39;explain this code&#39;</code></li><li>git： <code>git diff | llm -s &#39;Describe these changes&#39;</code></li></ol><p>这些都可以设置模型、系统提示和temperature等选项，在使用中可以配合好发挥出工具的最大作用！</p><h2 id="5-查看模型的参数"><a href="#5-查看模型的参数" class="headerlink" title="5. 查看模型的参数"></a>5. 查看模型的参数</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llm models --options</span><br></pre></td></tr></table></figure><p>没什么好说，会直接输出模型的参数，使用时按照需求设置即可。</p><blockquote><p>另外还有添加的附件的使用方式，这里就不再列出了，可以参考<a href="https://llm.datasette.io/en/stable/usage.html">官方文档</a></p></blockquote><blockquote><p>参考：<br><a href="https://github.com/simonw/llm">https://github.com/simonw/llm</a><br><a href="https://llm.datasette.io/en/stable/setup.html">https://llm.datasette.io/en/stable/setup.html</a><br><a href="https://llm.datasette.io/en/stable/other-models.html">https://llm.datasette.io/en/stable/other-models.html</a><br><a href="https://api-docs.deepseek.com/zh-cn/">https://api-docs.deepseek.com/zh-cn/</a><br><a href="https://github.com/simonw/llm/issues/393">https://github.com/simonw/llm/issues/393</a></p></blockquote>]]>
    </content>
    <id>https://blog.codewater.top/cli%E4%B8%AD%E9%9B%86%E6%88%90ai-%E4%BB%A5deepseek%E4%B8%BA%E4%BE%8B/</id>
    <link href="https://blog.codewater.top/cli%E4%B8%AD%E9%9B%86%E6%88%90ai-%E4%BB%A5deepseek%E4%B8%BA%E4%BE%8B/"/>
    <published>2025-01-15T06:55:47.000Z</published>
    <summary>
      <![CDATA[<p>最近国内的DeepSeek爆发出强劲的势头，纵观网友们的评价下来，很少有国产AI能有这样不错的评价。再加上最近DeepSeek注册就送500百万tokens，还有1块钱一百万tokens的这样的优惠力度，不得不去体验了一下。<br><a href="https://www.deepseek.com/">deepseek官网</a></p>]]>
    </summary>
    <title>cli中集成ai,以deepseek为例</title>
    <updated>2025-01-15T06:55:47.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="git" scheme="https://blog.codewater.top/categories/git/"/>
    <category term="submodule" scheme="https://blog.codewater.top/tags/submodule/"/>
    <content>
      <![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>某个工作中的项目需要包含并使用另一个项目，也许是第三方库，或者你独立开发的。 现在问题来了：你想要把它们当做两个独立的项目，同时又想在一个项目中使用另一个库。<br><code>Git submodule</code> 就是为了解决这个问题而生的,它允许你将一个仓库作为另一个仓库的子模块,同时让这两个库保持独立的提交记录。<br>这里记录一下submodule常见操作用法，方便以后查阅。</p><span id="more"></span><h1 id="git-submodule"><a href="#git-submodule" class="headerlink" title="git submodule"></a>git submodule</h1><h2 id="1-添加子仓库"><a href="#1-添加子仓库" class="headerlink" title="1. 添加子仓库"></a>1. 添加子仓库</h2><p>要将一个现有的 Git 仓库添加为子仓库，可以使用 <code>git submodule add</code> 命令。</p><p>操作步骤：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule add &lt;repository_url&gt; &lt;path&gt;</span><br></pre></td></tr></table></figure><ul><li><code>&lt;repository_url&gt;</code>：子仓库的 Git 仓库地址。</li><li><code>&lt;path&gt;</code>：子仓库在主仓库中的存放路径。</li></ul><p><strong>示例</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule add https://github.com/example/libfoo.git libs/libfoo</span><br></pre></td></tr></table></figure><h2 id="2-初始化子仓库"><a href="#2-初始化子仓库" class="headerlink" title="2. 初始化子仓库"></a>2. 初始化子仓库</h2><p>如果你克隆了一个包含子仓库的主仓库，你需要初始化子仓库。</p><p>操作步骤：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule init</span><br></pre></td></tr></table></figure><h2 id="3-更新子仓库"><a href="#3-更新子仓库" class="headerlink" title="3. 更新子仓库"></a>3. 更新子仓库</h2><p>初始化后，你需要更新子仓库以获取其内容。</p><p>操作步骤：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule update</span><br></pre></td></tr></table></figure><blockquote><p>当然，上面两个步骤可以合并为一个命令： <code>git submodule update --init</code></p></blockquote><h2 id="4-查看子仓库状态"><a href="#4-查看子仓库状态" class="headerlink" title="4. 查看子仓库状态"></a>4. 查看子仓库状态</h2><p>要查看子仓库的状态，可以使用以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule status</span><br></pre></td></tr></table></figure><h2 id="5-更新子仓库到最新版本"><a href="#5-更新子仓库到最新版本" class="headerlink" title="5. 更新子仓库到最新版本"></a>5. 更新子仓库到最新版本</h2><p>如果子仓库有更新，你可以进入<strong>子仓库目录并拉取最新的更改</strong>。</p><p>操作步骤：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> &lt;path_to_submodule&gt;</span><br><span class="line">git pull origin master</span><br></pre></td></tr></table></figure><h2 id="6-提交对子仓库的更改"><a href="#6-提交对子仓库的更改" class="headerlink" title="6. 提交对子仓库的更改"></a>6. 提交对子仓库的更改</h2><p>如果你在子仓库中做了更改并希望将其提交到主仓库，你需要先在子仓库中提交更改，然后在主仓库中更新子仓库的引用。</p><p>操作步骤：</p><ol><li>在<strong>子仓库</strong>中提交更改:</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;Update submodule&quot;</span></span><br></pre></td></tr></table></figure><ol start="2"><li>返回<strong>主仓库</strong>并更新子仓库引用:</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> ..</span><br><span class="line">git add &lt;path_to_submodule&gt;</span><br><span class="line">git commit -m <span class="string">&quot;Update submodule reference&quot;</span></span><br></pre></td></tr></table></figure><h2 id="7-删除子仓库"><a href="#7-删除子仓库" class="headerlink" title="7. 删除子仓库"></a>7. 删除子仓库</h2><p>如果你想要删除子仓库，可以按照以下步骤操作：</p><ol><li>从主仓库中删除子仓库的引用：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">rm</span> --cached &lt;path_to_submodule&gt;</span><br></pre></td></tr></table></figure><ol start="2"><li>删除子仓库的目录：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">rm</span> -rf &lt;path_to_submodule&gt;</span><br></pre></td></tr></table></figure><ol start="3"><li>提交更改：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;Remove submodule&quot;</span></span><br></pre></td></tr></table></figure><h2 id="8-克隆包含子仓库的主仓库"><a href="#8-克隆包含子仓库的主仓库" class="headerlink" title="8. 克隆包含子仓库的主仓库"></a>8. 克隆包含子仓库的主仓库</h2><p>如果你想要克隆一个包含子仓库的主仓库，可以使用以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --recurse-submodules &lt;repository_url&gt;</span><br></pre></td></tr></table></figure><p>如果已经克隆了主仓库，可以使用以下命令初始化和更新子仓库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule update --init --recursive</span><br></pre></td></tr></table></figure><h1 id="git-submodule-update-和在子仓库中执行git-pull-origin-master-的区别？"><a href="#git-submodule-update-和在子仓库中执行git-pull-origin-master-的区别？" class="headerlink" title="git submodule update 和在子仓库中执行git pull origin master 的区别？"></a><code>git submodule update</code> 和在子仓库中执行<code>git pull origin master</code> 的区别？</h1><p>如读者所见，上面关于子仓库的更新有两种方式：</p><ul><li>update：使用 <code>git submodule update</code> 命令更新子仓库</li><li>pull：在子仓库中执行 <code>git pull origin master</code> 命令更新子仓库</li></ul><p>那这两种区别是啥？以下是这两个命令的详细比较：</p><h2 id="git-submodule-update"><a href="#git-submodule-update" class="headerlink" title="git submodule update"></a><code>git submodule update</code></h2><h3 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h3><ul><li><code>git submodule update</code> 用于将子模块的状态<u>更新到主仓库所记录的特定提交</u>。这意味着它会<strong>将子模块检出到主仓库中记录的提交哈希值</strong>。</li></ul><h3 id="行为"><a href="#行为" class="headerlink" title="行为"></a>行为</h3><ul><li>当你在主仓库中执行 <code>git submodule update</code> 时，Git 会检查 <code>.gitmodules</code> 文件和主仓库的索引，找到每个子模块的最新提交哈希值，并将子模块的工作目录更新到这些特定的提交。</li><li>如果子模块的工作目录中有未提交的更改，<code>git submodule update</code> 不会影响这些更改，但它会将子模块的 HEAD 指针移动到主仓库所记录的提交。</li><li>另外，update这个命令并不需要在子模块中执行，它也可以在主仓库中执行。</li></ul><h2 id="git-pull-origin-master-在子仓库中执行"><a href="#git-pull-origin-master-在子仓库中执行" class="headerlink" title="git pull origin master 在子仓库中执行"></a><code>git pull origin master</code> 在子仓库中执行</h2><h3 id="目的-1"><a href="#目的-1" class="headerlink" title="目的"></a>目的</h3><ul><li><code>git pull origin master</code> 用于从远程仓库拉取最新的提交并合并到当前分支。这是一个常规的 Git 操作，用于更新子模块的内容。</li><li>pull这个命令必须在子模块中执行，才是对子仓库的更新；如果在主仓库中执行，那么就是对主仓库的更新。</li></ul><h3 id="行为-1"><a href="#行为-1" class="headerlink" title="行为"></a>行为</h3><ul><li>当你在子模块中执行 <code>git pull origin master</code> 时，Git 会从远程仓库拉取最新的提交，并将这些提交合并到当前分支。这可能会导致子模块的 HEAD 指针移动到远程仓库的最新提交。</li><li>这个操作会改变子模块的状态，并可能导致主仓库中的子模块引用与子模块的实际状态不一致。</li></ul><p>实际上就是说：主仓库下的子仓库的状态并不一定与子仓库的最新状态保持一致！</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul><li>**<code>git submodule update</code>**：将子模块更新到主仓库所记录的特定提交，保持主仓库和子模块之间的引用一致。</li><li>**<code>git pull origin master</code>**：从远程仓库拉取最新的提交并合并到当前分支，可能导致子模块的状态与主仓库中的引用不一致。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul><li>如果你想确保子模块的状态与主仓库的引用一致，使用 <code>git submodule update</code>。</li><li>如果你想更新子模块到远程仓库的最新状态，并且不介意主仓库中的引用可能变得不一致，可以使用 <code>git pull origin master</code>。</li></ul>]]>
    </content>
    <id>https://blog.codewater.top/Git-submodule%E5%B8%B8%E8%A7%81%E6%93%8D%E4%BD%9C%E7%94%A8%E6%B3%95/</id>
    <link href="https://blog.codewater.top/Git-submodule%E5%B8%B8%E8%A7%81%E6%93%8D%E4%BD%9C%E7%94%A8%E6%B3%95/"/>
    <published>2025-01-07T06:45:29.000Z</published>
    <summary>
      <![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>某个工作中的项目需要包含并使用另一个项目，也许是第三方库，或者你独立开发的。 现在问题来了：你想要把它们当做两个独立的项目，同时又想在一个项目中使用另一个库。<br><code>Git submodule</code> 就是为了解决这个问题而生的,它允许你将一个仓库作为另一个仓库的子模块,同时让这两个库保持独立的提交记录。<br>这里记录一下submodule常见操作用法，方便以后查阅。</p>]]>
    </summary>
    <title>Git: submodule常见操作用法</title>
    <updated>2025-01-07T06:45:29.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="git" scheme="https://blog.codewater.top/categories/git/"/>
    <category term="log" scheme="https://blog.codewater.top/tags/log/"/>
    <content>
      <![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>在IDE中，一般都提供通过搜索信息来寻找到某个commit。这里记录一下<strong>通过cmd命令</strong>来实现这个功能。</p><span id="more"></span><h1 id="git-log"><a href="#git-log" class="headerlink" title="git log"></a>git log</h1><p>在 Git 中，如果想要跨分支搜索提交信息，可以使用 <code>git log</code> 命令结合一些选项来实现。</p><h2 id="1-使用-git-log-搜索所有分支的提交信息"><a href="#1-使用-git-log-搜索所有分支的提交信息" class="headerlink" title="1. 使用 git log 搜索所有分支的提交信息"></a>1. 使用 <code>git log</code> 搜索所有分支的提交信息</h2><p>使用 <code>--all</code> 选项来搜索所有分支的提交信息。以下是一个基本的命令示例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --all --grep=<span class="string">&quot;搜索关键词&quot;</span></span><br></pre></td></tr></table></figure><ul><li><code>--all</code>：表示搜索所有分支。</li><li><code>--grep=&quot;搜索关键词&quot;</code>：用于匹配提交信息中包含特定关键词的提交。</li></ul><h2 id="2-搜索特定分支的提交信息"><a href="#2-搜索特定分支的提交信息" class="headerlink" title="2. 搜索特定分支的提交信息"></a>2. 搜索特定分支的提交信息</h2><p>如果只想在特定的分支中搜索，可以指定分支名称：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> branch_name --grep=<span class="string">&quot;搜索关键词&quot;</span></span><br></pre></td></tr></table></figure><h2 id="使用正则表达式-增强搜索"><a href="#使用正则表达式-增强搜索" class="headerlink" title="使用正则表达式,增强搜索"></a>使用正则表达式,增强搜索</h2><p>如果想使用正则表达式来进行更复杂的搜索，可以使用 <code>-G</code> 选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --all -G<span class="string">&quot;正则表达式&quot;</span></span><br></pre></td></tr></table></figure><h2 id="显示提交的详细信息"><a href="#显示提交的详细信息" class="headerlink" title="显示提交的详细信息"></a>显示提交的详细信息</h2><p>如果想查看提交的详细信息，可以使用 <code>-p</code> 选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --all --grep=<span class="string">&quot;搜索关键词&quot;</span> -p</span><br></pre></td></tr></table></figure><h2 id="限制输出的提交数量"><a href="#限制输出的提交数量" class="headerlink" title="限制输出的提交数量"></a>限制输出的提交数量</h2><p>如果只想查看最近的几个匹配的提交，可以使用 <code>-n</code> 选项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --all --grep=<span class="string">&quot;搜索关键词&quot;</span> -n 10</span><br></pre></td></tr></table></figure><h2 id="结合其他选项，美化输出"><a href="#结合其他选项，美化输出" class="headerlink" title="结合其他选项，美化输出"></a>结合其他选项，美化输出</h2><p>还可以结合其他选项来格式化输出，例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --all --grep=<span class="string">&quot;搜索关键词&quot;</span> --pretty=format:<span class="string">&quot;%h - %an, %ar : %s&quot;</span></span><br></pre></td></tr></table></figure><ul><li><code>--pretty=format:&quot;%h - %an, %ar : %s&quot;</code>：自定义输出格式，显示提交哈希、作者名、相对时间和提交信息。</li></ul><p>通过上述命令，可以方便地在 Git 中跨分支搜索提交信息。根据个人的需求，可以选择不同的选项来精确控制搜索结果。如果你有其他问题或需要进一步的帮助，欢迎在评论区讨论！</p>]]>
    </content>
    <id>https://blog.codewater.top/Git-search-commits-by-all-branches/</id>
    <link href="https://blog.codewater.top/Git-search-commits-by-all-branches/"/>
    <published>2025-01-07T06:17:32.000Z</published>
    <summary>
      <![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>在IDE中，一般都提供通过搜索信息来寻找到某个commit。这里记录一下<strong>通过cmd命令</strong>来实现这个功能。</p>]]>
    </summary>
    <title>Git: 跨分支搜索提交记录commits</title>
    <updated>2025-01-07T06:17:32.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Code Water</name>
    </author>
    <category term="git" scheme="https://blog.codewater.top/categories/git/"/>
    <category term="log" scheme="https://blog.codewater.top/tags/log/"/>
    <category term="blame" scheme="https://blog.codewater.top/tags/blame/"/>
    <category term="history" scheme="https://blog.codewater.top/tags/history/"/>
    <content>
      <![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>某次找bug的时候，发现代码被改过了，所以想要看这块代码修改的历史记录。目前IDE右键是支持<strong>文件的历史记录</strong>的，但是不支持代码块级别查看。<br>当然有一些可能会有，例如<code>cursor</code>，但是使用起来就比较麻烦,而且也不是我先想要的效果。<br><em><strong>痛点在于</strong></em>：cursor的行级别查看是可以，但是无法显示出所选行的所有历史记录，必须要选中某个commit，但实际上你如果出现我上面想要查看代码的情况，你是不知道错误的修改在哪次的commit里面，这样的话就导致你要多次重复的查看，很麻烦。</p><span id="more"></span><p><img src="/../images/git%20line%20history%20for%20cursor.png"><br><img src="/../images/git%20linie%20history%20examples%20for%20cursor.png"></p><h1 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h1><h2 id="git-blame、-git-log"><a href="#git-blame、-git-log" class="headerlink" title="git blame、 git log"></a>git blame、 git log</h2><p>既然IDE不支持，那么按照常规思路：</p><ul><li>官方命令支持</li><li>通过脚本解决</li></ul><p>我们先来看官方命令是否支持，经过搜索发现Git查看历史记录的有两种命令，分别是<code>git blame</code>和<code>git log</code>。</p><ol><li><code>git blame</code>: 逐行显示指定文件的每一行代码是由谁在什么时候引入或修改的。查看代码块的相关命令：<ol><li><code>git blame -L &lt;起始行号&gt;,&lt;结束行号&gt; &lt;文件&gt;</code>：查看指定行号范围内的代码注释。</li><li>可以看到这根本不是想要的，<em>pass</em>。</li></ol></li><li><code>git log</code>: 用于查看 Git 仓库中提交历史记录。查看代码块的相关命令：<ol><li><code>git -L &lt;start&gt;,&lt;end&gt;:&lt;file&gt;</code>: 查看某个文件指定行号范围的历史记录。</li><li><code>git -L:&lt;funcname&gt;:&lt;file&gt;</code>: 查看某个文件指定函数名的历史记录。</li><li>这两个命令符合想要的效果。</li></ol></li></ol><p><code>git log -L:discover:chat.py</code>效果如下：<br><img src="/../images/git%20log%20-L.png"></p><p>行号的就不演示了，效果一样，可以看到历史commit这部分代码块的记录。<br>既然官方命令支持我们想要的效果，那么就不去折腾脚本这种方法了。</p><h2 id="git-log-L-缺点"><a href="#git-log-L-缺点" class="headerlink" title="git log -L:&lt;funcname&gt;:&lt;file&gt;缺点"></a><code>git log -L:&lt;funcname&gt;:&lt;file&gt;</code>缺点</h2><p>在使用过程中，发现这个用法如果在某个文件中函数名第一次出现不是在声明函数的地方，那么显示的结果其实只是第一次出现相同字符串的地方，也就是说不能很好的识别真正函数的地方！</p><p>例如：<br><code>export</code>第一次出现在<code>from src.export import ChatExporter, MarkdownChatFormatter, MarkdownFileSaver</code>,但我们实际上想要看的是<code>export</code>函数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@app.command()</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">export</span>(<span class="params"></span></span><br><span class="line"><span class="params">    db_path: <span class="built_in">str</span> = typer.Argument(<span class="params"><span class="literal">None</span>, <span class="built_in">help</span>=<span class="string">&quot;The path to the SQLite database file. If not provided, the latest workspace will be used.&quot;</span></span>),</span></span><br><span class="line"><span class="params">    output_dir: <span class="built_in">str</span> = typer.Option(<span class="params"><span class="literal">None</span>, <span class="built_in">help</span>=<span class="string">&quot;The directory where the output markdown files will be saved. If not provided, prints to command line.&quot;</span></span>),</span></span><br><span class="line"><span class="params">    latest_tab: <span class="built_in">bool</span> = typer.Option(<span class="params"><span class="literal">False</span>, <span class="string">&quot;--latest-tab&quot;</span>, <span class="built_in">help</span>=<span class="string">&quot;Export only the latest tab. If not set, all tabs will be exported.&quot;</span></span>),</span></span><br><span class="line"><span class="params">    tab_ids: <span class="built_in">str</span> = typer.Option(<span class="params"><span class="literal">None</span>, <span class="built_in">help</span>=<span class="string">&quot;Comma-separated list of tab IDs to export. For example, &#x27;1,2,3&#x27;. If not set, all tabs will be exported.&quot;</span></span>)</span></span><br><span class="line"><span class="params"></span>):</span><br></pre></td></tr></table></figure><p><a href="https://github.com/somogyijanos/cursor-chat-export">代码例子</a></p><blockquote><p>参考：<br><a href="https://git-scm.com/docs/git-log">官方文档</a></p><p>如果你有更好的方法，换衣在评论区提出！</p></blockquote>]]>
    </content>
    <id>https://blog.codewater.top/Git-view-the-history-of-the-specified-code-block/</id>
    <link href="https://blog.codewater.top/Git-view-the-history-of-the-specified-code-block/"/>
    <published>2025-01-02T03:08:50.000Z</published>
    <summary>
      <![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>某次找bug的时候，发现代码被改过了，所以想要看这块代码修改的历史记录。目前IDE右键是支持<strong>文件的历史记录</strong>的，但是不支持代码块级别查看。<br>当然有一些可能会有，例如<code>cursor</code>，但是使用起来就比较麻烦,而且也不是我先想要的效果。<br><em><strong>痛点在于</strong></em>：cursor的行级别查看是可以，但是无法显示出所选行的所有历史记录，必须要选中某个commit，但实际上你如果出现我上面想要查看代码的情况，你是不知道错误的修改在哪次的commit里面，这样的话就导致你要多次重复的查看，很麻烦。</p>]]>
    </summary>
    <title>Git: 查看指定代码块的历史记录</title>
    <updated>2025-01-02T03:08:50.000Z</updated>
  </entry>
</feed>
